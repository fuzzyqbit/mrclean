---
quick_id: 260601-0e1
slug: fix-install-stub-dead-keys
date: 2026-06-01
status: in-progress
---

# Quick Task 260601-0e1: Fix misleading install stub (dead [words]/[detection] keys)

## Problem

`CONFIG_TOML_STUB` in `src/install/project-dir.ts:16-38` advertises `[words]` and
`[detection]` TOML sections that the config parser (`src/config/index.ts:174-230`)
never reads. Users who add proprietary terms under `[words]` get silent no-op — a
confidentiality footgun for a redaction tool. The real custom-term mechanism is the
separate `.mrclean/words.txt` file. Stub header also links to the wrong repo
(`mrclean/mrclean` vs `fuzzyqbit/mrclean`).

GitHub issue drafted at `.planning/debug/stub-dead-keys-issue.md` (filing blocked on
gh/MCP auth).

## Tasks

1. Rewrite `CONFIG_TOML_STUB` in `src/install/project-dir.ts`:
   - Fix repo URL → `fuzzyqbit/mrclean`.
   - Remove dead `[words]` and `[detection]` sections.
   - Add a note pointing custom-term users to `.mrclean/words.txt`.
   - Document only parser-real keys as commented examples: `dry_run`, `[allowlist]`
     (all 5 axes), `[entropy]`, `[[rules]]`.
   - Keep it comment-only (no live `key = value`) so it still parses as an empty layer.
   - verify: `grep` shows no `[words]`/`[detection]`/`mrclean/mrclean` in file.
   - done: stub honest, comment-only.

2. Update the cross-referenced `INSTALL_STUB` copy in `tests/config/reader.test.ts:17-39`
   to match the new stub verbatim.
   - verify: `npx vitest run tests/config/reader.test.ts tests/install/project-dir.test.ts`
   - done: both suites green.

3. Full test + build sanity.
   - verify: `npm test && npm run build`
   - done: green.

## must_haves

- truths: stub no longer references unimplemented keys; parser behavior unchanged.
- artifacts: `src/install/project-dir.ts`, `tests/config/reader.test.ts`.
- key_links: `src/config/index.ts` parseToml (unchanged, source of truth for real keys).
