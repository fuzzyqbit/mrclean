---
quick_id: 260601-0e1
slug: fix-install-stub-dead-keys
date: 2026-06-01
status: complete
commit: 1afefec
---

# Summary: Fix misleading install stub (dead [words]/[detection] keys)

## What changed

- `src/install/project-dir.ts` — rewrote `CONFIG_TOML_STUB`:
  - Removed dead `[words]` and `[detection]` sections (parser never read them).
  - Added a note directing custom-term users to `.mrclean/words.txt` (the real,
    richer mechanism with `word|action` overrides).
  - Documented only parser-real keys as commented examples: `dry_run`,
    `[allowlist]` (all 5 axes), `[entropy]`, `[[rules]]`.
  - Fixed repo URL `mrclean/mrclean` → `fuzzyqbit/mrclean`.
  - Kept stub comment-only, so it still parses as an empty config layer.
- `tests/config/reader.test.ts` — synced the cross-referenced `INSTALL_STUB`
  copy verbatim and refreshed the stale `[words]/[detection]` comment.

No parser/runtime behavior changed — this is a first-run UX/honesty fix.

## Verification

- `tests/config/reader.test.ts` + `tests/install/project-dir.test.ts`: 11 passed.
- Full suite: 54 files, 377 tests passed.
- `tsup` build: success.
- grep confirms no residual `[words]`/`[detection]`/`mrclean/mrclean` refs.

## Follow-up

- GitHub issue drafted at `.planning/debug/stub-dead-keys-issue.md`. Filing blocked
  on missing `gh` CLI / unauthenticated GitHub MCP. File manually with:
  `gh issue create --repo fuzzyqbit/mrclean --title "..." --body-file .planning/debug/stub-dead-keys-issue.md`
