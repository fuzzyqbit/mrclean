---
phase: 08-contract-verification-reversible-plumbing
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/doctor/checks.ts
  - src/doctor/index.ts
  - tests/doctor/checks.test.ts
  - tests/doctor/end-to-end.test.ts
autonomous: true
requirements: [REVMODE-12]

must_haves:
  truths:
    - "`mrclean doctor` reports reversible-mode state: 'disabled (default one-way)' when the table is absent, 'enabled — plumbing only' when set"
    - "Doctor's hooks check requires and reports 5 registered events including SessionEnd"
    - "The LOCKED doctor exit-code map is unchanged — the reversible check never FAILs in Phase 8 and documents its reserved exit code in the header"
  artifacts:
    - path: "src/doctor/checks.ts"
      provides: "checkReversibleState + 5-event REQUIRED_EVENTS + updated LOCKED header"
      contains: "checkReversibleState"
    - path: "src/doctor/index.ts"
      provides: "checkReversibleState wired into computeDoctorReport results array"
      contains: "checkReversibleState"
  key_links:
    - from: "src/doctor/index.ts"
      to: "src/doctor/checks.ts"
      via: "results.push(await checkReversibleState(homeDir, cwd)) after checkModelCache"
      pattern: "checkReversibleState"
    - from: "src/doctor/checks.ts"
      to: "src/config/index.ts"
      via: "loadEffectiveConfig({ homeDir, cwd }) in try/catch (checkConfigLoad precedent)"
      pattern: "loadEffectiveConfig"
---

<objective>
Make `mrclean doctor` honest about the Phase 8 surface: widen REQUIRED_EVENTS to the 5-event registration and add a reporting-only `checkReversibleState` check that surfaces the `[reversible]` state without touching the LOCKED exit-code map.

Purpose: REVMODE-12 groundwork (doctor "reports reversible-mode state" is REVMODE-12's first clause; FAIL-loud-on-unsupported-config semantics complete in Phase 10). Satisfies phase success criterion SC2's doctor half.

Output: doctor renders a reversible-mode line for every user, requires SessionEnd registration, and the exit-code map stays exactly as documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan CONSUMES. -->

From src/doctor/checks.ts (existing):

```typescript
export interface CheckResult {
  name: string
  status: 'PASS' | 'FAIL' | 'SKIP'
  detail: string
  exitCodeOnFail: number
}
```

From src/config/index.ts (08-01 landed `reversible` on the effective config):

```typescript
loadEffectiveConfig({ homeDir, cwd }): Promise<MrcleanConfig>  // config.reversible.enabled: boolean
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REQUIRED_EVENTS → 5 events with derived count detail</name>
  <files>src/doctor/checks.ts, tests/doctor/checks.test.ts, tests/doctor/end-to-end.test.ts</files>
  <read_first>
    - src/doctor/checks.ts (REQUIRED_EVENTS lines ~45-50; hardcoded "4" PASS detail lines ~160-165; LOCKED exit-code map header lines ~10-16; collectRegisteredBinPaths ~line 230 and extractRegisteredPaths ~line 289 both iterate REQUIRED_EVENTS — widening flows through automatically)
    - tests/doctor/checks.test.ts (buildSettings helper lines ~37-52 with SessionStart matcher hardcoded at ~line 46; allEvents array ~line 81; detail regex /4/ at ~line 88; inline 4-event fixtures at lines ~201-205 and ~247-251)
    - tests/doctor/end-to-end.test.ts (check for any hardcoded event counts or results-array length assertions)
  </read_first>
  <behavior>
    Tests first (update fixtures/assertions to the 5-event target, watch the hooks check fail, then widen the constant):
    - buildSettings with all 5 events → hooks check PASS, detail matches /5 hook events registered/
    - Settings missing SessionEnd → hooks check FAIL (missing-event path now covers SessionEnd)
    - buildSettings SessionStart branch emits matcher 'startup|resume|clear|compact'; new SessionEnd branch emits NO matcher key
  </behavior>
  <action>
    In src/doctor/checks.ts: add 'SessionEnd' to REQUIRED_EVENTS; replace the hardcoded "4" in the PASS detail with a derived count using REQUIRED_EVENTS.length (template: "N hook events registered (list)"). No other logic changes — collectRegisteredBinPaths and extractRegisteredPaths pick up the widened constant automatically.

    In tests/doctor/checks.test.ts: widen buildSettings (SessionStart matcher string; add SessionEnd no-matcher branch), allEvents to 5, detail regex /4/ → /5/, and add SessionEnd entries to the inline fixtures at ~201-205 and ~247-251.

    In tests/doctor/end-to-end.test.ts: update any hardcoded event-count or results-length assertions that the widening breaks (record findings in SUMMARY; if none exist, note "audited, no changes").
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/doctor/checks.test.ts && npx vitest run tests/doctor/end-to-end.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'SessionEnd'" src/doctor/checks.ts` returns >= 1 (REQUIRED_EVENTS member)
    - `grep -qE "\\\$\\{REQUIRED_EVENTS\\.length\\}" src/doctor/checks.ts` exits 0 (count derived, not hardcoded)
    - Doctor tests green with 5-event fixtures; missing-SessionEnd fixture FAILs the hooks check
  </acceptance_criteria>
  <done>Doctor requires and reports the 5-event surface; count is derived from the constant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: checkReversibleState (reporting-only) + wiring</name>
  <files>src/doctor/checks.ts, src/doctor/index.ts, tests/doctor/checks.test.ts</files>
  <read_first>
    - src/doctor/checks.ts (checkModelCache lines ~452-484 — the shipped "reporting check that stays green for opted-out users" shape; checkConfigLoad lines ~490-541 — loadEffectiveConfig in try/catch with `err instanceof ConfigReadError` handling; LOCKED exit-code map header lines ~10-16)
    - src/doctor/index.ts (computeDoctorReport results array — checkConfigLoad pushed at ~line 138, checkModelCache at ~line 142; numbered check-list comment at lines ~74-83)
    - tests/doctor/checks.test.ts (checkConfigLoad describe at ~line 391 — tmp dir + synthetic config.toml + assert { name, status, detail, exitCodeOnFail } shape)
  </read_first>
  <behavior>
    Tests first (checkModelCache-style trio, per-case tmp homeDir/cwd with synthetic config.toml):
    - No [reversible] table → { name: 'reversible', status: 'PASS', detail matching /disabled/ and /one-way/, exitCodeOnFail: 1 }
    - config.toml with [reversible] enabled = true → { status: 'PASS', detail matching /enabled/ and /Phase 9/ }
    - Malformed config.toml (unparseable TOML) → { status: 'SKIP', detail matching /config/ } (checkConfigLoad already owns the FAIL for this condition — no double-fail)
  </behavior>
  <action>
    Add `export async function checkReversibleState(homeDir: string, cwd: string): Promise<CheckResult>` to src/doctor/checks.ts:
    - Call loadEffectiveConfig({ homeDir, cwd }) in try/catch (checkConfigLoad precedent).
    - ConfigReadError → SKIP with detail 'config unreadable — see config check' (defer; never double-FAIL one root cause).
    - config.reversible.enabled === false → PASS, detail 'reversible mode: disabled (default one-way)'.
    - enabled === true → PASS, detail 'reversible mode: enabled — plumbing only (session state adapter lands in Phase 9)'.
    - exitCodeOnFail: 1 on all results (reserved, config domain).

    Phase 8 design pin (RESEARCH Open Question 3, planner decision): this check is REPORTING-ONLY and never returns FAIL — the LOCKED exit-code map (1,2,3,4,6) is not extended. Document the pin in the LOCKED header comment: append a line for check 8 `checkReversibleState — reporting-only in Phase 8; reserves exit 1 (config domain) for Phase 10's REVMODE-12 FAIL-loud semantics`.

    Wire in src/doctor/index.ts: push `await checkReversibleState(homeDir, cwd)` into the results array immediately after checkModelCache; add it to the numbered check-list comment (lines ~74-83). src/doctor/report.ts needs ZERO changes — renderReport iterates CheckResult[] generically (PATTERNS §report.ts).
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/doctor/checks.test.ts && npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Trio tests pass (PASS/disabled, PASS/enabled, SKIP/config-error) asserting exact { name, status, detail, exitCodeOnFail } shapes
    - `grep -c "checkReversibleState" src/doctor/index.ts` returns >= 2 (import + push)
    - `grep -q "reporting-only" src/doctor/checks.ts` exits 0 (LOCKED header documents the pin)
    - src/doctor/report.ts is NOT in the git diff
    - `npm test` exits 0 (byte-identical gate holds)
  </acceptance_criteria>
  <done>Doctor renders a reversible-state line for every run; exit-code map untouched and header documents why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → doctor | Untrusted TOML parsed during a diagnostic run (must not crash doctor) |
| doctor output → operator trust | Overclaiming or silent no-ops erode the honesty guarantee the tool sells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | checkReversibleState detail strings | mitigate | Detail reports state only (enabled/disabled) — never config values, paths, or map contents; test asserts exact detail strings |
| T-08-09 | Repudiation | Silent no-op on reversible misconfig (REVMODE-12 concern) | accept | Phase 8 explicitly defers FAIL-loud to Phase 10; the check PASSes with honest "plumbing only" copy so the operator is never told reversible mode works before it exists |
| T-08-10 | Denial of Service | Malformed config crashing doctor | mitigate | ConfigReadError caught → SKIP; checkConfigLoad owns the FAIL (single root cause, no crash, no double-fail) |
| T-08-11 | Tampering | Ad-hoc exit-code added to the LOCKED map (Pitfall 7) | mitigate | Reporting-only design — no FAIL path; reserved exit 1 documented in the LOCKED header; test asserts exitCodeOnFail === 1 |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — gate not applicable |
</threat_model>

<verification>
- `npx vitest run --project=unit tests/doctor/checks.test.ts` green.
- `npm test` full suite green (byte-identical gate).
- `npm run typecheck` green.
</verification>

<success_criteria>
- Doctor reports reversible-mode state on every run (SC2 doctor half).
- REQUIRED_EVENTS covers the 5-event surface; missing SessionEnd is a FAIL.
- LOCKED exit-code map byte-identical; design pin documented in the header.
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-03-SUMMARY.md` when done.
</output>
