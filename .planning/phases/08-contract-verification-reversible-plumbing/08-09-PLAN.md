---
phase: 08-contract-verification-reversible-plumbing
plan: 09
type: execute
wave: 6
depends_on: [08-08, 08-05]
files_modified:
  - tests/uat/artifacts/contract-findings.json
  - docs/HOOK-CONTRACT.md
  - THREAT_MODEL.md
autonomous: true
gap_closure: true
requirements: [REVMODE-10, REVMODE-03]

must_haves:
  truths:
    - "E4 has an empirical, version-stamped verdict produced via the object-shaped Bash payload against >10K output — the stale 'unanswerable — blocked by #68951' premise is gone from the artifact"
    - "The regenerated contract-findings.json still carries E1_shape_validation (with a verbatim hook error) and an E1.rendering object with a verdict — the live rerun proves the 08-08 writer preserves curated evidence"
    - "docs/HOOK-CONTRACT.md §E4 records the rerun verdict with a fresh stamp, and its downstream implications (Phase 10 restore-in-large-outputs, Phase 9 map-size) match the new answer"
    - "The E4-rerun and Read-object items are no longer unowned open follow-ups in docs/HOOK-CONTRACT.md"
    - "THREAT_MODEL.md's reversible section remains consistent with the rerun verdicts, and the copy-drift gate stays green"
  artifacts:
    - path: "tests/uat/artifacts/contract-findings.json"
      provides: "Regenerated findings: E4 object-leg verdict, E1_shape_validation preserved/refreshed, rendering preserved, fresh claude_version stamp"
      contains: "E1_shape_validation"
    - path: "docs/HOOK-CONTRACT.md"
      provides: "§E4 rerun verdict + stamp; per-tool matrix Read object cell filled; open follow-ups closed; re-verification procedure documents the guarded writer"
      contains: "e4-object-large-output-hook"
  key_links:
    - from: "docs/HOOK-CONTRACT.md"
      to: "tests/uat/artifacts/contract-findings.json"
      via: "§E4 verdict quotes trace byte-for-byte to experiments.E4"
      pattern: "contract-findings"
    - from: "tests/uat/artifacts/contract-findings.json"
      to: "tests/uat/fixtures/e4-object-large-output-hook.sh"
      via: "E4 method names the object fixture"
      pattern: "e4-object-large-output-hook"
---

<objective>
Run the live E4 rerun through the hardened 08-08 harness and refresh the contract documentation (gap 2 closure).

Purpose: E4's committed verdict ("unanswerable for built-in tools — blocked by #68951") was invalidated by the phase's own E1_shape_validation discovery — object-shaped payloads ARE honored for Bash on 2.1.209, so the 10K-cap question became answerable and was deliberately deferred with no phase owner. Phase 10 (restore-in-large-outputs scope) and Phase 9 (map-size expectations) consume this answer. The rerun also live-proves the 08-08 writer durability fix: after regeneration, the curated E1_shape_validation and rendering evidence must still be present.

Output: A settled (or honestly-bounded) E4 verdict in the findings artifact; docs/HOOK-CONTRACT.md §E4, per-tool matrix, and open follow-ups updated; THREAT_MODEL consistency confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-VERIFICATION.md
@docs/HOOK-CONTRACT.md
@tests/uat/artifacts/contract-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live E4 rerun through the hardened harness + artifact integrity assertions</name>
  <files>tests/uat/artifacts/contract-findings.json</files>
  <read_first>
    - tests/uat/contract-verification.test.ts (post-08-08 state — confirm the E4 object gating, the E1/Bash-object and E1/Read-object tests, and the buildFindingsArtifact wiring before spending tokens)
    - tests/uat/artifacts/contract-findings.json (committed pre-run state — capture experiments.E1.rendering.verdict and experiments.E1_shape_validation.verbatim_hook_error values so post-run preservation can be asserted against them)
    - tests/uat/harness.ts (runClaude/assertSessionRan — auth-failure behavior: BLOCKER throw with remediation text; if hit, this is an auth gate, not a code failure)
  </read_first>
  <action>
    Preflight: `claude --version` must succeed (authenticated CLI — 08-04 precedent: the executor runs live experiments itself; ~10-25 Haiku calls, cents). If authentication fails, stop and surface the harness BLOCKER text as an authentication gate.

    Run: `MRCLEAN_UAT=1 npx vitest run --project=uat tests/uat/contract-verification.test.ts` — the targeted file only (do NOT `npm run test:uat`, which also re-runs live-session.test.ts and spends extra tokens). All harness-integrity assertions must pass (11 tests green).

    Post-run integrity assertions on tests/uat/artifacts/contract-findings.json (each as a node -e check against the parsed JSON):
    1. experiments.E1_shape_validation exists, with a non-empty verbatim_hook_error string (carry-forward or fresh capture) and object_probe_honored recorded — the WR-01 live proof.
    2. experiments.E1.rendering is an object with a non-empty verdict (NOT the string 'pending-interactive') — the carry-forward live proof.
    3. experiments.E4.verdict does not match /blocked by #68951/ (stale premise gone) AND experiments.E4.method mentions 'object' (the rerun went through the object leg or honestly recorded why not).
    4. claude_version is non-empty and date is the run date.
    Expected E4 outcomes, all acceptable if honestly recorded: cap does NOT bind (~15K survived), cap binds (HEAD survived, TAIL lost), or indeterminate-with-evidence. CONTINGENCY: if the Bash-object leg is unexpectedly NOT honored on the current CC version (upstream drift since 2.1.209), the fallback verdict citing the object-leg result is the honest empirical answer — record it, flag prominently in the SUMMARY as a contract-drift finding, and Task 2 documents that reality instead.

    Commit the regenerated artifact: `test(08-09): rerun E4 via object-shaped Bash payload; regenerate findings (gap 2)`
  </action>
  <verify>
    <automated>node -e "const f=require('./tests/uat/artifacts/contract-findings.json');const e=f.experiments;if(!e.E1_shape_validation||!e.E1_shape_validation.verbatim_hook_error)process.exit(1);if(!e.E1.rendering||e.E1.rendering==='pending-interactive'||!e.E1.rendering.verdict)process.exit(2);if(/blocked by #68951/.test(e.E4.verdict))process.exit(3);if(!/object/i.test(e.E4.method+e.E4.verdict))process.exit(4);if(!f.claude_version)process.exit(5)"</automated>
  </verify>
  <acceptance_criteria>
    - `MRCLEAN_UAT=1 npx vitest run --project=uat tests/uat/contract-verification.test.ts` exits 0 (11 passed, harness integrity green)
    - The node -e integrity check above exits 0 (shape-validation preserved, rendering preserved, E4 premise refreshed, version stamped)
    - `git diff HEAD~1 --stat` for the artifact commit touches tests/uat/artifacts/contract-findings.json
    - No file under ~/.claude/ was modified by the run (sandbox isolation held — `git status` clean apart from the artifact)
  </acceptance_criteria>
  <done>E4 has a recorded empirical verdict produced through the object leg (or an honestly-recorded drift finding), and the regenerated artifact retains the curated E1 evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh docs/HOOK-CONTRACT.md and confirm THREAT_MODEL consistency</name>
  <files>docs/HOOK-CONTRACT.md, THREAT_MODEL.md</files>
  <read_first>
    - docs/HOOK-CONTRACT.md (ENTIRE file, 214 lines — §E4 at 129-153, per-tool matrix at 30-36, §Open follow-ups at 173-181, §Re-verification procedure at 205-214 are all edited)
    - tests/uat/artifacts/contract-findings.json (post-Task-1 state — every quoted verdict must byte-match this artifact; that is the file's opening traceability claim)
    - THREAT_MODEL.md (the `## Reversible Mode (v3.0)` section from line 141 — scan its five H3 subsections for any statement premised on the OLD E4 verdict before deciding whether an amendment is needed)
    - tests/copy-drift.test.ts (lines 1-80 — THREAT_MODEL.md is a scanned source; any new prose must avoid the banned CLAIM shapes: no "guarantees all...", no compliance claims)
  </read_first>
  <action>
    docs/HOOK-CONTRACT.md:
    1. §E4: replace the "Unanswerable for built-in tools..." verdict paragraph and the "Reinterpretation (post shape-validation discovery)" paragraph with the recorded rerun verdict, quoting experiments.E4.verdict from the regenerated artifact. New stamp line: `[verified on Claude Code {claude_version from artifact}, {date}, live headless -p session; >10K Bash output; ~15K OBJECT-shaped updatedToolOutput via tests/uat/fixtures/e4-object-large-output-hook.sh]`. Keep the additionalContext control finding sentence (still accurate).
    2. §E4 Downstream implications: rewrite the two bullets (Phase 10 restore-in-large-outputs scope; Phase 9 map-size expectations) to state what the measured verdict actually implies — e.g. if the cap does not bind, "no truncation bound applies to updatedToolOutput rewrites at ~15K on this version"; if it binds, state the observed bound. No speculative hedging that contradicts the recorded evidence.
    3. Per-tool matrix (§E1): fill the Read "Object payload" cell with the recorded Read-object probe outcome (honored / rejected-with-shape-error / indeterminate) quoting the E1_shape_validation read_object signals, replacing "UNTESTED — ... (open follow-up)".
    4. §Open follow-ups: both items (Read object-shape probe; E4 rerun) are now empirically answered — replace the numbered list with a short "Closed follow-ups" note stating each was settled by the 08-08/08-09 harness legs, pointing at experiments.E1_shape_validation and experiments.E4. If the Read probe returned indeterminate, keep it listed as open WITH an owner note ("rerunnable via the committed E1/Read-object leg").
    5. §Re-verification procedure: append two sentences documenting the hardened writer: the harness refuses to overwrite the artifact when a run records no verdicts, and carries forward interactive-only evidence (E1.rendering, verbatim hook error) it cannot reproduce — re-running on upgrades no longer destroys cited evidence (WR-01 closed).

    THREAT_MODEL.md consistency pass:
    6. Read the Reversible Mode section against the new E4/Read verdicts. Amend ONLY statements the rerun contradicts (candidates: any wire re-entry or map-size sentence premised on "no empirical cap evidence"). If nothing contradicts, make no edit and record "no amendment needed — checked {date}" in the SUMMARY. Any new prose must pass the copy-drift banned-phrase scan.

    Verification pass: every verdict string quoted in HOOK-CONTRACT §E4 and the matrix must byte-match the artifact (the doc's opening claim "Every verdict below traces to a recorded entry" must stay true).

    Commit: `docs(08-09): settle E4 + Read-object in HOOK-CONTRACT; document guarded re-verification (gap 2)`
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/copy-drift.test.ts && ! grep -q "rerun deferred" docs/HOOK-CONTRACT.md && grep -q "e4-object-large-output-hook" docs/HOOK-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "unanswerable" docs/HOOK-CONTRACT.md` returns 0 in §E4's verdict line UNLESS the Task-1 contingency fired (in which case the verdict must cite the object-leg evidence, not #68951)
    - `grep -c "rerun deferred" docs/HOOK-CONTRACT.md` returns 0 and the §Open follow-ups section no longer lists E4 or the Read probe as unowned open items
    - `grep -c "e4-object-large-output-hook" docs/HOOK-CONTRACT.md` returns >= 1 (stamp names the fixture)
    - The Read row of the per-tool matrix no longer contains "UNTESTED"
    - `npx vitest run --project=unit tests/copy-drift.test.ts` exits 0 (13+ assertions green — THREAT_MODEL presence gates + banned-phrase scan)
    - Quoted E4 verdict text in docs/HOOK-CONTRACT.md appears verbatim inside tests/uat/artifacts/contract-findings.json (byte-match spot check)
  </acceptance_criteria>
  <done>HOOK-CONTRACT.md reflects the settled E4 and Read-object answers with fresh stamps and a safe re-verification procedure; THREAT_MODEL is consistent and the copy-drift gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Claude sessions → recorded contract evidence | Verdicts produced by real sessions become the design substrate Phases 9-10 build on |
| findings artifact → operator-facing docs | HOOK-CONTRACT.md claims must trace to recorded evidence, not prose drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Repudiation | HOOK-CONTRACT verdict quotes | mitigate | Byte-match rule: every quoted verdict traces to the regenerated artifact; spot-check in acceptance criteria |
| T-08-09-02 | Tampering | live rerun vs curated evidence | mitigate | Task 1's node -e integrity gate proves E1_shape_validation + rendering survived regeneration (live proof of the 08-08 writer) |
| T-08-09-03 | Information Disclosure | fixture payloads in live sessions | mitigate | Marker strings only (E4 HEAD/TAIL, E1 markers); operator ~/.claude settings never written (sandbox --settings isolation, asserted in Task 1) |
| T-08-09-04 | Spoofing | unauthenticated/foreign claude CLI | mitigate | Preflight `claude --version` + assertSessionRan BLOCKER on auth failure — a failed run writes nothing (08-08 guard) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan — legitimacy gate not applicable |
</threat_model>

<verification>
- Live: 11/11 harness-integrity green under MRCLEAN_UAT=1; node -e artifact integrity gate exits 0.
- Docs: copy-drift gate green; "rerun deferred" gone; Read matrix cell filled; E4 stamp names the object fixture; quoted verdicts byte-match the artifact.
- Isolation: no ~/.claude writes; git status clean apart from planned files.
</verification>

<success_criteria>
- The 10K-cap question has a recorded empirical answer via the object-shaped Bash payload (or an honestly recorded, evidence-backed drift finding), consumed by fresh HOOK-CONTRACT copy.
- The rerun demonstrably did NOT destroy the phase's curated E1 evidence.
- No unowned open follow-ups remain in docs/HOOK-CONTRACT.md.
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-09-SUMMARY.md` when done (include the recorded E4 verdict verbatim, the Read-object outcome, token-spend estimate, and whether the Task-1 contingency fired).
</output>
