---
phase: 08-contract-verification-reversible-plumbing
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-04"]
files_modified:
  - docs/HOOK-CONTRACT.md
  - docs/upstream/display-only-channel-request.md
  - src/shared/types.ts
  - src/doctor/version-check.ts
  - tests/doctor/version-check.test.ts
autonomous: false
requirements: [REVMODE-10]

must_haves:
  truths:
    - "Operator can read documented, empirically-verified answers to each of the four contract questions, each stamped with CC version + date + method"
    - "The upstream feature request for a display-only rewrite channel is filed by the operator and its URL is linked in docs/HOOK-CONTRACT.md"
    - "Shipped copy no longer overclaims: types.ts JSDoc and doctor version-check detail reflect the E1 verdict with a verified-on stamp"
  artifacts:
    - path: "docs/HOOK-CONTRACT.md"
      provides: "One section per contract question: verdict, CC version + date, method, evidence excerpt, downstream implication; upstream issue URL"
      min_lines: 60
    - path: "docs/upstream/display-only-channel-request.md"
      provides: "Ready-to-file issue body per anthropics/claude-code feature_request template"
      min_lines: 30
  key_links:
    - from: "docs/HOOK-CONTRACT.md"
      to: "tests/uat/artifacts/contract-findings.json"
      via: "every verdict traces to a findings-artifact entry"
      pattern: "contract-findings"
    - from: "src/doctor/version-check.ts"
      to: "docs/HOOK-CONTRACT.md"
      via: "doctor copy references the verified contract doc"
      pattern: "HOOK-CONTRACT"
---

<objective>
Turn the recorded experiment verdicts into the durable operator-readable contract document, correct the shipped copy that overclaims (types.ts JSDoc, doctor version-check detail), and get the display-only rewrite channel feature request filed upstream and linked.

Purpose: REVMODE-10's documentation + upstream-request halves (phase success criterion SC1).

Output: docs/HOOK-CONTRACT.md (new artifact class — the phase's durable finding), honest shipped copy, filed-and-linked upstream issue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-04-SUMMARY.md
@tests/uat/artifacts/contract-findings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/HOOK-CONTRACT.md + shipped-copy honesty (types JSDoc, doctor detail)</name>
  <files>docs/HOOK-CONTRACT.md, src/shared/types.ts, src/doctor/version-check.ts, tests/doctor/version-check.test.ts</files>
  <read_first>
    - tests/uat/artifacts/contract-findings.json (ALL verdicts — the single source for every claim written in this task)
    - src/shared/types.ts (LOCKED header lines 1-10 citation discipline; PostToolUseOutput.updatedToolOutput JSDoc lines ~127-134 — currently claims the field "replaces the tool output that re-enters the model context" unqualified)
    - src/doctor/version-check.ts (green-detail copy lines ~111-117 — currently claims "PostToolUse updatedToolOutput supported" unqualified; yellow-path copy lines ~119-128 is the capability-specific versioned wording style to follow)
    - tests/doctor/version-check.test.ts (which detail strings are asserted — update alongside any copy change)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§Where answers land — required section structure)
  </read_first>
  <action>
    Create docs/HOOK-CONTRACT.md with one section per question (E1 updatedToolOutput honored-per-tool + terminal rendering, E2 updatedInput context echo, E3 session_id continuity across --resume, E4 10K-char cap applicability, E5 SessionEnd headless firing + reason). Every section carries: verdict, `[verified on Claude Code vX.Y.Z, YYYY-MM-DD, method]` stamp (types.ts header citation discipline), evidence excerpt (transcript/side-file pointer), and the downstream implication line (E3 → Phase 9 T5 retain-on-resume gate real-or-dead-code; E2 → Phase 10 input-restore scope fence; E1 → THREAT_MODEL wire re-entry + doctor copy; E4 → Phase 10 restore-in-large-outputs scope + Phase 9 map-size expectations). Include a placeholder section "Upstream feature request" with status 'drafted — link pending' (Task 3 fills the URL). Honest negative/unanswerable verdicts are written as such — SC1 requires documented answers, not positive ones.

    Update src/shared/types.ts PostToolUseOutput.updatedToolOutput JSDoc to state the E1 verdict per tool with the version stamp (e.g. per-tool honored/ignored status, referencing #68951 if confirmed) and point to docs/HOOK-CONTRACT.md. This satisfies the LOCKED header's upstream-verification citation requirement — this phase IS that verification.

    Update src/doctor/version-check.ts green detail (E1-gated, deterministic branches):
    - If E1 confirms updatedToolOutput ignored for Bash on the tested version: replace the unqualified "fully compatible (PostToolUse updatedToolOutput supported, full Phase 2 functionality)" with capability-specific wording naming the exception, e.g. "hook contract verified (note: updatedToolOutput ignored for built-in Bash on <tested version> — see docs/HOOK-CONTRACT.md)". Follow the yellow-path wording style.
    - If E1 shows the field honored for all tested tools: keep the claim but append the verified-on stamp "(verified on <tested version>, see docs/HOOK-CONTRACT.md)".
    Update tests/doctor/version-check.test.ts assertions to the new detail strings.
  </action>
  <verify>
    <automated>test -s docs/HOOK-CONTRACT.md && grep -q "verified on Claude Code" docs/HOOK-CONTRACT.md && grep -q "HOOK-CONTRACT" src/doctor/version-check.ts && npx vitest run --project=unit tests/doctor/version-check.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - docs/HOOK-CONTRACT.md has five question sections (E1-E5), each containing the literal string "verified on Claude Code" with a version and date
    - `grep -c "HOOK-CONTRACT" src/shared/types.ts` returns >= 1 (JSDoc cross-reference)
    - src/doctor/version-check.ts green detail no longer contains the unqualified claim UNLESS E1 proved it true — in which case it carries a verified-on stamp
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Durable contract doc exists; every shipped claim about updatedToolOutput is version-stamped and evidence-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the display-only rewrite channel feature request</name>
  <files>docs/upstream/display-only-channel-request.md</files>
  <read_first>
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§Upstream Feature Request — the ask, prior art to cite and DISTINGUISH, duplicate-search evidence already gathered)
    - tests/uat/artifacts/contract-findings.json (E1 evidence to embed)
    - docs/HOOK-CONTRACT.md (as written in Task 1 — the draft cross-references it)
  </read_first>
  <action>
    Draft docs/upstream/display-only-channel-request.md structured for the anthropics/claude-code feature_request.yml template (fetch the template fields via `gh api repos/anthropics/claude-code/contents/.github/ISSUE_TEMPLATE/feature_request.yml` to match section headings):
    - The ask: a display-only rewrite channel — a PostToolUse (ideally also UserPromptSubmit/assistant-message) field (e.g. `displayOverride`) that changes what the user's terminal renders WITHOUT entering model context or the transcript's model-facing content.
    - Motivation: sanitizers need placeholders model-side but readability user-side; today user view == model view (transcript ratchet), so any hook-path restore permanently re-exposes values to the API.
    - Prior art cited and DISTINGUISHED: #18653 (open — model-facing tool-result transform; different ask), #68951 (open bug — updatedToolOutput ignored for built-in Bash; evidence the existing model-facing channel is both broken and unsuitable; #54196/#65403/#67442 closed as duplicates), #64326/#62156/#66044 (redaction-request bucket referenced by #68951).
    - Preflight checklist: duplicate-search evidence (searches for "updatedToolOutput display", "hook display-only rewrite", "hook redact transcript display" found no existing display-only request — RESEARCH 2026-07-14; re-run the same `gh search issues` queries at draft time and record the date).
    - E1 empirical evidence from the findings artifact (honored-ness matrix + rendering observation).
  </action>
  <verify>
    <automated>test -s docs/upstream/display-only-channel-request.md && grep -q "18653" docs/upstream/display-only-channel-request.md && grep -q "68951" docs/upstream/display-only-channel-request.md && grep -qi "display" docs/upstream/display-only-channel-request.md</automated>
  </verify>
  <acceptance_criteria>
    - Draft exists, cites and distinguishes #18653 and #68951, includes the duplicate-search preflight evidence with a fresh search date, and embeds E1 evidence
    - Draft contains no non-public detail (only marker strings and public issue references)
  </acceptance_criteria>
  <done>A review-ready issue body exists in-repo; nothing has been posted anywhere.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator files the upstream issue and the link is recorded</name>
  <files>docs/HOOK-CONTRACT.md, docs/upstream/display-only-channel-request.md</files>
  <action>Pause for the operator to review and file the drafted feature request on their own GitHub account (public post — the agent must NEVER run `gh issue create` itself, RESEARCH Pitfall 8). On resume with the issue URL: replace 'link pending' in docs/HOOK-CONTRACT.md's "Upstream feature request" section with the URL, mirror it in the draft doc header, and commit. If the operator declines, record 'declined: <reason>' in the same section instead and flag SC1's filed-and-linked criterion as unmet in the SUMMARY.</action>
  <read_first>
    - docs/upstream/display-only-channel-request.md (the draft the operator reviews)
  </read_first>
  <what-built>A complete, evidence-backed feature-request draft for a display-only rewrite channel. Filing is a public post on YOUR GitHub account — an agent must never do it (RESEARCH Pitfall 8).</what-built>
  <how-to-verify>
    1. Review docs/upstream/display-only-channel-request.md — edit tone/content as you see fit.
    2. File it: `gh issue create --repo anthropics/claude-code --title "Feature: display-only rewrite channel for hook output (sanitizer use case)" --body-file docs/upstream/display-only-channel-request.md` (or via the web UI using the feature-request template).
    3. Copy the resulting issue URL.
  </how-to-verify>
  <resume-signal>Reply with the filed issue URL (or "declined" with a reason). On resume, the executor records the URL in docs/HOOK-CONTRACT.md's "Upstream feature request" section (replacing 'link pending'), mirrors it in the draft doc header, and commits — SC1 requires "filed and linked", greppable in docs/HOOK-CONTRACT.md.</resume-signal>
  <verify>
    <automated>grep -qE "github.com/anthropics/claude-code/issues/[0-9]+" docs/HOOK-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Operator provided the issue URL
    - `grep -qE "github.com/anthropics/claude-code/issues/[0-9]+" docs/HOOK-CONTRACT.md` exits 0 after recording
    - Final commit includes the link update
  </acceptance_criteria>
  <done>The upstream feature request is filed by the operator and its URL is greppable in docs/HOOK-CONTRACT.md (SC1 "filed and linked").</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs → public GitHub | The issue body is published publicly under the operator's account |
| Findings artifact → shipped copy | Claims written into shipped code (doctor/types) must not exceed the evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Information Disclosure | Upstream issue leaking non-public detail | mitigate | Human checkpoint review before filing; draft contains only marker strings + public issue refs; acceptance criterion checks draft content |
| T-08-17 | Spoofing | Agent posting publicly as the operator | mitigate | checkpoint:human-action — operator files via their own gh/web session; executor never runs `gh issue create` |
| T-08-18 | Repudiation | Doctor/types copy overclaiming contract support (shipped-behavior honesty) | mitigate | E1-gated deterministic copy branches; every claim version-stamped and traceable to contract-findings.json; version-check tests updated in lockstep |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — gate not applicable |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green after copy changes.
- docs/HOOK-CONTRACT.md: five verdict sections + upstream issue URL, all version-stamped.
- No `gh issue create` executed by the agent (git history + checkpoint flow prove operator filing).
</verification>

<success_criteria>
- SC1 fully satisfied: documented, empirically-verified answers to all four contract questions + upstream request filed and linked.
- Shipped copy (types JSDoc, doctor detail) matches the evidence exactly, with verified-on stamps.
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-05-SUMMARY.md` when done.
</output>
