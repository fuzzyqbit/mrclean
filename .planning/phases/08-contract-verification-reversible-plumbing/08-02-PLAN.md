---
phase: 08-contract-verification-reversible-plumbing
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/hook/handlers/session-end.ts
  - src/hook/dispatcher.ts
  - src/install/settings.ts
  - tests/hook/dispatcher.test.ts
  - tests/install/settings.test.ts
  - tests/install/idempotency.test.ts
  - tests/install/uninstall-roundtrip.test.ts
autonomous: true
requirements: [REVMODE-07]

must_haves:
  truths:
    - "dispatch() routes a SessionEnd payload with ANY reason string to a pure no-op returning null; unknown events still throw"
    - "A fresh `npx mrclean install` registers 5 hook events with SessionStart matcher 'startup|resume|clear|compact' and SessionEnd with NO matcher"
    - "Re-running install on a v2.0-shaped settings.json migrates without duplicate entries and preserves foreign user hooks"
    - "With the [reversible] table absent, all shipped v1/v2 behavior is byte-identical (existing detection/placeholder/audit suites pass unchanged)"
  artifacts:
    - path: "src/hook/handlers/session-end.ts"
      provides: "Pure no-op SessionEnd handler (return null, no config load, no I/O)"
      contains: "handleSessionEnd"
    - path: "src/hook/dispatcher.ts"
      provides: "SessionEnd routing case"
      contains: "case 'SessionEnd'"
    - path: "src/install/settings.ts"
      provides: "5-event HOOK_EVENTS + widened SessionStart matcher"
      contains: "startup|resume|clear|compact"
  key_links:
    - from: "src/hook/dispatcher.ts"
      to: "src/hook/handlers/session-end.ts"
      via: "case 'SessionEnd' → handleSessionEnd(input)"
      pattern: "handleSessionEnd"
    - from: "src/install/settings.ts"
      to: "src/hook/dispatcher.ts"
      via: "SessionEnd registration must never fire before the dispatcher route exists (Pitfall 1) — both land in this plan"
      pattern: "SessionEnd"
---

<objective>
Route SessionEnd through the hook dispatcher as a pure no-op and widen the installer to the 5-event surface (SessionEnd registration + SessionStart matcher `startup|resume|clear|compact`) with free migration via the existing idempotent replace loop.

Purpose: REVMODE-07 groundwork (SessionEnd routing + matcher widening + migration are REVMODE-07's installer/dispatcher clauses; the reason-aware janitor itself lands in Phase 9). Satisfies phase success criterion SC3.

Output: SessionEnd events produce zero exit-2 noise; fresh installs and v2.0→v3.0 re-installs both converge on the 5-event surface; one-way behavior stays byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan CONSUMES (created by 08-01) — use directly, no exploration needed. -->

From src/shared/types.ts (landed in 08-01):

```typescript
export interface SessionEndInput extends HookInputBase {
  hook_event_name: 'SessionEnd'
  reason: string   // OPEN string — tolerates any upstream reason value
}
// HookInput union already includes SessionEndInput
// null is already a member of HookOutput ("null means pass-through")
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionEnd no-op handler + dispatcher route (tests first)</name>
  <files>src/hook/handlers/session-end.ts, src/hook/dispatcher.ts, tests/hook/dispatcher.test.ts</files>
  <read_first>
    - src/hook/dispatcher.ts (switch cases lines ~32-50; the default throw at lines ~45-49 MUST be preserved unchanged)
    - src/hook/handlers/session-start.ts (file-header JSDoc + import conventions ONLY — do NOT copy the body: it loads config, which is exactly what session-end must never do)
    - tests/hook/dispatcher.test.ts (mock preamble lines ~13-51; Test 11d null-return shape lines ~102-113; Test 11e unknown-event guard lines ~115-120)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§Code Examples "No-op SessionEnd handler" lines 399-414 — use this body verbatim; §Pattern 4 for the why)
  </read_first>
  <behavior>
    - Test 1 (RED first): dispatch({ ...base, hook_event_name: 'SessionEnd', reason: 'other' }) → resolves to null
    - Test 2: dispatch with reason: 'bypass_permissions_disabled' → resolves to null (sixth documented reason, absent from milestone research)
    - Test 3: dispatch with reason: 'some_future_reason_xyz' → resolves to null (open-reason tolerance — no upstream reason addition can ever throw)
    - Test 4 (existing, must stay green UNCHANGED): dispatch with hook_event_name 'NotAnEvent' → rejects with 'unknown hook event: NotAnEvent'
  </behavior>
  <action>
    Write tests 1-3 first (copy Test 11d's null-return shape, not 11a's hookSpecificOutput shape), run tests/hook/dispatcher.test.ts, confirm they fail with 'unknown hook event: SessionEnd' (proof the default throw catches unrouted SessionEnd — this failure mode IS Pitfall 1's exit-2 noise scenario).

    Then create src/hook/handlers/session-end.ts using the RESEARCH lines 399-414 body verbatim: `export async function handleSessionEnd(_input: SessionEndInput): Promise<null> { return null }` with the JSDoc explaining (a) Claude Code ignores SessionEnd output/exit codes, (b) Phase 9 replaces the body with the reason-aware janitor, (c) the handler MUST stay free of config loads / I/O because any throw becomes exit-2 noise at every session end via the fail-closed wrapper. Follow session-start.ts file-header/import conventions (phase-stamped JSDoc, relative `.js`-suffixed ESM type import).

    Add `case 'SessionEnd': return await handleSessionEnd(input)` to the dispatcher switch, importing handleSessionEnd alongside the existing handler imports. The default-case throw stays byte-identical.

    The handler must import ONLY the SessionEndInput type — no config/index, no session-state, no node:fs (Pattern 4 / Pitfall 2).
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/hook/dispatcher.test.ts && ! grep -qE "loadEffectiveConfig|session-state|node:fs" src/hook/handlers/session-end.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1-3 pass; Test 11e (unknown event throws) passes unchanged
    - `grep -c "case 'SessionEnd'" src/hook/dispatcher.ts` returns 1
    - `grep -qE "loadEffectiveConfig|session-state|node:fs" src/hook/handlers/session-end.ts` exits non-zero (no forbidden imports)
    - src/hook/handlers/session-end.ts contains no await of any I/O — body is a bare `return null`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>SessionEnd routes to a pure no-op tolerating any reason string; unknown events still throw; dispatcher route exists BEFORE any registration ships (Pitfall 1 closed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Installer 5-event widening + v2.0→v3.0 migration test</name>
  <files>src/install/settings.ts, tests/install/settings.test.ts, tests/install/idempotency.test.ts, tests/install/uninstall-roundtrip.test.ts</files>
  <read_first>
    - src/install/settings.ts (HOOK_EVENTS line ~18, HOOK_MATCHERS lines ~22-27, filter-and-replace loop lines ~131-151 — this loop needs ZERO changes and IS the migration; undefined-matcher branch at lines ~146-148; removeHookEntries iterates Object.keys at line ~183)
    - tests/install/settings.test.ts (4-event loops at lines ~46, 127, 170, 369; matcher assertion line ~75; UserPromptSubmit no-matcher assertion lines ~90-98; OLD-shape migration test scaffold lines ~149-179; foreign-hook preservation pattern lines ~101-116)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§Code Examples "Widened installer constants" lines 384-394 — exact target shape; §Pattern 5)
  </read_first>
  <behavior>
    New/updated tests (write against the target shape first, watch them fail, then change the constants):
    - Fresh install writes exactly 5 events: SessionStart, SessionEnd, UserPromptSubmit, PreToolUse, PostToolUse
    - SessionStart entry matcher === 'startup|resume|clear|compact'
    - SessionEnd entry has NO matcher property (matcher would filter on `reason`; the Phase 9 janitor must see ALL reasons — copy the UserPromptSubmit no-matcher assertion shape)
    - v2.0→v3.0 migration: seed settings.json with the 4-event v2.0 shape (SessionStart matcher 'startup') PLUS a foreign user hook entry (non-_mrclean, matcher 'Bash'); run writeHookEntries; assert 5 events present, exactly ONE _mrclean entry per event (no duplicates), SessionStart matcher widened, foreign entry byte-identical
    - Uninstall after migration removes all 5 mrclean entries, leaves the foreign entry
  </behavior>
  <action>
    Update the two constants in src/install/settings.ts to the RESEARCH lines 384-394 target shape:
    - HOOK_EVENTS: add 'SessionEnd' (order: SessionStart, SessionEnd, UserPromptSubmit, PreToolUse, PostToolUse)
    - HOOK_MATCHERS: SessionStart → 'startup|resume|clear|compact' (regex alternation is the documented multi-value matcher syntax); SessionEnd → undefined with comment "no matcher — SessionEnd matchers filter on `reason`; the handler must see ALL reasons"; other entries unchanged.
    The writeHookEntries body gets ZERO changes — the existing `_mrclean`-filter-and-re-add loop is the migration (Pattern 5), and the existing undefined-matcher branch (used by UserPromptSubmit) carries SessionEnd.

    Update tests/install/settings.test.ts per the Pitfall 3 inventory: the four hardcoded 4-event loops → 5 events; the `matcher: 'startup'` assertion → the widened string; add the SessionEnd no-matcher test; add the v2.0→v3.0 migration test (clone the shipped OLD-shape migration scaffold at lines 149-179, reseed with the v2.0 4-event shape + foreign entry).

    Run tests/install/idempotency.test.ts and tests/install/uninstall-roundtrip.test.ts: if they iterate HOOK_EVENTS dynamically they pass unchanged; if they hardcode the 4-event list, update to 5. Record which case applied in the SUMMARY.

    These are deliberate registration-surface test updates (SC3), NOT byte-identical violations — SC2's guarantee covers the one-way redaction path and absent-[reversible] config, proven by the untouched detection/placeholder/audit suites.
  </action>
  <verify>
    <automated>npx vitest run tests/install/settings.test.ts tests/install/idempotency.test.ts tests/install/uninstall-roundtrip.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'SessionEnd'" src/install/settings.ts` returns >= 2 (HOOK_EVENTS + HOOK_MATCHERS)
    - `grep -c "startup|resume|clear|compact" src/install/settings.ts` returns 1
    - Migration test passes: 5 events, exactly one _mrclean entry per event, foreign 'Bash' entry preserved
    - `npm test` exits 0 with zero edits to detection/placeholder/audit test files (git diff shows changes ONLY in the files_modified list)
  </acceptance_criteria>
  <done>Installer converges fresh and v2.0 installs on the 5-event surface without duplicates; foreign hooks untouched; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mrclean install → ~/.claude/settings.json | Installer mutates operator-owned live Claude Code config shared with foreign hooks |
| Claude Code SessionEnd payload → hook stdin | Untrusted event payload (open `reason` string) crosses into the fail-closed wrapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | writeHookEntries migration | mitigate | Surgical `_mrclean: true`-tagged filter + timestamped backupJson (shipped); migration test asserts foreign entries byte-identical |
| T-08-05 | Denial of Service | handleSessionEnd throw → exit-2 at every session end → user distrust → uninstall (loss of protection) | mitigate | Pure `return null` body, no config load, no I/O; grep gate forbids loadEffectiveConfig/node:fs imports; open `reason` string absorbs upstream additions |
| T-08-06 | Denial of Service | SessionEnd registered before dispatcher route exists (Pitfall 1) | mitigate | Handler + dispatcher case + installer constants land in the SAME plan, handler task first; dispatcher test proves routing before install surface widens |
| T-08-07 | Spoofing | Widened SessionStart matcher firing on resume/clear/compact (Pitfall 4 second-order) | accept | Intended: re-init on resume fixes a latent stale-cache bug; exit-2 on malformed config is existing fail-closed behavior extended consistently; documented in SUMMARY |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — gate not applicable |
</threat_model>

<verification>
- `npx vitest run --project=unit tests/hook/dispatcher.test.ts tests/install/settings.test.ts` green.
- `npm test` full suite green; `git diff --stat` confirms only files in files_modified changed (byte-identical gate for the one-way path).
- `npm run typecheck` green.
</verification>

<success_criteria>
- SessionEnd dispatches to a pure no-op for ANY reason string; unknown events still throw.
- Fresh install → 5 events, widened SessionStart matcher, SessionEnd matcherless; re-install on v2.0 shape migrates without duplicates, foreign hooks preserved; uninstall removes all 5.
- Detection/placeholder/audit suites pass byte-identical.
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-02-SUMMARY.md` when done.
</output>
