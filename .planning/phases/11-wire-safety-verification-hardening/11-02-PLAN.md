---
phase: 11-wire-safety-verification-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/state/fs-interception.test.ts
autonomous: true
requirements: [REVMODE-11]

must_haves:
  truths:
    - "During a real two-event reversible flow plus a real restore run, NO buffer handed to any patched fs write API contains a canary original or the plaintext '\"entries\"' map-JSON marker — including write-file-atomic's fd-based temp-file writes"
    - "All three write channels are proven intercepted (non-vacuity): an fs.write capture whose bytes start with the MRCLNMAP envelope magic, a 32-byte promises.writeFile capture on the key temp path, and a promises.appendFile capture targeting audit.jsonl"
    - "Builtin fs functions are restored and syncBuiltinESMExports() re-run after the test — even when the test body throws — so no other test in the worker ever sees patched builtins"
  artifacts:
    - path: "tests/state/fs-interception.test.ts"
      provides: "SC2 fs-write interception gate — transient-write dimension the post-hoc inspection byte-scan cannot see"
      min_lines: 120
      contains: "syncBuiltinESMExports"
  key_links:
    - from: "tests/state/fs-interception.test.ts"
      to: "node:module"
      via: "syncBuiltinESMExports() after patch install AND after restore (reaches map-store's ESM named imports)"
      pattern: "syncBuiltinESMExports"
    - from: "tests/state/fs-interception.test.ts"
      to: "src/restore/cli.js"
      via: "captureRestoreRun duplicated by value (dynamic import + mirror-mocked exit/stdout/stderr)"
      pattern: "captureRestoreRun"
---

<objective>
Build SC2's gate: in-process capture of every buffer handed to an fs write API during a real reversible flow and a real restore run, asserting no plaintext canary ever reaches a write call — including the atomic-write temp files that a written-then-renamed-then-deleted lifecycle hides from post-hoc scans.

Purpose: tests/state/inspection.test.ts scans FINAL artifacts; a plaintext buffer written to a temp file and cleaned up would evade it. The monkey-patch seam (builtin fs property patch + module.syncBuiltinESMExports) was verified with a live probe on 2026-07-17 against the repo's actual write-file-atomic on Node v22.22.0 — it captures BOTH map-store's ESM named-import writes AND wfa's fd-based temp writes. vi.mock('node:fs') does NOT reach externalized CJS deps — do not substitute it.
Output: tests/state/fs-interception.test.ts (unit project — default glob, no config edit; per-file worker isolation makes process-global patching safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Pattern 3 — the verified seam + write-API inventory + probe output; §Pitfall 7 — patch hygiene; §Code Examples — executed probe source)
@.planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§tests/state/fs-interception.test.ts — flow recipe, capture harness convention, assertion set)
@tests/state/inspection.test.ts (runReversibleEvent 65-85; canary constants 52-55; captureRestoreRun 245-285; placeholder read-back 310-325 — READ ONLY, duplicate helpers by value)
@src/state/map-store.ts (fsPromises.writeFile key-tmp site ~line 210; MRCLNMAP envelope magic — READ ONLY)
@src/audit/restore-log.ts (appendFile site ~line 116 — READ ONLY)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch seam + real two-event reversible flow with absence and channel probes</name>
  <files>tests/state/fs-interception.test.ts</files>
  <read_first>
    - .planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Pattern 3 verbatim — the seam has NO in-repo analog; the probe sketch is the source of truth)
    - tests/state/inspection.test.ts (lines 65-85 runReversibleEvent — copy VERBATIM by value; hand-built state bypasses the serializer strip and invalidates the proof, Pitfall 10 lineage; lines 135-143 tmp baseDir lifecycle)
    - src/state/map-store.ts (the writeFile key-tmp call site near line 210 and the MRCLNMAP magic constant — confirm exact tmp-path naming for the key probe; READ ONLY)
    - tests/state/chaos.test.ts (lines 69, 315-321 — win32 visible-skip pattern)
  </read_first>
  <action>
    Create tests/state/fs-interception.test.ts in the unit project (default tests/** glob — no vitest.config edit).

    Patch seam (module scope helpers installPatch()/restorePatch(), restorePatch idempotent): import fs from 'node:fs', fsPromises from 'node:fs/promises', { syncBuiltinESMExports } from 'node:module'. Save originals for and wrap: fs.write, fs.writev, fs.writeFile, fs.writeFileSync, fs.appendFile, fs.appendFileSync, fsPromises.writeFile, fsPromises.appendFile. Each wrapper pushes { api, target (path string or 'fd:N'), bytes (Buffer — toBuf helper normalizing string/Buffer/TypedArray) } into a module-level captured array, then delegates to the saved original with identical arguments and this-binding. Call syncBuiltinESMExports() immediately after installing AND immediately after restoring (forgetting the re-sync silently misses all ESM named-import writes — the channel probes exist to catch exactly that). Hygiene: install the patch in the test body BEFORE the flow starts, restore in finally, AND register an afterEach(restorePatch) belt-and-braces so a mid-flow throw can never leak patched builtins into the worker (research Pitfall 7 + phase security note).

    Flow under the patch: fresh mkdtemp baseDir + randomUUID sid; drive TWO real reversible events via the runReversibleEvent recipe copied by value from inspection.test.ts — allocate WORD_CANARY 'zz-canary-project-path-7g2' as type 'WORD', then SECRET_CANARY 'AKIAIOSFODNN7EXAMPLX' as type 'AWS_KEY' (pinned 10-08 corpus; the EXAMPLE-suffixed AWS docs key is allowlisted and must not be used). Static imports of the state modules are fine — write-file-atomic re-does promisify(fs.write) per call (verified at node_modules/write-file-atomic/lib/index.js:116), and the channel probes fail loud if any channel were missed.

    Assertions for this task: (a) ABSENCE — no captured bytes Buffer.includes WORD_CANARY, SECRET_CANARY, or the string '"entries"' (the plaintext SessionMap JSON marker); (b) NON-VACUITY channel probes — at least one fs.write capture whose bytes start with the ASCII envelope magic 'MRCLNMAP' (wfa temp-file envelope), and at least one fsPromises.writeFile capture of exactly 32 bytes whose target path sits in the keys area (confirm exact tmp naming from map-store.ts near line 210); (c) captured array non-empty with a sane floor (>= 3 captures for two events). Cleanup: rm-recursive tmp dirs in afterEach.

    Commit `test(11-02): fs-write interception — no plaintext canary in any written buffer (SC2, map+key channels)`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/fs-interception.test.ts --project=unit && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Patch wraps all 8 named APIs and delegates with original this-binding; syncBuiltinESMExports called after install AND after restore (grep shows >= 2 call sites)
    - runReversibleEvent copied by value with the inspection.test.ts "duplicated by value" comment convention — no cross-import from other test files
    - Absence assertion covers WORD_CANARY, SECRET_CANARY, and '"entries"' over EVERY captured buffer
    - MRCLNMAP probe asserts on an fs.write (fd) capture specifically; 32-byte key probe asserts on a fsPromises.writeFile capture specifically
    - afterEach(restorePatch) present AND finally-restore present in the test body
    - Suite green in unit project; `npm run typecheck` clean of new-file errors
  </acceptance_criteria>
  <done>The reversible persist path (map envelope via wfa temp + session key publish) is proven to never hand a plaintext canary to any write API, with both channels proven intercepted.</done>
</task>

<task type="auto">
  <name>Task 2: Restore-run leg under the same patch + audit channel probe + hygiene proof</name>
  <files>tests/state/fs-interception.test.ts</files>
  <read_first>
    - tests/state/inspection.test.ts (lines 245-285 captureRestoreRun — duplicate BY VALUE per the file's own comment "never cross-imported"; lines 310-325 placeholder read-back via readSessionMapFile + hmacAddress)
    - src/audit/restore-log.ts (appendFile call site near line 116 — the audit channel target; hash-only record shape; READ ONLY)
    - src/restore/cli.ts (RunRestoreOpts injected seams baseDir/cwd/stdin — READ ONLY)
  </read_first>
  <action>
    Extend tests/state/fs-interception.test.ts with the restore leg, still under the installed patch: read the persisted placeholders back through readSessionMapFile + hmacAddress (inspection lines 310-325 recipe — never hand-format tokens), compose a document embedding both placeholders, then run captureRestoreRun (duplicated by value: dynamic import('../../src/restore/cli.js'), mirror-mock process.exit/stdout.write/stderr.write, repropagate non-exit throws) with stdin via Readable.from([doc]), baseDir = the same tmp baseDir, cwd = a tmp project dir so the restore audit record lands under the patch's view.

    Assertion ORDER (non-vacuity before absence — 10-08 discipline): (1) restore stdout contains WORD_CANARY and the stderr summary line reports restored >= 1 (the restore REALLY engaged; note restore stdout goes through the mirror-mocked process.stdout.write, which is NOT an fs API — the operator-local plaintext output is by design and must not appear in fs captures); (2) the secret placeholder byte-survives on stdout and SECRET_CANARY is absent from it; (3) at least one fsPromises.appendFile capture targets a path ending 'audit.jsonl' (third channel probe — the restore audit record via restore-log.ts); (4) re-run the FULL absence sweep over all captures accumulated since the start of the flow (both events + restore): no WORD_CANARY / SECRET_CANARY / '"entries"' bytes anywhere, and the audit capture's bytes carry hashes only (assert the canaries absent from that specific capture too).

    Hygiene proof documented in SUMMARY: temporarily insert a throwing expect mid-flow, confirm afterEach still restores the builtins (a subsequent trivial write in a sibling test is un-captured), remove the sabotage. Keep win32 visible skip via describe.skipIf(IS_WIN32) + placeholder describe (PATTERNS pin).

    Commit `test(11-02): restore leg under interception — audit channel probed, full-flow absence sweep (SC2 complete)`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/fs-interception.test.ts --project=unit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - captureRestoreRun duplicated by value with the convention comment; dynamic import of src/restore/cli.js (no static restore import — cold-path fence stays green)
    - Non-vacuity chain ordered before absence: restored>=1 + WORD on restore stdout asserted BEFORE the byte sweep
    - appendFile probe targets a path ending 'audit.jsonl'; that capture's bytes exclude both canaries
    - Full-flow absence sweep runs over the complete captured array (events + restore)
    - win32 placeholder describe present; `npm test` fully green (no patched-builtin leakage into sibling suites)
    - SUMMARY documents the throw-mid-flow hygiene sabotage check
  </acceptance_criteria>
  <done>SC2 is complete: every write channel on the reversible + restore surface is intercepted, probed non-vacuous, and canary-free — including transient atomic-write temp buffers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reversible persist path -> filesystem | every buffer crossing into a write API must be ciphertext or hash-only |
| test seam -> worker global state | patched builtins must never outlive the test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering (of the proof) | patch seam silently not engaged | mitigate | Three mandatory channel probes (MRCLNMAP via fs.write, 32-byte key via promises.writeFile, audit.jsonl via appendFile) — the test cannot pass vacuously with any channel unpatched |
| T-11-02-02 | Tampering | patched builtins escaping test scope | mitigate | finally-restore + idempotent afterEach(restorePatch) + syncBuiltinESMExports re-sync after restore; hygiene sabotage check documented; unit-project per-file worker isolation |
| T-11-02-03 | Info disclosure | plaintext canary in a transient write buffer | mitigate | Absence sweep over EVERY captured buffer incl. '"entries"' marker; real-pipeline state only (runReversibleEvent by value — hand-built state would bypass the serializer strip) |

No package installs this phase (11-RESEARCH Package Legitimacy Audit: none).
</threat_model>

<verification>
- `npx vitest run tests/state/fs-interception.test.ts --project=unit` green
- `npm test` green (proves no builtin-patch leakage); `npm run typecheck` at 38-error baseline
- `git diff --stat 8f370b9 -- src/hook src/detect src/placeholder tests/placeholder tests/detect` empty
</verification>

<success_criteria>
- SC2 proven at the write-call boundary: no plaintext canary in ANY written buffer including atomic-write temp files, with all three channels non-vacuously covered
- Seam hygiene guaranteed under failure (afterEach + finally + re-sync)
</success_criteria>

<output>
Create `.planning/phases/11-wire-safety-verification-hardening/11-02-SUMMARY.md` when done (include the hygiene sabotage-check result and the three channel-probe evidence lines).
</output>
