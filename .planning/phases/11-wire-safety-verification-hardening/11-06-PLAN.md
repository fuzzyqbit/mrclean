---
phase: 11-wire-safety-verification-hardening
plan: 06
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - .github/workflows/canary-leak.yml
  - .github/workflows/test.yml
  - tests/perf/post-tool-use-reversible.perf.test.ts
  - tests/state/stress.test.ts # conditional — Task 2 outcome (b) only (WORKER_DEADLINE_MS knob, T-09-08-06)
autonomous: true
requirements: [REVMODE-11]

must_haves:
  truths:
    - "canary-leak.yml names the wire-safety gate files explicitly (chaos + fs-interception + restore leak-grep in unit; dist-parity + stress in integration) with a collected-file-count guard that fails on zero collection (the demonstrated 10-07 vacuous-pass hazard is structurally closed)"
    - "The reversible canary corpus (zz-canary-project-path-7g2, kim.canary@zz.invalid, AKIAIOSFODNN7EXAMPLX) is in canary-leak.yml's defense-in-depth grep loop"
    - "test.yml and canary-leak.yml can fire from gsd/** pushes and manual dispatch; the stress gate's first ubuntu run is observed during the phase (A4 retired) or the contingency is documented with the deadline knob as the ONLY permitted adjustment"
    - "A reversible-mode PostToolUse perf row measures handlePostToolUse p95 under the 200 ms budget with the reversible branch proven engaged, closing the STATE.md TBD metric"
  artifacts:
    - path: ".github/workflows/canary-leak.yml"
      provides: "Explicit non-vacuous wire-safety CI steps + reversible corpus greps"
      contains: "dist-parity"
    - path: ".github/workflows/test.yml"
      provides: "workflow_dispatch + gsd/** push triggers (ubuntu pre-run affordance)"
      contains: "workflow_dispatch"
    - path: "tests/perf/post-tool-use-reversible.perf.test.ts"
      provides: "Reversible PostToolUse overhead row (p95 < 200 ms, overhead delta logged)"
      contains: "handlePostToolUse"
  key_links:
    - from: ".github/workflows/canary-leak.yml"
      to: "tests/hook/dist-parity.test.ts"
      via: "explicit vitest step with Test Files count guard"
      pattern: "dist-parity"
    - from: "tests/perf/post-tool-use-reversible.perf.test.ts"
      to: "src/hook/handlers/post-tool-use.ts"
      via: "read-only import driving the real handler under a reversible HOME stub"
      pattern: "handlePostToolUse"
---

<objective>
Elevate SC3/SC4 to explicit, non-vacuous CI gates and retire the ubuntu-first-run risk: name every wire-safety suite in canary-leak.yml with file-count guards, extend the defense-in-depth grep with the reversible corpus, add dispatch + gsd/** triggers so the stress gate runs on ubuntu BEFORE the milestone-merge PR, and land the reversible-overhead perf row (research OQ4 adopted — cut-first if the phase runs long).

Purpose: chaos and stress already assert everything SC3/SC4 name, but only implicitly via `npm test` on main — which has never fired from the milestone branch, and `npx vitest run <file> --project=X` exits 0 with ZERO files when mis-routed (demonstrated in-repo, 10-07). "Gates the build" requires named steps that cannot pass vacuously plus one observed ubuntu run.
Planner pins (research OQ1/OQ4 adopted): triggers = workflow_dispatch AND a 'gsd/**' push branch filter on test.yml + canary-leak.yml (the push trigger is the load-bearing pre-run mechanism — it fires on the very push that adds it; dispatch is the durable post-merge affordance); perf row adopted as the last task.
Output: two extended workflows + one new perf file (auto-collected by the existing tests/perf glob and perf.yml).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Pattern 4 — step shape + anti-vacuity options; §CI Wiring Facts — trigger table + vacuous-pass hazard; §Pitfall 6 — ubuntu-first-run options; §Open Questions 1 and 4 — recommendations adopted as pins)
@.planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§.github/workflows extension — canary-leak.yml step/grep line refs, test.yml facts, WORKER_DEADLINE_MS knob location; §tests/perf/post-tool-use-reversible.perf.test.ts — exact analog + delta)
@.github/workflows/canary-leak.yml (vitest step 33-39; defense-in-depth grep 41-62; array-driven grep 77-93)
@.github/workflows/test.yml (triggers 3-7; npm test matrix 38; QA-02 grep precedent 40-49)
@tests/perf/post-tool-use.perf.test.ts (the structure to copy: N=50, WARMUP=5, manual p95 44-48, headroom log 87-89)
@tests/state/chaos.test.ts (makeHome 171-179 + vi.stubEnv HOME seam; makeInput 128-139; V2_TAIL_PROBE 91)
@tests/state/stress.test.ts (WORKER_DEADLINE_MS line 59 + header 26-35 — the ONLY knob, T-09-08-06)
</context>

<tasks>

<task type="auto">
  <name>Task 1: canary-leak.yml wire-safety steps with file-count guards + reversible corpus greps</name>
  <files>.github/workflows/canary-leak.yml</files>
  <read_first>
    - .github/workflows/canary-leak.yml (ENTIRE file — where setup/build happen, the existing vitest step at 33-39, the PII_CANARIES array-driven grep loop at 77-93 to extend)
    - vitest.config.ts (confirm final routing: chaos + fs-interception + restore-canary-leak in unit; dist-parity + stress dual-listed into integration — the guard counts depend on this)
    - .planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Pattern 4 — the tee/grep guard sketch)
  </read_first>
  <action>
    Add two steps to canary-leak.yml after the existing test steps:

    Step 'Wire-safety gates — REVMODE-11 (SC2/SC3 unit + SC1a/SC4 integration)': the run block's FIRST line is `set -o pipefail`, then two commands — `npx vitest run --project=unit tests/audit/restore-canary-leak.test.ts tests/state/chaos.test.ts tests/state/fs-interception.test.ts | tee /dev/stderr | grep -Eq "Test Files\s+3 passed"` and `npx vitest run --project=integration tests/hook/dist-parity.test.ts tests/state/stress.test.ts | tee /dev/stderr | grep -Eq "Test Files\s+2 passed"`. The exact-count grep is the anti-vacuity guard: a mis-routed file changes the collected count and fails the step even though vitest exits 0 (the pipefail line is load-bearing and applies to BOTH new steps' run blocks: GitHub Actions runs plain `run:` steps with `bash -e {0}` — NO pipefail unless the step sets `shell: bash` — so without `set -o pipefail` a non-zero vitest exit whose output still contains an intact 'Test Files N passed' line, e.g. a teardown or unhandled-rejection failure, would pass the guard silently; state exactly this in a step comment). Ensure the integration command runs where dist is buildable (the integration project's globalSetup owns the tsup build; mirror how the existing workflow prepares node/deps).

    Step 'Defense-in-depth grep — reversible canary corpus': extend the existing array-driven grep loop pattern with the pinned 10-08 corpus — zz-canary-project-path-7g2, kim.canary@zz.invalid, AKIAIOSFODNN7EXAMPLX — over the same audit-log surface the existing loop scans, ::error + exit 1 on any hit.

    Local proof before commit (run the EXACT step commands): both vitest+grep pipelines pass locally; then the sabotage spot-check — run the integration command with `--project=unit` deliberately and confirm the count guard FAILS (zero files collected -> grep no match); document the sabotage output in SUMMARY (10-07 discipline). Commit `ci(11-06): explicit non-vacuous wire-safety gates + reversible corpus greps (SC3/SC4 elevation)`.
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/audit/restore-canary-leak.test.ts tests/state/chaos.test.ts tests/state/fs-interception.test.ts 2>&1 | grep -Eq "Test Files\s+3 passed" && npx vitest run --project=integration tests/hook/dist-parity.test.ts tests/state/stress.test.ts 2>&1 | grep -Eq "Test Files\s+2 passed"</automated>
  </verify>
  <acceptance_criteria>
    - Both new steps present; each new run block's first line is `set -o pipefail`; every vitest invocation in them carries the tee + exact-count grep guard
    - Grep loop includes all three corpus canaries; a planted canary line in a scratch audit file makes the loop exit 1 (spot-check documented, file removed)
    - The exact step commands pass locally; the deliberate mis-rooting sabotage fails the count guard (documented in SUMMARY)
    - No existing step modified beyond insertion points; YAML structure intact (workflow parses — see Task 2's push observation)
  </acceptance_criteria>
  <done>SC3 and SC4 are named, explicit, count-guarded CI gates — a routing regression or a vacuous zero-file run turns the build red.</done>
</task>

<task type="auto">
  <name>Task 2: dispatch + gsd/** triggers, push, and observe the first ubuntu run (A4 retirement)</name>
  <files>.github/workflows/test.yml, .github/workflows/canary-leak.yml</files>
  <read_first>
    - .github/workflows/test.yml (lines 3-7 `on:` block — the exact insertion site; the 3-Node matrix at 38)
    - .github/workflows/canary-leak.yml (its `on:` block lines 3-7)
    - tests/state/stress.test.ts (header 26-35 + WORKER_DEADLINE_MS at 59 — the ONLY knob if the ubuntu run flakes; the zero-degrade assertion is NEVER touched, T-09-08-06)
  </read_first>
  <action>
    Edit both workflows' `on:` blocks: add `workflow_dispatch:` and widen push branches from `[main]` to `[main, 'gsd/**']`. Leave pull_request at `[main]` (PR gating unchanged). Commit `ci(11-06): workflow_dispatch + gsd/** push triggers — ubuntu pre-run before milestone merge`.

    Then retire A4: determine the current branch with `git branch --show-current`. If it is the milestone branch (gsd/v3.0-*), push to origin and observe: `gh run list --workflow=test.yml --branch <branch> --limit 1` shows a new run (this very push fires it — the trigger is self-verifying), then `gh run watch <run-id> --exit-status` (tolerate the multi-minute 3-Node matrix). Same observation for canary-leak.yml. Outcomes: (a) green -> record in SUMMARY 'stress gate green on ubuntu-latest first run; 500 ms harness deadline held; A4 retired'; (b) stress deadline flake -> per T-09-08-06 re-measure and raise ONLY WORKER_DEADLINE_MS (tests/state/stress.test.ts line 59 — this is the conditional tests/state/stress.test.ts entry in files_modified, touched ONLY under this outcome (b)) with a measured-margin comment, commit `test(11-06): re-measure stress harness deadline for ubuntu (T-09-08-06 knob)`, push, re-observe — the zero-degrade assertion is never the knob; (c) executing in a worktree on a derived branch, or push/network unavailable -> do NOT push a non-milestone branch; document the exact deferred commands (push + gh run watch) in SUMMARY and flag A4 as open for verify-work.
  </action>
  <verify>
    <automated>grep -q "workflow_dispatch" .github/workflows/test.yml && grep -q "gsd/\*\*" .github/workflows/test.yml && grep -q "workflow_dispatch" .github/workflows/canary-leak.yml && grep -q "gsd/\*\*" .github/workflows/canary-leak.yml</automated>
  </verify>
  <acceptance_criteria>
    - Both `on:` blocks carry workflow_dispatch and the gsd/** push filter; pull_request remains main-only
    - One of the three documented outcomes recorded in SUMMARY with evidence (run URL/ID + conclusion for (a)/(b); exact deferred commands for (c))
    - If outcome (b): only WORKER_DEADLINE_MS changed, with a measured-value comment; zero-degrade assertion byte-identical (git diff proves it)
    - No secrets or new permissions added to either workflow
  </acceptance_criteria>
  <done>The stress gate's "gates the build" claim is backed by an observed (or explicitly deferred-with-commands) ubuntu execution — the milestone-merge PR cannot be its first-ever CI run without that being a documented, chosen risk.</done>
</task>

<task type="auto">
  <name>Task 3: Reversible PostToolUse overhead perf row (STATE.md TBD metric — cut-first)</name>
  <files>tests/perf/post-tool-use-reversible.perf.test.ts</files>
  <read_first>
    - tests/perf/post-tool-use.perf.test.ts (ENTIRE file — copy the structure: plain test() with {timeout}, N=50, WARMUP=5, THRESHOLD 200, manual p95 at 44-48, console headroom log 87-89; note its gap — it drives runDetection, never the handler)
    - tests/state/chaos.test.ts (makeHome 171-179 — the `[reversible] enabled = true` HOME fixture; vi.stubEnv('HOME', home) seam + cleanup 163-168; makeInput 128-139 payload; V2_TAIL_PROBE 91 non-global)
    - src/hook/handlers/post-tool-use.ts (handlePostToolUse import path and signature — READ ONLY, frozen tree; importing is not editing)
    - vitest.config.ts (confirm tests/perf/** sits in the integration include glob — no config edit needed)
  </read_first>
  <action>
    Create tests/perf/post-tool-use-reversible.perf.test.ts in the integration project via the existing tests/perf glob (perf.yml picks it up unchanged): two measured blocks driving the REAL handlePostToolUse with the chaos makeInput payload (secret-bearing, fresh randomUUID session_id per iteration so every iteration pays the full hydrate+persist+lock cost realistically) — block 1 baseline under makeHome(false) one-way HOME stub, block 2 under makeHome(true) reversible HOME stub. N=50, WARMUP=5, manual p95 identical to the analog. Non-vacuity: one sampled reversible output matches V2_TAIL_PROBE (duplicated non-global constant) and one sampled baseline output does NOT. Assertions: reversible p95 < 200 (the PostToolUse budget); console log both p95s, the overhead delta (reversible minus baseline), and headroom % — the numbers close STATE.md's 'Reversible-mode PostToolUse overhead TBD (Phase 9/11)' row (record measured values in SUMMARY for the state updater). Clean up stubbed HOME dirs afterEach. This task is the phase's designated cut-first item (research OQ4) — if execution context runs long, skip it and say so in the SUMMARY deviation section rather than rushing it.

    Commit `test(11-06): reversible PostToolUse overhead perf row — p95 budget + delta logged`.
  </action>
  <verify>
    <automated>npx vitest run --project=integration tests/perf/post-tool-use-reversible.perf.test.ts 2>&1 | grep -Eq "Test Files\s+1 passed"</automated>
  </verify>
  <acceptance_criteria>
    - File collected by `npx vitest run --project=integration tests/perf/` alongside the three existing perf files (count guard: 4 files)
    - Reversible p95 asserted < 200 ms; both p95s + delta printed in the reporter output
    - V2_TAIL_PROBE non-vacuity on the reversible block and its negative on the baseline block
    - Fresh session_id per iteration (grep: randomUUID inside the iteration loop)
    - Zero edits to src/** or vitest.config.ts (import-only usage of the frozen handler)
  </acceptance_criteria>
  <done>The last named landing zone for the reversible-overhead metric is filled: measured p95 + overhead delta under the real handler, gated at the 200 ms budget in perf CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI step definitions -> actual test execution | a step that collects zero files is a false security proof |
| harness timing knobs -> gate strength | tuning must never weaken assertions to get green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering (of the proof) | zero-file vacuous CI pass (demonstrated 10-07) | mitigate | Exact-count "Test Files N passed" guards on every named vitest step + documented mis-rooting sabotage check; explicit `set -o pipefail` first line (plain `run:` bash is `-e` only — never pipefail by default) preserves vitest failure through the pipe |
| T-11-06-02 | Tampering (of the gate) | knob-tuning the stress gate to green on ubuntu | mitigate | Only WORKER_DEADLINE_MS may move, only on measured evidence, with a margin comment (T-09-08-06); zero-degrade assertion byte-identical asserted in acceptance criteria |
| T-11-06-03 | Info disclosure | canary values printed in public CI logs | accept | Corpus values are synthetic markers pinned for exactly this purpose (non-EXAMPLE fake key, .invalid email, zz- word) — no real-secret shapes; grep loop prints file+value only on FAILURE |

No package installs this phase (11-RESEARCH Package Legitimacy Audit: none).
</threat_model>

<verification>
- Exact CI step commands pass locally (unit 3-file + integration 2-file count guards)
- Both workflows carry workflow_dispatch + gsd/** push; SUMMARY records the observed ubuntu run outcome (or the documented deferral)
- `npx vitest run --project=integration tests/perf/` collects 4 files, all green
- `npm test` green; `npm run typecheck` at 38-error baseline
</verification>

<success_criteria>
- SC3/SC4 "gates the build" is literal: named steps, count-guarded, corpus-grepped, with the ubuntu-first-run risk retired or explicitly deferred with commands
- STATE.md's reversible-overhead TBD metric has measured values (or a documented cut per OQ4's cut-first sanction)
</success_criteria>

<output>
Create `.planning/phases/11-wire-safety-verification-hardening/11-06-SUMMARY.md` when done (include: sabotage-check output, ubuntu run outcome with run ID/URL, measured p95s + delta).
</output>
