---
phase: 11-wire-safety-verification-hardening
plan: 07
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-05", "11-06"]
files_modified:
  - THREAT_MODEL.md
  - tests/copy-drift.test.ts
  - src/shared/strings.ts
autonomous: true
requirements: [REVMODE-11]

must_haves:
  truths:
    - "THREAT_MODEL's reversible section states shipped facts — store, restore CLI, doctor/status, and every Phase 11 gate cited by test file — with ZERO 'design commitment' phrasing remaining"
    - "The copy-drift gate's required anchors change in the SAME commit as the doc: 'verified against the shipped implementation' + 'not a defense against a same-user local attacker' required, 'transcript ratchet' kept — the suite is never red between commits"
    - "Encrypted-at-rest overclaim SHAPES are banned (additive BANNED_COPY_PHRASES entries) with a positive control and an honest-copy self-check per new regex; src/doctor/checks.ts is a scanned copy source"
    - "The residual-risks subsection reflects adopted hardening: AR-10-05 shrunk to a documented note (IN-02 demote shipped), AR-10-02/IN-03 stays accepted, #68951/#77587 cited as filed-and-open with the E1 emission fix explicitly out of scope"
  artifacts:
    - path: "THREAT_MODEL.md"
      provides: "Reversible-mode section finalized to shipped facts with gate citations and honest key-custody framing"
      contains: "verified against the shipped implementation"
    - path: "tests/copy-drift.test.ts"
      provides: "Swapped required anchors + encrypted-at-rest honesty rows + doctor copy source"
      contains: "not a defense against a same-user local attacker"
    - path: "src/shared/strings.ts"
      provides: "Additive encryption-overclaim entries in BANNED_COPY_PHRASES"
      contains: "BANNED_COPY_PHRASES"
  key_links:
    - from: "tests/copy-drift.test.ts"
      to: "THREAT_MODEL.md"
      via: "required-fragment assertions over the finalized section"
      pattern: "verified against the shipped implementation"
    - from: "tests/copy-drift.test.ts"
      to: "src/doctor/checks.ts"
      via: "SCANNED_SOURCES entry (doctor's encrypted-claim copy now under the gate)"
      pattern: "doctor/checks"
---

<objective>
Finalize SC5: rewrite THREAT_MODEL.md's reversible section from design-commitment framing to shipped-fact framing (the store, CLI, and every adversarial gate now exist — cite them), and evolve the copy-drift gate in the SAME commit, then extend the gate with encrypted-at-rest honesty coverage.

Purpose: the shipped copy-drift gate HARD-REQUIRES the phrase 'design commitment' (tests/copy-drift.test.ts) which finalization removes — the paired edit is forced by design, and doing it in one commit means CI can never pass with drifted claims in between. Planner pin (research OQ3 adopted): replacement anchors are the stamp fragment 'verified against the shipped implementation' and the key-custody fragment 'not a defense against a same-user local attacker'; 'transcript ratchet' is kept.
Output: finalized THREAT_MODEL.md + extended copy-drift gate + additive banned-phrase constants. This plan closes the phase — its verification section carries the phase regression gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Gap Analysis SC5 row; §Pitfall 8 — gate self-collision + ban-shapes-not-words lesson; §Hardening Residuals — what the residual subsection must now say; §State of the Art — #68951/#77587 both OPEN 2026-07-17)
@.planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§tests/copy-drift.test.ts extension — SCANNED_SOURCES 36-46, required-phrase site 166-168, kept row 162-164, three-test pattern 80-106, H3 fragments 131-137, UUID gate 188-215; §src/shared/strings.ts — BANNED_COPY_PHRASES 41-51 claim-shape discipline; §THREAT_MODEL.md — section line map: 141 section, H3s at 151/171/189/226/244, 'design commitment' at 147/154/210)
@THREAT_MODEL.md (the `## Reversible Mode (v3.0)` section — current framing to be rewritten)
@tests/copy-drift.test.ts (ENTIRE gate — extend using its own mechanics; note how strings.ts hosts bans yet passes its own scan)
@src/shared/strings.ts (BANNED_COPY_PHRASES 41-51)
@.planning/phases/10-operator-restore/10-SECURITY.md (AR-10-02, AR-10-05 wording — the accepted-risk source of truth the residuals subsection mirrors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paired single commit — THREAT_MODEL shipped-fact rewrite + copy-drift anchor swap</name>
  <files>THREAT_MODEL.md, tests/copy-drift.test.ts</files>
  <read_first>
    - THREAT_MODEL.md (the full `## Reversible Mode (v3.0)` section at line 141 with H3s at 151 Map blast radius / 171 structural secret floor / 189 Wire re-entry / 226 Key-custody honesty / 244 Accepted residual risks; the three 'design commitment' occurrences at 147, 154, 210)
    - tests/copy-drift.test.ts (required-phrase assertion 166-168 — the one the rewrite breaks; 'transcript ratchet' row 162-164 — kept; REQUIRED_REVERSIBLE_SUBSECTION_FRAGMENTS 131-137 — heading fragments that must keep matching)
    - .planning/phases/10-operator-restore/10-SECURITY.md (AR-10-05 exact rationale + 'Phase 11 is the landing zone' wording; AR-10-02 rationale — mirror, do not re-litigate)
    - .planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Hardening Residuals table — the adopt/keep/defer dispositions the residuals subsection documents)
  </read_first>
  <action>
    ONE commit containing both files (Pitfall 8 — a split would leave the unit suite red between commits):

    THREAT_MODEL.md `## Reversible Mode (v3.0)` rewrite, keeping all five H3 heading lines VERBATIM (the copy-drift heading-fragment gate its own rows depend on): convert prose from commitments to shipped facts — the encrypted per-session store (Phase 9), operator-only `mrclean restore` CLI + doctor/status surfaces (Phase 10), and the Phase 11 gates cited BY TEST FILE: tests/state/chaos.test.ts (SC3 corruption matrix), tests/state/stress.test.ts (SC4 16-process), tests/hook/dist-parity.test.ts (SC1a shipped-bundle parity), tests/state/fs-interception.test.ts (SC2 write-buffer interception), tests/audit/restore-canary-leak.test.ts + tests/restore/mixed-canary.test.ts (leak/round-trip), tests/state/cold-path.test.ts (both-direction fences), tests/uat/wire-safety.test.ts (SC1b live leg, operator-run opt-in). Remove all three 'design commitment' occurrences. Keep the phrase 'transcript ratchet'. Add a dated stamp line carrying the exact fragment 'verified against the shipped implementation'. In the Key-custody H3, carry the exact fragment 'not a defense against a same-user local attacker' adjacent to the encrypted-at-rest claim. Residual-risks H3 updated per the research dispositions: AR-10-05 cross-session collision now a documented note (IN-02 demote-to-unmatched shipped in 11-03 — ambiguous tokens restore nothing); AR-10-02/IN-03 truncated-hash dictionary-confirmability stays accepted with its salting-breaks-correlation rationale; the Bash string-form rejection (#68951) and mrclean's upstream ask (#77587) cited as filed-and-open on 2026-07-17 with the emission fix explicitly fenced out of v3.0 (identical in one-way mode — not a reversible delta).

    tests/copy-drift.test.ts, same commit: replace the 'design commitment' toContain assertion (166-168) with two assertions requiring 'verified against the shipped implementation' and 'not a defense against a same-user local attacker'; keep the 'transcript ratchet' row; leave REQUIRED_REVERSIBLE_SUBSECTION_FRAGMENTS untouched (headings unchanged makes this safe — verify all five still match).

    Run the full copy-drift suite green plus the whole unit project. Commit `docs(11-07): finalize THREAT_MODEL reversible section to shipped facts + paired copy-drift anchors (SC5)`.
  </action>
  <verify>
    <automated>npx vitest run tests/copy-drift.test.ts --project=unit && ! grep -q "design commitment" THREAT_MODEL.md && grep -q "verified against the shipped implementation" THREAT_MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD` for this commit lists BOTH THREAT_MODEL.md and tests/copy-drift.test.ts (paired edit proven)
    - `grep -c "design commitment" THREAT_MODEL.md` returns 0; 'transcript ratchet' still present; both new anchor fragments present verbatim
    - All five H3 heading lines byte-identical to before (git diff shows content-only changes under them)
    - Residuals subsection names AR-10-05 as shrunk-by-IN-02, AR-10-02 as accepted, #68951/#77587 as open, E1 fix as out of scope
    - copy-drift suite green at this commit (checkout-and-run proves no red window)
  </acceptance_criteria>
  <done>The reversible section is an audit of what shipped, not a promise — and the gate that locks it moved in lockstep.</done>
</task>

<task type="auto">
  <name>Task 2: Encrypted-at-rest honesty — banned overclaim shapes + doctor copy source</name>
  <files>src/shared/strings.ts, tests/copy-drift.test.ts</files>
  <read_first>
    - src/shared/strings.ts (BANNED_COPY_PHRASES 41-51 — the claim-SHAPE discipline: ban the positive overclaim form, never the words honest copy needs)
    - tests/copy-drift.test.ts (the three-test pattern per banned phrase at 80-106: scan + positive control + honest-copy self-check; SCANNED_SOURCES 36-46; observe how strings.ts itself is scanned without self-flagging its own regex literals — replicate that mechanism for the new entries)
    - src/doctor/checks.ts (the 'encrypted session state adapter active' detail string — the user-facing encryption copy now entering the scan; LOCKED constants, zero edits permitted here)
  </read_first>
  <action>
    src/shared/strings.ts: append three ADDITIVE entries to BANNED_COPY_PHRASES banning encrypted-at-rest overclaim shapes — (1) an encrypt-context absolute-safety claim: /\bencrypt\w*[^.]*\bcannot be (?:read|recovered|decrypted)\b/i; (2) an exclusive-access claim: /\bonly (?:you|the operator) can (?:read|decrypt)\b/i; (3) an unconditional-safety claim: /\bsafe even (?:if|when|from)\b/i. Each entry gets a one-line comment naming the overclaim shape it bans and why the honest qualification ('encrypted at rest; not a defense against a same-user local attacker') passes it.

    tests/copy-drift.test.ts: for EACH new regex add the file's standard three rows — full SCANNED_SOURCES scan clean, positive control (a synthetic overclaim string IS flagged), honest-copy self-check (the same-user qualification sentence and doctor's 'encrypted session state adapter active' detail are NOT flagged). Add `{ rel: 'src/doctor/checks.ts', isSource: true }` to SCANNED_SOURCES (its encrypted-claim detail is user-facing copy). Rule if any existing source trips a new ban: LOCKED constants (doctor details, byte-locked copy) are definitionally shipped-and-reviewed — NARROW THE REGEX, never edit the LOCKED copy and never weaken a row to vacuity (the positive control keeps the narrowed regex honest).

    Run copy-drift green, then the phase regression gate from the verification section. Commit `test(11-07): ban encrypted-at-rest overclaim shapes + scan doctor copy (SC5 complete)`.
  </action>
  <verify>
    <automated>npx vitest run tests/copy-drift.test.ts --project=unit && npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Three new BANNED_COPY_PHRASES entries, additive only (existing entries byte-identical in the diff)
    - Nine new copy-drift rows (3 regexes x scan/positive-control/self-check), all green; SCANNED_SOURCES includes src/doctor/checks.ts
    - src/doctor/checks.ts untouched (git diff empty for that path)
    - Full `npm test` green; typecheck at the 38-error baseline with zero Phase 11-file errors
  </acceptance_criteria>
  <done>The "encrypted at rest" claim is now drift-locked in both directions: the honest qualification is REQUIRED where the claim lives, and overclaim shapes are BANNED across every scanned copy surface including the doctor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project docs/copy -> user trust decisions | overclaimed security copy is a social-layer vulnerability |
| doc <-> gate pairing | any window where CI passes with drifted claims defeats the lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | Repudiation / Info disclosure (social) | THREAT_MODEL overclaim after finalization | mitigate | Required anchors ('verified against the shipped implementation', same-user-attacker fragment, 'transcript ratchet') + three banned overclaim shapes, each with a positive control |
| T-11-07-02 | Tampering (of the gate) | red-window or silent-drift between doc and gate | mitigate | Single paired commit for Task 1 (git show --stat proof); heading fragments kept verbatim so existing rows keep biting |
| T-11-07-03 | Tampering | new bans flagging honest/LOCKED copy, pressuring copy edits | mitigate | Honest-copy self-checks per regex; explicit rule: narrow the regex, never touch LOCKED constants, never vacuate a row |

No package installs this phase (11-RESEARCH Package Legitimacy Audit: none).
</threat_model>

<verification>
Phase regression gate (this is the phase's last plan):
- `npm test` fully green; `npm run typecheck` at the 38-error differential baseline (zero errors in Phase 11 files)
- Frozen trees: `git diff --stat 8f370b9 -- src/hook src/detect src/placeholder tests/placeholder tests/detect` EMPTY; `git diff --stat 8f370b9 -- tests/hook` shows ONLY the added tests/hook/dist-parity.test.ts
- `git status --porcelain dist/` empty across all phase commits (worktree rule)
- copy-drift suite green with the new anchors + bans; `grep -c "design commitment" THREAT_MODEL.md` = 0
- Reminder for verify-work: operator runs `MRCLEAN_UAT=1 npm run test:uat` (SC1b live leg + survey re-stamps, per 11-VALIDATION.md manual table)
</verification>

<success_criteria>
- SC5 complete: shipped-fact THREAT_MODEL with gate citations, paired-commit gate evolution, encrypted-at-rest honesty locked with positive controls
- Phase regression gate green — the milestone's negative claims are CI-enforced and the copy cannot silently drift
</success_criteria>

<output>
Create `.planning/phases/11-wire-safety-verification-hardening/11-07-SUMMARY.md` when done (include the paired-commit hash, the anchor list as shipped, and the phase regression gate outputs).
</output>
