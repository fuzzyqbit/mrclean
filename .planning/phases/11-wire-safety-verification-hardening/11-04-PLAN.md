---
phase: 11-wire-safety-verification-hardening
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/restore/cli.ts
  - tests/restore/degrade.test.ts
  - tests/cli/restore.test.ts
autonomous: true
requirements: [REVMODE-11]

must_haves:
  truths:
    - "When the restore CLI's outer belt-and-braces catch fires AFTER counts were computed, the stderr summary reports restored=0 unmatched=0 secret-skipped=0 sessions=0 — never stale pre-throw counts"
    - "A throwing fallback stdout write cannot escape the catch: exit stays 0, the constant-shape warning and the summary line (still LAST on stderr) are both emitted"
    - "The exitCode-and-return discipline (never process.exit) and the byte-locked summary-line pin survive unchanged; all degrade rows (a)-(g) stay green"
  artifacts:
    - path: "src/restore/cli.ts"
      provides: "IN-01 hardening: counts reset to ZERO_COUNTS first in the outer catch; fallback stdout write guarded"
      contains: "ZERO_COUNTS"
    - path: "tests/restore/degrade.test.ts"
      provides: "IN-01 rows: late-throw stale-counts row + double-throw containment row"
      contains: "restored=0 unmatched=0 secret-skipped=0 sessions=0"
  key_links:
    - from: "tests/restore/degrade.test.ts"
      to: "src/restore/cli.ts"
      via: "capture harness driving runRestore with a throwing stdout seam; summary-line pin"
      pattern: "secret-skipped"
---

<objective>
Adopt IN-01: make the restore CLI's degrade report honest under unexpected late failure — the outer catch resets counts to ZERO_COUNTS before the summary is emitted, and the fallback pass-through write is guarded so a second throw can never break the exit-0 one-way degrade contract.

Purpose: today the catch path keeps pre-computed counts (a summary can claim restored=1 when delivery failed) and the unguarded fallback `process.stdout.write(input)` can rethrow out of the catch. Both fixes fail in the safe direction and are squarely this phase's degrade-honesty remit (research Hardening Residuals, ADOPT).
Output: ~5-line fix in src/restore/cli.ts + two degrade rows, via RED -> GREEN commits (workflow.tdd_mode enforced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Hardening Residuals IN-01 row — fix scope and rationale)
@.planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§src/restore/cli.ts IN-01 fix — current catch block lines 211-221 annotated, ZERO_COUNTS at 111, counts let at 166, preservation list; §tests IN-01 row — capture harness 166-216, summary pin 55-61, degrade rows 281-335)
@src/restore/cli.ts (the outer catch 211-221; the main flow ordering of counts computation, stdout write, audit write, summary emission)
@tests/restore/degrade.test.ts (rows (a)-(g) and their harness — the extension target)
@tests/cli/restore.test.ts (capture harness 166-216 duplicated-by-value convention; byte-locked summaryLine pin 55-61)
</context>

<feature>
  <name>Zero-counts honesty + guarded fallback in the restore CLI outer catch</name>
  <files>src/restore/cli.ts, tests/restore/degrade.test.ts</files>
  <behavior>
    Row 1 (stale counts): seed a real map with one restorable WORD entry and feed a document containing its placeholder; force the OUTER catch to fire AFTER counts were computed by stubbing a post-counts step to throw (candidate seams, executor picks whichever provably fires the outer catch on current code: process.stdout.write throwing on its first call, or a late non-AuditWriteError throw past the audit-specific handler). Expected WITH fix: no escaped throw; process.exitCode stays 0/undefined; stderr carries the constant-shape warning; the summary is the LAST stderr line and reads exactly `[mrclean] restore: restored=0 unmatched=0 secret-skipped=0 sessions=0` (ZERO_COUNTS form). Expected on CURRENT code (RED): summary reports the stale restored=1.
    Row 2 (double-throw containment): stub process.stdout.write to throw on EVERY call => the fallback write in the catch also throws. Expected WITH fix: the throw is contained (runRestore returns normally), exit 0, warning + zero-counts summary still emitted on stderr. Expected on CURRENT code (RED): the rethrow escapes the catch (harness observes the propagated throw).
    Invariants both rows: never process.exit (harness's throwing process.exit stub is the regression guard); summary line remains byte-identical to the tests/cli/restore.test.ts pin; existing rows (a)-(g) untouched-green.
  </behavior>
  <implementation>
    In src/restore/cli.ts's outer catch (lines 211-221): FIRST statement sets counts = ZERO_COUNTS (the constant already defined at line 111; counts is already a let at line 166). Wrap the fallback `process.stdout.write(input)` in its own try/catch whose catch body is an intentional no-op with a one-line comment stating the contract (degrade is one-way: if even the fallback write fails there is nothing safer to do; the warning + zero-counts summary on stderr are the signal — this documented swallow IS the error handling). Do not touch: the stdoutWritten/warnedNoMaps flag logic, the audit-specific catch, the summary emission site (stays last on stderr), warning constants, exitCode discipline.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing IN-01 rows — stale-counts summary + escaping fallback throw</name>
  <files>tests/restore/degrade.test.ts</files>
  <read_first>
    - src/restore/cli.ts (ENTIRE outer flow — locate where counts are computed relative to the main stdout write and the audit write, to pick the seam that provably reaches the OUTER catch after counts exist; the catch block 211-221; summary emission)
    - tests/restore/degrade.test.ts (existing rows (a)-(g) at 281-335 and the harness they share — extend in-file, matching style)
    - tests/cli/restore.test.ts (capture harness 166-216 — duplicated-by-value convention if the degrade harness needs the throwing-stdout parameterization; summaryLine pin 55-61 — the byte-locked expected string)
    - .planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§IN-01 test row — seam suggestion + ZERO_COUNTS expected form)
  </read_first>
  <action>
    Append a describe block ('IN-01: outer-catch honesty — zero counts + guarded fallback') to tests/restore/degrade.test.ts implementing behavior Rows 1 and 2. Seed real state via the file's existing fixture helpers (real key + envelope — never raw JSON on disk). Parameterize the captured process.stdout.write stub to throw on first call (Row 1) or all calls (Row 2). Assert per the behavior block, including the exact ZERO_COUNTS summary string and summary-last-on-stderr ordering. Confirm BOTH rows fail on current code for the RIGHT reasons: Row 1's summary shows stale restored=1; Row 2 observes an escaped throw. All existing rows stay green. Commit `test(11-04): add failing outer-catch honesty rows (IN-01)`.
  </action>
  <verify>
    <automated>npx vitest run tests/restore/degrade.test.ts --project=unit 2>&1 | grep -q "FAIL\|failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - Two new rows present; Row 1 RED failure message shows a stale non-zero count in the summary; Row 2 RED failure is the escaped throw (not a fixture error)
    - Rows assert the byte-exact summary `[mrclean] restore: restored=0 unmatched=0 secret-skipped=0 sessions=0` and that it is the LAST stderr line
    - process.exit throwing-stub regression guard active in the harness used
    - Existing degrade rows (a)-(g) green
    - RED commit present: `test(11-04): ...`
  </acceptance_criteria>
  <done>The degrade-honesty contract is pinned by failing rows before the fix exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): counts=ZERO_COUNTS first-in-catch + guarded fallback write</name>
  <files>src/restore/cli.ts</files>
  <read_first>
    - src/restore/cli.ts (the catch block 211-221 — apply the minimal edit; ZERO_COUNTS at 111; the preservation list from PATTERNS: exitCode-and-return WR-03 discipline 142-159, summary-always-last 223-224, constant-shape warnings 75-77)
    - tests/state/cold-path.test.ts (rule 3 outbound allowlist — no new imports permitted; READ ONLY)
  </read_first>
  <action>
    Apply the feature implementation: `counts = ZERO_COUNTS` as the catch's first statement; inner try/catch around the fallback `process.stdout.write(input)` with the documented no-op catch comment. Zero import changes, zero flag-logic changes, zero warning-copy changes. Run: the degrade suite (new rows + (a)-(g)) green, tests/cli/restore.test.ts green (summary pin + exit discipline untouched), cold-path fence green, full `npm test`. Commit `feat(11-04): zero counts + guarded fallback in restore outer catch (IN-01)`.
  </action>
  <verify>
    <automated>npx vitest run tests/restore/degrade.test.ts tests/cli/restore.test.ts tests/state/cold-path.test.ts --project=unit && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both IN-01 rows green; degrade rows (a)-(g) green; tests/cli/restore.test.ts green unmodified (unless the harness parameterization required an additive-only change — then its summary pin is byte-identical)
    - `git diff` on src/restore/cli.ts confined to the catch block (~5 lines); no import lines touched; cold-path fence green
    - `npm run typecheck` at 38-error baseline
    - GREEN commit present: `feat(11-04): ...`
  </acceptance_criteria>
  <done>Cosmetic failure can never misreport counts or break the exit-0 degrade contract — the summary tells the truth on every path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restore CLI failure paths -> operator | the degrade report is the operator's only signal — it must never claim delivery that did not happen |
| src/restore/ -> rest of src/ | import surface must not grow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Repudiation | stale counts misreporting a failed run as successful | mitigate | counts=ZERO_COUNTS first statement in the outer catch; byte-exact summary assertion in Row 1 |
| T-11-04-02 | DoS (on the degrade contract) | fallback write rethrow breaking exit-0 one-way degrade | mitigate | Guarded fallback write with documented no-op catch; Row 2 containment assertion + throwing process.exit stub guard |
| T-11-04-03 | Elevation | fix widening restore imports / coupling into redaction | mitigate | Zero import changes; cold-path outbound allowlist fence in verify |

No package installs this phase (11-RESEARCH Package Legitimacy Audit: none).
</threat_model>

<verification>
- `npx vitest run tests/restore/degrade.test.ts tests/cli/restore.test.ts tests/state/cold-path.test.ts --project=unit` green
- `npm test` green; `npm run typecheck` at 38-error baseline
- git log shows test(11-04) commit BEFORE feat(11-04) commit (TDD gate order)
</verification>

<success_criteria>
- RED rows committed and failing for the right reasons; GREEN fix passes them; degrade matrix and summary pin unchanged-green
- The "cosmetic failure can never produce a nonzero exit" contract is now also "…and never a dishonest summary"
</success_criteria>

<output>
Create `.planning/phases/11-wire-safety-verification-hardening/11-04-SUMMARY.md` when done (record which throw seam was used and why it provably reaches the outer catch).
</output>
