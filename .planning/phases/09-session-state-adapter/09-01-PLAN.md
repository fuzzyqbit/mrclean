---
phase: 09-session-state-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/shared/types.ts
  - src/config/index.ts
  - src/config/defaults.ts
  - tests/config/reader.test.ts
  - tests/config/merge.test.ts
autonomous: true
requirements: [REVMODE-02, REVMODE-07]

must_haves:
  truths:
    - "proper-lockfile@^4.1.2 and write-file-atomic@^7.0.1 are in dependencies; @types/proper-lockfile@^4.1.4 is in devDependencies"
    - "Operator setting [reversible].ttl_hours to a non-integer or < 1 gets a fail-closed ConfigReadError naming the file and reason (D-09: ttl_hours is the ONLY new [reversible] key this phase)"
    - "Effective config exposes reversible.ttl_hours with default 24; a user-layer ttl_hours survives a partial project [reversible] table"
    - "Full suite stays green at the 642-passed / 14-skipped baseline (plus new config tests)"
  artifacts:
    - path: "package.json"
      provides: "Pinned Phase 9 runtime deps + types dev-dep"
      contains: "write-file-atomic"
    - path: "src/config/index.ts"
      provides: "validateReversibleConfig with ttl_hours clause + per-field reversible merge"
    - path: "src/config/defaults.ts"
      provides: "DEFAULT_CONFIG.reversible.ttl_hours: 24"
  key_links:
    - from: "src/config/index.ts"
      to: "src/shared/types.ts"
      via: "MrcleanReversibleConfig / MrcleanReversibleConfigLayer widened with ttl_hours"
      pattern: "ttl_hours"
    - from: "src/config/index.ts"
      to: "ConfigReadError"
      via: "fail-closed ttl_hours validation"
      pattern: "ttl_hours must be an integer"
---

<objective>
Install the two pinned Phase 9 runtime dependencies (supply-chain-gated) and widen the `[reversible]` config table with the single new knob `ttl_hours` (integer >= 1, default 24) following the shipped fail-closed + true-partial merge patterns.

Purpose: Every later plan needs these deps (`write-file-atomic` in 09-03, `proper-lockfile` in 09-05) and the TTL knob (janitor in 09-06). D-09 pins the config surface: ttl_hours is the ONLY new key this phase — no cipher choice, no path overrides.
Output: package.json with pinned deps, widened config types/validator/merge/defaults, extended config test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09-session-state-adapter/09-CONTEXT.md (D-09)
@.planning/phases/09-session-state-adapter/09-RESEARCH.md (§Package Legitimacy Audit, §Code Example 5)
@.planning/phases/09-session-state-adapter/09-PATTERNS.md (§src/config/index.ts, §package.json sections)

<interfaces>
<!-- Current shipped shapes the executor extends. Verified 2026-07-17. -->

From src/shared/types.ts (lines ~354-372):
```typescript
export interface MrcleanReversibleConfig {
  enabled: boolean
}
export interface MrcleanReversibleConfigLayer {
  enabled?: boolean
}
```

From src/config/index.ts (lines ~335-343) — extend in place:
```typescript
function validateReversibleConfig(raw: unknown, filePath: string): MrcleanReversibleConfigLayer {
  if (!isRecord(raw)) {
    throw new ConfigReadError(filePath, '[reversible] must be a TOML sub-table')
  }
  if (raw['enabled'] !== undefined && typeof raw['enabled'] !== 'boolean') {
    throw new ConfigReadError(filePath, '[reversible].enabled must be a boolean')
  }
  return 'enabled' in raw ? { enabled: raw['enabled'] as boolean } : {}
}
```

From src/config/index.ts (lines ~539-551) — current merge branch to replace with per-field accumulation:
```typescript
let reversible: MrcleanReversibleConfig = { enabled: DEFAULT_CONFIG.reversible.enabled }
// ...
if (layer.reversible?.enabled !== undefined) {
  reversible = { enabled: layer.reversible.enabled }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned deps (proper-lockfile, write-file-atomic, @types/proper-lockfile)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (verify current state: 10 dependencies, 7 devDependencies, NONE of the three packages present — research installs were reverted)
    - .planning/phases/09-session-state-adapter/09-RESEARCH.md §Package Legitimacy Audit (slopcheck [OK] x3, no postinstall scripts) and §Standard Stack (version pins + engines rationale)
  </read_first>
  <action>
    Run `npm install proper-lockfile@^4.1.2 write-file-atomic@^7.0.1` then `npm install -D @types/proper-lockfile@^4.1.4` (per STATE.md open-todo pins and T-09-SC below). CRITICAL PIN: write-file-atomic stays at `^7.0.1` — v8 engines (`^22.22.2 || ^24.15.0 || >=26`) break the project's Node `>=20.18.0` floor; record this rationale in the commit message so renovate/dependabot guards have a paper trail. `@types/proper-lockfile` goes in devDependencies (RESEARCH flagged it misplaced during the transient research install — do not repeat that). Do NOT run `npm audit fix`: the 5 reported vulnerabilities are pre-existing vite-transitive dev findings and the deprecated `boolean@3.2.0` warning is unrelated transitive noise — both out of scope (chore, not Phase 9). Do not touch any other dependency.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok = p.dependencies['proper-lockfile']==='^4.1.2' && p.dependencies['write-file-atomic']==='^7.0.1' && p.devDependencies['@types/proper-lockfile']==='^4.1.4' && !p.dependencies['@types/proper-lockfile']; process.exit(ok?0:1)" && npm ls proper-lockfile write-file-atomic</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` contains exactly `"proper-lockfile": "^4.1.2"` and `"write-file-atomic": "^7.0.1"`; `devDependencies` contains `"@types/proper-lockfile": "^4.1.4"`
    - `@types/proper-lockfile` is NOT in `dependencies`
    - `npm ls proper-lockfile write-file-atomic` resolves 4.1.2 and 7.0.1 with no invalid/missing markers
    - `npm test` remains at baseline: 642 passed / 14 skipped, 0 failures
    - No other package.json fields changed (files[], scripts, engines untouched)
  </acceptance_criteria>
  <done>Three packages installed at pinned versions in correct sections; lockfile updated; suite baseline green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen [reversible] with ttl_hours (types, validator, merge, defaults)</name>
  <files>src/shared/types.ts, src/config/index.ts, src/config/defaults.ts, tests/config/reader.test.ts, tests/config/merge.test.ts</files>
  <read_first>
    - src/config/index.ts (validateReversibleConfig ~335-343; validatePiiNerConfig conditional-spread idiom ~278-290; merge branch ~539-551; LoadConfigOpts ~606-611)
    - src/shared/types.ts (MrcleanReversibleConfig + Layer ~354-372 and their JSDoc "Phase 9 owns store-schema fields" note)
    - src/config/defaults.ts (frozen reversible sub-object ~70-73)
    - tests/config/reader.test.ts (Test B fail-closed shape ~144-162; Test C unknown-keys ~164-176; Test I true-partial ~178-188)
    - tests/config/merge.test.ts (Test G partial-table survival ~147-159; tmpdir fixture ~112-123)
    - .planning/phases/09-session-state-adapter/09-RESEARCH.md §Code Example 5 (verified ttl_hours validation clause)
  </read_first>
  <behavior>
    - Test: `[reversible]\nttl_hours = 0` => rejects with ConfigReadError, reason exactly `[reversible].ttl_hours must be an integer >= 1`, path = the config file path
    - Test: `ttl_hours = "24"` (string) => same ConfigReadError reason
    - Test: `ttl_hours = 1.5` (non-integer) => same ConfigReadError reason
    - Test: `ttl_hours = 48` => layer parses to `{ ttl_hours: 48 }` (true partial — `enabled` key ABSENT, not undefined-assigned)
    - Test: `[reversible]` with only unknown keys => parses to `{}` (Test C mirror)
    - Test: default effective config has `reversible.ttl_hours === 24` and `reversible.enabled === false`
    - Test: user layer `enabled = true, ttl_hours = 48` + project layer `[reversible]` partial table WITHOUT ttl_hours => effective keeps `ttl_hours: 48` and `enabled: true` (Test G mirror — partial layer never resets accumulated fields)
    - Test: project layer `ttl_hours = 2` overrides user `ttl_hours = 48` (last-wins-when-set)
  </behavior>
  <action>
    Follow the 08-01 fail-closed + 08-07 true-partial precedents exactly (per D-09). (1) src/shared/types.ts: add `ttl_hours: number` (required) to MrcleanReversibleConfig with JSDoc "Orphan-sweep TTL in hours; integer >= 1; default 24 (D-09)"; add `ttl_hours?: number` to MrcleanReversibleConfigLayer; update the "Phase 9 owns store-schema fields (ttl_hours etc.)" JSDoc to note this ownership has landed. (2) src/config/index.ts validateReversibleConfig: add the ttl_hours clause per RESEARCH Code Example 5 (reject non-number, non-integer via Number.isInteger, and < 1 with reason `[reversible].ttl_hours must be an integer >= 1`); switch the return to the conditional-spread idiom from validatePiiNerConfig so both keys stay true-partial. (3) merge branch: seed `let reversible` from both DEFAULT_CONFIG.reversible fields; replace the enabled-only branch with per-field `??` accumulation guarded by `if (layer.reversible !== undefined)` — new object each time, never a layer alias (immutability rule). (4) src/config/defaults.ts: frozen reversible sub-object gains `ttl_hours: 24` with the D-09 comment. (5) Making ttl_hours required may surface fixture objects that construct MrcleanReversibleConfig in tests or doctor code — run `npm run typecheck`, fix ONLY newly-surfaced sites by adding `ttl_hours: 24`, and keep the differential error count at the 36-error baseline (no new errors). Unknown `[reversible]` keys stay tolerated-and-dropped. RED first: write the eight behavior tests into tests/config/reader.test.ts (validator cases) and tests/config/merge.test.ts (default/merge cases) following the existing Test B/C/G/I shapes with AAA comments; run them, confirm they fail; then implement.
  </action>
  <verify>
    <automated>npx vitest run tests/config --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/config --project=unit` passes with the 8 new behavior tests present
    - grep `"ttl_hours must be an integer >= 1"` matches exactly one source line in src/config/index.ts
    - grep `ttl_hours: 24` matches src/config/defaults.ts
    - Effective-config test proves `loadEffectiveConfig` yields `reversible: { enabled: false, ttl_hours: 24 }` with no config files present
    - `npm run typecheck` error count <= 36-error baseline (differential gate, deferred-items.md discipline)
    - `npm test` fully green (unit + integration)
  </acceptance_criteria>
  <done>ttl_hours is validated fail-closed, merges true-partially across layers, defaults to 24, and the full suite + differential typecheck gate pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules | third-party code enters the runtime tree at install |
| operator TOML -> effective config | untrusted config values cross into janitor TTL math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering (supply chain) | proper-lockfile@^4.1.2, write-file-atomic@^7.0.1, @types/proper-lockfile@^4.1.4 | mitigate | All three slopcheck [OK] in RESEARCH §Package Legitimacy Audit (npm-org / moxystudio / DefinitelyTyped repos, no postinstall scripts, verified via `npm view` 2026-07-17). Versions pinned; wfa major PINNED at ^7 (v8 breaks Node 20 floor). No [ASSUMED]/[SUS] packages — no blocking checkpoint required. |
| T-09-01-01 | Tampering | validateReversibleConfig | mitigate | Fail-closed ConfigReadError on wrong-typed ttl_hours (integer >= 1) — a hostile/typo'd project config cannot set ttl to 0/negative/NaN to force instant orphan deletion or infinite retention |
| T-09-01-02 | Denial of service | mergeConfigs reversible branch | mitigate | Per-field ?? accumulation means a partial project table cannot clear a user opt-in (CR-01 regression class); tested by the Test G mirror |
</threat_model>

<verification>
- `npm test` green (642+new passed / 14 skipped, 0 failures)
- `npm run typecheck` differential vs 36-error baseline: zero new errors
- `git diff --stat tests/placeholder tests/detect tests/audit` is empty (byte-identical one-way discipline)
</verification>

<success_criteria>
- Pinned deps installed in correct package.json sections; lockfile committed
- ttl_hours validated fail-closed, defaulted to 24, merged per-field true-partially
- All acceptance criteria of both tasks met
</success_criteria>

<output>
Create `.planning/phases/09-session-state-adapter/09-01-SUMMARY.md` when done.
</output>
