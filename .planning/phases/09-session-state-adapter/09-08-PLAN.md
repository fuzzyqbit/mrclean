---
phase: 09-session-state-adapter
plan: 08
type: execute
wave: 5
depends_on: [09-06, 09-07]
files_modified:
  - tsup.config.ts
  - vitest.config.ts
  - tests/state/fixtures/stress-worker.ts
  - tests/state/stress.test.ts
  - tests/state/chaos.test.ts
  - tests/state/inspection.test.ts
  - src/doctor/checks.ts
  - tests/doctor/checks.test.ts
autonomous: true
requirements: [REVMODE-02, REVMODE-04]

must_haves:
  truths:
    - "16 real spawned processes allocating 20 shared + 5 unique originals each lose ZERO entries, produce ZERO NNN duplicates, ZERO cross-process placeholder disagreements, and ZERO degrade fallbacks (SC5, Phase 11 stress gate built-against now)"
    - "Corrupt / missing / chmod'd map => hook output identical to one-way mode at handler level (SC5 second half, chaos-shaped)"
    - "Operator inspection holds: sessions/ contains ciphertext-only files, keys live in the separate 0700 keys/ dir, nothing state-related inside the project tree (SC1 literal dir-inspection test)"
    - "Doctor check-8 enabled copy no longer says 'plumbing only'; the paired test asserts the new constant byte-for-byte"
    - "Full suite green with ZERO edits to detection/placeholder/audit suites; differential typecheck at the 36-error baseline; dist rebuild landed as a separate chore commit"
  artifacts:
    - path: "tests/state/stress.test.ts"
      provides: "SC5 16-process integration stress gate"
      min_lines: 80
    - path: "tests/state/fixtures/stress-worker.ts"
      provides: "spawn-barrier worker exercising the REAL facade + manager per transaction"
      min_lines: 60
    - path: "tests/state/chaos.test.ts"
      provides: "corrupt/missing/chmod/dir/delete-mid-session one-way-parity proofs"
      min_lines: 80
    - path: "tests/state/inspection.test.ts"
      provides: "SC1 operator dir-inspection as a literal test"
      min_lines: 50
  key_links:
    - from: "tsup.config.ts"
      to: "tests/state/fixtures/stress-worker.ts"
      via: "TEST-ONLY entry 'state-stress-worker' (detect-layer1 precedent; package.json files[] enumeration already excludes new dist artifacts)"
      pattern: "state-stress-worker"
    - from: "tests/state/stress.test.ts"
      to: "dist/state-stress-worker.js"
      via: "spawn(process.execPath, [WORKER, base, id, goFile, deadline]) x16 with go-file barrier"
      pattern: "spawn"
    - from: "src/doctor/checks.ts"
      to: "tests/doctor/checks.test.ts"
      via: "REVERSIBLE_DETAIL_ENABLED constant asserted byte-for-byte (Test 16)"
      pattern: "reversible mode: enabled"
---

<objective>
Prove the adapter holds at integration level against the named Phase 11 gates (SC5: 8-16-process stress with zero loss/dup/disagreement/degrade; chaos: corrupt/missing/chmod'd map treated as absent with redaction provably unaffected), make SC1's operator-inspection framing a literal test, update the doctor check-8 copy now the adapter is real, and close the phase with the byte-identical + differential-typecheck + dist-chore regression gates.

Purpose: Phase 11 elevates these to CI gates; Phase 9 must already pass them at integration level (ROADMAP SC5, CONTEXT specifics). The degrade-count assertion is load-bearing — a stress pass that quietly lost entries to process-local fallback must FAIL (Pitfall 3).
Output: stress worker + harness, chaos + inspection suites, doctor copy update, phase-gate verification, separate dist chore commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-session-state-adapter/09-CONTEXT.md (§Specific Ideas — SC1/SC3/SC5 literal test shapes)
@.planning/phases/09-session-state-adapter/09-RESEARCH.md (§Code Example 4, §Pitfall 3, §Validation Architecture)
@.planning/phases/09-session-state-adapter/09-PATTERNS.md (§tsup.config.ts, §vitest.config.ts, §stress harness, §doctor checks)
@src/state/index.ts, @src/state/map-store.ts (the surface under stress)
@tsup.config.ts
@vitest.config.ts
@src/doctor/checks.ts (constants ~557-560, checkReversibleState ~577-597)
@tests/doctor/checks.test.ts (Test 16 ~505-517)
@tests/hook/integration-detection.globalSetup.ts (existing build-before-integration setup)

<interfaces>
<!-- Consumed contracts — no exploration needed. -->

```typescript
// stress worker drives (from dist-bundled src/state + src/placeholder):
readSessionMapForHydration({ sessionId, baseDir })
new PlaceholderManager({ sessionId }) + hydrateReversible(h) + allocate(value, type) + drainPendingAllocations()
persistAllocations({ sessionId, baseDir, pending, deadlineMs })

// worker argv contract (pin exactly): baseDir, workerId, goFile, deadlineMs
// worker stdout (single JSON line): { workerId: number, results: Record<originalValue, finalPlaceholder>, degradeCount: number }
// worker loop: for each of its 25 originals (20 SHARED constants + 5 unique `w<id>-original-<n>`):
//   hydrate fresh -> allocate ONE value -> drain -> persist -> record final placeholder (after applying returned renames to the provisional)
//   status 'degraded' => degradeCount++
// SHARED originals alternate types: even index 'WORD', odd index 'AWS_KEY' (secret-floor coverage under stress)

// doctor copy (pin the new constant exactly):
const REVERSIBLE_DETAIL_ENABLED = 'reversible mode: enabled — encrypted session state adapter active'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Stress worker fixture + build/test config + SC5 stress gate</name>
  <files>tsup.config.ts, vitest.config.ts, tests/state/fixtures/stress-worker.ts, tests/state/stress.test.ts</files>
  <read_first>
    - tsup.config.ts (detect-layer1 TEST-ONLY entry shape ~6-13 + banner/createRequire note)
    - vitest.config.ts (unit exclude / integration include dual-listing dance + the pii-canary-leak precedent comments; integration project settings: fileParallelism false, globalSetup)
    - .planning/phases/09-session-state-adapter/09-RESEARCH.md §Code Example 4 (validated harness shape: barrier, assertions, reference numbers 400 txns/117ms/0-0-0)
    - src/state/index.ts + src/placeholder/manager.ts (exact call signatures the worker uses)
    - package.json (files[] explicit enumeration — confirm new dist artifacts are auto-excluded; do NOT edit files[])
  </read_first>
  <action>
    (1) tsup.config.ts: add entry `'state-stress-worker': 'tests/state/fixtures/stress-worker.ts'` with the same TEST-ONLY comment discipline as detect-layer1 (not shipped — files[] enumeration excludes it; say so in the comment). (2) vitest.config.ts: add 'tests/state/stress.test.ts' to the unit project's exclude array AND to the integration project's include array (explicit allow-list entry with the non-vacuity comment precedent). (3) Worker per the pinned argv/stdout/loop contract — uses ONLY public facade + manager APIs with baseDir injection and a fixed valid UUID sid constant shared with the test; polls goFile with a short sleep loop as the start barrier. (4) tests/state/stress.test.ts (integration project, timeout 60_000): create tmpdir base + goFile; spawn 16 workers via `spawn(process.execPath, [dist/state-stress-worker.js, base, i, goFile, '150'])`; settle ~200ms; write goFile; collect all stdout JSON. Assertions (every one mandatory): decrypted final map (via map-store readSessionMapFile with the test's key material path) has exactly 20 + 16*5 = 100 entries; map.counter === 100; the set of NNN placeholders has 100 distinct values (OVF exempt — counts here never reach 999, assert NO OVF present); every worker reports the SAME placeholder for each of the 20 shared originals; sum of degradeCount === 0 (Pitfall 3 load-bearing assertion); secret-class shared originals ('AWS_KEY'-typed) lack `original` in the decrypted map. deadlineMs 150 matches the RESEARCH-validated reference run (production constant stays 100 — comment this). Commit `test(09-08): add 16-process stress gate + worker fixture`.
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/state/stress.test.ts --project=integration</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/state/stress.test.ts --project=integration` passes with all six assertion families present in source
    - `npx vitest run tests/state --project=unit` still green (stress excluded from unit — no vacuous double-run)
    - dist/state-stress-worker.js exists after build; package.json files[] unchanged (git diff shows no files[] hunk)
    - Worker source contains no direct node:crypto cipher usage (goes through the facade only)
  </acceptance_criteria>
  <done>SC5 stress holds: 0 lost / 0 dups / 0 disagreements / 0 degrades across 16 real processes.</done>
</task>

<task type="auto">
  <name>Task 2: Chaos parity suite + SC1 operator-inspection test</name>
  <files>tests/state/chaos.test.ts, tests/state/inspection.test.ts</files>
  <read_first>
    - .planning/phases/09-session-state-adapter/09-CONTEXT.md §Specific Ideas (SC1 literal `ls -la` framing + SC5 corrupt/missing/chmod parity)
    - .planning/phases/09-session-state-adapter/09-RESEARCH.md §Pitfall 6 (failure classes to cover) + §Validation Architecture (chaos cases stay unit-level this phase; Phase 11 elevates)
    - src/hook/handlers/post-tool-use.ts (the handler driven for parity — single-string path is the simplest full-stack probe)
    - tests/state/map-store.test.ts (09-03's corruption fixtures — REUSE builder helpers by import or small duplication, do not re-derive offsets)
    - tests/hook/reversible-handlers.test.ts (09-07's mock seam — chaos parity uses REAL state modules, only config/homedir seams stubbed)
  </read_first>
  <action>
    chaos.test.ts (unit project, real src/state modules, tmpdir): for each chaos shape — (a) corrupt byte mid-envelope, (b) truncated envelope, (c) chmod 000 map (POSIX-only, win32 skip comment), (d) map path is a directory, (e) map deleted between hydrate and persist, (f) garbage key file — drive handlePostToolUse twice with identical input containing one detectable secret: once with reversible DISABLED (baseline) and once ENABLED against the chaos fixture, stubbing HOME to the tmpdir (vi.stubEnv('HOME', ...) — os.homedir() reads env HOME on POSIX; 08-12 fresh-HOME precedent) and writing a minimal user config TOML enabling [reversible]. Assert: BOTH responses are deep-equal EXCEPT the enabled run may carry v2-format tokens where the baseline has v1 (normalize by regex-stripping the `:nonce8` tail before comparing — the SHAPE and count must match exactly, additionalContext counts equal, never a thrown error, never a deny). Case (e) additionally asserts persist returned degraded-or-ok without throwing and the emitted output still substituted every secret. inspection.test.ts: run a real two-event reversible flow against tmp base (facade-level with baseDir injection — allocate WORD + AWS_KEY originals, persist), then literally inspect: readdir(sessionsDir) => only `<sid>.map` (no *.tmp residue); every file's raw bytes lack the two planted originals and '"entries"'; readdir(keysDir) => `<sid>.key` only; stat modes 0700/0700/0600/0600 (POSIX, win32 skip); recursive scan of process.cwd() finds NO new .mrclean/keys|sessions dirs (nothing inside the project tree — SC1 clause 3). Commit `test(09-08): add chaos parity + SC1 inspection suites`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/chaos.test.ts tests/state/inspection.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - Six chaos shapes each assert one-way-parity (shape-equal output, zero throws, zero denies) — SC5 second half at handler level
    - inspection.test.ts asserts: ciphertext-only bytes, no tmp residue, separate key dir, POSIX modes, nothing in project tree
    - Both suites green in the unit project; full `npm test` green
  </acceptance_criteria>
  <done>Corrupt/missing/chmod'd map provably leaves redaction identical to one-way; SC1 inspection is a literal executable test.</done>
</task>

<task type="auto">
  <name>Task 3: Doctor check-8 copy + phase regression gate + dist chore commit</name>
  <files>src/doctor/checks.ts, tests/doctor/checks.test.ts</files>
  <read_first>
    - src/doctor/checks.ts (REVERSIBLE_DETAIL_* constants ~557-560; checkReversibleState ~577-597 — stays REPORTING-ONLY, FAIL-loud is Phase 10/REVMODE-12)
    - tests/doctor/checks.test.ts (Test 16 ~505-517 enabled copy; Test 15 ~482-497 disabled copy — untouched; SKIP case ~537)
    - .planning/phases/08-contract-verification-reversible-plumbing/deferred-items.md (36-error typecheck baseline + differential gate discipline; dist-rebuild-as-chore precedent)
  </read_first>
  <action>
    (1) Update ONLY the REVERSIBLE_DETAIL_ENABLED constant to exactly `reversible mode: enabled — encrypted session state adapter active` (state-only copy — no paths, no config values, T-08-08 discipline); update Test 16's expected string byte-for-byte in the same commit; Tests 15 + SKIP untouched; check remains reporting-only (never FAIL). (2) Phase regression gate, run and record results in SUMMARY: `npm test` fully green (unit + integration incl. stress); `npm run test:coverage` meets 80/80/75/70; `git diff --stat <phase-start-ref> -- tests/placeholder tests/detect tests/audit` EMPTY (byte-identical one-way proof across the whole phase — the SC2-style discipline); `npm run typecheck` error count <= 36 baseline. (3) Separate dist chore commit LAST: `npm run build`, commit dist/* changes (including new dist/state-stress-worker.js artifacts) alone as `chore(09-08): rebuild dist`. Feature/test changes must NOT ride the chore commit.
  </action>
  <verify>
    <automated>npx vitest run tests/doctor/checks.test.ts --project=unit && npm test && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep `plumbing only` matches NOTHING under src/ (old copy gone); new constant matches the pinned string in both checks.ts and checks.test.ts
    - `npm test` green; `npm run test:coverage` thresholds met; typecheck error count <= 36
    - `git log --oneline -1` on the final commit shows a dist-only chore commit (dist/* paths only)
    - Zero edits across the phase to tests/placeholder, tests/detect, tests/audit (git diff vs phase start)
  </acceptance_criteria>
  <done>Doctor honest about the real adapter; phase-wide regression gates green; dist rebuilt as its own chore commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 16 concurrent untrusted-order processes -> one map file | the stress gate proves the mutex + reconcile hold under real contention |
| corrupted on-disk state -> hook output | chaos parity proves the fail-open-to-one-way wall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Tampering | concurrent allocation integrity | mitigate | Stress gate asserts zero lost entries, zero NNN dups, counter integrity, cross-process identity — with the zero-degrade assertion so silent fallback cannot fake a pass (Pitfall 3) |
| T-09-08-02 | Denial of service | corrupted map blocking tools | mitigate | Chaos parity asserts no throw/no deny and shape-identical output for all six corruption classes (SC5; Phase 11 chaos gate built-against) |
| T-09-08-03 | Information disclosure | plaintext residue on disk | mitigate | Inspection test scans raw bytes of every sessions/ file (incl. tmp residue check) for planted originals and JSON markers (SC1) |
| T-09-08-04 | Information disclosure | doctor copy leaking state internals | mitigate | New enabled copy is state-only (no paths/values); byte-for-byte test lock prevents drift |
| T-09-08-05 | Tampering | test-only worker shipping to npm | mitigate | package.json files[] explicit enumeration excludes new dist artifacts; acceptance criterion asserts files[] unchanged |
| T-09-08-06 | Denial of service | CI timing variance vs 150ms stress deadline | accept | Deadline matches the RESEARCH-validated reference (400 txns/117ms, 0 timeouts); A1 assumption notes Linux CI re-measures; if flaky, tune deadline constant on perf-gate evidence only — never by removing the zero-degrade assertion |
</threat_model>

<verification>
- `npm run build && npx vitest run tests/state/stress.test.ts --project=integration` green
- `npx vitest run tests/state/chaos.test.ts tests/state/inspection.test.ts tests/doctor/checks.test.ts --project=unit` green
- `npm test` + coverage + differential typecheck green; dist chore commit isolated
</verification>

<success_criteria>
- SC5 proven at integration level (stress 0/0/0/0 + chaos parity) — Phase 11 gates have a passing substrate
- SC1 proven by literal inspection test
- Phase closes with byte-identical one-way proof, coverage, typecheck baseline, and clean dist chore commit
</success_criteria>

<output>
Create `.planning/phases/09-session-state-adapter/09-08-SUMMARY.md` when done (record stress numbers: entries/dups/disagreements/degrades + burst wall-time).
</output>
