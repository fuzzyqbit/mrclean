---
phase: 09-session-state-adapter
plan: 04
type: tdd
wave: 2
depends_on: [09-02]
files_modified:
  - src/placeholder/manager.ts
  - src/detect/index.ts
  - tests/state/tokens-v2.test.ts
autonomous: true
requirements: [REVMODE-05]

must_haves:
  truths:
    - "A hydrated (reversible) manager allocates v2 tokens `<MRCLEAN:TYPE:NNN:nonce8>` using the session nonce; a default-constructed manager still emits v1 `<MRCLEAN:TYPE:NNN>` byte-identically"
    - "tests/placeholder/manager.test.ts passes with ZERO edits (byte-identical v1 proof, Phase 8 SC2 discipline)"
    - "Hydrated store entries win: allocating a value whose HMAC is in entriesByHmac returns the store placeholder without burning a counter or creating a pending entry"
    - "New reversible allocations are recorded as PendingAllocation and drainable exactly once per event"
  artifacts:
    - path: "src/placeholder/manager.ts"
      provides: "hydrateReversible/drainPending seam + v2 allocation branch; v1 formatter lines untouched"
      contains: "hydrateReversible"
    - path: "src/detect/index.ts"
      provides: "hydrateSessionManager / drainSessionAllocations pass-through exports on the cached-manager seam (additive only)"
      exports: ["hydrateSessionManager", "drainSessionAllocations"]
    - path: "tests/state/tokens-v2.test.ts"
      provides: "REVMODE-05 v2-format + hydration-semantics proof"
      min_lines: 80
  key_links:
    - from: "src/placeholder/manager.ts"
      to: "src/state/session-map.ts"
      via: "TYPE-ONLY import of ReversibleHydration/PendingAllocation (erased at runtime — cold-path safe)"
      pattern: "import type .* from '../state/session-map.js'"
    - from: "src/detect/index.ts"
      to: "src/placeholder/manager.ts"
      via: "getOrCreateManager(sessionId).hydrateReversible(h) inside the new exports"
      pattern: "hydrateSessionManager"
---

<objective>
Layer the v2 token path onto PlaceholderManager as an additive, hydration-gated branch: `hydrateReversible(h)` installs per-session nonce/counter-floor/HMAC lookup (all injected as data+closures from the future facade — the manager never imports state runtime), `allocate()` short-circuits into the v2 path only when hydrated, and new allocations accumulate as drainable PendingAllocations. Expose the seam to handlers via two additive exports on src/detect/index.ts (which owns the cached-manager map).

Purpose: REVMODE-05 — reversible sessions issue session-tagged v2 tokens while the one-way default keeps the shipped v1 format and code path byte-identical. The design constraint discovered in PATTERNS is load-bearing: allocate() is SYNCHRONOUS inside runDetection's substitution path, so store I/O cannot live here — hydrate-before / reconcile-after (09-05) is the only shape that works.
Output: manager v2 seam + detect pass-through exports + tokens-v2 suite; zero edits to v1 suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-session-state-adapter/09-CONTEXT.md (D-10)
@.planning/phases/09-session-state-adapter/09-RESEARCH.md (§Pattern 4, §Pitfall 7)
@.planning/phases/09-session-state-adapter/09-PATTERNS.md (§src/placeholder/manager.ts)
@src/placeholder/manager.ts
@src/detect/index.ts (getOrCreateManager ~156-173; allocate call site ~295-297)
@src/state/session-map.ts (ReversibleHydration/PendingAllocation contracts from 09-02)

<interfaces>
<!-- What this plan ADDS. Existing v1 surface (allocate/getByPlaceholder/size) unchanged. -->

```typescript
// src/placeholder/manager.ts — additive members
class PlaceholderManager {
  // existing: allocate(value, type), getByPlaceholder(ph), size()
  hydrateReversible(h: ReversibleHydration): void
    // installs/refreshes v2 state: nonce+closures, counter = Math.max(this.counter, h.counterFloor),
    // v2 lookup cache seeded from h.entriesByHmac; clears any stale pending (defensive)
  drainPendingAllocations(): PendingAllocation[]
    // returns accumulated NEW allocations since hydrate/last drain; clears the buffer
}

// src/detect/index.ts — additive exports (type-only imports from ../state/session-map.js)
export function hydrateSessionManager(sessionId: string, h: ReversibleHydration): void
export function drainSessionAllocations(sessionId: string): PendingAllocation[]
```

From src/state/session-map.ts (09-02 — consume, do not redefine):
```typescript
export interface ReversibleHydration {
  nonce8: string
  counterFloor: number
  entriesByHmac: ReadonlyMap<string, { placeholder: string; type: string }>
  hmacOf: (value: string) => string
  formatToken: (type: string, counter: number) => string
}
export interface PendingAllocation { value: string; type: string; provisionalPlaceholder: string }
```
</interfaces>
</context>

<feature>
  <name>v2 token layer with hydration seam (v1 byte-identical)</name>
  <files>src/placeholder/manager.ts, src/detect/index.ts, tests/state/tokens-v2.test.ts</files>
  <behavior>
    Test hydration fixture: nonce8 'aabbccdd', counterFloor 0, empty entriesByHmac, hmacOf = real hmacAddress closure over a fixed salt, formatToken = formatV2Token closure over nonce8 (both from session-map.ts).
    Cases:
    - hydrated manager, allocate('secret-x', 'AWS_KEY') => placeholder '<MRCLEAN:AWS_KEY:001:aabbccdd>' matching V2_TOKEN_RE; one PendingAllocation { value: 'secret-x', type: 'AWS_KEY', provisionalPlaceholder: '<MRCLEAN:AWS_KEY:001:aabbccdd>' }
    - same value allocated twice => same placeholder, counter advanced once, exactly ONE pending entry (PH-02 stability via HMAC cache)
    - same value under DIFFERENT type => same-placeholder semantics follow content addressing (D-10 "same original (any TYPE) → identical placeholder"): second allocate returns the first placeholder, no new pending
    - hydration with entriesByHmac pre-seeded for value 'known' => allocate('known', 'WORD') returns the store placeholder, counter NOT advanced, NO pending entry
    - counterFloor 5 => first new allocation is NNN '006'
    - counterFloor 999 => next allocation is '<MRCLEAN:TYPE:OVF:aabbccdd>' and the overflow stderr warn fires once (same JSON shape as v1: warn 'mrclean placeholder overflow')
    - drainPendingAllocations returns the buffer and clears it; second drain => []
    - re-hydration (fresh h with higher counterFloor + seeded entries) refreshes state: previously-pending values now present in entriesByHmac resolve to the seeded (final) placeholders
    - DEFAULT manager (no hydrate): allocate('v', 'WORD') => '<MRCLEAN:WORD:001>' (v1); drainPendingAllocations returns [] and does not throw
    - getByPlaceholder works for v2 placeholders (reverse lookup registered)
  </behavior>
  <implementation>
    Pitfall 7 is the constraint: the v1 counter/formatter block (manager.ts lines 77-97) gets ZERO diff hunks. Implement the v2 branch as an early return at the TOP of allocate(): `if (this.reversible !== null) return this.allocateReversible(value, type)` — private method below the v1 body. allocateReversible: hmac = h.hmacOf(value); consult a private v2 cache Map<hmacHex, PlaceholderEntry> (seeded at hydrate from entriesByHmac; entries built with index parsed from the placeholder NNN or 0 for OVF, hash = hmac, firstSeenTs = now); hit => return cached entry; miss => this.counter++, placeholder = h.formatToken(type, this.counter) (formatToken owns the OVF branch), reuse the existing overflowed-flag warn-once shape for counter > 999 (same stderr JSON message text as v1), build entry with hash = hmac (document: v2 keys by HMAC, not sha256 — the v1 byHash map is NOT consulted or populated on this path), register in byPlaceholder for reverse lookup, push { value, type, provisionalPlaceholder: placeholder } onto the pending buffer. hydrateReversible sets counter = Math.max(current, counterFloor) (monotonic under in-process degrade), replaces the v2 cache wholesale (immutability: build a new Map), clears pending. Type-only imports ONLY: `import type { ReversibleHydration, PendingAllocation } from '../state/session-map.js'` — the 09-07 import-graph test distinguishes type-only (allowed) from runtime static (banned). In src/detect/index.ts add the two pass-through exports delegating to getOrCreateManager(sessionId) — additive, no change to runDetection steps, and the imports there are type-only as well.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing tokens-v2 suite</name>
  <files>tests/state/tokens-v2.test.ts</files>
  <read_first>
    - src/placeholder/manager.ts (whole file — current v1 contract and the DO-NOT-TOUCH zone lines 77-97)
    - tests/placeholder/manager.test.ts (existing stderr-spy pattern ~64-87 and naming conventions — this suite must keep passing with ZERO edits)
    - src/state/session-map.ts (hmacAddress/formatV2Token/V2_TOKEN_RE to build the hydration fixture with real closures)
  </read_first>
  <action>
    Create tests/state/tokens-v2.test.ts with every behavior case above. Build ReversibleHydration fixtures from real session-map exports (hmacAddress closure over a fixed 64-hex salt, formatV2Token closure over 'aabbccdd'). Include the drain-twice, re-hydration-refresh, and default-manager-v1 cases, plus the stderr-spy OVF warn-once case copying the manager.test.ts spy shape. Confirm failure (hydrateReversible not a function). Commit `test(09-04): add failing v2 token layer suite`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/tokens-v2.test.ts --project=unit 2>&1 | grep -q "FAIL\|not a function\|failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - Suite covers >= 10 cases including v2 format regex match, PH-02-via-HMAC, cross-TYPE identity, store-hit no-burn, counterFloor offset, OVF + warn-once, drain semantics, re-hydration refresh, v1 default path, reverse lookup
    - RED commit present: `test(09-04): ...`
  </acceptance_criteria>
  <done>Failing suite pins the v2 seam contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement the v2 branch + detect pass-through exports</name>
  <files>src/placeholder/manager.ts, src/detect/index.ts</files>
  <read_first>
    - tests/state/tokens-v2.test.ts (the contract)
    - src/detect/index.ts (getOrCreateManager ~156-173 — the seam the new exports wrap; confirm no other edit needed)
    - .planning/phases/09-session-state-adapter/09-PATTERNS.md §src/placeholder/manager.ts (the load-bearing sync-allocate constraint + hydrate/serialize seam description)
  </read_first>
  <action>
    Implement per the feature implementation notes. HARD CONSTRAINTS: (1) `git diff src/placeholder/manager.ts` must show NO hunk touching lines 77-97's v1 counter/format/overflow block — v2 is an early return above it plus new private members below; (2) no runtime import from ../state/ in either file (type-only `import type`); (3) `tests/placeholder/manager.test.ts` passes with zero edits. In detect/index.ts place the two exports next to getOrCreateManager with JSDoc noting they are the Phase 9 reversible seam consumed only by handlers under `config.reversible.enabled`. Run both suites green. Commit `feat(09-04): implement v2 token layer with hydration seam`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/tokens-v2.test.ts tests/placeholder --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - tests/state/tokens-v2.test.ts green; tests/placeholder/manager.test.ts green with `git diff --stat tests/placeholder/` EMPTY
    - `git diff src/placeholder/manager.ts` contains no changes to the literal lines "placeholder = `<MRCLEAN:${type}:OVF>`" or "placeholder = `<MRCLEAN:${type}:${String(this.counter).padStart(3, '0')}>`"
    - grep confirms `import type` (not bare `import`) for every ../state/ reference in src/placeholder/manager.ts and src/detect/index.ts
    - `npm run typecheck` <= 36-error baseline; `npm test` fully green
    - GREEN commit present: `feat(09-04): ...`
  </acceptance_criteria>
  <done>v2 seam live and drainable; v1 path byte-identical by construction and by suite proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR, if needed): Consolidate v2 private state</name>
  <files>src/placeholder/manager.ts</files>
  <read_first>
    - src/placeholder/manager.ts (as implemented)
  </read_first>
  <action>
    Only if warranted (e.g. group v2 fields into one private object, extract entry-builder helper). v1 zone stays untouched. Re-run both suites. Commit only if changed: `refactor(09-04): clean up v2 allocation branch`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/tokens-v2.test.ts tests/placeholder --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - Both suites green, zero test edits, v1 lines still untouched
  </acceptance_criteria>
  <done>2-3 atomic commits present per TDD gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detection values -> pending buffer | raw originals held in memory awaiting the 09-05 persist (never logged) |
| store-fed hydration -> allocation decisions | a hostile map could seed placeholders — bounded by 09-03's GCM auth + schema guards upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information disclosure | v2 tokens | mitigate | Per-session CSPRNG nonce8 in every token via formatToken closure — planted-token enumeration killed (D-10) |
| T-09-04-02 | Information disclosure | pending buffer / warns | mitigate | PendingAllocation values live in memory only; the only stderr emission is the existing hash-free overflow warn (counter + sessionId, no values) |
| T-09-04-03 | Tampering | v1 path regression | mitigate | Early-return branch + zero-diff constraint on lines 77-97 + zero-edit pass of tests/placeholder — byte-identical gate (Pitfall 7) |
| T-09-04-04 | Denial of service | hook cold path | mitigate | Type-only imports erased at compile — src/state never enters the module graph when reversible is off (verified by 09-07 import-graph test) |
| T-09-04-05 | Spoofing | OVF token collisions | accept | Multiple distinct originals of one TYPE can share an OVF token string (same as v1 degradation); OVF is never restored (Phase 10 rule); stress assertions exempt OVF |
</threat_model>

<verification>
- `npx vitest run tests/state/tokens-v2.test.ts tests/placeholder --project=unit` green
- TDD gates: test(09-04) then feat(09-04) commits present
- Zero edits to tests/placeholder, tests/detect, tests/audit
</verification>

<success_criteria>
- REVMODE-05's both halves proven: v2 format in reversible sessions, v1 byte-identical by default
- Seam ready for 09-05 (drain -> persist) and 09-07 (handler wiring)
- RED -> GREEN (-> REFACTOR) commits in order
</success_criteria>

<output>
Create `.planning/phases/09-session-state-adapter/09-04-SUMMARY.md` when done (RED/GREEN/REFACTOR narrative + commits).
</output>
