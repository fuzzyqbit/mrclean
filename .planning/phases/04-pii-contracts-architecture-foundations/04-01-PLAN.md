---
phase: 04-pii-contracts-architecture-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/detect/findings.ts
  - src/detect/type-map.ts
  - src/audit/log.ts
  - tests/detect/findings.test.ts
  - tests/detect/type-map.test.ts
  - tests/audit/log.test.ts
autonomous: true
requirements: [PII-03, MODEL-01, PIISEC-03]
must_haves:
  truths:
    - "A Finding can carry source 'pii-regex' or 'pii-ner' without TypeScript error"
    - "SOURCE_PRECEDENCE ranks pii-regex above pii-ner, both below all secret layers"
    - "getTypeForRuleId maps pii:EMAIL/SSN/CC/PHONE/IP/PERSON/ORG/LOC rule-ids to PII_* TYPEs"
    - "An AuditRecord can carry engine/model_rev/quant/backend provenance fields"
    - "findingToAuditRecord still emits no raw value, env-var name, or PII text"
  artifacts:
    - path: "src/detect/findings.ts"
      provides: "Finding.source union + SOURCE_PRECEDENCE extended with pii-regex/pii-ner"
      contains: "pii-regex"
    - path: "src/detect/type-map.ts"
      provides: "PII_* TYPE vocabulary + pii: rule-id mappings"
      contains: "PII_EMAIL"
    - path: "src/audit/log.ts"
      provides: "AuditRecord PII-provenance optional fields + no-raw rule extended to PII"
      contains: "model_rev"
  key_links:
    - from: "src/detect/type-map.ts"
      to: "TYPE_VOCABULARY"
      via: "PII_* entries appended (vocabulary count grows from 17)"
      pattern: "PII_(EMAIL|SSN|CREDIT_CARD|PHONE|IP|PERSON|ORG|LOC)"
    - from: "src/audit/log.ts"
      to: "findingToAuditRecord"
      via: "optional provenance params, value still excluded"
      pattern: "model_rev|engine"
---

<objective>
Lock the finding-shape and audit-schema ADDITIONS that every later v2.0 phase (5-7) imports: new PII finding `source` tags, PII placeholder `TYPE`s + rule-id mappings, and PII-capable audit provenance fields — all contract-only, with NO detector emitting them yet.

Purpose: These additions touch Plan-02-00-owned single-source-of-truth modules (`findings.ts`, `type-map.ts`) and the locked `audit/log.ts` schema. They are cheap to add now and expensive to retrofit once Phases 5-7 build against them. Adding the source-union + precedence + TYPE vocabulary + audit fields first means Phase 5's regex lane and Phase 6's NER lane plug into a stable contract.

Output: Extended `Finding.source` union + `SOURCE_PRECEDENCE`; PII `TYPE_VOCABULARY` entries + `pii:` rule-id mappings; `AuditRecord` provenance fields (`engine`, `model_rev`, `quant`, `backend`) with the no-raw-value rule explicitly extended to PII. No behavior change: no layer produces a PII finding in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE-v2-pii.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Contracts the executor extends. Already in the codebase — do not re-create. -->

From src/detect/findings.ts (Plan-02-00-owned — extend, never rewrite):
```typescript
export interface Finding {
  ruleId: string
  severity: 'CRITICAL' | 'HIGH' | 'MEDIUM' | 'LOW'
  span: { start: number; end: number }
  value: string            // NEVER log or persist
  redactedHash: string
  fingerprint: string
  source: 'secretlint' | 'gitleaks' | 'entropy' | 'env' | 'words'   // <-- extend
  action?: 'block' | 'substitute' | 'audit' | 'off' | 'warn'
}
const SOURCE_PRECEDENCE = ['secretlint', 'gitleaks', 'entropy', 'env', 'words'] as const  // <-- append
```

From src/detect/type-map.ts (Plan-02-00-owned — extend, never rewrite):
```typescript
export const TYPE_VOCABULARY = Object.freeze([ /* 17 secret/layer TYPEs */ ] as const)   // <-- append PII_*
const RULE_ID_TO_TYPE: Readonly<Record<string, string>> = Object.freeze({ /* ... */ })   // <-- add pii: entries
export function getTypeForRuleId(ruleId: string): string  // word: prefix, then map, then 'SECRET'
```

From src/audit/log.ts (LOCKED schema — extend, never weaken the no-raw rule):
```typescript
export interface AuditRecord {
  ts: string; sessionId: string; hookEvent: ...; ruleId: string
  severity: ...; action: 'block' | 'substitute' | 'audit'
  redactedHash: string; fingerprint: string
  location: { hookEvent: string; offset: number; length: number }
  // <-- add optional engine/model_rev/quant/backend
}
export function findingToAuditRecord(finding, sessionId, hookEvent, action): AuditRecord
// LOCKED comment: NEVER add raw value, env-var name, or file path here.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Finding.source union + SOURCE_PRECEDENCE with PII lanes</name>
  <files>src/detect/findings.ts, tests/detect/findings.test.ts</files>
  <read_first>
    - src/detect/findings.ts (the file being modified — note the Plan-02-00 ownership banner and the existing `SOURCE_PRECEDENCE` precedence comment)
    - tests/detect/findings.test.ts (existing dedupBySpan + precedence assertions — extend, do not break)
    - .planning/research/ARCHITECTURE-v2-pii.md §"Data Flow" (the locked precedence chain: secretlint > gitleaks > entropy > env > words > pii-regex > pii-ner)
    - STATE.md §"Additional Decisions (Phase 2 — Plan 04)" — the line "append pii-regex > pii-ner at the tail of SOURCE_PRECEDENCE"
  </read_first>
  <behavior>
    - Test: a Finding object literal with `source: 'pii-regex'` type-checks and is accepted by dedupBySpan.
    - Test: a Finding with `source: 'pii-ner'` type-checks.
    - Test: dedupBySpan, given an equal-length overlap between a `words` finding and a `pii-regex` finding, keeps the `words` finding (words outranks pii-regex).
    - Test: dedupBySpan, given an equal-length overlap between `pii-regex` and `pii-ner`, keeps `pii-regex` (regex outranks ner).
    - Test: existing secret-layer precedence assertions (secretlint > gitleaks > entropy > env > words) still pass unchanged.
  </behavior>
  <action>
    Append `'pii-regex'` then `'pii-ner'` (in that order) to the `Finding.source` union type, and append the same two strings, in the same order, to the end of the `SOURCE_PRECEDENCE` tuple so the full locked order becomes secretlint, gitleaks, entropy, env, words, pii-regex, pii-ner. Do not reorder or remove any existing entry — placeholder stability and precedence depend on the existing indices. Update the SOURCE_PRECEDENCE JSDoc to name the two new tail entries and cite ARCHITECTURE-v2-pii.md §Data Flow as the source of the ordering. Leave dedupBySpan logic untouched (it reads precedence by index, so the new tail entries get the lowest priority automatically). Add the behavior tests above to findings.test.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/detect/findings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pii-regex" src/detect/findings.ts` shows the token in BOTH the union type and the SOURCE_PRECEDENCE tuple.
    - `grep -n "pii-ner" src/detect/findings.ts` shows the token in BOTH locations, after pii-regex.
    - tests/detect/findings.test.ts passes including the new precedence assertions.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>Finding.source accepts pii-regex/pii-ner; SOURCE_PRECEDENCE ranks them last (regex before ner); all existing precedence tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PII TYPE vocabulary + pii: rule-id mappings to type-map</name>
  <files>src/detect/type-map.ts, tests/detect/type-map.test.ts</files>
  <read_first>
    - src/detect/type-map.ts (the file being modified — note TYPE_VOCABULARY is frozen and "Existing entries MUST NOT be removed or reordered")
    - tests/detect/type-map.test.ts (the `has exactly 17 entries` assertion at line ~118 — this count MUST be updated)
    - .planning/research/ARCHITECTURE-v2-pii.md §"Component Responsibilities" (type-map.ts row: "Add PII TYPEs (PERSON, ORG, LOC, EMAIL, SSN, CC, PHONE, IP)") and §"Config Surface" (regex entities email/ssn/credit_card/phone/ip; ner entities PERSON/ORG/LOC)
    - REQUIREMENTS.md PII-01 (regex entity set) and NER-01 (PERSON/ORG/LOCATION)
  </read_first>
  <behavior>
    - Test: TYPE_VOCABULARY now has exactly 25 entries (17 existing + 8 PII: PII_EMAIL, PII_SSN, PII_CREDIT_CARD, PII_PHONE, PII_IP, PII_PERSON, PII_ORG, PII_LOC).
    - Test: TYPE_VOCABULARY still contains every one of the original 17 secret/layer TYPEs (no removals).
    - Test: getTypeForRuleId('pii:email') === 'PII_EMAIL'; same for ssn→PII_SSN, credit_card→PII_CREDIT_CARD, phone→PII_PHONE, ip→PII_IP.
    - Test: getTypeForRuleId('pii:PERSON') === 'PII_PERSON'; same for ORG→PII_ORG, LOC→PII_LOC.
    - Test: getTypeForRuleId('AWSAccessKeyID') still === 'AWS_KEY' (existing mapping untouched).
    - Test: getTypeForRuleId('pii:unknown-entity') falls back to 'SECRET' (catch-all unchanged) — documents that only listed pii rule-ids map to PII TYPEs.
  </behavior>
  <action>
    Append the 8 PII TYPE strings to the END of the TYPE_VOCABULARY frozen array, in this exact order: PII_EMAIL, PII_SSN, PII_CREDIT_CARD, PII_PHONE, PII_IP, PII_PERSON, PII_ORG, PII_LOC. Do not remove or reorder any of the existing 17. Add explicit entries to RULE_ID_TO_TYPE mapping the regex-lane rule-ids (pii:email, pii:ssn, pii:credit_card, pii:phone, pii:ip) and the NER-lane rule-ids (pii:PERSON, pii:ORG, pii:LOC) to their respective PII_* TYPEs. Keep the rule-id naming consistent with ARCHITECTURE-v2-pii.md: regex entities use lowercase snake (matching the `[pii.regex].entities` config tokens), NER entities use the upper-case model label. Update the file JSDoc: change "frozen array of 17 TYPE strings" to "25", and note the PII block. Update the count assertion in type-map.test.ts from 17 to 25 and add the behavior tests above.
  </action>
  <verify>
    <automated>npx vitest run tests/detect/type-map.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PII_" src/detect/type-map.ts` is at least 8 (TYPE_VOCABULARY entries) — verify the 8 distinct PII TYPEs are present.
    - `grep -n "'pii:email'\|'pii:PERSON'" src/detect/type-map.ts` shows both rule-id keys mapped.
    - tests/detect/type-map.test.ts passes with the updated count of 25.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>TYPE_VOCABULARY has 25 entries (8 PII appended, 17 originals intact); getTypeForRuleId resolves all 8 pii: rule-ids to PII_* TYPEs and unknown pii: ids still fall back to SECRET.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add PII-provenance fields to AuditRecord; extend no-raw rule to PII</name>
  <files>src/audit/log.ts, tests/audit/log.test.ts</files>
  <read_first>
    - src/audit/log.ts (the file being modified — note the LOCKED AuditRecord schema banner and the "LOCKED: NEVER add raw value" comment inside findingToAuditRecord)
    - tests/audit/log.test.ts (existing AuditRecord shape + findingToAuditRecord assertions)
    - .planning/research/PITFALLS.md Pitfall 6 (record model_rev + quant + backend for reproducibility) and Pitfall 8 (hash-only entries `{entity_type, severity, token_hash, engine, model_rev, offset}`, never the matched text)
    - REQUIREMENTS.md MODEL-04 ("Audit entries for PII findings record model revision + quantization + backend ... contain no raw PII value (hash/fingerprint only)")
  </read_first>
  <behavior>
    - Test: an AuditRecord object literal accepts optional engine/model_rev/quant/backend string fields and type-checks.
    - Test: findingToAuditRecord called WITHOUT provenance produces a record with engine/model_rev/quant/backend absent or undefined — byte-identical to the v1 record for secret findings (no behavior change for existing callers).
    - Test: findingToAuditRecord called WITH a provenance argument carrying engine/model_rev/quant/backend includes those fields and STILL omits finding.value, env-var name, and any raw text.
    - Test: a record built from a finding whose value is a known test SSN/email does not contain that raw string anywhere in JSON.stringify(record) (no-raw rule holds for PII).
  </behavior>
  <action>
    Add four OPTIONAL string fields to the AuditRecord interface: engine (e.g. 'pii-ner@<sha>' or 'pii-regex'), model_rev (model revision SHA), quant (e.g. 'int8'), backend (e.g. 'onnxruntime-node' | 'wasm'). Document each in JSDoc citing Pitfall 6 (reproducibility) and that they are populated only for PII-NER findings in Phase 6 — left undefined for secret findings. Extend findingToAuditRecord with an OPTIONAL trailing parameter (a `provenance?: { engine?; model_rev?; quant?; backend? }` object) that, when present, is spread into the returned record; when absent, the record is unchanged from today. Update the "LOCKED: NEVER add raw value" comment to read "NEVER add raw value, env-var name, file path, or raw PII" so the no-raw rule is explicitly extended to PII. Do NOT change any existing field or the existing call sites. Add the behavior tests above.
  </action>
  <verify>
    <automated>npx vitest run tests/audit/log.test.ts tests/audit/canary-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "model_rev\|backend\|quant\|engine" src/audit/log.ts` shows all four fields in the AuditRecord interface.
    - `grep -v '^ *\*' src/audit/log.ts | grep -c "finding.value"` returns 0 (raw value still never read into the record).
    - The updated LOCKED comment contains the word "PII".
    - tests/audit/log.test.ts and tests/audit/canary-leak.test.ts both pass.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>AuditRecord carries optional engine/model_rev/quant/backend; findingToAuditRecord is backward-compatible (no-provenance call unchanged) and never serializes raw PII; canary-leak test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finding.value → AuditRecord | The raw matched substring (secret OR PII) must never cross into the persisted audit record. |
| Future PII detector → audit schema | Provenance fields must record model identity for reproducibility without ever carrying matched text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information disclosure | findingToAuditRecord (new provenance param) | mitigate | Provenance param is a fixed-shape object with only engine/model_rev/quant/backend; no field accepts free text from the finding. Grep gate `grep -v '^ *\*' \| grep -c "finding.value" == 0` enforces value exclusion. |
| T-04-01-02 | Tampering | SOURCE_PRECEDENCE reorder | mitigate | Tests assert existing secret-layer order is unchanged; PII entries appended at the tail only. A reorder would fail the precedence assertions. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO package-manager installs (schema-only). No package legitimacy gate required. |
</threat_model>

<verification>
- `npm run typecheck` passes with the extended unions and optional fields.
- `npx vitest run tests/detect/findings.test.ts tests/detect/type-map.test.ts tests/audit/log.test.ts tests/audit/canary-leak.test.ts` all green.
- No detection layer emits a PII finding (grep `src/detect/layer*.ts` for `pii-regex`/`pii-ner` returns nothing — contract-only phase).
- The canary-leak invariant still holds for the new provenance path.
</verification>

<success_criteria>
- Finding.source accepts `pii-regex` and `pii-ner`; SOURCE_PRECEDENCE ranks both at the tail (regex before ner).
- TYPE_VOCABULARY has 25 entries; all 8 PII rule-ids resolve to PII_* TYPEs; unknown pii: ids still fall back to SECRET.
- AuditRecord carries optional engine/model_rev/quant/backend; findingToAuditRecord is backward-compatible and never serializes raw PII.
- Zero production behavior change: no layer produces a PII finding in this phase.
</success_criteria>

<output>
Create `.planning/phases/04-pii-contracts-architecture-foundations/04-01-SUMMARY.md` when done.
</output>
