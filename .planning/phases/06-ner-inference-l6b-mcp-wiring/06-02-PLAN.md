---
phase: 06-ner-inference-l6b-mcp-wiring
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/detect/index.ts
  - tests/detect/orchestrator-ner.test.ts
  - tests/detect/ner-unreachable.test.ts
  - tests/perf/user-prompt-submit.perf.test.ts
autonomous: true
requirements: [NER-01, MODEL-04]
must_haves:
  truths:
    - "runDetection and runDetectionReadOnly accept a trailing opts: DetectionOptions = {} with an ner?: boolean flag"
    - "The L6b branch (and its dynamic import of layer6b-ner.js) is entered ONLY when opts.ner && config.pii.ner.enabled — hook handlers never pass opts.ner so the branch is structurally unreachable from the hook path"
    - "dropNerOverlaps runs immediately before dedupBySpan in BOTH runDetection and runDetectionReadOnly"
    - "DetectionResult carries nerStatus reflecting the L6b outcome (disabled when not run, ready/unavailable when run)"
    - "Every pii-ner audit entry populates engine/model_rev/quant/backend via findingToAuditRecord provenance — no raw PII value"
    - "An import-graph test proves no hook-reachable module statically imports pipeline-singleton.ts or layer6b-ner.ts"
    - "The UserPromptSubmit perf gate still passes (hook cold-start unchanged) with NER code present but opts.ner unset"
  artifacts:
    - path: "src/detect/index.ts"
      provides: "opts.ner wiring + L6b branch + D-11 filter + audit provenance + nerStatus in DetectionResult"
      contains: "DetectionOptions"
    - path: "tests/detect/ner-unreachable.test.ts"
      provides: "structural-unreachability import-graph proof"
    - path: "tests/detect/orchestrator-ner.test.ts"
      provides: "opts.ner gating + D-11 integration + provenance assertions"
  key_links:
    - from: "src/detect/index.ts"
      to: "src/detect/layer6b-ner.js"
      via: "await import('./layer6b-ner.js') inside the opts.ner && pii.ner.enabled branch"
      pattern: "await import\\('./layer6b-ner.js'\\)"
    - from: "src/detect/index.ts"
      to: "src/audit/log.ts findingToAuditRecord"
      via: "provenance 5th arg for pii-ner findings"
      pattern: "findingToAuditRecord\\(.*provenance"
    - from: "src/detect/index.ts"
      to: "src/detect/ner-overlap.js dropNerOverlaps"
      via: "pre-dedup filter call"
      pattern: "dropNerOverlaps"
---

<objective>
Wire the 06-01 building blocks into the detection orchestrator behind an MCP-only opt-in, prove the
hook path can never reach the NER code, and record model provenance in every pii-ner audit entry.
This is the structural-unreachability + reproducibility slice.

Purpose: NER-01 (the orchestrator side of "structurally unreachable from the hook" + L6b execution +
D-11 overlap drop) and MODEL-04 (audit engine/model_rev/quant/backend, no raw PII). The MCP tools
flip `{ner:true}` in 06-03; here the hook path's existing `runDetection(...)` calls (no 5th arg) prove
the branch is dead code on the cold path.

Output: modified src/detect/index.ts (opts.ner, L6b branch, dropNerOverlaps pre-dedup, nerStatus,
provenance audit), an import-graph unreachability test, an orchestrator NER test, and a perf-gate
assertion that the hook cold-start is unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-CONTEXT.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-RESEARCH.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-PATTERNS.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-01-SUMMARY.md

<interfaces>
<!-- From 06-01 (Wave 1) and the shipped orchestrator. Use directly. -->

From src/detect/layer6b-ner.ts (06-01):
```typescript
export type NerStatus = 'ready' | 'unavailable' | 'loading' | 'disabled'
export async function runLayer6bNer(
  text: string, ner: MrcleanPiiNerConfig, config: MrcleanConfig,
  coveredSpans?: readonly { start: number; end: number }[],
): Promise<{ findings: Finding[]; status: NerStatus }>
```

From src/detect/ner-overlap.ts (06-01):
```typescript
export function dropNerOverlaps(findings: Finding[]): Finding[]
```

From src/model/pipeline-singleton.ts (06-01):
```typescript
export function getNerBackend(): string   // 'onnxruntime-node' | 'wasm' | 'unknown'
export function resetNerSingleton(): void
```

From src/model/constants.ts:
```typescript
export const PINNED_MODEL_SHA256 = '7de0a4606c65...'
```

From src/audit/log.ts (provenance arg already shipped — no schema change):
```typescript
export interface FindingProvenance { engine?: string; model_rev?: string; quant?: string; backend?: string }
export function findingToAuditRecord(finding, sessionId, hookEvent, action, provenance?): AuditRecord
```

Shipped orchestrator (src/detect/index.ts):
- runDetectionReadOnly(text, config, sessionState, ctx) — L6a block at lines 225-228, dedup at 230
- runDetection(text, config, sessionState, ctx) — L6a block at lines 315-318, dedup at 321, audit write at 372-376
- DetectionResult interface at lines 96-105 (add nerStatus?: NerStatus)
- Hook handlers in src/hook/handlers/*.ts call runDetection WITHOUT a 5th arg (the unreachability proof)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: opts.ner wiring + L6b branch + D-11 pre-dedup filter + nerStatus + audit provenance</name>
  <files>src/detect/index.ts, tests/detect/orchestrator-ner.test.ts</files>
  <read_first>
    - src/detect/index.ts (full file — runDetection lines 278-395, runDetectionReadOnly lines 200-262, DetectionResult 96-105, audit write 372-376, shutdownDetection 148-154)
    - src/detect/layer6b-ner.ts + src/detect/ner-overlap.ts + src/model/pipeline-singleton.ts (06-01 outputs)
    - src/model/constants.ts (PINNED_MODEL_SHA256)
    - src/audit/log.ts lines 133-195 (FindingProvenance + findingToAuditRecord LOCKED destructure-pick)
    - tests/detect/orchestrator-pii.test.ts (existing L6a orchestrator test — mirror its structure)
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-RESEARCH.md §Pattern 2, §Pattern 4, Code E5
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-PATTERNS.md §src/detect/index.ts, §Shared Patterns "Audit provenance"
  </read_first>
  <behavior>
    - runDetection with no opts → behaves byte-identically to today (nerStatus 'disabled', no L6b, no import of layer6b-ner.js)
    - runDetection with opts.ner=true but config.pii.ner.enabled=false → nerStatus 'disabled', no L6b
    - runDetection with opts.ner=true and config.pii.ner.enabled=true → calls runLayer6bNer, appends its findings, sets nerStatus from its status
    - dropNerOverlaps applied to the combined findings BEFORE dedupBySpan in both functions
    - for resolved findings with source 'pii-ner', the audit write passes provenance { engine: `pii-ner@${PINNED_MODEL_SHA256.slice(0,12)}`, model_rev: PINNED_MODEL_SHA256, quant: config.pii.ner.dtype, backend: getNerBackend() }; non-pii-ner findings pass undefined provenance (byte-identical to v1)
    - resetNerSingleton() is invoked inside shutdownDetection()
  </behavior>
  <action>
    Add `export interface DetectionOptions { ner?: boolean }` to src/detect/index.ts and `nerStatus?: NerStatus`
    to DetectionResult (import NerStatus from layer6b-ner.js as a TYPE-only import — do NOT statically import
    the engine module's runtime; the engine is reached only via `await import('./layer6b-ner.js')`).
    Add a trailing `opts: DetectionOptions = {}` param to BOTH runDetection and runDetectionReadOnly.
    In each, AFTER the existing L6a block and BEFORE dedupBySpan, insert the L6b branch per RESEARCH Pattern 2:
    initialize `let nerStatus: NerStatus = 'disabled'`; `if (opts.ner && config.pii.ner.enabled) { const { runLayer6bNer }
    = await import('./layer6b-ner.js'); const out = await runLayer6bNer(text, config.pii.ner, config,
    findings.map(f => f.span)); findings.push(...out.findings); nerStatus = out.status }`. Then add
    `const filtered = dropNerOverlaps(findings)` and feed `filtered` into `dedupBySpan(...)` (replacing the direct
    `dedupBySpan(findings)`). Add `nerStatus` to both returned DetectionResult objects.
    In runDetection's audit-write step (lines 372-376), compute per-finding provenance: for `f.source === 'pii-ner'`
    pass the provenance object (engine/model_rev/quant/backend as in behavior); otherwise pass undefined. Use
    getNerBackend() from pipeline-singleton (type/value import is fine here — index.ts is hook-reachable, but
    getNerBackend does NOT statically import transformers; it only reads a module-level label string. Confirm
    pipeline-singleton.ts has NO top-level transformers import — it is all behind the dynamic import in getNerPipeline).
    Wire `resetNerSingleton()` into shutdownDetection() (mirrors how pool.terminate is called) so the singleton is
    cleared on MCP shutdown (shutdownMcpSupervisor re-exports shutdownDetection — verified supervisor.ts:37).
    Write tests/detect/orchestrator-ner.test.ts: mock layer6b-ner.js (or pipeline-singleton.js) so no download
    occurs; assert the three gating cases, D-11 integration (a pii-ner span overlapping a secret is gone post-dedup),
    nerStatus propagation, and that a pii-ner finding's audit record carries engine/model_rev/quant/backend while a
    secret finding's record does not (and neither carries `value`/raw text).
  </action>
  <acceptance_criteria>
    - `grep -c "await import('./layer6b-ner.js')" src/detect/index.ts` returns exactly 2 (one per orchestrator fn)
    - `grep -c "from './layer6b-ner.js'" src/detect/index.ts` shows only a `import type` line (0 runtime static imports of the engine)
    - `grep -n "dropNerOverlaps" src/detect/index.ts` matches in both functions, positioned before each dedupBySpan call
    - `grep -n "resetNerSingleton" src/detect/index.ts` matches inside shutdownDetection
    - orchestrator-ner.test.ts asserts: a pii-ner audit record has engine starting `pii-ner@`, model_rev === PINNED_MODEL_SHA256, quant === dtype, backend a non-empty string; a secretlint record has none of these and no `value` key
    - orchestrator-ner.test.ts asserts default `runDetection(text,cfg,state,ctx)` (no opts) yields nerStatus 'disabled' and never loads the engine
    - `npm test -- orchestrator-ner orchestrator-pii orchestrator` all pass (no regression)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c "await import('./layer6b-ner.js')" src/detect/index.ts)" = "2" && grep -q "dropNerOverlaps" src/detect/index.ts && grep -q "resetNerSingleton" src/detect/index.ts && npm test -- orchestrator-ner orchestrator-pii orchestrator</automated>
  </verify>
  <done>opts.ner threads an MCP-only L6b branch into both orchestrators; D-11 runs pre-dedup; nerStatus is returned; pii-ner audit entries carry full provenance with no raw value; singleton reset on shutdown; default (no-opts) behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: structural-unreachability import-graph test + hook cold-start perf gate</name>
  <files>tests/detect/ner-unreachable.test.ts, tests/perf/user-prompt-submit.perf.test.ts</files>
  <read_first>
    - src/hook/index.ts, src/hook/dispatcher.ts, src/hook/handlers/*.ts (the hook-reachable module set — confirm none pass opts.ner and none statically import the NER modules)
    - src/detect/index.ts (Task 1 output — confirm the engine is only behind await import())
    - tests/perf/user-prompt-submit.perf.test.ts (existing p95<=100ms gate on the 4KB fixture — extend, do not replace)
    - tests/mcp/check-hook-parity.test.ts (existing parity/graph-style test for analog structure)
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-RESEARCH.md §Pattern 2 (verification task), §Security Domain (perf DoS row)
  </read_first>
  <action>
    Create tests/detect/ner-unreachable.test.ts. Statically read the source of every hook-reachable module —
    start from src/hook/index.ts and transitively follow the hook handler set (src/hook/dispatcher.ts,
    src/hook/handlers/*.ts) plus src/detect/index.ts — and assert that NONE of them contain a STATIC import of
    `pipeline-singleton` or `layer6b-ner` as a runtime import (a `import type { NerStatus }` line in index.ts is
    permitted; a runtime `import { ... } from './layer6b-ner.js'` is forbidden). Assert the only references to
    `layer6b-ner` / `@huggingface/transformers` in src/ are dynamic `import(...)` expressions. Also assert that
    no hook handler call site passes a 5th argument to runDetection (grep the handler sources for
    `runDetection(` and confirm the 4-arg shape). Keep this a fast pure-source-reading test (no model, no spawn).
    Extend tests/perf/user-prompt-submit.perf.test.ts with an assertion (or a sibling test in the same file) that
    the 4KB-prompt p95 stays <= 100ms with the NER code present and opts.ner UNSET — i.e. the hook cold path is
    unchanged. Use the existing N=50/WARMUP=5/THRESHOLD=100 harness and the existing 4kb fixture; the new case
    runs runDetection WITHOUT opts so the L6b branch is never entered. Document the measured p95 vs the 17.4ms
    baseline in the SUMMARY.
  </action>
  <acceptance_criteria>
    - ner-unreachable.test.ts passes and FAILS if a static `import { ... } from './layer6b-ner.js'` is added to index.ts or any hook handler (verify by asserting the test's grep/AST logic targets runtime imports, not `import type`)
    - the test enumerates the hook-reachable set explicitly (src/hook/index.ts + dispatcher + handlers/*.ts + src/detect/index.ts) and asserts zero runtime NER imports
    - the perf gate asserts p95 <= 100ms on the 4KB fixture for the no-opts path; SUMMARY records the measured p95
    - `npm test -- ner-unreachable` passes; `npm test --project=integration -- user-prompt-submit` passes
  </acceptance_criteria>
  <verify>
    <automated>npm test -- ner-unreachable && npm test --project=integration -- user-prompt-submit</automated>
  </verify>
  <done>An import-graph test proves the NER modules are unreachable from the hook path via static imports; the hook cold-start perf gate confirms p95 <= 100ms is unchanged with NER code present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook process → detection | hook handlers call runDetection with NO ner opt; must stay that way |
| pii-ner finding → audit.jsonl | provenance must carry model identity only, never raw PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Denial of Service | NER reachable from the hook cold path | mitigate | L6b only behind `opts.ner` + dynamic `import('./layer6b-ner.js')`; import-graph test asserts no static runtime import from the hook-reachable set; perf gate asserts p95 unchanged |
| T-06-02-02 | Information Disclosure | raw PII name in audit provenance | mitigate | findingToAuditRecord LOCKED destructure-picks ONLY engine/model_rev/quant/backend; provenance object built from model constants + config + backend label, never from finding.value (RESEARCH Pitfall 5) |
| T-06-02-03 | Repudiation | non-reproducible NER audit entries | mitigate | every pii-ner entry pins model_rev (content SHA), quant (dtype), backend (MODEL-04) so the same input+model reproduces identical entries |
| T-06-02-04 | Denial of Service | NER error crashing the secret gate via the orchestrator | mitigate | runLayer6bNer catches internally (06-01); index.ts never wraps it in a throw-propagating path; secret findings (L1–L6a) returned regardless |
| T-06-02-SC | Tampering | dependency installs | accept | no installs in this plan; ML deps remain pre-declared optionalDependencies (RESEARCH Package Legitimacy Audit, both `[OK]`) |
</threat_model>

<verification>
- `npm test` full suite green (no regression to existing orchestrator/hook/perf tests)
- ner-unreachable test green; perf gate p95 <= 100ms on the no-opts path
- opts.ner gating: disabled-by-default, MCP-only-enable behavior proven
- pii-ner audit provenance present; secret-finding records unchanged
- dedupBySpan / findings.ts still unchanged from 06-01
</verification>

<success_criteria>
- DetectionOptions.ner threaded into both orchestrators; default behavior byte-identical
- L6b branch + D-11 pre-dedup filter + nerStatus integrated
- pii-ner audit entries carry engine/model_rev/quant/backend, no raw value (MODEL-04)
- Structural unreachability proven by an import-graph test
- Hook cold-start perf gate unchanged (p95 <= 100ms)
</success_criteria>

<output>
Create `.planning/phases/06-ner-inference-l6b-mcp-wiring/06-02-SUMMARY.md` when done.
Record: the measured hook p95 vs the 17.4ms baseline, the final opts.ner threading shape, and the
nerStatus field added to DetectionResult (06-03 surfaces it in check/redact structuredContent).
</output>
