---
phase: 06-ner-inference-l6b-mcp-wiring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/model/constants.ts
  - src/model/model-cache.ts
  - src/model/pipeline-singleton.ts
  - src/config/index.ts
  - src/detect/index.ts
  - tests/model/model-cache.test.ts
  - tests/model/pipeline-singleton.test.ts
  - tests/detect/orchestrator-ner.test.ts
autonomous: true
gap_closure: true
requirements: [MODEL-04, NER-04]

must_haves:
  truths:
    - "getNerPipeline enforces the per-model pinned SHA-256 on the inference load path: it ensures the file is present-and-verified before calling pipeline(), and sets env.allowRemoteModels = false so transformers.js loads ONLY the locally verified file"
    - "On integrity mismatch, or on a cache miss when download is disabled, getNerPipeline THROWS so runLayer6bNer's existing try/catch fails closed (nerStatus 'unavailable') — never executes unverified bytes"
    - "Selecting the piiranha tier (pii.ner.model = PIIRANHA_MODEL_ID) resolves to the piiranha download URL + piiranha pinned SHA-256 + piiranha cache path and is integrity-verified before load — no unverified ~317 MB download"
    - "An unknown/non-pinned pii.ner.model is rejected at config-load time (fail-closed), never silently downloaded"
    - "Audit provenance (model_rev/quant/backend/engine) reflects the ACTUALLY-loaded model's resolved descriptor (pinned sha of the selected tier + resolved dtype), never a hardcoded bert constant — for piiranha the stamped hash is the piiranha hash"
    - "The integrity gate on the inference path is covered by tests that inject a mock model-cache + mock pipeline and assert verify/acquire-before-pipeline, allowRemoteModels=false, throw-on-mismatch, and per-model hash stamping — without any real model download"
  artifacts:
    - path: "src/model/constants.ts"
      provides: "ModelDescriptor type + MODEL_DESCRIPTORS map keyed by model id, wiring the dead PIIRANHA_* constants into a real descriptor"
      contains: "MODEL_DESCRIPTORS"
    - path: "src/model/model-cache.ts"
      provides: "isModelCached / verifyModelIntegrity / downloadModel / sideLoadModel parameterized by a per-model descriptor (back-compatible bert default)"
      contains: "ModelDescriptor"
    - path: "src/model/pipeline-singleton.ts"
      provides: "SHA-verified inference load path: acquire-and-verify, allowRemoteModels=false, fail-closed throw, resolved-provenance accessors"
      contains: "allowRemoteModels = false"
    - path: "src/config/index.ts"
      provides: "fail-closed rejection of unknown/unpinned pii.ner.model at config load"
      contains: "MODEL_DESCRIPTORS"
    - path: "src/detect/index.ts"
      provides: "truthful per-model provenance derived from the resolved descriptor + resolved dtype/backend"
    - path: "tests/model/pipeline-singleton.test.ts"
      provides: "integrity-path coverage for the inference load (mocked cache + pipeline)"
      contains: "allowRemoteModels"
  key_links:
    - from: "src/model/pipeline-singleton.ts"
      to: "src/model/model-cache.ts"
      via: "verifyModelIntegrity / isModelCached / downloadModel on the inference load path"
      pattern: "verifyModelIntegrity|isModelCached|downloadModel"
    - from: "src/model/model-cache.ts"
      to: "src/model/constants.ts MODEL_DESCRIPTORS"
      via: "descriptor parameter resolving url+hash+path per model id"
      pattern: "ModelDescriptor|MODEL_DESCRIPTORS"
    - from: "src/detect/index.ts"
      to: "resolved descriptor pinned sha + resolved dtype"
      via: "provenance model_rev/quant reflect the loaded model, not a hardcoded constant"
      pattern: "model_rev"
    - from: "src/config/index.ts"
      to: "src/model/constants.ts MODEL_DESCRIPTORS"
      via: "config-load rejection of unknown pii.ner.model"
      pattern: "MODEL_DESCRIPTORS"
---

<objective>
Close the single verified SC-4 gap from 06-VERIFICATION.md: the NER engine loads the
model the inference path actually executes WITHOUT enforcing the Phase-5 pinned SHA-256,
yet stamps `model_rev = PINNED_MODEL_SHA256` on every audit entry — and the piiranha tier's
three integrity constants are dead code, so selecting piiranha silently downloads an
unverified ~317 MB model and stamps the wrong (bert) hash.

This is a supply-chain integrity fix (CR-01, CR-02). The root cause is "one missing wire":
`getNerPipeline` must route through the SHA-256-verified `model-cache.ts` path, parameterized
per model id, and the audit provenance must reflect the bytes actually loaded.

Purpose: Honor D-06 (singleton loads via the SHA-256-verified model-cache.ts path), D-12 /
MODEL-04 (truthful provenance), and NER-04 (piiranha tier verified, not just selectable).
Output: parameterized integrity infrastructure, a fail-closed verified inference load path,
truthful per-model provenance, and tests that lock the D-06 invariant on the inference path.

SCOPE GUARD: Do NOT re-do the three PASSED success criteria (hook-unreachability, advisory/
substitute-not-deny, fail-closed-for-NER). Those are verified ✓ — leave their wiring intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-VERIFICATION.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-REVIEW.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-CONTEXT.md

<interfaces>
<!-- Current contracts the executor must preserve/extend. Extracted from the codebase. -->

From src/model/constants.ts (the dead PIIRANHA_* constants to wire):
```typescript
export const MODEL_ID = 'Xenova/bert-base-NER'
export const MODEL_DOWNLOAD_URL = 'https://huggingface.co/Xenova/bert-base-NER/resolve/main/onnx/model_int8.onnx'
export const PINNED_MODEL_SHA256 = '7de0a4606c65b60da275a72f37b76a102c41e2b79c6463096a9d0cb800bf3f2c'
export const MODEL_CACHE_PATH = (homeDir: string): string => /* <home>/.mrclean/models/Xenova/bert-base-NER/onnx/model_int8.onnx */
export const PIIRANHA_MODEL_ID = 'onnx-community/piiranha-v1-detect-personal-information-ONNX'
export const PIIRANHA_DOWNLOAD_URL = 'https://huggingface.co/.../piiranha.../onnx/model_int8.onnx'
export const PIIRANHA_PINNED_SHA256 = 'd5f4d139371b9eeab687d705604e928c46a28a8169654323888bb3160e839076'
export const PIIRANHA_CACHE_PATH = (homeDir: string): string => /* <home>/.mrclean/models/onnx-community/.../onnx/model_int8.onnx */
```

From src/model/model-cache.ts (current bert-hardwired signatures — must stay back-compatible):
```typescript
export async function isModelCached(homeDir: string): Promise<boolean>
export async function verifyModelIntegrity(homeDir: string, expectedHash?: string): Promise<boolean>
export async function downloadModel(homeDir: string, opts?: DownloadModelOptions): Promise<void>
export async function sideLoadModel(homeDir: string, fromPath: string, expectedHash?: string): Promise<void>
export class ModelIntegrityError extends Error { kind; expected; actual }
// NOTE: model-cache.ts imports ONLY Node stdlib (createHash, fs/promises, path) — ZERO ML deps.
// It is cold-path-safe. Importing it into pipeline-singleton is fine — pipeline-singleton is
// already MCP-only (its @huggingface/transformers import is a dynamic import()). The descriptor
// import path MUST NOT introduce any static @huggingface/transformers / onnxruntime-node import.
```

From src/model/pipeline-singleton.ts (the inference load to fix — CR-01):
```typescript
export type NerPipeline = (text: string, opts?: Record<string, unknown>) => Promise<unknown>
export function getNerBackend(): string
export function resetNerSingleton(): void
export function getNerPipeline(ner: MrcleanPiiNerConfig): Promise<NerPipeline>
// Current body sets env.allowRemoteModels = ner.allowDownload then calls
// pipeline('token-classification', ner.model, { dtype: ner.dtype }) with NO integrity gate.
```

From src/detect/index.ts (lines 443-448 — the provenance lie to fix, D-12/MODEL-04):
```typescript
const nerProvenance: FindingProvenance = {
  engine: `pii-ner@${PINNED_MODEL_SHA256.slice(0, 12)}`,
  model_rev: PINNED_MODEL_SHA256,        // hardcoded bert hash regardless of loaded model
  quant: config.pii.ner.dtype,           // requested, not resolved (WR-04)
  backend: getNerBackend(),
}
```

From src/audit/log.ts (provenance shape — all fields optional strings, no raw PII):
```typescript
export interface FindingProvenance { engine?: string; model_rev?: string; quant?: string; backend?: string }
```

From src/detect/layer6b-ner.ts (the fail-closed wrapper that MUST catch the new throw):
```typescript
try { pipe = await getNerPipeline(ner) } catch { return { findings: [], status: 'unavailable' } }
```

From src/config/index.ts (validatePiiNerConfig — where the config-load model-id guard goes, ~line 242):
```typescript
if ('model' in raw && typeof raw['model'] !== 'string') {
  throw new ConfigReadError(filePath, '[pii.ner].model must be a string')
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize model-cache by a per-model descriptor + build MODEL_DESCRIPTORS in constants</name>
  <files>src/model/constants.ts, src/model/model-cache.ts, tests/model/model-cache.test.ts</files>
  <read_first>
    - src/model/constants.ts (MODEL_ID, MODEL_DOWNLOAD_URL, PINNED_MODEL_SHA256, MODEL_CACHE_PATH and the dead PIIRANHA_* quartet)
    - src/model/model-cache.ts (the four bert-hardwired functions + ModelIntegrityError + DownloadModelOptions)
    - tests/model/model-cache.test.ts (existing test shape, mock fetch injection pattern)
  </read_first>
  <behavior>
    - Test: MODEL_DESCRIPTORS[MODEL_ID] resolves to { downloadUrl: MODEL_DOWNLOAD_URL, pinnedSha256: PINNED_MODEL_SHA256, cachePath(home) === MODEL_CACHE_PATH(home) }.
    - Test: MODEL_DESCRIPTORS[PIIRANHA_MODEL_ID] resolves to the piiranha url/hash/path (wires the previously-dead constants).
    - Test: verifyModelIntegrity(home, piiranhaDescriptor) streams the piiranha cache path and compares against PIIRANHA_PINNED_SHA256 (not the bert hash) — assert with a fixture file whose bytes hash to a known value passed as an override.
    - Test: isModelCached(home, piiranhaDescriptor) checks the piiranha cache path, not the bert path.
    - Test: downloadModel(home, piiranhaDescriptor, { fetchImpl }) fetches PIIRANHA_DOWNLOAD_URL and on a hash mismatch throws ModelIntegrityError and unlinks the temp file (verify-before-rename preserved).
    - Test (back-compat): isModelCached(home) / verifyModelIntegrity(home) / downloadModel(home, opts) with NO descriptor argument behave exactly as before (default to the bert descriptor) — existing tests still pass unchanged.
  </behavior>
  <action>
    In constants.ts: add an immutable ModelDescriptor type — `{ id: string; downloadUrl: string; pinnedSha256: string; cachePath: (homeDir: string) => string }` — and export a frozen `MODEL_DESCRIPTORS: Readonly<Record<string, ModelDescriptor>>` map containing the bert descriptor (keyed by MODEL_ID, built from the existing MODEL_DOWNLOAD_URL / PINNED_MODEL_SHA256 / MODEL_CACHE_PATH) and the piiranha descriptor (keyed by PIIRANHA_MODEL_ID, built from PIIRANHA_DOWNLOAD_URL / PIIRANHA_PINNED_SHA256 / PIIRANHA_CACHE_PATH — this wires the dead constants, closing CR-02's "zero references" finding). Export a `BERT_DESCRIPTOR = MODEL_DESCRIPTORS[MODEL_ID]` convenience binding. Keep constants.ts ML-dep-free (Node stdlib only) — do NOT import anything from model-cache or transformers.

    In model-cache.ts: parameterize all four functions by an OPTIONAL trailing `descriptor: ModelDescriptor = BERT_DESCRIPTOR` parameter so existing bert call sites keep working unchanged (back-compatible per the gap scope). Each function must resolve `descriptor.cachePath(homeDir)`, `descriptor.downloadUrl`, and `descriptor.pinnedSha256` instead of the hardwired bert constants. For verifyModelIntegrity, the existing `expectedHash` override stays (defaulting now to `descriptor.pinnedSha256`); for downloadModel, `DownloadModelOptions.expectedHash` defaults to `descriptor.pinnedSha256` and the fetch target becomes `descriptor.downloadUrl`. Preserve every security invariant: hash verified BEFORE atomic rename, temp file unlinked on mismatch, cache path always under homeDir (never cwd-relative), ModelIntegrityError thrown on mismatch. Per CLAUDE.md immutability: descriptors are frozen; no in-place mutation of inputs.

    Add the tests described in <behavior> to tests/model/model-cache.test.ts using the existing mock-fetch + fixture-file + expectedHash-override pattern (NO real network, NO real 108/317 MB download).

    Do NOT delete the standalone PIIRANHA_* / MODEL_* constants — other modules (ner-entities.ts imports PIIRANHA_MODEL_ID; cli/doctor import the bert trio) still reference them; the descriptor map composes them.
  </action>
  <verify>
    <automated>npx vitest run tests/model/model-cache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MODEL_DESCRIPTORS exported and frozen; keys are MODEL_ID and PIIRANHA_MODEL_ID.
    - `grep -nE "PIIRANHA_(DOWNLOAD_URL|PINNED_SHA256|CACHE_PATH)" src/model/constants.ts` shows them now consumed by the descriptor map (no longer dead): `grep -rn "MODEL_DESCRIPTORS" src/` returns ≥1 import site outside constants.ts after Task 2/Task 3.
    - All four model-cache functions accept an optional descriptor and resolve per-model url/hash/path; calling them with no descriptor is byte-identical to prior bert behavior (existing model-cache tests pass).
    - New tests assert piiranha descriptor uses PIIRANHA_PINNED_SHA256 and the piiranha cache path, and that mismatch throws ModelIntegrityError + unlinks temp.
    - `grep -nE "@huggingface/transformers|onnxruntime" src/model/constants.ts src/model/model-cache.ts` returns nothing (cold-path purity preserved).
  </acceptance_criteria>
  <done>model-cache.ts verifies BOTH bert and piiranha against their own pinned hashes via a descriptor; constants expose MODEL_DESCRIPTORS; back-compat preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route getNerPipeline through the SHA-verified cache (fail-closed) + reject unknown models at config load</name>
  <files>src/model/pipeline-singleton.ts, src/config/index.ts, tests/model/pipeline-singleton.test.ts</files>
  <read_first>
    - src/model/pipeline-singleton.ts (getNerPipeline body lines 78-105: env.allowRemoteModels = ner.allowDownload, pipeline() call, getNerBackend, resetNerSingleton)
    - src/model/model-cache.ts (isModelCached / verifyModelIntegrity / downloadModel signatures AFTER Task 1 — now descriptor-aware)
    - src/model/constants.ts (MODEL_DESCRIPTORS after Task 1)
    - src/config/index.ts (validatePiiNerConfig ~line 234-284; ConfigReadError shape)
    - tests/model/pipeline-singleton.test.ts (the mock-transformers harness; the test at lines 125-132 that codifies allowRemoteModels = allowDownload — MUST be repointed)
    - src/detect/layer6b-ner.ts (the load try/catch at ~line 204 that must catch the new throw)
  </read_first>
  <behavior>
    - Test: getNerPipeline resolves the descriptor for ner.model from MODEL_DESCRIPTORS; before pipeline() it calls isModelCached + verifyModelIntegrity (injected mock model-cache) and sets env.allowRemoteModels = FALSE.
    - Test: on a cache miss (isModelCached → false) with allowDownload=true, it calls downloadModel (which fails closed on mismatch) then proceeds; verify the acquire-then-verify order precedes pipeline().
    - Test: on a cache miss with allowDownload=false, getNerPipeline THROWS (does not call pipeline()).
    - Test: on verifyModelIntegrity → false (integrity mismatch), getNerPipeline THROWS and never calls pipeline() — and the existing layer6b-ner try/catch turns that into status 'unavailable' (assert via the existing fail-closed test or a focused one).
    - Test (repoint of old lines 125-132): assert env.allowRemoteModels === false ALWAYS (the unverified-download behavior is removed); delete/replace the assertion that it tracks ner.allowDownload.
    - Test: for ner.model = PIIRANHA_MODEL_ID, the piiranha descriptor (piiranha hash + path) is the one verified/acquired — not bert.
    - Test (config): validatePiiNerConfig rejects pii.ner.model that is not a key of MODEL_DESCRIPTORS with a ConfigReadError; accepts MODEL_ID and PIIRANHA_MODEL_ID.
    - Test: getNerResolvedSha256() / getNerResolvedDtype() (new accessors) return the descriptor's pinnedSha256 and the resolved dtype after a build (used by Task 3).
  </behavior>
  <action>
    In pipeline-singleton.ts, inside the cached async build BEFORE the `pipeline()` call (D-06 / CR-01):
    1. Resolve `const descriptor = MODEL_DESCRIPTORS[ner.model]`. If undefined, throw (defense-in-depth; config load is the primary guard). Import MODEL_DESCRIPTORS from '../model/constants.js' (stdlib-only, cold-path-safe). Stash the resolved descriptor's pinnedSha256 into a module-level `resolvedModelSha` and the resolved dtype into `resolvedDtype` for the provenance accessors.
    2. Dynamically import model-cache: `const { isModelCached, verifyModelIntegrity, downloadModel } = await import('./model-cache.js')` (model-cache is stdlib-only; a static import is also acceptable since it pulls no ML dep — choose whichever keeps the build clean, but DO NOT statically import @huggingface/transformers).
    3. Acquire-and-verify: `const home = homedir()`. If `!(await isModelCached(home, descriptor)) || !(await verifyModelIntegrity(home, descriptor.pinnedSha256, descriptor))` (use the descriptor-aware signature from Task 1) then: if `!ner.allowDownload` THROW a clear Error (no raw PII; e.g. "NER model absent/unverified and download disabled"); else `await downloadModel(home, { ...descriptorOpts })` — downloadModel already verifies-before-rename and throws ModelIntegrityError on mismatch. After download, re-verify (or rely on downloadModel's internal verify) so a passing path guarantees on-disk bytes match the pin.
    4. Set `env.cacheDir = join(home, '.mrclean', 'models')` and `env.allowRemoteModels = false` (was `ner.allowDownload`) — transformers.js now loads ONLY the SHA-verified local file. Keep cacheDir/allowRemoteModels set BEFORE pipeline().
    5. Capture backend as today. Add exports `getNerResolvedSha256(): string | undefined` and `getNerResolvedDtype(): string | undefined` returning the stashed values (reset to undefined in resetNerSingleton). These let Task 3 stamp truthful provenance. Do NOT throw inside getNerBackend.
    Errors thrown here are caught by runLayer6bNer's existing load try/catch → status 'unavailable' (fail-closed-for-NER, the PASSED SC-3 — preserve it; do not move the catch).

    In config/index.ts validatePiiNerConfig: after the existing `model` string-type check (~line 242-244), reject an unknown/unpinned model fail-closed — `if ('model' in raw && !(raw['model'] in MODEL_DESCRIPTORS)) throw new ConfigReadError(filePath, ...)`. Import MODEL_DESCRIPTORS from '../model/constants.js'. Both default bert and piiranha resolve (both are descriptor keys), so the legitimate swap still works; only unknown ids are rejected. (This is the gap scope's accepted alternative AND complements the integrity gate.)

    Update tests/model/pipeline-singleton.test.ts: inject a mock model-cache module (vi.mock '../../src/model/model-cache.js' exposing isModelCached/verifyModelIntegrity/downloadModel as vi.fn) alongside the existing transformers mock. Add the <behavior> tests. REPOINT the lines 125-132 test: it must now assert `env.allowRemoteModels === false` unconditionally and that verify/acquire ran before pipeline() — remove the assertion that allowRemoteModels tracks allowDownload (that codified the bug).
  </action>
  <verify>
    <automated>npx vitest run tests/model/pipeline-singleton.test.ts tests/config</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allowRemoteModels" src/model/pipeline-singleton.ts` shows `= false` (no longer `= ner.allowDownload`).
    - `grep -nE "verifyModelIntegrity|isModelCached|downloadModel" src/model/pipeline-singleton.ts` shows all three wired before pipeline().
    - getNerPipeline throws when integrity fails OR (cache miss AND allowDownload=false); pipeline() is NOT called in those cases (asserted via mock call counts).
    - For PIIRANHA_MODEL_ID the piiranha descriptor (piiranha hash/path) is verified, not bert.
    - `grep -n "MODEL_DESCRIPTORS" src/config/index.ts` present; unknown pii.ner.model throws ConfigReadError; MODEL_ID and PIIRANHA_MODEL_ID accepted.
    - `grep -nE "@huggingface/transformers" src/model/pipeline-singleton.ts` shows it ONLY inside a dynamic `await import(...)` (no new static import); `grep -c "from '@huggingface/transformers'" src/model/pipeline-singleton.ts` is 0.
    - The repointed test asserts allowRemoteModels === false; no remaining test asserts allowRemoteModels === allowDownload.
    - getNerResolvedSha256/getNerResolvedDtype exported and reset by resetNerSingleton.
  </acceptance_criteria>
  <done>The inference path loads ONLY a SHA-verified local model (allowRemoteModels=false), fails closed on mismatch/missing, verifies the correct per-model hash, rejects unknown models at config load, and exposes resolved provenance — all covered by mocked tests with no real download.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make audit provenance truthful (resolved per-model sha + resolved dtype) + verify hook-path purity intact</name>
  <files>src/detect/index.ts, tests/detect/orchestrator-ner.test.ts</files>
  <read_first>
    - src/detect/index.ts (lines 51-55 imports; lines 443-448 nerProvenance block; line 459 source === 'pii-ner' branch)
    - src/model/pipeline-singleton.ts (getNerResolvedSha256 / getNerResolvedDtype / getNerBackend after Task 2)
    - src/audit/log.ts (FindingProvenance shape — optional strings, no raw PII)
    - tests/detect/orchestrator-ner.test.ts (how provenance is asserted on pii-ner findings today)
  </read_first>
  <behavior>
    - Test: for a pii-ner finding loaded as bert, audit provenance.model_rev === PINNED_MODEL_SHA256 (resolved, via getNerResolvedSha256) and engine === `pii-ner@<first 12 of resolved sha>`.
    - Test: for a pii-ner finding loaded as piiranha (mock getNerResolvedSha256 → PIIRANHA_PINNED_SHA256), provenance.model_rev === PIIRANHA_PINNED_SHA256 (NOT the bert hash) — this is the core SC-4 reproducibility fix for the swap.
    - Test: provenance.quant === the RESOLVED dtype (getNerResolvedDtype), not unconditionally config.pii.ner.dtype (WR-04). If resolved dtype is unavailable, fall back to config dtype is acceptable; assert resolved wins when present.
    - Test: provenance carries NO raw value/span (findingToAuditRecord destructure-pick unchanged) — re-assert no-raw-PII invariant (the PASSED guarantee must not regress).
    - Test (purity): an import-graph / dist assertion still shows @huggingface/transformers is reached ONLY via dynamic import from the hook path (re-run the existing ner-unreachable proof; importing model-cache/constants into pipeline-singleton must not add a static ML import).
  </behavior>
  <action>
    In src/detect/index.ts: replace the hardcoded provenance with resolved values from the singleton accessors added in Task 2. Import `getNerResolvedSha256, getNerResolvedDtype` (alongside existing getNerBackend) from '../model/pipeline-singleton.js'. Build nerProvenance as:
      - `const sha = getNerResolvedSha256()` — model_rev = sha (truthful, per-model). engine = `pii-ner@${sha.slice(0,12)}` when sha present.
      - quant = `getNerResolvedDtype() ?? config.pii.ner.dtype` (resolved wins; config only as last-resort fallback — WR-04).
      - backend = getNerBackend() (unchanged).
    Drop the static import of PINNED_MODEL_SHA256 from this file for provenance purposes (line 55) IF no longer otherwise used; if it is used elsewhere keep the import but stop sourcing model_rev from it. The provenance object is built ONLY for `f.source === 'pii-ner'` (existing gate at line 459) — non-NER findings still pass undefined (byte-identical audit records preserved). Keep the destructure-pick in findingToAuditRecord untouched so no raw PII can enter provenance.

    Guard against undefined: a pii-ner finding only exists after a successful verified load, so getNerResolvedSha256() is populated; but defensively, if sha is undefined, omit model_rev/engine rather than asserting a wrong/empty hash (per the verifier's "emit resolved or omit" instruction). Per CLAUDE.md: no mutation of finding objects; build a fresh provenance object.

    Update tests/detect/orchestrator-ner.test.ts with the <behavior> assertions (mock the singleton accessors to simulate bert vs piiranha resolved sha). Re-run the existing hook-unreachability proof test (tests/detect/ner-unreachable.test.ts) to confirm Task 1/2 imports did not create a static ML import on the cold path.
  </action>
  <verify>
    <automated>npx vitest run tests/detect/orchestrator-ner.test.ts tests/detect/ner-unreachable.test.ts && npm run build && grep -c "from '@huggingface/transformers'" dist/cli.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "model_rev" src/detect/index.ts` shows it sourced from getNerResolvedSha256(), NOT a hardcoded PINNED_MODEL_SHA256.
    - For a piiranha-resolved run, provenance.model_rev === PIIRANHA_PINNED_SHA256 (test asserts the swap stamps the right hash).
    - quant prefers the resolved dtype over config.pii.ner.dtype.
    - No raw value/span appears in any provenance/audit field (no-raw-PII test passes).
    - tests/detect/ner-unreachable.test.ts still passes; the dist grep confirms @huggingface/transformers appears in dist only via dynamic import (no static top-level import) — the structural-unreachability invariant (PASSED SC-1) is intact.
  </acceptance_criteria>
  <done>Every NER audit entry records the ACTUALLY-loaded model's pinned sha + resolved dtype + backend; piiranha stamps the piiranha hash; hook-path ML purity verified unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HuggingFace Hub → local disk | The NER model is fetched from a remote registry; the bytes are untrusted until SHA-256-verified against the pin. |
| local model file → inference engine | transformers.js loads ONNX bytes and executes them; a poisoned/substituted file is arbitrary-behavior input to the engine. |
| operator config (pii.ner.model/dtype) → model selection | A free-form model id could point the loader at an arbitrary, unpinned model. |
| loaded model → audit log provenance | The audit entry asserts which model produced a finding; a false assertion breaks the reproducibility contract (MODEL-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | getNerPipeline inference load (pipeline-singleton.ts) | mitigate | Route through model-cache verifyModelIntegrity + set env.allowRemoteModels=false so only the SHA-verified local file is loaded (Task 2). Fail closed (throw → nerStatus 'unavailable') on mismatch. |
| T-06-04-02 | Tampering | piiranha tier (constants.ts / model-cache.ts) | mitigate | Wire PIIRANHA_* into MODEL_DESCRIPTORS so the piiranha tier is verified against its own pinned hash before load (Task 1+2) — no unverified ~317 MB download. |
| T-06-04-03 | Spoofing | config pii.ner.model | mitigate | Reject any model id not in MODEL_DESCRIPTORS at config load (fail-closed), preventing selection of an arbitrary unpinned model (Task 2). |
| T-06-04-04 | Repudiation / Integrity | audit provenance (detect/index.ts) | mitigate | Stamp model_rev/quant from the RESOLVED descriptor + resolved dtype, not a hardcoded constant, so the audit honestly records the bytes executed (Task 3). |
| T-06-04-05 | Information Disclosure | provenance / error strings | mitigate | Provenance is a destructure-pick of model-identity strings only; error messages on integrity failure contain NO raw PII or secret values (CLAUDE.md, Task 2/3). |
| T-06-04-06 | Elevation / cold-path | importing model-cache/constants into pipeline-singleton | accept (verified) | model-cache + constants are Node-stdlib-only (zero ML deps); the existing ner-unreachable dist proof is re-run (Task 3) to confirm no static @huggingface/transformers import enters the hook cold path. Low risk: structurally verified, not assumed. |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this gap fix — only existing deps (@huggingface/transformers dynamic import, node stdlib). No legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run tests/model/model-cache.test.ts tests/model/pipeline-singleton.test.ts tests/detect/orchestrator-ner.test.ts tests/detect/ner-unreachable.test.ts` — all green.
- `grep -n "allowRemoteModels = false" src/model/pipeline-singleton.ts` — present.
- `grep -nE "verifyModelIntegrity|isModelCached|downloadModel" src/model/pipeline-singleton.ts` — all three on the inference load path.
- `grep -n "model_rev" src/detect/index.ts` — sourced from getNerResolvedSha256(), not a hardcoded constant.
- `grep -rn "MODEL_DESCRIPTORS" src/` — referenced by model-cache (or pipeline-singleton), pipeline-singleton, and config/index.ts (PIIRANHA_* no longer dead).
- `npm run build && grep -c "from '@huggingface/transformers'" dist/cli.js` — 0 static imports (dynamic import only); hook cold path unchanged.
- Full suite `npx vitest run` — no regression in the three PASSED success criteria (hook-unreachability, advisory/substitute, fail-closed-for-NER).
</verification>

<success_criteria>
- SC-4 closed: the model the inference engine executes is loaded ONLY from a SHA-256-verified local cache (D-06), per-model (bert AND piiranha), failing closed on mismatch/missing-with-download-disabled.
- Audit provenance is truthful: model_rev = the resolved per-model pinned sha (piiranha stamps the piiranha hash), quant = resolved dtype (MODEL-04 / D-12).
- piiranha tier is integrity-verified, not merely selectable (NER-04); unknown models rejected at config load.
- The integrity-on-inference invariant is locked by tests that inject mock cache + mock pipeline (no real download).
- The three PASSED success criteria are untouched and still pass (hook-unreachability re-proven in dist).
</success_criteria>

<output>
Create `.planning/phases/06-ner-inference-l6b-mcp-wiring/06-04-SUMMARY.md` when done.
</output>
