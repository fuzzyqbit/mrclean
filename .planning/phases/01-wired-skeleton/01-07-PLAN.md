---
phase: 01-wired-skeleton
plan: 07
type: execute
wave: 2
depends_on: [01-06]
gap_closure: true
autonomous: false
requirements: [HOOK-05]
files_modified:
  - .planning/phases/01-wired-skeleton/01-UAT.md

must_haves:
  truths:
    - "In a real headless Claude Code session, deleting the mrclean hook bin causes the tool call to be BLOCKED — the canary filename never reaches the model (SC4/HOOK-05)."
    - "The internal-crash (UAT-2a) and banner+MCP (UAT-1) behaviors still pass after the wrapper change — no regression."
  artifacts:
    - path: ".planning/phases/01-wired-skeleton/01-UAT.md"
      provides: "Gap marked resolved after the live confirmation"
      contains: "resolved"
  key_links:
    - from: "npm run test:uat"
      to: "tests/uat/live-session.test.ts UAT-2b"
      via: "MRCLEAN_UAT=1 live claude session"
      pattern: "test:uat"
---

<objective>
Confirm, in a real headless Claude Code session, that the fail-closed wrapper shipped in 01-06 actually closes the SC4/HOOK-05 hole: with the mrclean bin missing, the wrapper exits 2 and Claude Code blocks the tool call so the canary never leaks.

The mechanism is already proven deterministically in 01-06 (spawnSync exit-code remap + Claude Code's documented "only exit 2 blocks" behavior). This plan is the paid, live end-to-end confirmation — it spends real Haiku tokens and needs an authenticated `claude` CLI, so it cannot run unattended.

Purpose: flip the original failing live test (UAT-2b) green and mark the gap resolved.
Output: `npm run test:uat` fully green (UAT-1, UAT-2a, UAT-2b) and 01-UAT.md updated to resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-wired-skeleton/01-UAT.md
@tests/uat/live-session.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    01-06 changed the installed hook command to a fail-closed `/bin/sh` wrapper (`"$1" "$2" hook || exit 2`)
    and updated `tests/uat/live-session.test.ts` `buildHookSettings` to use the shared `buildHookCommand`,
    so UAT-2b now exercises the shipped wrapper. The deterministic spawnSync remap test already passed in 01-06.
  </what-built>
  <how-to-verify>
    Security gate — do NOT auto-approve (this confirms a fail-closed guarantee; see project memory on the
    auto-advance/human-gate race).

    PRECONDITION (do this FIRST, before any test run): `.planning/config.json` currently has
    `workflow.auto_advance: true`, and project memory documents that a background auto-advance chain can
    auto-traverse a blocking human gate and self-approve it. Before starting this checkpoint the executor
    MUST confirm ONE of: (a) `workflow.auto_advance` is `false`, OR (b) the orchestrator has explicitly
    confirmed it will hard-halt on `gate="blocking"` for this task and not auto-advance. THEN run
    `git log --oneline -15` and scan for any unexpected 01-07 commits (a 01-07-SUMMARY or an 01-UAT.md
    resolution written without your action) — if found, a background chain is racing this gate: ABORT the
    checkpoint, do NOT approve, and report the detected chain rather than proceeding. Only continue to
    step 1 once no background traversal is possible.

    Steps:
    1. Ensure the `claude` CLI is installed and authenticated (`claude --version` exits 0; if OAuth expired,
       run `claude` in a fresh terminal or `claude login`).
    2. From the repo root run: `npm run test:uat` (this sets `MRCLEAN_UAT=1` and spends ~3 Haiku calls).
    3. Expected: all three tests pass — UAT-1 (banner + mrclean MCP connected), UAT-2a (internal crash blocks,
       canary absent), and critically UAT-2b (missing hook bin no longer silently passes the tool call through —
       the canary `UAT_CANARY_7Q3X.txt` never appears in the model reply or raw stdout).
    4. If UAT-2b still shows the canary, STOP — the wrapper is not blocking; report back rather than approve.
  </how-to-verify>
  <resume-signal>Type "approved — test:uat green (UAT-2b flipped)" once all three pass, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task: Mark the gap resolved in 01-UAT.md</name>
  <files>.planning/phases/01-wired-skeleton/01-UAT.md</files>
  <action>
    After the human-verify checkpoint confirms `npm run test:uat` is fully green, update `.planning/phases/01-wired-skeleton/01-UAT.md` so the WHOLE doc is internally consistent: (1) set the frontmatter `status:` to `resolved`; (2) change Test 3's `result:` from `issue` to `pass` with dated evidence (npm run test:uat UAT-2b green — canary absent; fail-closed wrapper exits 2) and remove/supersede the stale `reported:` line; (3) in the `## Gaps` entry itself, flip its `status: failed` to `status: resolved` and append a resolution note referencing plan 01-06 (fail-closed `/bin/sh` wrapper) and 01-07 (live confirmation); (4) update the `## Summary` block (passed: 3, issues: 0). Do not fabricate an evidence line if the checkpoint reported a failure — in that case leave every status unchanged and surface the failure.
  </action>
  <verify>
    <automated>grep -qi "resolved" .planning/phases/01-wired-skeleton/01-UAT.md &amp;&amp; ! grep -q "status: failed" .planning/phases/01-wired-skeleton/01-UAT.md &amp;&amp; grep -c "result: pass" .planning/phases/01-wired-skeleton/01-UAT.md | grep -qx 3</automated>
  </verify>
  <done>01-UAT.md frontmatter status is resolved, the `## Gaps` entry status is flipped from failed to resolved, no `status: failed` remains, all three tests read `result: pass`, and the gap entry notes closure via 01-06 + 01-07.</done>
</task>

</tasks>

<verification>
- `npm run test:uat` reports 3 passed / 0 failed (UAT-2b flipped from issue to pass).
- 01-UAT.md frontmatter status: resolved AND the Gaps entry status: resolved (no lingering `status: failed`); Test 3 result: pass with dated evidence.
</verification>

<success_criteria>
- Live headless session confirms a missing mrclean bin blocks the tool call (canary never reaches the model).
- No regression in UAT-1 (banner/MCP) or UAT-2a (internal-crash block).
- Gap in 01-UAT.md marked resolved (frontmatter + Gaps entry) with traceability to 01-06/01-07.
</success_criteria>

<output>
Create `.planning/phases/01-wired-skeleton/01-07-SUMMARY.md` when done.
</output>
