---
phase: 10-operator-restore
plan: 07
type: execute
wave: 3
depends_on: ["10-05", "10-06"]
files_modified:
  - tests/state/cold-path.test.ts
  - tests/restore/mixed-canary.test.ts
autonomous: true
requirements: [REVMODE-01, REVMODE-08]

must_haves:
  truths:
    - "No hook-reachable module — and no module anywhere in src/ except the sanctioned cli.ts dynamic-import site — references src/restore/ (source-level fence with positive controls, CI-locked)"
    - "src/restore/ modules import ONLY from the allowlist: node: builtins, intra-restore siblings, ../state/, ../detect/findings.js, ../audit/restore-log.js — no hook, no config, no detect engine, no mcp (structural no-shared-kill-switch proof)"
    - "A mixed document produced by the REAL reversible pipeline round-trips: the WORD-term 'fake path' restores to its original while the secret-class AWS placeholder survives byte-identical (SC2 / Phase 11 mixed-content canary substrate)"
    - "tools/list still returns exactly three tools and `restore` stays on FORBIDDEN_TOOL_NAMES (T2/T2b re-verified green)"
  artifacts:
    - path: "tests/state/cold-path.test.ts"
      provides: "Restore-import fence invariants (both directions) with anti-vacuity positive controls"
      contains: "restore"
    - path: "tests/restore/mixed-canary.test.ts"
      provides: "Real-pipeline mixed-content round-trip via runRestore (allocation -> persist -> restore)"
      min_lines: 80
  key_links:
    - from: "tests/state/cold-path.test.ts"
      to: "src/cli.ts"
      via: "positive control: exactly one await import('./restore/cli.js') site"
      pattern: "countAwaitImports"
    - from: "tests/restore/mixed-canary.test.ts"
      to: "src/restore/cli.ts"
      via: "runRestore with injected baseDir/cwd/stdin over state persisted by PlaceholderManager + persistAllocations"
      pattern: "runRestore"
---

<objective>
Regression-lock the trust boundary (REVMODE-01's zero-model-facing-surface + REVMODE-08's separate error domains, proven structurally) and build the mixed-content canary against the REAL reversible pipeline — the named Phase 11 gate substrate the ROADMAP requires Phase 10 to build against, not retrofit.

Purpose: STATE.md pins the trust direction ("src/restore/ introduces sensitive data — opposite direction from src/placeholder/"). Fences make that a build failure instead of a convention: restore is unreachable from every hook/MCP/detect path, and restore reaches nothing that could couple its failures to redaction.
Output: Extended cold-path fence suite + tests/restore/mixed-canary.test.ts, plus re-verification that T2 exact-three/T2b forbidden-names stay green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 5 fence strategy, §Pitfall 6, §Pitfall 10, Leak-Grep Extension Strategy item 1 — canary corpus)
@.planning/phases/10-operator-restore/10-PATTERNS.md (§tests/state/cold-path.test.ts — invariant machinery excerpts; §tests/restore/mixed-canary.test.ts — runReversibleEvent harness; Landmines — FORBIDDEN_TOOL_NAMES + vitest project routing)
@tests/state/cold-path.test.ts (HOOK_REACHABLE 42-52; hasRuntimeStaticImport 78-92; stripComments 71-75; countAwaitImports 106-111; walkTsFiles 114-121; invariant-4 full-tree walk 204-229 — reuse ALL machinery, fork none)
@tests/state/inspection.test.ts (runReversibleEvent 65-85 — the real-pipeline event driver to clone; canary constants 52-55)
@tests/mcp/tools-list.test.ts (FORBIDDEN_TOOL_NAMES 53-69; T2 exact-three assertion line 100 — must stay green, zero edits)

<interfaces>
<!-- No new production contracts. Fence rules pinned here are the executable spec. -->

Fence rules (implemented as new `it` blocks in the existing cold-path describe structure):
1. INBOUND: no file in HOOK_REACHABLE contains the substring '/restore/' in stripped source (runtime, type-only, or dynamic — total ban on hook paths).
2. INBOUND (full-src): walkTsFiles(SRC) — every file OUTSIDE src/restore/ whose stripped source contains a '/restore/' import specifier is an offender, EXCEPT cli.ts. Positive controls: offender scan finds >= 60 files walked; countAwaitImports(read cli.ts, './restore/cli.js') === 1; hasRuntimeStaticImport(cli.ts, '/restore/') === false.
3. OUTBOUND (allowlist): for each file under src/restore/, every import specifier must match one of: /^node:/, /^\.\//  (intra-restore), /^\.\.\/state\//, exactly '../detect/findings.js', exactly '../audit/restore-log.js'. Offenders listed by file + specifier. Positive control: the walk finds >= 3 restore files.
4. Existing invariants 1-4 stay green unmodified (cipher/lock confinement to src/state/ now spans a larger walk).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cold-path fence extension — restore-import confinement both directions</name>
  <files>tests/state/cold-path.test.ts</files>
  <read_first>
    - tests/state/cold-path.test.ts (ENTIRE file — reuse stripComments/hasRuntimeStaticImport/countAwaitImports/walkTsFiles and the offenders-array + positive-control assertion style of invariant 4; extend, never fork)
    - src/cli.ts (the sanctioned dynamic-import site as registered by 10-05)
    - src/restore/index.ts, src/restore/session-index.ts, src/restore/cli.ts (current import lists — the allowlist ground truth)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 5 — why structural separation IS the REVMODE-08 no-shared-kill-switch proof)
  </read_first>
  <action>
    Append a new describe block ('restore trust-boundary fences (Phase 10)') to tests/state/cold-path.test.ts implementing fence rules 1-3 from the interfaces block, reusing the file's existing helpers verbatim (no duplicated stripComments). Each rule collects offenders into an array asserted toEqual([]) with a descriptive failure message, plus its anti-vacuity positive control (walk-count floors, the exactly-one dynamic-import count, the >= 3 restore-file floor). Rule 3's allowlist is exactly the five-pattern list — a new import in src/restore/ of, e.g., '../config/index.js' or '../hook/index.js' must turn CI red. Run the full cold-path suite (old + new invariants) green. Commit `test(10-07): fence src/restore both directions — cli.ts dynamic site only, allowlist imports`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/cold-path.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - New describe block with >= 3 fence tests + >= 3 positive controls; all green
    - Existing invariants 1-4 pass unmodified (git diff shows only appended block + any shared-constant hoist)
    - Sabotage spot-check documented in SUMMARY: temporarily adding `import '../restore/index.js'` to src/detect/index.ts makes rule 2 fail (revert immediately — verifies the fence actually trips; mirror of the 09-07 planted-import verification)
    - `npm run typecheck` clean of new-file errors
  </acceptance_criteria>
  <done>Restore reachability is CI-locked in both directions with proven-live fences.</done>
</task>

<task type="auto">
  <name>Task 2: Mixed-content canary — real pipeline round-trip via runRestore</name>
  <files>tests/restore/mixed-canary.test.ts</files>
  <read_first>
    - tests/state/inspection.test.ts (runReversibleEvent 65-85 — clone the hydrate->allocate->drain->persist driver; tmpdir baseDir lifecycle 135-143; canary constant style 52-55)
    - tests/state/chaos.test.ts (lines 74-78 — why the secret canary must NOT end in 'EXAMPLE': AKIAIOSFODNN7EXAMPLE is gitleaks-allowlisted)
    - src/restore/cli.ts (RunRestoreOpts — the injected-seam signature from 10-05)
    - src/placeholder/manager.ts (allocate + drainPendingAllocations — READ ONLY, zero edits; frozen tree)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 2 — "the mixed-content canary's fake path must be a WORD-list term; there is no PATH type")
  </read_first>
  <behavior>
    Canaries: WORD_ORIGINAL = 'zz-canary-project-path-7g2' (the "fake path" — WORD-typed by design), SECRET_ORIGINAL = a fake AWS key NOT ending in 'EXAMPLE' (e.g. 'AKIAIOSFODNN7EXAMPLX').
    Flow: (1) drive TWO real reversible events against a tmp baseDir + sid via the runReversibleEvent recipe — allocate WORD_ORIGINAL as 'WORD' and SECRET_ORIGINAL as 'AWS_KEY'; capture the emitted v2 placeholders from the persisted map (readSessionMapFile) — never hand-format tokens; (2) compose a mixed document embedding both placeholders in prose; (3) runRestore({ baseDir, cwd: tmpCwd, stdin: Readable.from([doc]) }) with stdout/stderr captured.
    Assertions:
    - stdout contains WORD_ORIGINAL (round-trip works) — non-vacuity: restored count in the summary line is >= 1
    - stdout contains the AWS placeholder BYTE-IDENTICAL and does NOT contain SECRET_ORIGINAL (secret survives)
    - summary line reports secret-skipped=1
    - the audit line written to tmpCwd contains neither canary raw (hash-only end-to-end sanity; full leak sweep is 10-08's job)
  </behavior>
  <action>
    Create tests/restore/mixed-canary.test.ts per the behavior block, in the unit project (pure-source harness — do NOT add to the integration include list; a file cannot belong to both projects, vitest.config.ts landmine). Reuse the persistAllocations POST deadline constant the inspection harness uses. Name the describe with the Phase 11 gate name ('mixed-content canary (Phase 11 gate substrate)') so verify-work and Phase 11 planning can find it by grep. After it passes, re-run the shipped no-model-surface gate to close SC2: npm run build && tools-list integration — dist artifacts stay UNCOMMITTED (worktree rule: dist rebuilds happen on main post-merge; verify with git status before committing). Commit `test(10-07): mixed-content canary — WORD round-trips, secret placeholder survives`.
  </action>
  <verify>
    <automated>npx vitest run tests/restore/mixed-canary.test.ts --project=unit && npm run build && npx vitest run tests/mcp/tools-list.test.ts --project=integration</automated>
  </verify>
  <acceptance_criteria>
    - Mixed-canary suite green with all four assertion groups; placeholders sourced from the persisted map, not hand-formatted (grep: zero formatV2Token calls used to BUILD the document's tokens — reading them from readSessionMapFile entries instead)
    - tests/mcp/tools-list.test.ts green unmodified: exactly ['mrclean_check','mrclean_redact','mrclean_status'], `restore` absent (T2/T2b — the REVMODE-01 CI check)
    - `git status --porcelain dist/` empty at commit time (no dist committed from the worktree)
    - Secret canary string does not end in 'EXAMPLE' (grep the test file)
  </acceptance_criteria>
  <done>SC2 proven end-to-end: mixed document round-trips restorables, secrets survive, model surface unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-facing surfaces (hook, MCP tools) -> src/restore/ | must be structurally unreachable — the fence turns the ban into a build failure |
| src/restore/ -> redaction machinery | outbound allowlist proves restore cannot couple into detection/hook state (no shared kill switch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Elevation | prompt-injected model invoking restore | mitigate | T2 exact-three + T2b FORBIDDEN_TOOL_NAMES re-verified; fence rule 1 bans '/restore/' from every hook-reachable module; rule 2 confines the import to cli.ts's dynamic site with a positive control |
| T-10-07-02 | DoS on protection | restore failure cascading into redaction | mitigate | Outbound allowlist (rule 3): restore cannot import config/hook/detect-engine — no shared flags or state exist to break; REVMODE-08 structural proof |
| T-10-07-03 | Info disclosure | secret-class round-trip regression | mitigate | Mixed-content canary drives the REAL allocator + store (Pitfall 10 — never synthetic-only); AWS placeholder byte-survival + secret-skipped=1 assertions |
| T-10-07-04 | Tampering (vacuous pass) | fence tests that never trip | mitigate | Positive controls (walk floors, exactly-one dynamic import) + documented sabotage spot-check (planted import must fail the suite, 09-07 precedent) |
</threat_model>

<verification>
- `npx vitest run tests/state/cold-path.test.ts tests/restore/mixed-canary.test.ts --project=unit` green
- `npm run build && npx vitest run tests/mcp/tools-list.test.ts --project=integration` green
- `git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder tests/placeholder tests/detect` empty; no dist/ paths in any commit
</verification>

<success_criteria>
- SC2 complete: mixed-content canary green against the real pipeline; zero model-facing restore surface re-proven by CI (T2/T2b + both-direction fences)
- REVMODE-08's "no shared kill switch" is a structural, grep-provable property — not an assertion
</success_criteria>

<output>
Create `.planning/phases/10-operator-restore/10-07-SUMMARY.md` when done (include the sabotage spot-check result).
</output>
