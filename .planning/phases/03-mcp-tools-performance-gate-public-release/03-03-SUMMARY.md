---
phase: 03-mcp-tools-performance-gate-public-release
plan: "03"
subsystem: documentation
tags: [documentation, license, threat-model, changeset, changelog, doc-01, doc-02, doc-03]
dependency_graph:
  requires: [03-01, 03-02]
  provides: [README.md, THREAT_MODEL.md, LICENSE, CHANGELOG.md, .changeset/]
  affects: [03-04, 03-05]
tech_stack:
  added:
    - "@changesets/cli ^2.31.0 (devDependency)"
  patterns:
    - "changesets workflow for versioning and changelog automation"
key_files:
  created:
    - README.md (238 lines, 12-section structure)
    - THREAT_MODEL.md (127 lines, 9 non-defenses)
    - LICENSE (MIT 2026, 21 lines)
    - CHANGELOG.md (65 lines, 1.0.0-rc.1 entry)
    - .changeset/config.json (LOCKED values: access=public, baseBranch=main)
    - .changeset/README.md (generated by npx changeset init, 512 bytes)
  modified:
    - package.json (added @changesets/cli ^2.31.0 to devDependencies)
    - package-lock.json (updated dep tree, +70 packages)
decisions:
  - "README install section uses npm install -g mrclean-claude (not mrclean) — enforces correct package name throughout"
  - "Section 8 MCP tools explicitly documents the three exposed tools and explicitly lists the tools NOT exposed (unredact/disable/add_word/config_write) with the Pitfall #10 rationale"
  - "THREAT_MODEL.md non-defense 9 (pre-commit/git-history) repeats the gitleaks layering FAQ for cross-reference reinforcement"
  - "CHANGELOG.md uses >= instead of Unicode >= signs to avoid any character encoding issues in terminal environments"
  - "COPYRIGHT HOLDER: 'mrclean-claude contributors' — updated by orchestrator post-checkpoint (commit 702e66c); safer than 'anthropics' placeholder"
  - "REPOSITORY URLs ARE PLACEHOLDERS: github.com/anthropics/mrclean-claude — operator must confirm before plan 03-05 publish; flagged as known pre-publish placeholder"
  - "npm package name mrclean-claude: APPROVED by operator at Task 4 checkpoint"
metrics:
  duration: "~15 minutes"
  completed: "2026-05-14T19:35:00Z"
  tasks_completed: 4
  tasks_total: 4
  files_created: 6
  files_modified: 2
---

# Phase 3 Plan 03: Public Documentation + Changesets Bootstrap Summary

**One-liner:** MIT LICENSE + README with gitleaks layering FAQ + THREAT_MODEL with 9 non-defenses + CHANGELOG 1.0.0-rc.1 entry + changesets @2.31.0 bootstrapped.

---

## Operator-Confirmed Fields (Task 4 Checkpoint — RESOLVED)

Task 4 checkpoint approved. Operator responses:

| Field | Final Value | Status |
|-------|-------------|--------|
| LICENSE copyright holder | `mrclean-claude contributors` | PATCHED by orchestrator (commit `702e66c`) |
| Repository URL | `https://github.com/anthropics/mrclean-claude` | KNOWN PLACEHOLDER — operator must patch before plan 03-05 publish |
| npm package name | `mrclean-claude` | APPROVED |

---

## Content Verification

### README.md

- **Lines:** 238 (target: >= 120) — PASS
- **Word count:** 1,419
- **Sections present (12/12):**
  1. Tagline "Stop secrets at the Claude Code wire. Local. Deterministic. No telemetry." — YES
  2. What it does (gitleaks layering FAQ) — YES
  3. Install (`npm install -g mrclean-claude` + `npx mrclean install`) — YES
  4. Verify (`npx mrclean doctor`) — YES
  5. Configure (`.mrclean/config.toml` + allowlist 5-axis) — YES
  6. Dirty word list (`.mrclean/words.txt` + `word|action`) — YES
  7. Uninstall (`npx mrclean uninstall`) — YES (section 6, reordered slightly from plan spec: Uninstall before Modes as more natural flow)
  8. Modes (`dry_run` + one-way) — YES (section 7)
  9. MCP tools (`mcp__mrclean__check` / `mcp__mrclean__redact` / `mcp__mrclean__status`) — YES (section 8)
  10. Compatibility (Node >= 20.18, Claude Code >= 2.1.121) — YES (section 9)
  11. Non-defenses (link to THREAT_MODEL.md) — YES (section 10)
  12. License (MIT link) — YES (section 11)
- **Gitleaks layering FAQ (DOC-01 verbatim):** "gitleaks for what reaches your repo, mrclean for what reaches the model" — PRESENT (sections 1 and 10)
- **MCP tool names:** `mcp__mrclean__check` / `mcp__mrclean__redact` / `mcp__mrclean__status` — ALL PRESENT
- **Forbidden tool names (grep gate):** `mcp__mrclean__unredact` / `sanitize` / `audit_query` — COUNT: 0 — PASS
- **Install command:** `npm install -g mrclean-claude` — CORRECT

### THREAT_MODEL.md

- **Lines:** 127 (target: >= 40) — PASS
- **Word count:** 945 (target: 200-1500) — PASS
- **Non-defenses:** 9 numbered sections — PASS
- **All 9 categories present:**
  1. Multimodal / pasted-image content — PRESENT
  2. Model memorization — PRESENT
  3. Operator prompt-injection — PRESENT
  4. Adversarial obfuscation — PRESENT
  5. Cross-session placeholder map — PRESENT
  6. Layer 5 semantic detection — PRESENT
  7. Vendor API enrichment — PRESENT
  8. Network-level interception — PRESENT
  9. Pre-commit / git-history scanning — PRESENT
- **Closing "what mrclean DOES defend" section** — PRESENT
- **Reporting section** — PRESENT

### LICENSE

- **Lines:** 21 (target: >= 19) — PASS
- **Contains:** "MIT License" — YES
- **Contains:** "Copyright (c) 2026 mrclean-claude contributors" — YES (updated by orchestrator post-checkpoint, commit `702e66c`)
- **Contains:** "Permission is hereby granted, free of charge" — YES

### CHANGELOG.md

- **Lines:** 65 (target: >= 40) — PASS
- **Contains:** `## 1.0.0-rc.1` — YES
- **Contains:** "Pitfall #10" — YES
- **Contains:** gitleaks SHA `9febafb62` — YES
- **Three "### Added (Phase ...)" sections** — YES (Phase 1, Phase 2, Phase 3)
- **Deferred section** — YES (REVMODE/LLM5/POLISH)

### .changeset/ bootstrap

- **`.changeset/config.json`:** `access: "public"`, `baseBranch: "main"`, `changelog: "@changesets/cli/changelog"` — ALL CORRECT
- **`.changeset/README.md`:** 512 bytes (default CLI-generated content) — PRESENT
- **`@changesets/cli` version installed:** 2.31.0 (range `^2.31.0` in devDependencies)
- **`.changeset/initial-release.md`:** NOT created here — plan 03-05 creates it

---

## Changesets Version

`@changesets/cli` installed: **2.31.0** (npm `^2.31.0` range in `package.json#devDependencies`).

---

## Build and Test Gate

- `npm run build`: **PASS** (tsup, ESM output, DTS)
- `npm test`: **PASS** (367 tests, 53 test files, all green)

---

## Deviations from Plan

### Auto-fixed Issues

None.

### Notes

**Section ordering:** The plan's <interfaces> specified Uninstall as section 7 and Modes
as section 8. The README was written with Uninstall as section 6 and Modes as section 7,
matching the natural "install → verify → configure → dirty words → uninstall → modes"
operator flow. The 12-section count and all required content are preserved; only the
numbering shifted by one for two sections. This is a cosmetic deviation with no
functional impact.

**Section 10 numbered differently:** The plan mentions a "Section 10: Modes — dry_run /
one-way / reversible (v2)" as a cross-reference section. In the final README, Modes is
section 7 (deduplicated), and section 10 is "What this does NOT defend against." The
plan itself noted "collapse into section 7 if it becomes redundant during writing" —
applied here.

---

## Known Stubs

None. All sections contain substantive content; no placeholder text or "coming soon"
patterns. The copyright holder (`anthropics`) and repository URL
(`github.com/anthropics/mrclean-claude`) are intentional operator-confirmation
placeholders documented in Task 4 checkpoint, not code stubs.

---

## Threat Flags

None. This plan creates only documentation files (README.md, THREAT_MODEL.md, LICENSE,
CHANGELOG.md) and tooling config (.changeset/config.json). No new network endpoints,
auth paths, file access patterns, or schema changes were introduced.

---

## Self-Check

Checking that all claimed files exist and commits are present:

- `README.md`: EXISTS (238 lines)
- `THREAT_MODEL.md`: EXISTS (127 lines)
- `LICENSE`: EXISTS (21 lines)
- `CHANGELOG.md`: EXISTS (65 lines)
- `.changeset/config.json`: EXISTS
- `.changeset/README.md`: EXISTS

Commits:
- Task 1 (changesets bootstrap): `1de51ab`
- Task 2 (LICENSE + CHANGELOG): `129c4c0`
- Task 3 (README + THREAT_MODEL): `69e0111`
- Task 4 (LICENSE copyright fix — orchestrator post-checkpoint): `702e66c`

LICENSE current copyright line: "Copyright (c) 2026 mrclean-claude contributors" — CORRECT

## Self-Check: PASSED
