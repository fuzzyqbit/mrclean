---
phase: 07-pii-security-hardening-honest-framing
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/shared/strings.ts
  - src/mcp/tools/check.ts
  - src/mcp/tools/redact.ts
  - src/doctor/checks.ts
  - src/hook/handlers/session-start.ts
  - README.md
  - docs/SCOPE-FENCE.md
  - tests/copy-drift.test.ts
  - .github/workflows/canary-leak.yml
autonomous: false
requirements: [PIISEC-01, PIISEC-02]
user_setup: []
must_haves:
  truths:
    - "A single best-effort PII disclaimer string is centralized in src/shared/strings.ts and reused across all surfaces"
    - "The disclaimer surfaces ONCE per output on the README PII section, mrclean doctor, the SessionStart banner line, and the MCP tool descriptions"
    - "User-facing copy contains no compliance/guarantee CLAIM language (redacts all PII / GDPR/HIPAA compliant / guarantees all|every) — but the allowed negation 'not a guarantee' is permitted"
    - "A vitest copy-drift test fails the build if a banned claim phrase appears in any scanned string source, and is non-vacuous (a positive control fires)"
    - "The PII leak-grep regression test (07-01) has a CI job plus a belt-and-suspenders grep pass over .mrclean/audit*.jsonl"
    - "README PII framing is consistent with docs/SCOPE-FENCE.md and never contradicts the locked scope fence"
  artifacts:
    - path: "src/shared/strings.ts"
      provides: "Centralized best-effort disclaimer text + banned-phrase list (single source of truth)"
      contains: "export const"
    - path: "README.md"
      provides: "New PII/NER framing section (best-effort recall aid, not a guarantee — D-07)"
      contains: "best-effort"
    - path: "tests/copy-drift.test.ts"
      provides: "Banned-phrase scan + disclaimer-presence assertions (D-05/D-08)"
    - path: ".github/workflows/canary-leak.yml"
      provides: "PII leak-test CI job + belt-and-suspenders PII grep (PIISEC-01 CI gate)"
      contains: "pii-canary-leak"
  key_links:
    - from: "README.md / src/doctor/checks.ts / src/hook/handlers/session-start.ts / src/mcp/tools/*.ts"
      to: "src/shared/strings.ts"
      via: "all surfaces import/reuse the one disclaimer constant (no copy drift)"
      pattern: "PII_BEST_EFFORT|strings"
    - from: "tests/copy-drift.test.ts"
      to: "README.md + src/shared/strings.ts + MCP tool descriptions + banner + doctor"
      via: "regex scan of user-facing string sources for banned claim phrases"
      pattern: "BANNED|redacts? all PII"
    - from: ".github/workflows/canary-leak.yml"
      to: "tests/audit/pii-canary-leak.test.ts (from 07-01)"
      via: "integration vitest job + grep -F over audit*.jsonl"
      pattern: "pii-canary-leak"
---

<objective>
Frame the PII/NER layer honestly everywhere it surfaces, lock that framing against future copy-drift, and wire the PIISEC-01 leak test into CI. Delivers the D-05/D-07/D-08 half of PIISEC-02 plus the CI gate for PIISEC-01: a single centralized best-effort disclaimer (src/shared/strings.ts) surfaced once-per-output on README + `mrclean doctor` + the SessionStart banner + the MCP tool descriptions, a vitest copy-drift test (D-08) that fails the build on compliance/guarantee CLAIM language, and a canary-leak.yml PII job that runs 07-01's leak test plus a belt-and-suspenders grep.

Purpose: Secrets are the deterministic guarantee; PII/NER is a best-effort recall aid whose false negatives CAN leak. Copy must never blur the two into "mrclean redacts your PII"; the disclaimer points users to `words.txt` + deterministic layers as the real must-not-leak lever (D-07). The CI job makes the leak guarantee permanent.
Output: src/shared/strings.ts; README PII section; doctor + banner + MCP-description disclaimer surfacing; tests/copy-drift.test.ts; canary-leak.yml PII job. Copy wording is staged for human review (D-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-CONTEXT.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-PATTERNS.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-02-PLAN.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase, use directly. -->

From src/shared/version.ts (the const-export module shape to mirror for the NEW strings.ts):
```typescript
export const VERSION: string = pkg.version   // single-purpose const-export module in src/shared/
```

From src/hook/banner.ts (KEEP PURE — do NOT append the disclaimer here; strict tests assert exact banner text):
```typescript
export function buildBanner(config, ruleCount, allowlistCount): string
// returns EXACTLY: `mrclean active vN.N.N (rules: N, allowlist: N, mode: active|dry-run)`
// tests/hook/handlers.test.ts:67 BANNER_PATTERN = /^mrclean active v...mode: (active|dry-run)\)$/  ← anchored, would BREAK if suffixed
// src/doctor/canary.ts:87 asserts additionalContext.startsWith('mrclean active v')  ← a separate line is safe
```

From src/hook/handlers/session-start.ts (where the banner is emitted to stderr — emit the disclaimer as a SEPARATE adjacent line here, NOT inside buildBanner):
```typescript
// line 46: const banner = buildBanner(config, ruleCount, allowlistCount)
// banner is written to stderr; add a second stderr line with the centralized disclaimer (once per SessionStart)
```

From src/doctor/checks.ts (CheckResult.detail is the D-05 note home — a PASS/SKIP detail string, never a new failing exit code):
```typescript
export interface CheckResult { name: string; status: 'PASS'|'FAIL'|'SKIP'; detail: string; exitCodeOnFail: number }
// checkModelCache returns a CheckResult; append the best-effort note to its `detail` (do NOT add a check or change exitCodeOnFail)
```

From src/mcp/tools/check.ts / redact.ts (description string is the once-per-output MCP surface — 07-02 added bestEffort to findingSchema; THIS plan adds the disclaimer to the tool description):
```typescript
// check.ts:99-102 description: 'Scan text ... return findings. ...'  → append the centralized best-effort clause ONCE
// redact.ts:93-97 description: '...'                                  → same (mirror)
```

From src/audit/canary-leak.ts (the line-by-line scan + offender-accumulation shape to INVERT for the banned-phrase test):
```typescript
export async function assertNoCanaryLeak(logPath: string, canaries: string[]): Promise<{ ok: boolean; leaked: ... }>
```

From .github/workflows/canary-leak.yml (the double-pass gate pattern to extend — see read_first):
```yaml
# existing job: npx vitest run --project=integration tests/fixtures-corpus.test.ts ...
# + belt-and-suspenders: for f in tests/fixtures/positive/*.txt; grep -F "$val" .mrclean/audit*.jsonl
```
</interfaces>

<read_first>
@src/shared/version.ts            # const-export module shape to mirror for the new strings.ts
@src/hook/banner.ts               # buildBanner — MUST stay pure; do NOT suffix (BANNER_PATTERN would break)
@src/hook/handlers/session-start.ts  # line 46 — emit the disclaimer as a separate stderr line here
@tests/hook/handlers.test.ts      # line 67 BANNER_PATTERN — the anchored regex that forbids suffixing buildBanner
@src/doctor/checks.ts             # checkModelCache CheckResult.detail — the D-05 note home; exitCodeOnFail must not change
@src/doctor/report.ts             # renderReport — confirms one line per check detail (no new render branch)
@src/mcp/tools/check.ts           # description (99-102) + structuredContent — once-per-output disclaimer site (post-07-02)
@src/mcp/tools/redact.ts          # description (93-97) — mirror site
@README.md                        # §8 MCP tools + §10 "What this does NOT defend against" (242-259) tone analog; no PII section yet
@docs/SCOPE-FENCE.md              # framing voice; new README section MUST NOT contradict the locked scope fence
@src/audit/canary-leak.ts         # scan-and-report engine to invert for the banned-phrase test
@tests/audit/canary-leak.test.ts  # AAA + tmpdir isolation structure for the new copy-drift test
@.github/workflows/canary-leak.yml  # double-pass gate to extend with the PII leak job (07-01's test)
  </read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize disclaimer + banned-phrase list; surface disclaimer once-per-output on all runtime surfaces</name>
  <files>src/shared/strings.ts, src/mcp/tools/check.ts, src/mcp/tools/redact.ts, src/doctor/checks.ts, src/hook/handlers/session-start.ts</files>
  <read_first>
@src/shared/version.ts
@src/hook/banner.ts
@src/hook/handlers/session-start.ts
@tests/hook/handlers.test.ts
@src/doctor/checks.ts
@src/mcp/tools/check.ts
@src/mcp/tools/redact.ts
@docs/SCOPE-FENCE.md
  </read_first>
  <action>
    Create src/shared/strings.ts mirroring the single-const-export shape of src/shared/version.ts. Export TWO constants: (1) `PII_BEST_EFFORT_DISCLAIMER` — one short sentence per the D-07 stance: "best-effort ML PII hint, not a guarantee; NER false negatives can leak; use words.txt + the deterministic layers (secrets + checksum'd PII) for must-not-leak data." (draft per stance; exact wording reviewed in Task 4). (2) `BANNED_COPY_PHRASES` — the regex list consumed by the copy-drift test (Task 3). Keep the file < 200 lines, no I/O, no engine imports.
    Surface the disclaimer ONCE per output (D-05) on each runtime surface by importing `PII_BEST_EFFORT_DISCLAIMER`:
    - MCP: append the disclaimer clause to the `description` string in src/mcp/tools/check.ts (99-102) and src/mcp/tools/redact.ts (93-97) — once per tool description, NOT per finding. (07-02 already added the per-finding bestEffort boolean; this is the human-readable once-per-output note.)
    - doctor: append the disclaimer to the `detail` string of checkModelCache's CheckResult in src/doctor/checks.ts (a PASS/SKIP detail; do NOT add a new check or change exitCodeOnFail).
    - banner: in src/hook/handlers/session-start.ts (line 46), AFTER writing the buildBanner line to stderr, write the disclaimer as a SEPARATE adjacent stderr line (once per SessionStart). DO NOT modify src/hook/banner.ts buildBanner — tests/hook/handlers.test.ts:67 BANNER_PATTERN is anchored `^...$` and would break if the banner string is suffixed; the doctor canary (canary.ts:87) only checks startsWith, so a separate line is safe.
    All disclaimer copy must avoid banned CLAIM phrases (Task 3 enforces) — the negation "not a guarantee" is allowed by design.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp tests/hook tests/doctor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PII_BEST_EFFORT_DISCLAIMER\|BANNED_COPY_PHRASES" src/shared/strings.ts` shows both exports.
    - `grep -rln "PII_BEST_EFFORT_DISCLAIMER" src/mcp/tools/check.ts src/mcp/tools/redact.ts src/doctor/checks.ts src/hook/handlers/session-start.ts` lists all four consumers (single source of truth, no inline copies).
    - `git diff --stat src/hook/banner.ts` shows NO change to banner.ts (buildBanner stays pure; BANNER_PATTERN intact).
    - checkModelCache `exitCodeOnFail` value is unchanged (note rides on `detail`, no new failing exit code).
    - `npx vitest run tests/mcp tests/hook tests/doctor` is green (BANNER_PATTERN test still passes — disclaimer is a separate line).
  </acceptance_criteria>
  <done>One centralized disclaimer constant reused across MCP descriptions, doctor detail, and a separate SessionStart stderr line; banner.ts unchanged; doctor exit codes unchanged; existing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: README PII/NER framing section + SCOPE-FENCE consistency</name>
  <files>README.md, docs/SCOPE-FENCE.md</files>
  <read_first>
@README.md
@docs/SCOPE-FENCE.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-CONTEXT.md
@src/shared/strings.ts
  </read_first>
  <action>
    Add a NEW PII/NER framing section to README.md (greenfield — §1-§11 currently have no PII heading). Place it near §8 "MCP tools (in-session, opt-in)" (which exposes NER) or alongside §10 "What this does NOT defend against"; match the candid, bulleted limitations tone of §10 (242-259) and the framing voice of docs/SCOPE-FENCE.md. Stance is LOCKED per D-07: "best-effort ML PII hint, not a guarantee"; NER false negatives CAN leak; `words.txt` + deterministic layers (secrets + checksum'd PII like SSN/credit-card) are the real must-not-leak mechanism; secrets remain the deterministic guarantee. Keep the prose consistent with the centralized PII_BEST_EFFORT_DISCLAIMER (same stance, README may expand it).
    Review docs/SCOPE-FENCE.md for the secrets=deterministic-guarantee vs PII/NER=best-effort framing; align the README section so it does not contradict the locked scope fence. Only edit SCOPE-FENCE.md if a contradiction or a missing cross-reference is found (e.g. add a one-line pointer to the new README section); do not weaken any ban.
    The section MUST NOT contain banned CLAIM phrases ("redacts all PII", "GDPR/HIPAA compliant", "guarantees all/every") — Task 3's test will fail the build otherwise. The negation "not a guarantee" is acceptable.
  </action>
  <verify>
    <automated>grep -niE "best-effort|not a guarantee|words.txt|false negative|can leak" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md has a new heading containing "PII": `grep -nE "^#+ .*PII" README.md` matches.
    - The section names `words.txt` + deterministic layers as the real must-not-leak lever AND states NER false negatives can leak: `grep -niE "words.txt" README.md` and `grep -niE "false negative|can leak" README.md` both match.
    - No banned overclaim phrases: `grep -niE "redacts? all pii|gdpr|hipaa" README.md` returns nothing; any "compliant" hit is not a self-claim.
    - docs/SCOPE-FENCE.md is unchanged OR changed only to add a cross-reference / fix a contradiction (no ban weakened): `git diff docs/SCOPE-FENCE.md` shows no removal of a Ban section.
  </acceptance_criteria>
  <done>README has an honest PII/NER section matching D-07 stance, consistent with SCOPE-FENCE.md, free of overclaim phrases.</done>
</task>

<task type="auto">
  <name>Task 3: Copy-drift (banned-phrase) vitest test + canary-leak.yml PII job</name>
  <files>tests/copy-drift.test.ts, .github/workflows/canary-leak.yml</files>
  <read_first>
@src/audit/canary-leak.ts
@tests/audit/canary-leak.test.ts
@.github/workflows/canary-leak.yml
@src/shared/strings.ts
@README.md
  </read_first>
  <action>
    Create tests/copy-drift.test.ts. Import `BANNED_COPY_PHRASES` and `PII_BEST_EFFORT_DISCLAIMER` from src/shared/strings.ts (single source of truth). Reuse the line-by-line scan + offender-accumulation shape from src/audit/canary-leak.ts (inverted intent: forbidden phrases). Scan these user-facing SOURCES with `readFileSync` (D-08): README.md, src/shared/strings.ts, src/hook/banner.ts, src/doctor/checks.ts, src/mcp/tools/check.ts, src/mcp/tools/redact.ts.
    Banned CLAIM-form regexes (CRITICAL — Pitfall 5: ban the CLAIM, never the bare word; the disclaimer itself says "not a guarantee"): `/redacts? all PII/i`, `/\b(GDPR|HIPAA|CCPA)\b[^.]*compliant/i`, `/\bfully compliant\b/i`, `/\bguarantees? (that )?(all|every) /i`. Do NOT ban a bare `/guarantee/`. Grep-hygiene: strip comment-only lines before scanning source files (skip lines matching `^\s*[/*]`) so JSDoc prose mentioning "guarantee" cannot self-trip the gate; README is prose so scan it whole.
    Add a non-vacuous positive control: assert the scanner flags a synthetic input string containing "redacts all PII" (offenders length >= 1) — proves the detector works.
    Add a disclaimer-presence assertion (D-05): assert `PII_BEST_EFFORT_DISCLAIMER` (or its key phrase) appears in README, and that the disclaimer string itself passes the banned-phrase scan (Pitfall 5 self-check).
    Place the test under tests/ so the default unit project picks it up (runs in `npm test`).
    Extend .github/workflows/canary-leak.yml: add a step running 07-01's PII leak test — `npx vitest run --project=integration tests/audit/pii-canary-leak.test.ts` — and a belt-and-suspenders second pass mirroring the existing `grep -F` loop (48-62): for each synthetic PII canary value (the 07-01 PII_CANARIES set), `grep -F` against `.mrclean/audit*.jsonl` and `exit 1` with `::error::` on any hit. Copy the `set -e` + per-value loop discipline.
  </action>
  <verify>
    <automated>npx vitest run tests/copy-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/copy-drift.test.ts` passes against current real sources.
    - Non-vacuous: a synthetic "redacts all PII" input is detected (the positive-control assertion is present and passes).
    - Banned regexes use claim-form and allow the negation: `grep -n "all\|every\|compliant\|redacts" tests/copy-drift.test.ts` matches AND the test asserts PII_BEST_EFFORT_DISCLAIMER (containing "not a guarantee") is NOT flagged.
    - Scanned-sources list includes README.md + both MCP tool files + banner + doctor + strings.ts: `grep -nE "README|banner|checks|check.ts|redact.ts|strings" tests/copy-drift.test.ts` matches.
    - canary-leak.yml runs the PII leak test and a PII grep pass: `grep -n "pii-canary-leak" .github/workflows/canary-leak.yml` matches and a `grep -F`/`::error::` PII block is present.
  </acceptance_criteria>
  <done>copy-drift test green over real copy, non-vacuous, comment-safe, allows "not a guarantee"; canary-leak.yml gains a PII leak-test job + belt-and-suspenders grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review of PII framing copy wording (D-07 sign-off)</name>
  <what-built>The user-facing PII framing copy: the centralized PII_BEST_EFFORT_DISCLAIMER (src/shared/strings.ts), the README PII/NER section, the `mrclean doctor` note, the SessionStart banner disclaimer line, and the MCP tool description clauses. All copy passes the automated banned-phrase gate; this checkpoint is the human sign-off on the WORDING (D-07 stance is locked, exact phrasing is Claude's discretion + user review per VALIDATION.md Manual-Only).</what-built>
  <how-to-verify>
    1. Read the new README PII/NER section: `grep -nA 20 -E "^#+ .*PII" README.md` — confirm it frames NER as "best-effort ML PII hint, not a guarantee," states NER false negatives can leak, and points to words.txt + deterministic layers as the real must-not-leak mechanism. No drift toward "redacts all PII" or compliance claims.
    2. Run `npm run build && node dist/cli.js doctor` (or the project's doctor entry) and confirm the once-per-output best-effort note appears in the report.
    3. Inspect the SessionStart banner disclaimer line and the MCP tool description clause: `grep -n "PII_BEST_EFFORT_DISCLAIMER" src/shared/strings.ts` then read the constant — confirm the wording reads honestly and consistently across all four surfaces.
    4. Confirm the asymmetry is clear: secrets = deterministic guarantee; PII/NER = best-effort recall aid.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the wording, or describe specific wording changes (the disclaimer constant is the single edit point — changing it updates all surfaces).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NER (probabilistic) → operator/model expectation | Best-effort recall must never be represented as a guarantee; false negatives can leak |
| user-facing copy (README/CLI/doctor/MCP desc) → operator trust | Overclaim/compliance language would misrepresent the security posture and induce unsafe behavior |
| detection internals → .mrclean/audit.jsonl (CI gate) | The PIISEC-01 leak guarantee must be enforced on every push/PR, not only locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Repudiation/Misrepresentation | user-facing copy (README/banner/doctor/MCP desc) | mitigate | Centralized disclaimer (D-07 stance) on all surfaces + banned-phrase copy-drift test (Task 3) failing the build on compliance/guarantee CLAIM drift |
| T-07-03-02 | Tampering | future copy-drift toward overclaiming | mitigate | copy-drift vitest test wired into npm test + scans the single-source strings.ts (D-08); non-vacuous positive control proves the gate works |
| T-07-03-03 | Repudiation | README framing contradicting docs/SCOPE-FENCE.md | mitigate | README authored against SCOPE-FENCE voice; acceptance check confirms no ban weakened, no contradiction |
| T-07-03-04 | Information Disclosure | .mrclean/audit*.jsonl leaking raw PII (regression over time) | mitigate | canary-leak.yml PII job runs 07-01's leak test + belt-and-suspenders `grep -F` per PII canary on every push/PR |
| T-07-03-05 | Tampering | banned-phrase gate self-tripping on the honest negation | mitigate | Pitfall 5: claim-form regexes only; test asserts the disclaimer ("not a guarantee") is NOT flagged; comment lines stripped before scanning |
| T-07-03-SC | Tampering | npm installs | accept | No new runtime or dev dependency added (uses installed vitest + Node built-ins + GNU grep on the runner) — slopcheck not applicable |
</threat_model>

<verification>
- `npx vitest run tests/copy-drift.test.ts` green (no overclaim copy; non-vacuous; allows "not a guarantee")
- `npx vitest run tests/mcp tests/hook tests/doctor` green (disclaimer surfaced; banner.ts unchanged; no doctor exit-code change)
- `grep -rln "PII_BEST_EFFORT_DISCLAIMER" src/` lists strings.ts + all four consumers (single source of truth)
- `grep -nE "^#+ .*PII" README.md` shows the new framing section
- `grep -n "pii-canary-leak" .github/workflows/canary-leak.yml` shows the PII CI job
- Human checkpoint: copy wording approved
</verification>

<success_criteria>
The D-05/D-07/D-08 half of PIISEC-02 plus the PIISEC-01 CI gate are satisfied: a single centralized best-effort disclaimer surfaces once-per-output on README, mrclean doctor, the SessionStart banner, and the MCP tool descriptions; a copy-drift vitest test permanently fails the build if compliance/guarantee CLAIM language enters user-facing copy (while allowing the honest negation); the README framing is consistent with the locked scope fence; and the PIISEC-01 leak test runs in CI with a belt-and-suspenders grep. Framing is consistent across all surfaces and never drifts toward "redacts all PII." Copy wording is human-approved.
</success_criteria>

<output>
Create `.planning/phases/07-pii-security-hardening-honest-framing/07-03-SUMMARY.md` when done.
</output>
