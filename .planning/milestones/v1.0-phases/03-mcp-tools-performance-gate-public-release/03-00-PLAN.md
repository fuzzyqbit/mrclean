---
phase: 03-mcp-tools-performance-gate-public-release
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
requirements:
  - QA-01
autonomous: true
tags: [infrastructure, coverage, parallel-test-fix, package-metadata, vitest-projects]

must_haves:
  truths:
    - "package.json declares the publishable npm name (mrclean-claude), version 1.0.0-rc.1, MIT license, full repo/homepage/bugs/keywords metadata"
    - "package.json#files tightens the npm tarball to only dist/, vendor/gitleaks-rules.toml, vendor/SKIPPED_GITLEAKS_RULES.md, README.md, LICENSE, THREAT_MODEL.md, CHANGELOG.md — no test files, no .planning/, no dist/detect-layer1*"
    - "vitest.config.ts uses the projects API: a `unit` project running in parallel and an `integration` project with fileParallelism:false running install/doctor-e2e/hook-integration/fixtures-corpus/perf tests sequentially"
    - "vitest coverage block declares thresholds: lines 80, statements 80, functions 75, branches 70, with src/mcp/tools/sanitize.ts, restore.ts, audit-query.ts excluded (they are deleted in plan 03-01) and tests/, vendor/, dist/, scripts/, *.config.ts, tests/fixtures/ excluded"
    - "`npm test` runs BOTH projects (unit + integration) in a single invocation; the pre-existing parallel-pollution flake on tests/doctor/end-to-end.test.ts and tests/install/* no longer occurs because globalSetup (tsup --clean) is scoped to the integration project where the consumers live"
  artifacts:
    - path: package.json
      provides: "publish metadata + dev-deps + scripts; mrclean-claude name; coverage-v8 already present"
      contains: '"name": "mrclean-claude"'
    - path: vitest.config.ts
      provides: "two-project test config with coverage thresholds"
      contains: "projects"
  key_links:
    - from: vitest.config.ts
      to: tests/doctor/end-to-end.test.ts
      via: "integration project include glob"
      pattern: "tests/doctor/end-to-end\\.test\\.ts"
    - from: vitest.config.ts
      to: tests/hook/integration-detection.globalSetup.ts
      via: "integration project globalSetup binding"
      pattern: "globalSetup"
    - from: package.json#files
      to: dist/cli.js + dist/mcp.js + vendor/
      via: "npm pack tarball whitelist"
      pattern: '"files":'
---

<objective>
Lock down the publish-ready package metadata, fix Phase 2's known parallel-test-pollution
flake by restructuring vitest into a unit/integration project split, and enable
@vitest/coverage-v8 threshold enforcement so plan 03-04's QA-01 gate has something to
stand on.

This is pure infrastructure. No src/ behavior changes. Every later plan in Phase 3
depends on `npm test` running BOTH the unit-parallel and integration-sequential
projects without the dist/-mid-run-delete race that bit Phase 2.

Purpose: QA-01 coverage-threshold infrastructure, plus the parallel-test-pollution
fix RESEARCH §6 / Pitfall #4 identified as the precondition for all Phase 3 work.
Output: package.json + vitest.config.ts updated; `npm test` green on a single
invocation with all pre-existing integration suites passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mcp-tools-performance-gate-public-release/03-CONTEXT.md
@.planning/phases/03-mcp-tools-performance-gate-public-release/03-RESEARCH.md
@CLAUDE.md
@package.json
@vitest.config.ts

<interfaces>
<!-- Files this plan touches; structure already exists. -->
Current package.json:
- name: "mrclean" (PLACEHOLDER — needs rename to "mrclean-claude" per RESEARCH §6 / Pitfall #2: npm view mrclean returned 0 = taken since 2012)
- version: "0.1.0" (PLACEHOLDER — bump to "1.0.0-rc.1" for the release candidate phase)
- files[]: 11 entries (NEEDS trimming — remove dist/detect-layer1* exclusion is implicit since not listed, but tighten further)
- @vitest/coverage-v8 ^4.1.6 already in devDependencies — no new deps needed

Current vitest.config.ts:
- Single defineConfig({ test: { ... } })
- globalSetup: ['./tests/hook/integration-detection.globalSetup.ts'] (runs tsup --clean — root cause of parallel pollution per RESEARCH §Pitfall 4)
- coverage block already exists with provider: 'v8' (needs thresholds + excludes added)

Files that LIVE in integration project (must run sequentially per RESEARCH §6):
  - tests/install/atomic-json.test.ts
  - tests/install/gitignore.test.ts
  - tests/install/idempotency.test.ts
  - tests/install/mcp-config.test.ts
  - tests/install/path-resolver.test.ts
  - tests/install/project-dir.test.ts
  - tests/install/settings.test.ts
  - tests/install/uninstall-roundtrip.test.ts
  - tests/doctor/end-to-end.test.ts
  - tests/hook/integration.test.ts
  - tests/hook/integration-detection.test.ts
  - tests/fixtures-corpus.test.ts
  - tests/fixtures-corpus-bundle.test.ts
  - tests/perf/**/*.test.ts (created in plan 03-02)

All other tests/**/*.test.ts stay in the unit project.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json publish metadata + tighten files allow-list</name>
  <files>package.json</files>
  <read_first>
    - package.json (the file being modified)
    - .planning/phases/03-mcp-tools-performance-gate-public-release/03-CONTEXT.md §"npm Publish (DOC-03)"
    - .planning/phases/03-mcp-tools-performance-gate-public-release/03-RESEARCH.md §Pitfall 2 "npm name mrclean is taken"
    - CLAUDE.md (engines.node floor = 20.18, MIT license, project description text)
  </read_first>
  <action>
    Edit package.json in place. Apply these field-by-field changes — every change is
    a locked decision from CONTEXT.md §"npm Publish (DOC-03)" or RESEARCH §6/Pitfall 2:

    1. Set `"name": "mrclean-claude"` (RESEARCH §Pitfall 2 — `mrclean` is taken; `mrclean-claude` verified available; this is the locked package name).

    2. Set `"version": "1.0.0-rc.1"` (release-candidate. Plan 03-05 flips to 1.0.0 at publish time).

    3. Set `"description"` to: `"In-session sanitizer for Claude Code that prevents secrets and proprietary terms from reaching Anthropic, MCP servers, or cloud agents. Local, deterministic, no telemetry."` (refines the Phase 1 stub; mirrors CLAUDE.md core-value language).

    4. Add `"repository": { "type": "git", "url": "git+https://github.com/anthropics/mrclean-claude.git" }`. If the operator's GitHub repo URL differs, this is the placeholder — note in the SUMMARY that the URL is a placeholder for operator confirmation before publish.

    5. Add `"homepage": "https://github.com/anthropics/mrclean-claude#readme"` (same caveat).

    6. Add `"bugs": { "url": "https://github.com/anthropics/mrclean-claude/issues" }` (same caveat).

    7. Add `"keywords": [ "claude-code", "claude", "mcp", "security", "secrets", "redaction", "sanitizer", "hook", "anthropic", "dlp" ]` (npm SEO surface; aligns with CLAUDE.md positioning).

    8. Set `"author"` to `""` (empty string placeholder; flagged in SUMMARY for operator confirmation).

    9. Keep `"license": "MIT"` (already present; MIT is locked per DOC-03).

    10. Keep `"engines": { "node": ">=20.18.0" }` (CLAUDE.md hard floor; unchanged).

    11. Keep `"type": "module"` (unchanged).

    12. Keep `"bin"` block unchanged (mrclean -> dist/cli.js, mrclean-mcp -> dist/mcp.js; INST-08 locked).

    13. Keep `"main": "./dist/cli.js"` (unchanged).

    14. REPLACE `"files"` array with EXACTLY this list (per CONTEXT.md §"npm Publish" — tighten Phase 2's list):
        ```
        "files": [
          "dist/cli.js",
          "dist/cli.js.map",
          "dist/cli.d.ts",
          "dist/mcp.js",
          "dist/mcp.js.map",
          "dist/mcp.d.ts",
          "vendor/gitleaks-rules.toml",
          "vendor/gitleaks-rules.toml.sha256",
          "vendor/SKIPPED_GITLEAKS_RULES.md",
          "README.md",
          "LICENSE",
          "THREAT_MODEL.md",
          "CHANGELOG.md"
        ]
        ```
        This explicitly excludes `dist/detect-layer1*` (Phase 2 test-only entries), `tests/`, `.planning/`, `scripts/`, `.changeset/`, `.github/`. Note: README.md / LICENSE / THREAT_MODEL.md / CHANGELOG.md are created in plan 03-03; including them here pre-emptively means the publish job in 03-05 will fail loudly if any are missing.

    15. Keep `"scripts"` mostly unchanged but ADD `"release": "npm run build && changeset publish --provenance --access public"` (used by plan 03-05's release workflow; harmless if not invoked until then).

    16. Keep all existing dependencies and devDependencies (do not bump versions in this plan). `@vitest/coverage-v8 ^4.1.6` is already present.

    Do NOT bump any dependency versions, install new deps, or modify the lockfile in this task.
    Do NOT add @changesets/cli yet — plan 03-03 owns that (it's part of the docs/changeset bootstrap).
    Do NOT touch tsup config or scripts.

    After editing, run `npm pack --dry-run` and capture the tarball file list. Save the output for plan 03-04's smoke test to compare against. The dry-run output must show ONLY the 13 files listed in step 14 (modulo dist/* and vendor/* files that actually exist — README/LICENSE/THREAT_MODEL/CHANGELOG will warn-missing, which is expected at this point).
  </action>
  <verify>
    <automated>
      node -e "const p=require('./package.json'); if(p.name!=='mrclean-claude') process.exit(11); if(p.version!=='1.0.0-rc.1') process.exit(12); if(p.license!=='MIT') process.exit(13); if(!Array.isArray(p.files)||p.files.length!==13) process.exit(14); if(!p.files.includes('THREAT_MODEL.md')) process.exit(15); if(!p.files.includes('CHANGELOG.md')) process.exit(16); if(p.files.some(f=>f.includes('detect-layer1'))) process.exit(17); if(!p.keywords||!p.keywords.includes('claude-code')) process.exit(18); if(!p.repository||!p.bugs||!p.homepage) process.exit(19); if(p.engines.node!=='>=20.18.0') process.exit(20); console.log('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json#name === "mrclean-claude"` (exact string).
    - `package.json#version === "1.0.0-rc.1"`.
    - `package.json#license === "MIT"`.
    - `package.json#engines.node === ">=20.18.0"`.
    - `package.json#files` has exactly 13 entries: 6 dist/* entries + 3 vendor/* entries + README.md + LICENSE + THREAT_MODEL.md + CHANGELOG.md.
    - `package.json#files` does NOT contain any "detect-layer1" substring (Phase 2 test-only entry excluded).
    - `package.json#keywords` contains "claude-code" and at least 8 entries total.
    - `package.json#repository`, `package.json#homepage`, `package.json#bugs` are all present (placeholder URLs OK; flagged in SUMMARY).
    - `package.json#scripts.release` exists and contains `"changeset publish --provenance"` substring.
    - `npm pack --dry-run` exits 0. Tarball file list is captured in the SUMMARY.
    - The existing tests still build and pass: `npm run build` exits 0 (no script changed that would break tsup).
  </acceptance_criteria>
  <done>
    package.json carries publish-ready metadata under the locked mrclean-claude name; files[] tightened to the 13-entry whitelist; npm pack --dry-run lists only the whitelist members.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restructure vitest.config.ts into unit + integration projects with coverage thresholds</name>
  <files>vitest.config.ts</files>
  <behavior>
    - `npm test` runs BOTH projects sequentially and exits 0 when the suite is clean
    - All currently-passing tests still pass (no test was modified, only the runner config)
    - Tests in tests/install/**, tests/doctor/end-to-end.test.ts, tests/hook/integration*.test.ts, tests/fixtures-corpus*.test.ts run inside the `integration` project with fileParallelism:false
    - tests/hook/integration-detection.globalSetup.ts runs ONLY for the integration project (not the unit project)
    - All other tests/**/*.test.ts run inside the `unit` project in parallel
    - Coverage thresholds enforced: lines 80, statements 80, functions 75, branches 70 — `npm run test:coverage` exits non-zero if a threshold is violated
    - Coverage excludes the soon-to-be-deleted files (src/mcp/tools/sanitize.ts, restore.ts, audit-query.ts) so plan 03-01's deletion doesn't leave dead-code coverage holes
    - The flaky tests that failed under parallel mode in Phase 2 (`tests/doctor/end-to-end.test.ts`, `tests/install/idempotency.test.ts`) pass in the full-suite run
  </behavior>
  <read_first>
    - vitest.config.ts (the file being modified)
    - .planning/phases/03-mcp-tools-performance-gate-public-release/03-RESEARCH.md §Pattern 6 "Parallel Test Isolation Fix" + §Pattern 7 "@vitest/coverage-v8 Threshold Configuration" + §Pitfall 4 "globalSetup build deletes dist/ during parallel test runs"
    - .planning/phases/03-mcp-tools-performance-gate-public-release/03-CONTEXT.md §"Quality Gates (QA-01..03)" — coverage threshold values are locked
    - .planning/phases/02-live-redaction-layers-1-4-one-way/02-05-SUMMARY.md (documents the existing globalSetup and known limitation)
    - tests/hook/integration-detection.globalSetup.ts (referenced by the new integration project)
  </read_first>
  <action>
    Rewrite vitest.config.ts to use the vitest 4.x `projects` API per RESEARCH §Pattern 6.
    The resulting file structure is `defineConfig({ test: { coverage: { ... }, projects: [unit, integration] } })`.

    Concrete structure (all field names/values are LOCKED; deviate only on the operator's instruction):

    1. Top-level `test.coverage` block (applies to BOTH projects since they share the same workspace) per RESEARCH §Pattern 7:
       - provider: 'v8'
       - reporter: ['text', 'lcov']
       - include: ['src/**/*.ts']
       - exclude: [
           'src/**/*.d.ts',
           'src/mcp/tools/sanitize.ts',
           'src/mcp/tools/restore.ts',
           'src/mcp/tools/audit-query.ts',
           'tests/**',
           'vendor/**',
           'dist/**',
           'scripts/**',
           '*.config.ts',
           'tests/fixtures/**'
         ]
       - thresholds: { lines: 80, statements: 80, functions: 75, branches: 70 } — LOCKED values from CONTEXT.md §"Quality Gates"; do NOT lower them.
       - reportsDirectory: 'coverage' (default; explicit for visibility)

    2. `test.projects` array with EXACTLY two entries:

       Project A — name 'unit':
         - test.name: 'unit'
         - test.include: ['tests/**/*.test.ts']
         - test.exclude: [
             'tests/install/**',
             'tests/doctor/end-to-end.test.ts',
             'tests/hook/integration.test.ts',
             'tests/hook/integration-detection.test.ts',
             'tests/fixtures-corpus.test.ts',
             'tests/fixtures-corpus-bundle.test.ts',
             'tests/perf/**'
           ]
         - test.environment: 'node'
         - test.testTimeout: 30_000
         - (NO globalSetup — unit tests should not trigger tsup --clean)

       Project B — name 'integration':
         - test.name: 'integration'
         - test.include: [
             'tests/install/**/*.test.ts',
             'tests/doctor/end-to-end.test.ts',
             'tests/hook/integration.test.ts',
             'tests/hook/integration-detection.test.ts',
             'tests/fixtures-corpus.test.ts',
             'tests/fixtures-corpus-bundle.test.ts',
             'tests/perf/**/*.test.ts'
           ]
         - test.fileParallelism: false (sequential — this is the fix for Pitfall #4)
         - test.environment: 'node'
         - test.testTimeout: 60_000 (perf tests need 60s per RESEARCH §Pitfall 7)
         - test.globalSetup: ['./tests/hook/integration-detection.globalSetup.ts'] (moved here from top-level — tsup --clean only runs before the sequential project, never racing with unit tests)

    3. REMOVE the existing top-level `globalSetup` from `test.` — it is moved into the integration project. This is the locked fix per RESEARCH §Pattern 6.

    4. Keep imports minimal: `import { defineConfig } from 'vitest/config'`.

    5. Add a one-block JSDoc at the top explaining the unit/integration split, citing CONTEXT.md §"Quality Gates" and RESEARCH §Pattern 6.

    CRITICAL CONSTRAINTS:
    - The `tests/perf/**` glob in the integration project will not match anything until plan 03-02 creates those files. That's intentional — including the glob now means plan 03-02 doesn't need to touch vitest.config.ts.
    - Do NOT add `tests/mcp/**` to the integration project. The new tests/mcp/check.test.ts / redact.test.ts / status.test.ts / supervisor.test.ts (created in plan 03-01) use InMemoryTransport and run safely in parallel — they stay in the unit project. tests/mcp/server-lifecycle.test.ts and tests/mcp/tools-list.test.ts spawn dist/mcp.js as a child process; verify they currently sit in tests/mcp/ and decide based on whether they tolerate parallel dist/ access. Based on the Phase 1 SUMMARY (01-04) and the existing pass rate, they run fine in parallel; leave them in the unit project. Only flip to integration if `npm test` fails after this restructure.
    - Do NOT add typecheck.tsconfig or any other vitest options beyond what is specified.
    - If `npm test` fails after the restructure, the most likely cause is a test file path that needs to move between projects. Use the actual test failure to decide which project it belongs in — do NOT lower coverage thresholds or relax fileParallelism.

    After writing the new config, run `npm test` and verify exit 0. If a test fails, diagnose project membership and adjust the include/exclude globs.

    Finally, run `npm run test:coverage` and capture the coverage report. The thresholds will likely fail on this first run — that is EXPECTED and acceptable in this plan. Plan 03-04 owns the threshold-passing work. Document the current coverage numbers in the SUMMARY for plan 03-04 to consume.
  </action>
  <verify>
    <automated>
      npm test 2>&1 | tail -20 && grep -c "projects" vitest.config.ts && grep -c "fileParallelism" vitest.config.ts && grep -c "thresholds" vitest.config.ts && grep -cE "lines.*80" vitest.config.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains exactly one `projects` array with exactly two entries named 'unit' and 'integration'.
    - `grep -c "fileParallelism: false" vitest.config.ts` == 1 (only in the integration project).
    - `grep -c "thresholds" vitest.config.ts` >= 1.
    - `grep -E "lines:\\s*80" vitest.config.ts` matches (lines threshold locked at 80).
    - `grep -E "functions:\\s*75" vitest.config.ts` matches.
    - `grep -E "branches:\\s*70" vitest.config.ts` matches.
    - The top-level `test.globalSetup` is GONE; globalSetup appears only inside the integration project (`grep -c "integration-detection.globalSetup" vitest.config.ts` == 1).
    - `npm test` exits 0 — all previously-passing tests still pass.
    - `npm test` runs both projects (visible in vitest output: "PASS unit" + "PASS integration" lines).
    - `tests/doctor/end-to-end.test.ts` and `tests/install/idempotency.test.ts` PASS in the full-suite run (they were the parallel-pollution flake victims per RESEARCH §Pitfall 4).
    - `npm run test:coverage` runs to completion and prints a coverage report (it MAY fail the thresholds on this first run; that is OK — plan 03-04 fixes threshold violations). The SUMMARY MUST record the actual lines/statements/functions/branches percentages so plan 03-04 has a baseline.
  </acceptance_criteria>
  <done>
    vitest.config.ts split into unit + integration projects; parallel pollution fixed (full suite passes); coverage threshold infrastructure in place ready for plan 03-04 enforcement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm registry | Tarball contents will be published; files[] determines what is exposed publicly |
| vitest.config.ts → CI pipeline | Misconfigured thresholds could allow regressions through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Information Disclosure | package.json#files | mitigate | Explicit allow-list (13 entries) of dist/, vendor/, and 4 doc files — no glob; tests/, .planning/, scripts/, .github/ never enter the tarball. `npm pack --dry-run` verified in task 1. |
| T-03-00-02 | Tampering | package.json#name | mitigate | Locked to `mrclean-claude` (verified available); operator override path documented in task acceptance. Wrong name would publish under a squatter or fail loudly. |
| T-03-00-03 | Denial of Service | vitest globalSetup race | mitigate | `tsup --clean` moved into integration project (fileParallelism:false) — cannot race with unit tests reading dist/. Locked per RESEARCH §Pitfall 4. |
| T-03-00-04 | Repudiation | coverage threshold accuracy | accept | Thresholds are project-wide; `tests/` excluded; vendored/dist code excluded. Coverage numbers are advisory until 03-04 enforces them. |
</threat_model>

<verification>
  - `npm test` exits 0 with both `unit` and `integration` project names visible in output.
  - `npm run test:coverage` runs to completion (may fail thresholds — plan 03-04 fixes).
  - `npm pack --dry-run` shows ONLY the 13 whitelisted files.
  - `node -e "require('./package.json').name === 'mrclean-claude' || process.exit(1)"` exits 0.
</verification>

<success_criteria>
  - All Task 1 acceptance criteria checks pass.
  - All Task 2 acceptance criteria checks pass.
  - `npm test` is a single-invocation green run (no `--no-file-parallelism` needed).
  - SUMMARY records current coverage percentages for plan 03-04 baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mcp-tools-performance-gate-public-release/03-00-SUMMARY.md` recording:
  - Final `package.json#files` array
  - Coverage percentages from `npm run test:coverage` (lines/statements/functions/branches)
  - Whether `tests/mcp/server-lifecycle.test.ts` and `tests/mcp/tools-list.test.ts` had to be moved to the integration project (default: no)
  - Any deviation from the locked thresholds or the locked package name (default: none)
  - Confirmation that `tests/doctor/end-to-end.test.ts` and `tests/install/idempotency.test.ts` pass in the full suite
  - Repository / homepage / bugs URLs that need operator confirmation before publish (default: github.com/anthropics/mrclean-claude — operator may swap)
</output>
