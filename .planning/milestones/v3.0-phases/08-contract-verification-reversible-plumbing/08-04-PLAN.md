---
phase: 08-contract-verification-reversible-plumbing
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - tests/uat/harness.ts
  - tests/uat/contract-verification.test.ts
  - tests/uat/live-session.test.ts
  - tests/uat/fixtures/e1-rewrite-hook.sh
  - tests/uat/fixtures/e2-updated-input-hook.sh
  - tests/uat/fixtures/e4-large-output-hook.sh
  - tests/uat/fixtures/log-hook.sh
  - tests/uat/artifacts/contract-findings.json
autonomous: false
requirements: [REVMODE-10]

must_haves:
  truths:
    - "Each of the four LOW-confidence contract questions (updatedToolOutput honored/rendered, updatedInput echo, session_id continuity across --resume, 10K cap) has a recorded verdict with CC version + date + method"
    - "SessionEnd fires (or provably does not) in headless -p mode with the observed reason recorded, and mrclean's real hook produces zero exit-2 noise on session start/end in a live sandbox session"
    - "The harness is rerunnable on Claude Code upgrades: contract verdicts are recorded findings, never CI-gating assertions"
    - "The interactive terminal-rendering half of E1 has a human-observed verdict"
  artifacts:
    - path: "tests/uat/contract-verification.test.ts"
      provides: "E1-E5 opt-in live experiment harness (record-don't-assert)"
      min_lines: 150
    - path: "tests/uat/harness.ts"
      provides: "Shared runClaude/assertSessionRan/ClaudeRun extracted from live-session.test.ts"
      exports: ["runClaude", "assertSessionRan"]
    - path: "tests/uat/artifacts/contract-findings.json"
      provides: "Version-stamped verdicts for E1-E5 (the docs-wave input)"
      contains: "claude_version"
  key_links:
    - from: "tests/uat/contract-verification.test.ts"
      to: "tests/uat/harness.ts"
      via: "import { runClaude, assertSessionRan }"
      pattern: "from './harness"
    - from: "tests/uat/contract-verification.test.ts"
      to: "tests/uat/artifacts/contract-findings.json"
      via: "findings writer at suite end"
      pattern: "contract-findings"
---

<objective>
Settle the four LOW-confidence hook-contract questions empirically in live headless sessions (E1-E4), plus the SessionEnd firing/reason bonus check (E5), and capture version-stamped verdicts in a committed findings artifact. Includes the one interactive human observation (terminal rendering) that headless runs cannot answer.

Purpose: REVMODE-10 core. The verdicts gate Phase 9's resume/rehydration design (T5), Phase 10's input-restore scope fence, the THREAT_MODEL wire re-entry section, and doctor honesty copy.

Output: rerunnable `tests/uat/contract-verification.test.ts` harness + `tests/uat/artifacts/contract-findings.json` with all E1-E5 verdicts including the interactive rendering observation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md
@tests/uat/live-session.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared UAT harness + author E1-E5 experiment suite</name>
  <files>tests/uat/harness.ts, tests/uat/contract-verification.test.ts, tests/uat/live-session.test.ts, tests/uat/fixtures/e1-rewrite-hook.sh, tests/uat/fixtures/e2-updated-input-hook.sh, tests/uat/fixtures/e4-large-output-hook.sh, tests/uat/fixtures/log-hook.sh</files>
  <read_first>
    - tests/uat/live-session.test.ts (ENTIRE file — opt-in gate line ~41, ClaudeRun/runClaude/parse lines ~120-170, assertSessionRan lines ~81-94, buildHookSettings lines ~103-117, sandbox beforeAll lines ~173-223, constants lines ~48-53)
    - vitest.config.ts (uat project include globs — confirm harness.ts and fixtures/ are not matched as test files)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§Experiment Design E1-E5 — the full observable matrix; §Pattern 1 fixture-hook settings shape lines 164-192; §Pattern 2 record-don't-assert; §Code Examples session-id side-file hook lines 433-444)
  </read_first>
  <action>
    Extraction: move ClaudeRun, runClaude, assertSessionRan, and the shared constants (CLAUDE_MODEL 'claude-haiku-4-5', MAX_TURNS 4, CLAUDE_TIMEOUT_MS 150_000) from tests/uat/live-session.test.ts into new tests/uat/harness.ts; update live-session.test.ts to import them. Behavior must be identical — verified by typecheck (live-session is opt-in; do not spend tokens re-running it).

    Fixture hook scripts (committed, chmod +x, POSIX sh, marker strings ONLY — never real secret shapes, leak-grep discipline):
    - e1-rewrite-hook.sh: consume stdin, emit hookSpecificOutput JSON with hookEventName PostToolUse and updatedToolOutput 'REWRITTEN_E1_MARKER_x9k2'; append a fired-marker line to $HOOK_LOG if set (RESEARCH lines 164-178 shape).
    - e2-updated-input-hook.sh: PreToolUse — emit permissionDecision 'allow' + updatedInput with the COMPLETE tool_input object rewriting `echo INPUT_ORIG_MARKER_e2` to `echo INPUT_UPDATED_MARKER_e2` (partial tool_input objects are wrong — shipped pre-tool-use.ts pitfall).
    - e4-large-output-hook.sh: emit ~15K-char updatedToolOutput with 'E4_HEAD_MARKER' at start and 'E4_TAIL_MARKER' at end.
    - log-hook.sh: the jq-free node one-liner from RESEARCH lines 433-444 — append {event, session_id, source-or-reason} from the stdin payload to $E_LOG (serves E3 and E5).

    tests/uat/contract-verification.test.ts (describe.skipIf(!UAT_ENABLED), mkdtemp sandbox with afterAll rmSync, sandbox-local settings JSON per experiment referencing the fixture scripts by absolute path, npm run build in beforeAll unless SKIP_BUILD=1, per-test timeout above CLAUDE_TIMEOUT_MS):
    - E1 (updatedToolOutput honored, per tool): matrix Bash + Read + one MCP tool. For MCP: register mrclean's own dist MCP server via sandbox --mcp-config with fixture PostToolUse matcher 'mcp__mrclean__.*' — the FIXTURE hook does the rewriting, so mrclean's MRCLEAN_TOOL_RE self-exemption does not apply. Prompt asks the model to run/read and quote back exactly what it saw ('ORIGINAL_E1_MARKER_q7v4' vs 'REWRITTEN_E1_MARKER_x9k2'). Record three signals per tool: model's next-turn quote, transcript jsonl tool_result content (~/.claude/projects/<cwd-slug>/<session_id>.jsonl), stream-json user-event tool_result.
    - E2 (updatedInput echo): fixture PreToolUse hook, matcher Bash; signals: tool output contains UPDATED marker (rewrite executed), transcript tool_use input ORIGINAL vs UPDATED, model's answer to "quote the exact command you ran".
    - E3 (session_id continuity): log-hook on SessionStart + UserPromptSubmit; run 1 captures sid; run 2 `--resume <sid>` from same cwd; control run `--resume <sid> --fork-session` (expect NEW id). Signals: side-file sid equality; SessionStart source 'resume' observed (doubles as live proof the widened-matcher pattern works via sandbox settings). If --resume misbehaves in print mode, fall back to --continue or --session-id seeding and record the limitation (RESEARCH A3).
    - E4 (10K cap vs updatedToolOutput): Bash `seq 1 3000` (>10K output) + e4-large-output-hook; control experiment with >10K additionalContext (documented capped — validates the harness can observe the file-preview replacement). Run E4 against whichever tool E1 proved honored; if none honored, the verdict is 'unanswerable on this CC version — blocked by #68951' (a legitimate documented outcome).
    - E5 (SessionEnd firing + reason): log-hook registered for SessionEnd (no matcher) in sandbox settings; normal -p session to completion; record whether SessionEnd fires headlessly and which reason. Second half: a sandbox settings registering mrclean's REAL built hook (buildHookSettings pattern from live-session.test.ts) for SessionStart with the widened matcher AND SessionEnd; assert stderr contains no hook-error/exit-2 wrapper noise — the SC3 live observable.

    Record-don't-assert discipline (Pattern 2): hard-assert ONLY harness integrity (assertSessionRan, fixture hook fired, side file written). Contract verdicts are RECORDED — a findings writer collects per-experiment verdict objects {question, verdict, signals, claude_version, date, method, evidence_paths} into tests/uat/artifacts/contract-findings.json plus a console.table summary. A "failing" contract answer is a finding, never a test failure.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run --project=unit && test -x tests/uat/fixtures/e1-rewrite-hook.sh && test -x tests/uat/fixtures/log-hook.sh</automated>
  </verify>
  <acceptance_criteria>
    - tests/uat/harness.ts exports runClaude, assertSessionRan, ClaudeRun; live-session.test.ts imports them; `npm run typecheck` exits 0
    - contract-verification.test.ts contains describe.skipIf gated on MRCLEAN_UAT === '1' and never writes to ~/.claude/settings.json (all settings via --settings sandbox paths; grep shows no homedir settings write)
    - All four fixture scripts exist, are executable, and contain only MARKER strings (no AKIA/ghp_/sk- shaped strings: `! grep -rqE "AKIA|ghp_|sk-ant" tests/uat/fixtures/`)
    - Without MRCLEAN_UAT set, `npx vitest run --project=uat tests/uat/contract-verification.test.ts` reports the suite as skipped (no tokens spent in CI)
  </acceptance_criteria>
  <done>Harness compiles, is opt-in-gated, sandbox-isolated, and encodes all five experiments with record-don't-assert discipline.</done>
</task>

<task type="auto">
  <name>Task 2: Run the live experiments and commit the findings artifact</name>
  <files>tests/uat/artifacts/contract-findings.json</files>
  <read_first>
    - tests/uat/contract-verification.test.ts (as authored in Task 1)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§State of the Art — E1 hypothesis is evidence-backed: #68951 open, ignored-for-Bash repro'd through 2.1.207; §Metadata validity note — recheck #68951 before running)
  </read_first>
  <action>
    Preflight (fail loud, do not burn tokens on a broken substrate):
    - `claude --version` — record the exact version; if it differs from 2.1.209, note it (a fix landing mid-phase changes E1's hypothesis, not the harness).
    - `gh issue view 68951 --repo anthropics/claude-code --json state,title` — record open/closed state alongside the findings (RESEARCH validity window).

    Run: `npm run build && MRCLEAN_UAT=1 npx vitest run --project=uat tests/uat/contract-verification.test.ts` (target ONLY this file — do not re-run live-session.test.ts and spend its tokens). Budget: ~10-20 Haiku calls, cents.

    Triage: harness-integrity failures (auth expired, session never ran) are BLOCKERS — fix and rerun. Contract verdicts land in tests/uat/artifacts/contract-findings.json regardless of direction. Every verdict object must carry claude_version, date, method, and evidence pointers (transcript excerpt paths). The E1 rendering field is set to 'pending-interactive' — Task 3 fills it.

    Commit the findings artifact + a one-paragraph verdict summary per experiment in the commit body.
  </action>
  <verify>
    <automated>test -s tests/uat/artifacts/contract-findings.json && node -e "const f=require('./tests/uat/artifacts/contract-findings.json'); if(!f.claude_version||!f.experiments||!f.experiments.E1||!f.experiments.E3||!f.experiments.E5) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - contract-findings.json exists, parses, and contains claude_version, date, and experiments.E1 through experiments.E5 entries each with a verdict field
    - E1 entry has per-tool results for at least Bash and Read
    - E3 entry records sid-equality verdicts for plain --resume AND the --fork-session control
    - E5 entry records whether SessionEnd fired headlessly + observed reason, and the mrclean-real-hook run's stderr noise verdict (SC3 observable)
    - No experiment test FAILED for a contract-answer reason (only harness-integrity assertions may fail, and none did)
  </acceptance_criteria>
  <done>All headless verdicts recorded and committed; only the interactive rendering field remains pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Interactive terminal-rendering observation (E1 rendering half)</name>
  <files>tests/uat/artifacts/contract-findings.json</files>
  <action>Pause for the operator to run ONE interactive Claude session with the E1 rewrite fixture (steps below) and report which marker the terminal rendered. On resume, update experiments.E1.rendering in tests/uat/artifacts/contract-findings.json with the reported verdict + the operator's claude version + date + method 'interactive human observation', then commit the updated artifact. Do NOT attempt PTY/tmux automation of the TUI (RESEARCH Don't Hand-Roll).</action>
  <read_first>
    - tests/uat/artifacts/contract-findings.json (E1 headless verdicts — tells you whether a rewrite is even visible to the model on this version)
    - tests/uat/fixtures/e1-rewrite-hook.sh (the instrument the interactive session uses)
  </read_first>
  <what-built>Headless E1 runs answered the model-facing half of updatedToolOutput (what re-enters model context). The literal terminal-display half needs one human observation in an interactive session — PTY automation was rejected as brittle (RESEARCH Don't Hand-Roll).</what-built>
  <how-to-verify>
    1. From the repo root, write an isolated settings file (never your real ~/.claude/settings.json):
       `printf '{"hooks":{"PostToolUse":[{"matcher":"Bash","hooks":[{"type":"command","command":"'"$PWD"'/tests/uat/fixtures/e1-rewrite-hook.sh","timeout":10}]}]}}' > /tmp/e1-interactive-settings.json`
    2. Launch an interactive session: `claude --settings /tmp/e1-interactive-settings.json --model claude-haiku-4-5`
    3. Prompt: "Run the bash command `echo ORIGINAL_E1_MARKER_q7v4` and show me the output."
    4. Observe the TOOL RESULT block your terminal renders (expand it if collapsed): does it display ORIGINAL_E1_MARKER_q7v4 or REWRITTEN_E1_MARKER_x9k2?
    5. Exit the session (`/exit`) and `rm /tmp/e1-interactive-settings.json`.
  </how-to-verify>
  <resume-signal>Reply with the marker your terminal displayed (ORIGINAL or REWRITTEN), plus your `claude --version`. On resume, the executor updates experiments.E1.rendering in tests/uat/artifacts/contract-findings.json with your observation (verdict, claude_version, date, method: 'interactive human observation') and commits it.</resume-signal>
  <verify>
    <automated>node -e "const f=require('./tests/uat/artifacts/contract-findings.json'); const r=f.experiments.E1.rendering; if(!r||r==='pending-interactive'||!r.verdict) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - Operator reported which marker the terminal rendered
    - contract-findings.json experiments.E1.rendering no longer reads 'pending-interactive' and carries verdict + claude_version + date + method
    - Updated artifact committed
  </acceptance_criteria>
  <done>The interactive rendering verdict is recorded in the findings artifact and committed — E1 is fully answered (model-facing + terminal-display halves).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → operator's live Claude Code config | Experiments must never mutate ~/.claude/settings.json or ~/.claude.json |
| Fixture hooks → Anthropic API | Everything a fixture emits reaches the wire — marker strings only |
| Harness → ~/.claude/projects transcripts | Evidence reads of local transcripts (sandbox sessions only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | Experiment sandbox leaking into operator config | mitigate | --settings/--mcp-config/--strict-mcp-config isolation (UAT-2b precedent); mkdtemp sandbox + afterAll cleanup; acceptance grep proves no homedir settings write |
| T-08-13 | Information Disclosure | Fixture hooks containing real secret shapes | mitigate | Marker-string-only discipline; automated grep gate over tests/uat/fixtures/ for AKIA/ghp_/sk-ant shapes |
| T-08-14 | Denial of Service | Contract verdicts hard-asserted → flaky CI on CC upgrades (Pitfall: record-don't-assert) | mitigate | Verdicts recorded to findings artifact; only harness-integrity assertions are hard; suite is MRCLEAN_UAT opt-in, never CI-gating |
| T-08-15 | Information Disclosure | Interactive session driven against the operator's real settings | mitigate | Checkpoint instructions build a /tmp isolated settings file and delete it after; fixture emits markers only |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — gate not applicable |
</threat_model>

<verification>
- `npm run typecheck` green (harness extraction did not break live-session.test.ts).
- `npx vitest run --project=unit` green (no unit-suite impact).
- contract-findings.json complete: E1-E5 verdicts + interactive rendering, all version-stamped.
</verification>

<success_criteria>
- All four contract questions have recorded, version-stamped verdicts (including honest 'unanswerable — blocked by #68951' outcomes where applicable).
- SessionEnd headless firing + reason recorded; mrclean real-hook sandbox session shows zero exit-2 noise (SC3 live observable).
- Harness is rerunnable on future CC releases without CI breakage.
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-04-SUMMARY.md` when done (include the verdict table — plans 08-05/08-06 consume it).
</output>
