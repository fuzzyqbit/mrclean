---
phase: 08-contract-verification-reversible-plumbing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/config/defaults.ts
  - src/config/index.ts
  - tests/config/reader.test.ts
  - tests/config/merge.test.ts
autonomous: true
requirements: [REVMODE-02]

must_haves:
  truths:
    - "Operator can write `[reversible]` with `enabled = true` in config.toml and the merged effective config carries reversible.enabled === true"
    - "With the `[reversible]` table absent, the merged effective config carries reversible.enabled === false and all shipped behavior is byte-identical (full suite green unchanged)"
    - "A wrong-typed `[reversible].enabled` fails closed with a ConfigReadError naming the file and reason"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "SessionEndInput, hook_event_name union incl. SessionEnd, MrcleanReversibleConfig, MrcleanConfig.reversible"
      contains: "SessionEndInput"
    - path: "src/config/defaults.ts"
      provides: "Frozen reversible default { enabled: false }"
      contains: "reversible"
    - path: "src/config/index.ts"
      provides: "validateReversibleConfig + parseToml branch + mergeConfigs last-wins"
      contains: "validateReversibleConfig"
  key_links:
    - from: "src/config/index.ts"
      to: "src/config/defaults.ts"
      via: "mergeConfigs seeds reversible from DEFAULT_CONFIG.reversible"
      pattern: "DEFAULT_CONFIG\\.reversible"
    - from: "src/config/index.ts"
      to: "src/shared/types.ts"
      via: "validateReversibleConfig returns MrcleanReversibleConfig"
      pattern: "MrcleanReversibleConfig"
---

<objective>
Land the `[reversible]` config table (default OFF, absent-table == shipped one-way guarantee) plus all Phase 8 type contracts in `src/shared/types.ts` — the interfaces every other Phase 8 plan builds against.

Purpose: REVMODE-02 groundwork (the config table is REVMODE-02's first clause; the requirement itself completes in Phase 9 with the session state adapter). This plan is also the types owner for the phase: SessionEndInput and the widened event unions land here so plans 08-02/08-03 can consume them without file conflicts.

Output: `[reversible]` parses/validates/merges exactly like the `[pii]` precedent; absent table is provably byte-identical to shipped v2.0 behavior; SessionEnd contract types exist.

TDD feature: `[reversible]` config parse/validate/merge — defined I/O (TOML string in, typed config or ConfigReadError out).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (08-02 dispatcher/handler, 08-03 doctor) import these. -->

From src/shared/types.ts (to be added by Task 1):

```typescript
export interface SessionEndInput extends HookInputBase {
  hook_event_name: 'SessionEnd'
  /** Documented values as of CC 2.1.209: clear | resume | logout | prompt_input_exit
   *  | bypass_permissions_disabled | other. Upstream may add more — treat as open string.
   *  [CITED: code.claude.com/docs/en/hooks, fetched 2026-07-14] */
  reason: string
}

export interface MrcleanReversibleConfig {
  /** Master switch. Default: false. Absent-[reversible] == shipped one-way guarantee. */
  enabled: boolean
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SessionEnd + reversible type contracts (scaffolding, commit as chore)</name>
  <files>src/shared/types.ts, src/config/defaults.ts, src/config/index.ts</files>
  <read_first>
    - src/shared/types.ts (current state: HookInputBase lines ~17-22, SessionStartInput lines ~28-32, HookInput union lines ~71-75, MrcleanPiiConfig lines ~264-274, MrcleanConfig lines ~289-311, LOCKED header lines 1-10)
    - src/config/defaults.ts (pii frozen sub-table lines ~33-66; Object.freeze + `as unknown as` cast pattern lines ~18-26)
    - src/config/index.ts (mergeConfigs seed/return-literal shape around lines ~490-556 — needed to know where the compile-forced seed line goes)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md (§src/shared/types.ts, §src/config/defaults.ts — exact target shapes)
  </read_first>
  <action>
    Add ALL Phase 8 type contracts to src/shared/types.ts in one pass:
    1. Widen HookInputBase.hook_event_name union with 'SessionEnd'.
    2. Add SessionEndInput per the interfaces block above (RESEARCH.md lines 219-228 gives the exact JSDoc). CRITICAL: `reason` is an OPEN `string`, NOT a closed union — do not copy SessionStartInput.source's closedness. Upstream added a sixth reason (`bypass_permissions_disabled`) after the milestone research; more may follow, and a closed union would make future reasons a type error.
    3. Widen the HookInput union with `| SessionEndInput`.
    4. Add MrcleanReversibleConfig modeled on MrcleanPiiConfig — ONLY the `enabled: boolean` field (YAGNI fence: Phase 9 owns ttl_hours etc.; do not speculate fields).
    5. Add `reversible: MrcleanReversibleConfig` to MrcleanConfig, after `pii`.
    6. Refresh the LOCKED header's verification date to 2026-07-14 (contract re-verified from code.claude.com/docs/en/hooks during Phase 8 research).

    Compile-forced scaffolding (required because MrcleanConfig.reversible is a required field — without these, typecheck breaks at this commit boundary):
    7. src/config/defaults.ts: add the frozen `reversible` sub-table after `pii` — `Object.freeze({ enabled: false })` with the `as unknown as MrcleanReversibleConfig` cast and the comment "master switch OFF; absent-[reversible] == shipped one-way guarantee" (mirrors the [pii] comment convention, RESEARCH lines 205-210).
    8. src/config/index.ts mergeConfigs: seed `let reversible = { enabled: DEFAULT_CONFIG.reversible.enabled }` (copy into a new object for immutability — do NOT alias the frozen default) and include `reversible` in the return literal. Do NOT add the layer-override loop line yet — that is the GREEN phase of Task 3; the last-wins behavior must fail RED first.

    Do NOT touch parseToml or add validateReversibleConfig in this task.
    Commit as `chore(08-01): add SessionEnd + reversible type contracts` — chore, not feat, so the TDD gate greps (test-before-feat ordering) stay clean.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run --project=unit tests/config/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SessionEndInput" src/shared/types.ts` returns >= 2 (interface + union member)
    - `grep -c "reason: string" src/shared/types.ts` returns >= 1
    - `grep -c "MrcleanReversibleConfig" src/shared/types.ts` returns >= 2
    - `grep -c "reversible" src/config/defaults.ts` returns >= 1
    - `npm run typecheck` exits 0
    - Existing config unit tests pass unchanged (no behavior change from types + frozen default + seed)
  </acceptance_criteria>
  <done>All Phase 8 type contracts exist, typecheck is green, and no runtime behavior changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — failing tests for [reversible] parse/validate/merge</name>
  <files>tests/config/reader.test.ts, tests/config/merge.test.ts</files>
  <read_first>
    - tests/config/reader.test.ts (Test 4 parse-positive pattern lines ~97-102; Test 6 ConfigReadError pattern lines ~114-129)
    - tests/config/merge.test.ts (existing mergeConfigs test conventions — layer construction, expected-object assertions)
    - tests/config/pii-schema.test.ts (the [pii] validation test suite — closest schema-test precedent)
  </read_first>
  <behavior>
    reader.test.ts (readConfigLayer):
    - Test A (MUST FAIL at RED): file containing `[reversible]\nenabled = true` → result.reversible deep-equals { enabled: true }
    - Test B (MUST FAIL at RED): file containing `[reversible]\nenabled = "yes"` → rejects with ConfigReadError; re-catch and assert .reason === '[reversible].enabled must be a boolean' and .path === the config path
    - Test C (MUST FAIL at RED): unknown keys inside the table are ignored — `[reversible]\nenabled = true\nfuture_key = 1` → result.reversible deep-equals { enabled: true } (matches parseToml tolerance; FAIL-loud on unsupported keys is Phase 10 / REVMODE-12)
    merge.test.ts (mergeConfigs):
    - Test D (MUST FAIL at RED): layer with reversible { enabled: true } followed by layer with reversible { enabled: false } → merged.reversible.enabled === false (LAST-WINS)
    - Test E (MUST FAIL at RED): single layer with reversible { enabled: true } → merged.reversible.enabled === true
    - Test F (regression guard — EXPECTED TO PASS at RED, provided by Task 1 scaffolding): no layer sets reversible → merged.reversible deep-equals { enabled: false }
  </behavior>
  <action>
    Write tests A-F following the exact in-file precedents (Test 4 / Test 6 shapes in reader.test.ts; existing layer-list construction in merge.test.ts). Use descriptive AAA-style names ("throws ConfigReadError when [reversible].enabled is not a boolean", etc.).

    Run the config test files and confirm A-E fail on ASSERTIONS (result.reversible undefined / no throw), not on compile or import errors. If any of A-E passes, STOP — the feature already exists or the test is wrong; investigate before proceeding (TDD fail-fast rule). Test F passing is expected and must be noted in the commit body.

    Commit: `test(08-01): add failing tests for [reversible] config parse/validate/merge`
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/config/reader.test.ts tests/config/merge.test.ts 2>&1 | grep -E "failed|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Tests A, B, C, D, E present and failing with assertion errors (not import/syntax errors)
    - Test F present and passing
    - `npm run typecheck` exits 0 (types from Task 1 make result.reversible a legal property access)
    - Commit message starts with `test(08-01):`
  </acceptance_criteria>
  <done>Five failing tests + one passing regression guard committed; failures are assertion-level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — validateReversibleConfig + parseToml branch + last-wins merge</name>
  <files>src/config/index.ts</files>
  <read_first>
    - src/config/index.ts (validatePiiConfig lines ~306-315, isRecord lines ~62-64, ConfigReadError lines ~48-56, parseToml pii branch lines ~415-421, mergeConfigs scalar last-wins loop lines ~518-522)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§Code Examples — finished validateReversibleConfig at lines 419-429)
  </read_first>
  <behavior>
    - All six tests from Task 2 pass
    - Full unit suite green; full `npm test` green (byte-identical gate: absent table changes nothing)
  </behavior>
  <action>
    Three insertion points in src/config/index.ts, each 1:1 with the [pii] precedent:
    1. Add `validateReversibleConfig(raw, filePath)` — use the finished body from RESEARCH lines 419-429: isRecord guard throwing ConfigReadError('[reversible] must be a TOML sub-table'), boolean type-check on `enabled` throwing ConfigReadError('[reversible].enabled must be a boolean'), return `{ enabled: raw['enabled'] === true }` (unknown keys silently dropped).
    2. Add the parseToml branch: `if ('reversible' in parsed) { result.reversible = validateReversibleConfig(parsed['reversible'], filePath) }` — mirror the pii branch placement and comment style, tagged Phase 8 / plan 08-01.
    3. Add the last-wins line to the mergeConfigs layer loop: when `layer.reversible !== undefined`, replace the local `reversible` with a NEW object `{ enabled: layer.reversible.enabled }` (immutability — never mutate, never alias layer objects).

    Run tests A-F: all green. Run `npm test`: full suite green with zero changes to detection/placeholder/audit suites (SC2 byte-identical proof).

    Commit: `feat(08-01): implement [reversible] config table (parse/validate/merge, default OFF)`

    REFACTOR phase: only if an obvious cleanup exists (e.g., shared boolean-field validation helper across pii/reversible validators — apply DRY only if the extraction is trivial). Commit `refactor(08-01): ...` only if changes were made; skip otherwise.
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/config/reader.test.ts tests/config/merge.test.ts && npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project=unit tests/config/reader.test.ts tests/config/merge.test.ts` exits 0
    - `npm test` exits 0 with no modifications to any file under tests/detect/, tests/placeholder/, or audit-related suites
    - `grep -c "validateReversibleConfig" src/config/index.ts` returns >= 2 (definition + parseToml call)
    - Git log shows `test(08-01):` commit BEFORE `feat(08-01):` commit
  </acceptance_criteria>
  <done>[reversible] fully plumbed; all tests green; TDD gate commits present in order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → config loader | Untrusted user/project TOML crosses into typed runtime config |
| types.ts contract → all hook consumers | A wrong SessionEnd type shape would break the fail-closed hook path in later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | validateReversibleConfig | mitigate | Typed validator throws ConfigReadError on any non-boolean `enabled`; never silently coerces (ASVS V5 input validation; Test B proves it) |
| T-08-02 | Elevation of Privilege | absent-[reversible] default path | mitigate | Frozen `Object.freeze({ enabled: false })` default; absent table structurally equals shipped one-way guarantee; Test F + full-suite byte-identical gate prove it |
| T-08-03 | Denial of Service | closed SessionEnd reason union (hypothetical) | mitigate | `reason` typed as open `string` so no future upstream reason value can become a type/parse failure inside the fail-closed wrapper |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH §Package Legitimacy Audit: none) — gate not applicable |
</threat_model>

<verification>
- `npm run typecheck` green at every commit boundary.
- `npm test` green after Task 3 — the existing detection/placeholder/audit suites passing UNCHANGED is the SC2 byte-identical proof for the config surface.
- TDD gate: `git log --oneline --grep="^test(08-01)"` and `--grep="^feat(08-01)"` each return a commit, test before feat.
</verification>

<success_criteria>
- `[reversible]` table parses, validates fail-closed, merges last-wins, defaults to `{ enabled: false }` when absent.
- SessionEndInput + widened unions + MrcleanReversibleConfig exist for plans 08-02/08-03 to consume.
- Zero behavior change with the table absent (full suite green, no detection-suite edits).
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-01-SUMMARY.md` when done (include RED/GREEN/REFACTOR narrative + commit list per TDD summary format).
</output>
