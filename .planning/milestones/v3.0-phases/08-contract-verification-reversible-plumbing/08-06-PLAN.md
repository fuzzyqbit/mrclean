---
phase: 08-contract-verification-reversible-plumbing
plan: 06
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - THREAT_MODEL.md
  - tests/copy-drift.test.ts
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements: [REVMODE-03]

must_haves:
  truths:
    - "Operator can read a THREAT_MODEL.md reversible-mode section covering map blast radius, the structural secret floor, wire re-entry analysis, key-custody honesty, and accepted residual risks"
    - "Claims about unbuilt code are framed as design commitments, not shipped facts"
    - "PROJECT.md's stale return-path restore wording (and its CLAUDE.md mirror) is amended to operator-only-restore framing"
    - "A copy-drift gate asserts the reversible section and its required subsections exist"
  artifacts:
    - path: "THREAT_MODEL.md"
      provides: "## Reversible Mode (v3.0) section with five H3 subsections"
      contains: "Reversible Mode"
    - path: "tests/copy-drift.test.ts"
      provides: "Presence gate for the reversible section + subsection headings"
      contains: "Reversible Mode"
  key_links:
    - from: "tests/copy-drift.test.ts"
      to: "THREAT_MODEL.md"
      via: "readFileSync + toContain assertions on section headings"
      pattern: "THREAT_MODEL"
    - from: "THREAT_MODEL.md"
      to: "docs/HOOK-CONTRACT.md"
      via: "wire re-entry section cites the E1 evidence + filed upstream request by document reference"
      pattern: "HOOK-CONTRACT"
---

<objective>
Draft the THREAT_MODEL.md reversible-mode section from the made T1-T6 decisions plus the Phase 8 empirical answers, gate it against drift, and amend the stale in-session-restore wording in PROJECT.md and its CLAUDE.md mirror.

Purpose: REVMODE-03 in full (phase success criterion SC4). Phase 11 finalizes this section against the shipped implementation and locks it with the copy-drift gate — build the section so Phase 11 extends rather than rewrites it.

Output: honest reversible-mode threat framing before any state code is written; zero stale return-path-restore copy anywhere in the project docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-PATTERNS.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-04-SUMMARY.md
@tests/uat/artifacts/contract-findings.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Copy-drift presence gate (RED) + THREAT_MODEL reversible section (GREEN)</name>
  <files>tests/copy-drift.test.ts, THREAT_MODEL.md</files>
  <read_first>
    - THREAT_MODEL.md (ENTIRE file — H3-numbered Non-Defense section style §3 lines ~34-44 and §10 lines ~109-137; insertion point between §10's end ~line 137 and '## What mrclean DOES defend against' ~line 141)
    - tests/copy-drift.test.ts (presence-gate pattern lines ~106-117; SCANNED_SOURCES lines ~36-43; Pitfall-5 honest-phrasing self-check lines ~100-103)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§THREAT_MODEL.md Section Design — the five-subsection outline mapped to T1-T6)
    - tests/uat/artifacts/contract-findings.json (E1 verdict — feeds the wire re-entry subsection and the shipped-behavior caveat if #68951 confirmed)
  </read_first>
  <behavior>
    RED (must fail before the section exists):
    - Test: THREAT_MODEL.md contains the heading '## Reversible Mode (v3.0)'
    - Test: THREAT_MODEL.md contains all five required subsection topics (assert on stable heading fragments: 'blast radius', 'secret floor', 'wire re-entry', 'key-custody', 'residual risks')
    - Test: the section contains the key honest-framing phrases 'transcript ratchet' and 'design commitment'
    GREEN: all presence assertions pass after the section is written; full copy-drift suite green (no banned-CLAIM regressions from the new prose)
  </behavior>
  <action>
    RED first: extend tests/copy-drift.test.ts with the presence gate (readFileSync THREAT_MODEL.md + toContain assertions per the shipped disclaimer-gate pattern). Run — all new assertions fail. Commit `test(08-06): add THREAT_MODEL reversible-section presence gate`.

    GREEN: insert `## Reversible Mode (v3.0)` into THREAT_MODEL.md between §10 and '## What mrclean DOES defend against', using the existing H3-numbered style (statement → mechanism → bold mitigation/consequence paragraph). Five subsections per RESEARCH §THREAT_MODEL.md Section Design:
    1. Map blast radius — what a fully exfiltrated session map leaks (ONE session's paths/names/identifiers, never keys/secrets) and why session-scoping caps it (T2/T3).
    2. The structural secret floor — secret-class originals (all secret TYPEs, ENV, ENTROPY, checksum'd PII) are never persisted; map entries structurally lack an `original` field; no configuration can widen the restorable set, only narrow it (T3). Contrast with ecosystem tools (no reference tool ships a non-configurable exclusion).
    3. Wire re-entry: why in-session restore is deferred — updatedToolOutput is model-facing; the transcript ratchet is permanent (history re-ships on every request and on resume); user view == model view; cite the E1 evidence and the filed upstream request BY DOCUMENT REFERENCE ('see docs/HOOK-CONTRACT.md') — do not inline the issue URL (08-05 records it there in parallel). If E1 confirmed #68951 on the tested version, add the shipped-behavior caveat: mrclean's own PostToolUse redaction of Bash results is inert on affected versions (known-gap entry).
    4. Key-custody honesty — file-based key custody defends against casual exfiltration of the map file alone, NOT against a same-user local attacker who can read both directories; name both adversary classes explicitly (this feeds Phase 11's copy-drift gate over 'encrypted at rest' claims).
    5. Accepted residual risks — retain-on-resume TTL window; enumeration residual under v2 tokens; SessionEnd is best-effort (crash ⇒ TTL sweep backstop); win32 fail-open known-gap interaction.

    Framing discipline: every claim about UNBUILT code (Phase 9 store, Phase 10 CLI) is written as a design commitment ('will', 'is designed to'), never as a shipped fact — include the phrase 'design commitment' in the section preamble. If the new prose contains guarantee-adjacent copy, add THREAT_MODEL.md to SCANNED_SOURCES (isSource: false) so the banned-CLAIM scan covers it.

    Commit `feat(08-06): draft THREAT_MODEL reversible-mode section` — run the full copy-drift suite green first.
  </action>
  <verify>
    <automated>npx vitest run --project=unit tests/copy-drift.test.ts && grep -q "## Reversible Mode (v3.0)" THREAT_MODEL.md && grep -qi "transcript ratchet" THREAT_MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - THREAT_MODEL.md contains '## Reversible Mode (v3.0)' with five H3 subsections covering blast radius / secret floor / wire re-entry / key-custody / residual risks
    - The wire re-entry subsection references docs/HOOK-CONTRACT.md
    - copy-drift suite green including the new presence assertions
    - Git log shows test(08-06) commit before feat(08-06) commit
  </acceptance_criteria>
  <done>Reversible-mode threat framing exists, is drift-gated, and is honest about what is designed vs shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Amend stale return-path restore wording (PROJECT.md + CLAUDE.md mirror)</name>
  <files>.planning/PROJECT.md, CLAUDE.md</files>
  <read_first>
    - .planning/PROJECT.md ('What This Is' line 5 — the stale sentence ends '...optionally restoring placeholders on the return path so file paths, names, and identifiers round-trip cleanly back into the user's view')
    - CLAUDE.md (Project section mirrors the identical sentence)
    - .planning/phases/08-contract-verification-reversible-plumbing/08-RESEARCH.md (§PROJECT.md Amendment Audit — the literal 'restore MCP tool stub' wording is ALREADY gone; only the return-path sentence remains)
  </read_first>
  <action>
    In both files, replace the stale clause 'and optionally restoring placeholders on the return path so file paths, names, and identifiers round-trip cleanly back into the user's view' with the operator-only framing: 'and, in opt-in reversible mode, letting the operator restore file paths, names, and identifiers locally via the `mrclean restore` CLI — never on the model-facing return path'.

    Then audit both files for any OTHER stale in-session-restore wording: grep for 'return path', 'round-trip', and 'restoring placeholders' in .planning/PROJECT.md and CLAUDE.md; amend any hit that describes in-session/hook-path restore as a current capability (hits inside Out of Scope / decision-log entries that correctly describe the DEFERRAL stay untouched). Record the audit result in the SUMMARY as 'audited, N instances amended' (RESEARCH expects N=1 per file; report the actual count).

    Do not modify any other PROJECT.md section — Key Decisions T1-T4 rows are already accurate.
  </action>
  <verify>
    <automated>! grep -q "round-trip cleanly back into the user's view" .planning/PROJECT.md && ! grep -q "round-trip cleanly back into the user's view" CLAUDE.md && grep -q "mrclean restore" .planning/PROJECT.md && grep -q "mrclean restore" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The stale sentence is absent from both files; the operator-only replacement is present in both
    - 'What This Is' still reads as one coherent sentence (manual read of the paragraph)
    - No other section of PROJECT.md changed (git diff shows only the audited amendments)
  </acceptance_criteria>
  <done>Project-level docs describe reversible mode as operator-only restore; SC4's amendment clause satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| THREAT_MODEL.md → operator/user trust | Security documentation is itself a security surface — overclaims become exploitable expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Repudiation | Unbuilt-code claims presented as shipped facts | mitigate | Design-commitment framing discipline enforced in prose + 'design commitment' phrase asserted by the presence gate; Phase 11 finalizes against the shipped implementation |
| T-08-20 | Information Disclosure | Understated map blast radius or key-custody claims ('encrypted at rest' overclaim) | mitigate | Key-custody subsection names the adversary classes file custody does NOT stop; feeds Phase 11 copy-drift lock |
| T-08-21 | Tampering | Future edits silently dropping the reversible section or its subsections | mitigate | copy-drift presence gate asserts heading + five subsection fragments on every unit run |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — gate not applicable |
</threat_model>

<verification>
- `npx vitest run --project=unit tests/copy-drift.test.ts` green (presence gate + no banned-CLAIM regressions).
- `npm test` full suite green.
- Stale-wording greps clean in both amended files.
</verification>

<success_criteria>
- SC4 satisfied: THREAT_MODEL.md reversible-mode section with all five required topics, honest framing, drift-gated; PROJECT.md + CLAUDE.md stale wording amended with audit trail.
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-06-SUMMARY.md` when done.
</output>
