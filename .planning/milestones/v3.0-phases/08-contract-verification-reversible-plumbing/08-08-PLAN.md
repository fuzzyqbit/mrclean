---
phase: 08-contract-verification-reversible-plumbing
plan: 08
type: execute
wave: 5
depends_on: [08-04]
files_modified:
  - tests/uat/contract-verification.test.ts
  - tests/uat/findings-builder.ts
  - tests/uat/findings-builder.test.ts
  - tests/uat/fixtures/e4-object-large-output-hook.sh
autonomous: true
gap_closure: true
requirements: [REVMODE-10]

must_haves:
  truths:
    - "The findings writer can no longer destroy recorded verdicts: it refuses to write when zero experiments recorded a verdict (beforeAll-throw safe), and it carries forward E1.rendering and E1_shape_validation from the committed artifact when the run did not produce them"
    - "e1-object-rewrite-hook.sh is wired to a committed test — the object-shape discovery (the phase's pivotal E1 finding) is reproducible by the harness"
    - "The harness owns an E1/Bash-object experiment that records an E1_shape_validation-shaped verdict, and a Read-object probe that records Read's object-payload behavior (closing the open follow-up)"
    - "E4 is gated on the object-shape leg: when Bash-object is honored, E4 runs a >10K object-shaped updatedToolOutput instead of recording 'unanswerable — blocked by #68951'"
    - "The writer merge/guard logic is proven offline by unit tests — no live sessions or tokens needed to verify durability"
  artifacts:
    - path: "tests/uat/findings-builder.ts"
      provides: "Pure buildFindingsArtifact(previous, run) — guard + carry-forward + merge, unit-testable"
      exports: ["buildFindingsArtifact"]
      min_lines: 60
    - path: "tests/uat/findings-builder.test.ts"
      provides: "Offline unit tests for guard, carry-forward, run-wins, verbatim_hook_error field fallback"
      min_lines: 60
    - path: "tests/uat/contract-verification.test.ts"
      provides: "E1/Bash-object + E1/Read-object experiments; E4 object-shape gating; afterAll rewired through buildFindingsArtifact"
      contains: "e1-object-rewrite-hook"
    - path: "tests/uat/fixtures/e4-object-large-output-hook.sh"
      provides: "~15K object-shaped updatedToolOutput fixture ({stdout, stderr, interrupted, isImage}) with E4 HEAD/TAIL markers"
      contains: "isImage"
  key_links:
    - from: "tests/uat/contract-verification.test.ts"
      to: "tests/uat/fixtures/e1-object-rewrite-hook.sh"
      via: "E1/Bash-object test invokes the fixture (WR-02 closed)"
      pattern: "e1-object-rewrite-hook"
    - from: "tests/uat/contract-verification.test.ts"
      to: "tests/uat/findings-builder.ts"
      via: "afterAll imports buildFindingsArtifact"
      pattern: "findings-builder"
    - from: "tests/uat/contract-verification.test.ts"
      to: "tests/uat/fixtures/e4-object-large-output-hook.sh"
      via: "E4 object leg invokes the new fixture"
      pattern: "e4-object-large-output-hook"
---

<objective>
Make the contract-verification harness own the object-shape leg and stop the findings writer from destroying committed evidence (gaps 2 and 3 root cause, WR-01/WR-02).

Purpose: The documented re-verification procedure (`MRCLEAN_UAT=1 npm run test:uat`) currently clobbers the exact evidence THREAT_MODEL.md §Reversible-3, docs/HOOK-CONTRACT.md §E1, and src/shared/types.ts JSDoc cite — the writer hardcodes `rendering: 'pending-interactive'`, emits no E1_shape_validation key, and overwrites even when beforeAll throws. And the load-bearing fixture e1-object-rewrite-hook.sh is wired to zero tests, so the phase's pivotal discovery is not reproducible. This plan fixes the harness OFFLINE (no live sessions, no tokens); plan 08-09 then runs the live E4 rerun safely on top of it.

Output: A pure, unit-tested findings builder (guard + carry-forward); the harness gains E1/Bash-object, E1/Read-object, and object-gated E4 legs; a new object-shaped ~15K E4 fixture. Everything verifiable without MRCLEAN_UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-contract-verification-reversible-plumbing/08-VERIFICATION.md
@tests/uat/contract-verification.test.ts
@tests/uat/artifacts/contract-findings.json
</context>

<interfaces>
<!-- Contract this plan CREATES. Plan 08-09's live rerun and the rewired afterAll consume it. -->

```typescript
// tests/uat/findings-builder.ts (new — pure module, no I/O, no vitest imports)

export interface ToolVerdict {
  verdict: string
  signals: Record<string, unknown>
  evidence_paths: string[]
}

export interface ExperimentRecord {
  question: string
  verdict: string
  method: string
  claude_version: string
  date: string
  signals: Record<string, unknown>
  evidence_paths: string[]
}

/** Everything a harness run may have recorded. Absent field == experiment not run. */
export interface RunRecords {
  claudeVersion: string
  date: string
  issue68951State: string
  e1Tools: Record<string, ToolVerdict>            // keys: Bash | Read | MCP
  shapeValidation?: Record<string, unknown>       // E1_shape_validation-shaped (fresh from the object legs)
  e2?: ExperimentRecord
  e3?: ExperimentRecord
  e4?: ExperimentRecord
  e4Control?: ExperimentRecord
  e5Headless?: ExperimentRecord
  e5Noise?: ExperimentRecord
}

/**
 * Pure findings assembly. Returns null when the run recorded ZERO verdicts
 * (caller must then leave the committed artifact untouched).
 * Carry-forward rule: for any experiment the run did not produce, re-emit the
 * previous artifact's record verbatim; stubs only when neither side has it.
 */
export function buildFindingsArtifact(
  previous: unknown,                               // parsed committed artifact, or undefined
  run: RunRecords,
): Record<string, unknown> | null
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract a pure, guarded findings builder and rewire the afterAll writer</name>
  <files>tests/uat/findings-builder.ts, tests/uat/findings-builder.test.ts, tests/uat/contract-verification.test.ts</files>
  <read_first>
    - tests/uat/contract-verification.test.ts (ENTIRE file, 813 lines — the module-level records at 90-102, missingRecord at 286-293, and the afterAll writer at 351-409 are what the builder absorbs)
    - tests/uat/artifacts/contract-findings.json (the committed artifact — its exact key shapes ARE the carry-forward contract: experiments.E1.rendering object at lines 66-82, experiments.E1_shape_validation at 84-114 including verbatim_hook_error and reinterpretation_notes)
    - vitest.config.ts (uat project definition — findings-builder.test.ts rides the tests/uat/**/*.test.ts glob; it must NOT self-skip: it is pure logic, zero tokens, zero claude spawns, safe in any run)
  </read_first>
  <behavior>
    tests/uat/findings-builder.test.ts (AAA style, NO describe.skipIf gate — runs offline):
    - Test 1: run with zero recorded verdicts (empty e1Tools, all optional records undefined) → buildFindingsArtifact returns null
    - Test 2: run with only e1Tools.Bash recorded, previous artifact containing E1.rendering object + E1_shape_validation → output re-emits previous experiments.E1.rendering verbatim AND previous experiments.E1_shape_validation verbatim (deep-equal)
    - Test 3: run.shapeValidation defined → output experiments.E1_shape_validation uses the run record (run wins over previous)
    - Test 4: run.shapeValidation defined but WITHOUT a verbatim_hook_error field, previous has one → output E1_shape_validation carries the run record with verbatim_hook_error filled from previous (field-level fallback for the one interactively-sourced field)
    - Test 5: experiment absent from BOTH previous and run (e.g. e2 undefined, previous has no E2) → output E2 is a 'not-recorded' stub (missingRecord shape preserved from current writer)
    - Test 6: previous artifact undefined and no rendering produced → output E1.rendering === 'pending-interactive' (the literal only appears as this last-resort fallback)
  </behavior>
  <action>
    RED first: create tests/uat/findings-builder.test.ts with the six behaviors above importing from './findings-builder.js' — run `npx vitest run --project=uat tests/uat/findings-builder.test.ts` and confirm failure (module missing). Commit `test(08-08): add failing findings-builder guard/carry-forward tests`.

    GREEN: create tests/uat/findings-builder.ts implementing buildFindingsArtifact per the interfaces block:
    - Guard: recorded-verdict count = Object.keys(run.e1Tools).length plus one per defined optional record (shapeValidation, e2, e3, e4, e4Control, e5Headless, e5Noise). Zero → return null.
    - Assemble the same top-level shape the current writer emits (claude_version, date, generated_by, issue_68951, experiments) — move the E1 tools/verdict-summary assembly, missingRecord stubs, and E4 control_additionalContext nesting out of the afterAll into this pure function (immutable: build new objects, never mutate inputs).
    - Carry-forward: read previous?.experiments (defensively — previous is unknown; tolerate missing/malformed via type guards, never throw). Per experiment key (E1.rendering, E1_shape_validation, E2, E3, E4, E5 sub-records): run record wins; else previous record re-emitted verbatim; else stub. E1_shape_validation additionally does the field-level verbatim_hook_error fallback (Test 4).
    - Rewire contract-verification.test.ts afterAll: read FINDINGS_PATH inside try/catch (existsSync + readFileSync + JSON.parse; parse failure → treat as undefined previous), call buildFindingsArtifact(previous, runRecords). Null → console.warn('mrclean findings: no experiment recorded a verdict — artifact left untouched') and DO NOT write. Non-null → write + console.table exactly as today. Keep the finally-block sandbox rmSync.
    - Delete the now-dead inline assembly from afterAll (the experiments literal at ~355-384) and the module-level missingRecord if fully absorbed. The hardcoded `rendering: 'pending-interactive' as unknown` line MUST be gone from contract-verification.test.ts.
    Commit `feat(08-08): guarded findings writer with carry-forward (WR-01)`.
  </action>
  <verify>
    <automated>npx vitest run --project=uat tests/uat/findings-builder.test.ts && npx vitest run --project=uat tests/uat/contract-verification.test.ts 2>&1 | grep -E "skipped|passed"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project=uat tests/uat/findings-builder.test.ts` (NO MRCLEAN_UAT env) exits 0 with 6 tests passed, 0 skipped
    - `npx vitest run --project=uat tests/uat/contract-verification.test.ts` (NO MRCLEAN_UAT) reports all contract tests skipped and MUST NOT modify tests/uat/artifacts/contract-findings.json (`git diff --stat tests/uat/artifacts/` empty — proves the guard: skipped run records zero verdicts, writer refuses)
    - `grep -c "pending-interactive" tests/uat/contract-verification.test.ts` returns 0 (fallback literal lives only in findings-builder.ts)
    - `grep -c "findings-builder" tests/uat/contract-verification.test.ts` returns >= 1 (import wired)
    - `git log --oneline` shows test(08-08) before feat(08-08)
  </acceptance_criteria>
  <done>Writer logic is pure, guarded, carry-forward-safe, and proven by offline unit tests; a skipped/failed run can no longer clobber the committed artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the object-shape legs — E1/Bash-object, E1/Read-object, object-gated E4 + new fixture</name>
  <files>tests/uat/contract-verification.test.ts, tests/uat/fixtures/e4-object-large-output-hook.sh</files>
  <read_first>
    - tests/uat/contract-verification.test.ts (current state after Task 1 — E1/Bash test at ~415-433 is the template for the object legs; E4 test gating at ~629-650; buildE1Verdict at 245-275; findTranscript/extractTranscriptToolData helpers)
    - tests/uat/fixtures/e1-object-rewrite-hook.sh (the orphaned fixture being wired — 26 lines; its payload string is reused verbatim in the shape-validation record's object_probe_payload signal)
    - tests/uat/fixtures/e4-large-output-hook.sh (30 lines — the string-form template the new object fixture clones: node -e body construction, HOOK_LOG side-file convention, exit 0)
    - tests/uat/artifacts/contract-findings.json (experiments.E1_shape_validation at lines 84-114 — the record shape the new tests must reproduce: signals object_probe_*, string_shape_*, reinterpretation_notes keys)
  </read_first>
  <action>
    1. New fixture tests/uat/fixtures/e4-object-large-output-hook.sh: clone e4-large-output-hook.sh but emit the OBJECT shape — node -e builds body "E4_HEAD_MARKER " + "x".repeat(15000) + " E4_TAIL_MARKER" and writes JSON.stringify({hookSpecificOutput:{hookEventName:"PostToolUse",updatedToolOutput:{stdout: body, stderr: "", interrupted: false, isImage: false}}}). HOOK_LOG side-file line: e4-object-large-output-hook-fired. Header comment: E4 rerun instrument (plan 08-08, gap 2) — object shape per e1-object-rewrite-hook.sh, the form CC 2.1.209 honors for Bash. MARKER STRINGS ONLY. chmod +x.

    2. E1/Bash-object test — insert DIRECTLY AFTER the existing 'E1/Bash' test (declaration order is execution order; it must run after E1/Bash so it can read that leg's rejection evidence, and before E4). Same structure as E1/Bash with: E1_OBJECT_HOOK = path.join(FIXTURES_DIR, 'e1-object-rewrite-hook.sh'); hookLog 'e1-bash-object-hook.log'; matcher 'Bash'; same echo-the-marker prompt. Record buildE1Verdict(run, true) into a new module-level `e1ObjectBash: ToolVerdict | undefined`. To capture the string-leg rejection evidence: have the E1/Bash test stash its transcript path in a module variable; in this test, read that transcript raw (readFileSync, tolerate null path) and regex-scan for the shape-rejection hook error (pattern matching "updatedToolOutput" plus "does not match" or "invalid_type"); record the matched excerpt (trimmed, <= 600 chars) or undefined.

    3. E1/Read-object probe test — after E1/Bash-object. Record-don't-assert: register e1-object-rewrite-hook.sh on a 'Read' matcher, prompt Claude to read e1-read-marker.txt and quote contents verbatim (same as E1/Read). Hard-assert only harness integrity (session ran, HOOK_LOG side file exists). Verdict logic: REWRITTEN marker in tool_result → 'honored (Bash-style object accepted for Read)'; else scan the transcript raw text for a hook-error excerpt (same regex) and record 'rejected — zod error names Read's expected output shape (see verbatim excerpt)' with the excerpt; else 'indeterminate'. Either outcome empirically closes the Read object-shape open follow-up — a rejection error that names Read's expected shape is itself the answer.

    4. Assemble `shapeValidationRecord` (module-level, built once both object legs have run — do it at the end of the Read-object test): an E1_shape_validation-shaped object with question/method/claude_version/date, verdict derived from the observed combination (object honored + string rejected → the SHAPE-VALIDATES verdict wording; degraded combinations get honest wording), fixture_path 'tests/uat/fixtures/e1-object-rewrite-hook.sh', signals: object_probe_honored, object_probe_session_id (from the Bash-object run init), object_probe_payload (the fixture's JSON payload string), string_shape_rejected + verbatim_hook_error (from step 2's scan, undefined when not observed — the builder's field-level fallback preserves the committed one), read_object_verdict + read_object_hook_error, and regenerated reinterpretation_notes (E4 note becomes 'answerable via object-shaped Bash payload — E4 leg now gated on the object verdict'; read_object_shape note reflects the recorded probe outcome). Pass it as run.shapeValidation to buildFindingsArtifact.

    5. Re-gate E4: replace `const honored = (['Bash','Read'] as const).find(t => e1Tools[t]?.verdict === 'honored')` logic — FIRST preference: e1ObjectBash?.verdict === 'honored' → run the Bash leg with E4_OBJECT_HOOK (the new fixture) and method text 'live headless -p session; Bash produces >10K output; fixture hook returns ~15K OBJECT-shaped updatedToolOutput ({stdout,...}) with HEAD/TAIL markers (fixture tests/uat/fixtures/e4-object-large-output-hook.sh)'. Fallback: the old string-form gating unchanged. Only when neither leg is honored: record the honest fallback verdict, reworded to cite the object-leg result instead of the stale '#68951 blocked' premise (e.g. 'unanswerable on this CC version — object-shaped updatedToolOutput not honored for Bash in this run (see E1_shape_validation)'). Keep verdict derivation (HEAD/TAIL marker logic) unchanged.

    6. Update the file-header doc comment (E1/E4 bullet points) to mention the object-shape legs, and keep every new test inside the existing describe.skipIf(!UAT_ENABLED) block so the opt-in gate covers them.

    Commit: `test(08-08): wire object-shape legs — E1/Bash-object, E1/Read-object, object-gated E4 (WR-02)`
  </action>
  <verify>
    <automated>npx vitest run --project=uat tests/uat/contract-verification.test.ts 2>&1 | grep -E "skipped" && test -x tests/uat/fixtures/e4-object-large-output-hook.sh && ! grep -rqE "AKIA|ghp_|sk-ant" tests/uat/fixtures/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project=uat tests/uat/contract-verification.test.ts` (NO MRCLEAN_UAT) reports 11 skipped (9 existing + E1/Bash-object + E1/Read-object) and 0 failed; contract-findings.json untouched
    - `grep -c "e1-object-rewrite-hook" tests/uat/contract-verification.test.ts` returns >= 1 (WR-02: fixture no longer orphaned)
    - `grep -c "e4-object-large-output-hook" tests/uat/contract-verification.test.ts` returns >= 1
    - tests/uat/fixtures/e4-object-large-output-hook.sh is executable and contains "isImage" and both E4_HEAD_MARKER/E4_TAIL_MARKER strings; leak-grep (`grep -rE "AKIA|ghp_|sk-ant" tests/uat/fixtures/`) clean
    - The stale gating expression on the string-form Bash/Read verdicts is no longer E4's first preference (source assertion: E4 test checks e1ObjectBash verdict before the string-leg find)
    - Typecheck differential vs the 36-error baseline shows zero NEW errors
  </acceptance_criteria>
  <done>The harness owns every leg the docs cite: object-shape E1 evidence, Read-object probe, and an E4 that can actually answer the cap question; all offline gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness run → committed findings artifact | A test run (possibly broken/skipped) writes over version-stamped evidence that THREAT_MODEL.md and HOOK-CONTRACT.md cite |
| fixture hooks → live Claude sessions | Fixture payloads cross into real hook execution — must stay marker-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering | afterAll findings writer | mitigate (HIGH — documented procedure destroys cited security evidence) | Pure guarded builder: null on zero verdicts, carry-forward for run-absent experiments; six offline unit tests prove it before any live run |
| T-08-08-02 | Repudiation | e1-object-rewrite-hook.sh (orphaned) | mitigate | E1/Bash-object test wires the fixture; the pivotal discovery becomes reproducible + re-stampable on CC upgrades |
| T-08-08-03 | Information Disclosure | fixture scripts / marker strings | mitigate | New fixture is marker-strings-only; leak-grep gate (AKIA/ghp_/sk-ant) re-run in Task 2 verify |
| T-08-08-04 | Denial of Service | non-gated builder test in the uat project | accept | findings-builder.test.ts is pure (no claude spawn, no tokens, <1s); safe under `npm test` even in CI |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan — legitimacy gate not applicable |
</threat_model>

<verification>
- Offline: findings-builder unit tests 6/6 green WITHOUT MRCLEAN_UAT; contract suite reports 11 skipped and leaves the artifact byte-identical (guard proof).
- Greps: fixture wiring (e1-object-rewrite-hook, e4-object-large-output-hook referenced), 'pending-interactive' hardcode gone from the suite file.
- Fixture hygiene: executable bits + leak-grep clean.
- Typecheck differential: zero new errors vs the 36-error baseline.
</verification>

<success_criteria>
- A skipped, partial, or beforeAll-broken run can no longer destroy contract-findings.json content.
- The object-shape discovery and the E4 cap question are both exercisable by the committed harness (no live run required to prove the wiring — plan 08-09 performs the live rerun).
</success_criteria>

<output>
Create `.planning/phases/08-contract-verification-reversible-plumbing/08-08-SUMMARY.md` when done (note the RED/GREEN gate commits for Task 1 and the final skip-count).
</output>
