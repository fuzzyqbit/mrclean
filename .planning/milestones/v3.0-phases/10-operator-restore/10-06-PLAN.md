---
phase: 10-operator-restore
plan: 06
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - src/state/counts.ts
  - src/mcp/tools/status.ts
  - tests/state/counts.test.ts
  - tests/mcp/status-reversible.test.ts
autonomous: true
requirements: [REVMODE-12]

must_haves:
  truths:
    - "mrclean_status reports a reversible counters block — entry counts by class, session count, restored/unmatched totals — numbers and booleans ONLY, never values (SC5)"
    - "Entry counts are computed by a reducer INSIDE src/state/ — decrypted originals never enter src/mcp/ stack frames"
    - "The status tool stays zero-argument (z.object({})) — no sid can ever be injected by a prompt-influenced model"
    - "Missing sessions dir, zero maps, or unreadable audit yield zero counts, never an error (read-only never-throw posture preserved)"
    - "Field naming is honest about scope: session/entry counts are machine-global (~/.mrclean); restored/unmatched totals are project-cwd audit aggregates"
  artifacts:
    - path: "src/state/counts.ts"
      provides: "Counts-only projection over the encrypted session store (originals confined to src/state/)"
      exports: ["countSessionEntries", "SessionEntryCounts"]
      min_lines: 40
    - path: "src/mcp/tools/status.ts"
      provides: "statusOutputSchema extended with nested reversible counters block; zero-argument input unchanged"
      contains: "reversible"
    - path: "tests/mcp/status-reversible.test.ts"
      provides: "InMemoryTransport suite: zero-state defaults, seeded counts, schema lock, planted-canary absence"
      min_lines: 100
  key_links:
    - from: "src/mcp/tools/status.ts"
      to: "src/state/counts.ts"
      via: "countSessionEntries(baseDir) — NEVER readSessionMapFile directly from src/mcp/"
      pattern: "countSessionEntries"
    - from: "src/mcp/tools/status.ts"
      to: "src/audit/restore-log.ts"
      via: "aggregateRestoreCounters(getCwd()) for restored/unmatched totals (A2 pin)"
      pattern: "aggregateRestoreCounters"
---

<objective>
Extend `mrclean_status` with the REVMODE-12 reversible counters block: sessions + entries by class (via a new counts-only reducer in `src/state/`) and restored/unmatched totals (via 10-03's audit aggregator).

Purpose: SC5's status half — operators see honest counts, models see no values and get no arguments to inject. This is the first sanctioned MCP-side state READ; the reducer keeps `original` strings confined to `src/state/` stack frames (buildHydration precedent), and the tool's `z.object({})` input stays byte-identical.
Output: `src/state/counts.ts` + extended status tool + suites. RESEARCH Open Question 2 pin: report machine-global session counts with honest field naming — never claim project scoping the data cannot support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 8, §Pitfall 5, Assumption A2, Open Question 2)
@.planning/phases/10-operator-restore/10-PATTERNS.md (§src/mcp/tools/status.ts — schema/registration excerpts; §src/state/ counts reducer — buildHydration analog; §tests/mcp/status-reversible.test.ts — InMemoryTransport harness)
@src/mcp/tools/status.ts (statusInputSchema/statusOutputSchema 28-37; registerStatusTool signature 51-56; never-throw catch posture line 72; typed build + structuredContent return 80-93)
@src/state/index.ts (buildHydration 131-143 — the safe-projection-inside-state precedent)
@src/state/janitor.ts (sidFromName 215-222 + readDirSafe 120-129 — candidacy idioms to replicate)
@src/audit/restore-log.ts (aggregateRestoreCounters — created by 10-03)
@tests/mcp/status.test.ts (makeConnectedPair 57-70 — the linked-pair harness to clone; structuredContent cast idiom 102-110)

<interfaces>
<!-- Contracts this plan CREATES. -->

```typescript
// src/state/counts.ts — read-only, zero writes, zero locks; total-error per map.
export interface SessionEntryCounts {
  sessions: number     // decryptable maps under baseDir/sessions
  restorable: number   // entries where isRestorableType(entry.type)
  secret: number       // all other entries
}
export async function countSessionEntries(baseDir: string): Promise<SessionEntryCounts>
// absent dir / no candidates / all-corrupt => { sessions: 0, restorable: 0, secret: 0 }

// src/mcp/tools/status.ts — statusOutputSchema gains (existing six fields unchanged):
// reversible: z.object({
//   enabled: z.boolean(),            // effective config [reversible].enabled
//   sessions: z.number(),            // machine-global decryptable session maps
//   entries_restorable: z.number(),  // machine-global, by frozen class partition
//   entries_secret: z.number(),
//   restored_total: z.number(),      // THIS project's audit.jsonl aggregation
//   unmatched_total: z.number(),
// })
// registerStatusTool gains an OPTIONAL 5th param (call sites untouched):
//   getStateBaseDir?: () => string   // default: () => join(homedir(), '.mrclean')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Counts-only reducer in src/state/ (tests first)</name>
  <files>tests/state/counts.test.ts, src/state/counts.ts</files>
  <read_first>
    - src/state/index.ts (buildHydration 131-143 — the projection pattern; note how it deliberately drops `original`)
    - src/state/janitor.ts (sidFromName 215-222, readDirSafe 120-129 — candidacy loop to replicate locally with the deliberate-duplication comment)
    - tests/state/inspection.test.ts (135-143 tmpdir baseDir lifecycle; chaos.test.ts buildSeedMap 98-108 + writeSeedState 111-114 fixture recipes)
    - .planning/phases/10-operator-restore/10-PATTERNS.md (§src/state/ counts reducer)
  </read_first>
  <behavior>
    tests/state/counts.test.ts (write FIRST, watch fail on module-not-found):
    - seeded baseDir: sid1 map { 2 WORD w/ original, 1 AWS_KEY } + sid2 map { 1 PII_EMAIL } => { sessions: 2, restorable: 3, secret: 1 }
    - corrupt sid2 map (byte-flip at offset 37) => { sessions: 1, restorable: 2, secret: 1 } (silent skip)
    - absent sessions dir => all zeros, no throw
    - non-conforming filenames invisible
    - return value contains ONLY numbers: JSON.stringify(result) matches /^\{"sessions":\d+,"restorable":\d+,"secret":\d+\}$/ (no original text can escape the reducer)
    - no-write proof: sessions/keys listings + mtimes unchanged across the call
  </behavior>
  <action>
    Implement src/state/counts.ts per the interfaces block: local candidacy loop (endsWith '.map' + SESSION_ID_RE stem — comment notes deliberate duplication with janitor/session-index, different trust tiers), readSessionMapFile per candidate (null => skip), classify each Object.values(map.entries) entry via isRestorableType, tally into a fresh object. Imports ONLY from './map-store.js' and './session-map.js' plus node:fs/promises readdir. Module header notes: "counts-only projection — `original` strings never leave this module's frames (first sanctioned MCP-side state read, 10-06)". File under 100 lines. Run green. Commit `feat(10-06): counts-only session entry reducer in src/state`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/counts.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - All behavior rows pass; failing run observed before implementation
    - `grep -v '^\s*\*\|^\s*//' src/state/counts.ts | grep -c "original"` outputs 0 (the reducer never touches the original field — classification is type-based only)
    - `grep -v '^\s*\*\|^\s*//' src/state/counts.ts | grep -c "writeFile\|proper-lockfile\|createDecipheriv"` outputs 0 (read-only via the chokepoint; cold-path invariant 4 stays green)
    - `npm run typecheck` reports no errors referencing src/state/counts or tests/state/counts
  </acceptance_criteria>
  <done>Reducer returns counts only; originals provably confined to src/state/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Status tool reversible block + InMemoryTransport suite</name>
  <files>tests/mcp/status-reversible.test.ts, src/mcp/tools/status.ts</files>
  <read_first>
    - src/mcp/tools/status.ts (entire file — schema 28-37, registration 51-95; the never-throw catch idiom line 72 to replicate per data source)
    - tests/mcp/status.test.ts (makeConnectedPair 57-70, structuredContent cast 102-110 — clone the harness; check whether any existing assertion full-object-equals structuredContent)
    - src/audit/restore-log.ts (aggregateRestoreCounters signature — 10-03 contract)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 8 — data sources + honest-scoping copy)
  </read_first>
  <behavior>
    tests/mcp/status-reversible.test.ts (write FIRST):
    - zero state: fresh tmp baseDir + tmp cwd (no audit.jsonl) => structuredContent.reversible === { enabled: false, sessions: 0, entries_restorable: 0, entries_secret: 0, restored_total: 0, unmatched_total: 0 }
    - seeded: baseDir with the Task 1 fixture maps + cwd audit.jsonl containing two action:'restore' lines (restored 2/1, unmatched 1/0) and one hook AuditRecord line => reversible === { enabled: false, sessions: 2, entries_restorable: 3, entries_secret: 1, restored_total: 3, unmatched_total: 1 }
    - zero-argument lock: tools/list inputSchema for mrclean_status has no properties; callTool with arguments {} succeeds; the SIX pre-existing top-level fields are unchanged in shape
    - never-throw: baseDir pointing at a FILE (not dir) + unreadable audit => zero counts, tool still returns structuredContent
    - planted-canary absence: seed a map whose WORD original is 'zz-canary-status-leak' => JSON.stringify(structuredContent) does NOT contain 'zz-canary-status-leak'
  </behavior>
  <action>
    Extend src/mcp/tools/status.ts per the interfaces block: add the nested reversible object to statusOutputSchema; add the optional getStateBaseDir 5th parameter with default () => join(homedir(), '.mrclean') so existing call sites (src/mcp server wiring) need zero edits; in the handler, compute enabled from the already-loaded effective config's reversible.enabled === true, counts via countSessionEntries(getStateBaseDir()).catch(() => zeros), totals via aggregateRestoreCounters(getCwd()).catch(() => zeros) — each data source individually wrapped (line-72 posture). Build the reversible object field-by-field (no spread of foreign objects). Keep annotations readOnlyHint/idempotentHint and the JSON text + structuredContent dual return. Update the tool description string to mention the counters block with honest scoping ("session counts are machine-wide; restore totals are per-project"). If tests/mcp/status.test.ts full-object-asserts structuredContent, extend ONLY its expected object with the new zero-state reversible block (minimal diff); otherwise leave it untouched. Run both status suites green. Commit `feat(10-06): mrclean_status reversible counters — counts only, zero-argument`.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp/status-reversible.test.ts tests/mcp/status.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - All behavior rows pass; existing tests/mcp/status.test.ts green (with at most the zero-state reversible block added to a full-object expectation)
    - `grep -c "z.object({})" src/mcp/tools/status.ts` >= 1 unchanged (zero-argument input schema intact)
    - `grep -v '^\s*\*\|^\s*//' src/mcp/tools/status.ts | grep -c "readSessionMapFile\|session-map.js\|map-store.js"` outputs 0 (state access ONLY through the counts reducer)
    - Nested reversible schema fields are z.boolean()/z.number() only — `grep -A8 "reversible: z.object" src/mcp/tools/status.ts | grep -c "z.string"` outputs 0
    - `npm run typecheck` reports no errors referencing src/mcp/ or tests/mcp/ beyond the 38-error baseline set
  </acceptance_criteria>
  <done>Status reports honest counts; no values, no arguments, no direct state reads from MCP frames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decrypted map entries -> MCP tool response | prompt-injected model can read structuredContent — ONLY counts may cross; the reducer confines originals to src/state/ |
| model -> tool arguments | must stay an empty object forever — any sid/path argument turns status into an oracle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Info disclosure | status structuredContent | mitigate | Counts-only reducer inside src/state/ (buildHydration precedent); numbers/boolean-only schema (grep gate); planted-canary absence test |
| T-10-06-02 | Elevation | tool input schema | mitigate | z.object({}) byte-locked by test; optional baseDir closure is registration-time (server-side), never model-reachable |
| T-10-06-03 | Spoofing (false confidence) | dishonest scoping copy | mitigate | Field naming + description pin: machine-global sessions vs cwd-scoped restore totals (Open Question 2 resolution) |
| T-10-06-04 | DoS | corrupt store/audit crashing status | mitigate | Per-source .catch(() => zeros) + total-error reads; never-throw row in the suite |
| T-10-06-05 | Tampering | audit-derived counters manipulation | accept | audit.jsonl is operator-writable by design (0600-class local file); counters are informational, gate nothing, and carry no values — matching the existing audit trust posture |
</threat_model>

<verification>
- `npx vitest run tests/state/counts.test.ts tests/mcp/status-reversible.test.ts tests/mcp/status.test.ts --project=unit` green
- `git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder src/mcp/server.ts` shows no server wiring edits
</verification>

<success_criteria>
- SC5 status half: `mrclean_status` exposes entries-by-class, session count, and restored/unmatched totals — never values, zero arguments
- Originals provably never enter src/mcp/ frames (reducer boundary + grep gates)
</success_criteria>

<output>
Create `.planning/phases/10-operator-restore/10-06-SUMMARY.md` when done.
</output>
