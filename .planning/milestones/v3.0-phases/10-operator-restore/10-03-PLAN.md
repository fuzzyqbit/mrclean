---
phase: 10-operator-restore
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/audit/restore-log.ts
  - tests/audit/restore-log.test.ts
autonomous: true
requirements: [REVMODE-09]

must_haves:
  truths:
    - "A restore audit line carries counts + 16-hex hashes ONLY — the builder's input type cannot even accept raw originals (hash-at-the-boundary contract)"
    - "Restore records and hook AuditRecord lines coexist in one audit.jsonl; assertNoCanaryLeak and the aggregator both handle the heterogeneous stream"
    - "aggregateRestoreCounters sums restored/unmatched across action:'restore' lines; missing file and malformed lines yield zeros/skips, never throws"
    - "AuditRecord's LOCKED unions in src/audit/log.ts are byte-untouched (A3 pin: discriminated sibling, not a widening)"
  artifacts:
    - path: "src/audit/restore-log.ts"
      provides: "RestoreAuditRecord discriminated type + LOCKED no-raw builder + JSONL writer + counters aggregator"
      exports: ["RestoreAuditRecord", "buildRestoreAuditRecord", "writeRestoreAuditRecord", "aggregateRestoreCounters"]
      min_lines: 60
    - path: "tests/audit/restore-log.test.ts"
      provides: "Builder shape lock, heterogeneous round-trip, aggregator matrix, raw-absence grep"
      min_lines: 100
  key_links:
    - from: "src/audit/restore-log.ts"
      to: "src/audit/log.ts"
      via: "AuditWriteError import (single error type) + append discipline mirror"
      pattern: "AuditWriteError"
    - from: "src/restore/cli.ts (10-05)"
      to: "src/audit/restore-log.ts"
      via: "buildRestoreAuditRecord with pre-hashed values (redactedHash at the CLI boundary)"
      pattern: "buildRestoreAuditRecord"
---

<objective>
Create `src/audit/restore-log.ts`: the hash-only restore audit record (NEW discriminated line type in the existing `<cwd>/.mrclean/audit.jsonl` stream), its LOCKED no-raw builder, the O_APPEND writer, and the counters aggregator that 10-06's status tool reads.

Purpose: REVMODE-09 requires every restore operation audited hash-only. Planner pin A3: a discriminated sibling record (`action: 'restore'`) — NOT a widening of AuditRecord's LOCKED unions, which would force fake values into required fields (ruleId/severity/fingerprint/location) and touch a schema two shipped canary tests guard. Pin A2: restored/unmatched status counters aggregate from these records — no sidecar state files.
Output: Sibling audit module + suite. 10-05 (CLI) writes through it; 10-06 (status) aggregates from it; 10-08 leak-greps it end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 6, Code Example 4, Assumption A2/A3)
@.planning/phases/10-operator-restore/10-PATTERNS.md (§src/audit/restore-log.ts — writeAuditRecord + LOCKED-builder excerpts)
@src/audit/log.ts (writeAuditRecord 109-122 append discipline; AuditWriteError 88; LOCKED builder comment + destructure-pick 145-195 — the discipline to replicate, the unions to NOT touch)
@src/audit/canary-leak.ts (assertNoCanaryLeak 56-97 — per-line JSON parse + substring scan; works unchanged over heterogeneous JSONL)
@src/detect/findings.ts (sha256hex 63; redactedHash 74 — the 16-hex discipline callers use)

<interfaces>
<!-- Contracts this plan CREATES. 10-05 and 10-06 import these. -->

```typescript
// src/audit/restore-log.ts
export interface RestoreAuditRecord {
  ts: string              // ISO-8601 (new Date().toISOString())
  action: 'restore'       // discriminant vs hook AuditRecord lines (which have no `action` field)
  sessionScope: string    // 'all' (union index, A4 default) or the validated sid when --session used
  restored: number
  unmatched: number
  skippedSecret: number
  hashes: string[]        // 16-hex redactedHash values of DISTINCT restored originals, sorted (deterministic)
}

// LOCKED no-raw builder: input carries counts + PRE-hashed values only. Hashing happens
// at the caller boundary (src/restore/cli.ts) — this module never sees a raw original.
export function buildRestoreAuditRecord(input: {
  sessionScope: string
  restored: number
  unmatched: number
  skippedSecret: number
  hashes: string[]
}): RestoreAuditRecord

export async function writeRestoreAuditRecord(cwd: string, record: RestoreAuditRecord): Promise<void>
// appends one JSON line to join(cwd, '.mrclean', 'audit.jsonl'); throws AuditWriteError
// (imported from './log.js') with the same ENOENT install-hint copy shape as writeAuditRecord

export async function aggregateRestoreCounters(
  cwd: string,
): Promise<{ restored_total: number; unmatched_total: number }>
// reads audit.jsonl; per-line JSON.parse; sums restored/unmatched over lines where
// parsed.action === 'restore'; ENOENT => zeros; malformed lines skipped; never throws
```
</interfaces>
</context>

<feature>
  <name>Hash-only restore audit record + counters aggregation</name>
  <files>src/audit/restore-log.ts, tests/audit/restore-log.test.ts</files>
  <behavior>
    Builder:
    - buildRestoreAuditRecord({ sessionScope: 'all', restored: 2, unmatched: 1, skippedSecret: 1, hashes: ['a1..', 'b2..'] }) => record with action 'restore', ts matching /^\d{4}-\d{2}-\d{2}T.*Z$/, all fields copied field-by-field (destructure-pick — never blind-spread)
    - builder output has EXACTLY the 7 declared keys (Object.keys length lock — an over-shaped input object with an extra `value` property does not serialize: pass `{...validInput, value: 'raw-canary'} as never` and assert 'raw-canary' absent from JSON.stringify(record))
    Writer:
    - writeRestoreAuditRecord appends exactly one '\n'-terminated JSON line to <cwd>/.mrclean/audit.jsonl (flag 'a'); two sequential writes => two lines
    - cwd without .mrclean/ => throws AuditWriteError whose message includes 'mrclean install'
    Aggregator:
    - file containing: 1 hook-style AuditRecord line (no `action` key) + 2 restore lines (restored 2/1, unmatched 1/0) => { restored_total: 3, unmatched_total: 1 }
    - missing audit.jsonl => { restored_total: 0, unmatched_total: 0 }
    - malformed line between valid lines => skipped, valid lines still summed
    - restore line with non-number restored (tampered) => treated as 0 for that line (Number coercion guard), no throw
    Leak invariants (module-level, non-vacuous):
    - write a record whose hashes = [redactedHash('zz-canary-restorable')] => file line contains the 16-hex hash AND does not contain 'zz-canary-restorable'; assertNoCanaryLeak(auditPath, ['zz-canary-restorable']) returns ok:true (heterogeneous-stream compatibility proof: include one hook AuditRecord line in the same file first)
  </behavior>
  <implementation>
    Mirror writeAuditRecord's 12-line append discipline (appendFile flag 'a', utf8, AuditWriteError on failure with isEnoent branch) — import AuditWriteError from './log.js' so one error type spans the audit module; do NOT edit log.ts. Builder carries the LOCKED comment verbatim in spirit: "LOCKED: NEVER add raw value, original text, file path, or map contents here. CI canary test enforces this." and builds the record field-by-field (CR-01 never-blind-spread lesson). Aggregator: readFile utf8 in try/catch (ENOENT => zeros), split '\n', filter non-empty, JSON.parse per line in try/catch (skip malformed), pick lines where parsed is a record with action === 'restore', sum Number.isFinite-guarded restored/unmatched. Pure module otherwise: no config, no state imports. File under 200 lines.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing restore-audit record suite</name>
  <files>tests/audit/restore-log.test.ts</files>
  <read_first>
    - .planning/phases/10-operator-restore/10-PATTERNS.md (§src/audit/restore-log.ts — append + LOCKED-builder excerpts; §tests/audit/restore-canary-leak.test.ts non-vacuity guard shape)
    - src/audit/log.ts (findingToAuditRecord 160-195 — the record-shape test style to invert; AuditRecord interface 35-76 to build the heterogeneous fixture line)
    - src/audit/canary-leak.ts (assertNoCanaryLeak signature + CanaryLeakResult 36-97)
    - src/detect/findings.ts (redactedHash 74-76 — used to compute expected hash fixtures)
  </read_first>
  <action>
    Create tests/audit/restore-log.test.ts covering every behavior case, importing from '../../src/audit/restore-log.js', assertNoCanaryLeak from '../../src/audit/canary-leak.js', redactedHash from '../../src/detect/findings.js'. Use mkdtemp cwd fixtures with a pre-created .mrclean/ subdir (and one WITHOUT it for the AuditWriteError case); rm-recursive afterEach. The heterogeneous fixture writes one hand-built hook-style AuditRecord JSON line (minimal valid shape from log.ts unions) before the restore lines. Include the over-shaped-input serialization lock and the Object.keys(record).length === 7 lock. Run and confirm failure on module-not-found for src/audit/restore-log.ts. Commit `test(10-03): add failing hash-only restore audit suite`.
  </action>
  <verify>
    <automated>npx vitest run tests/audit/restore-log.test.ts --project=unit 2>&1 | grep -q "FAIL\|Cannot find\|failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - Suite has >= 10 tests: builder shape + ts format, exact-7-keys lock, over-shaped-input no-serialize, two-line append, ENOENT AuditWriteError with 'mrclean install' hint, heterogeneous aggregation sum, missing-file zeros, malformed-line skip, tampered-non-number guard, hash-present-raw-absent + assertNoCanaryLeak ok:true
    - Suite FAILS before implementation
    - RED commit present: `test(10-03): ...`
  </acceptance_criteria>
  <done>Failing suite committed pinning the hash-only record contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement src/audit/restore-log.ts</name>
  <files>src/audit/restore-log.ts</files>
  <read_first>
    - tests/audit/restore-log.test.ts (the contract just written)
    - src/audit/log.ts (writeAuditRecord 109-122 — append discipline to mirror; AuditWriteError 88; LOCKED comment style 145-195)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 6 — record rationale; Assumption A3)
  </read_first>
  <action>
    Implement per the interfaces block and implementation notes. Module header JSDoc: "Phase 10 (10-03) — discriminated restore audit record; hook findings stay in log.ts (LOCKED — do not widen its unions)". The builder's LOCKED comment sits directly above the return. Zero edits to src/audit/log.ts (import AuditWriteError only). Run the suite green plus the existing audit suites to prove coexistence. Commit `feat(10-03): implement hash-only restore audit record + aggregator`.
  </action>
  <verify>
    <automated>npx vitest run tests/audit/ --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 tests pass unmodified; existing tests/audit/*.test.ts all green
    - `git diff --stat -- src/audit/log.ts` empty (LOCKED module untouched)
    - `grep -c "LOCKED" src/audit/restore-log.ts` >= 1 (builder discipline comment present)
    - `grep -v '^\s*\*\|^\s*//' src/audit/restore-log.ts | grep -c "sha256hex\|redactedHash\|createHash"` outputs 0 (module never hashes — it never sees raws; hashing is the 10-05 caller's boundary job)
    - `npm run typecheck` reports no errors referencing src/audit/restore-log or tests/audit/restore-log
    - GREEN commit present: `feat(10-03): ...`
  </acceptance_criteria>
  <done>Record type, writer, and aggregator implemented; heterogeneous stream proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR, if needed): Tidy line-reader helpers</name>
  <files>src/audit/restore-log.ts</files>
  <read_first>
    - src/audit/restore-log.ts (as just implemented)
  </read_first>
  <action>
    Only if obvious improvements exist (shared audit-path join worth a local constant, aggregator parse loop over 30 lines): extract, re-run suite. Skip the commit if nothing to clean. Commit (only if changed): `refactor(10-03): clean up restore audit module`.
  </action>
  <verify>
    <automated>npx vitest run tests/audit/restore-log.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - Suite still green with zero test edits
    - Export surface unchanged (4 exports)
  </acceptance_criteria>
  <done>Module clean; 2-3 atomic commits present per TDD gate rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restored originals (memory) -> audit.jsonl (disk) | THE REVMODE-09 boundary — only 16-hex hashes may cross; the builder's input type is the structural gate |
| audit.jsonl (untrusted after write) -> aggregator | file may be tampered/malformed; reader must never throw or leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Info disclosure | buildRestoreAuditRecord | mitigate | Input type carries counts + pre-hashed strings only; field-by-field build (never blind-spread — CR-01); LOCKED comment + over-shaped-input serialization test |
| T-10-03-02 | Info disclosure | hashes field | accept | 16-hex truncated sha256 of restorable-class values (WORD/PII only — secret-class never restores, so never hashes here); byte-consistent with the shipped redactedHash discipline; brute-force of low-entropy WORD terms against a truncated hash is a documented residual matching the existing audit log posture |
| T-10-03-03 | Tampering / DoS | aggregateRestoreCounters | mitigate | Total-error reader: ENOENT zeros, per-line try/catch, Number.isFinite guards — tampered audit lines cannot crash status |
| T-10-03-04 | Repudiation | restore without audit trail | mitigate (this plan's scope) | Writer throws AuditWriteError so the caller (10-05) must handle it visibly (warn) rather than silently dropping the record; end-to-end enforcement in 10-08's leak-grep + non-vacuity guards |
| T-10-03-05 | Tampering | widening the LOCKED AuditRecord schema | mitigate | Discriminated sibling module (A3 pin); `git diff` gate proves log.ts byte-untouched; existing canary suites re-run green |
</threat_model>

<verification>
- `npx vitest run tests/audit/ --project=unit` green (new + existing coexist)
- TDD gates: `git log --oneline --grep="test(10-03)"` and `--grep="feat(10-03)"` each non-empty
- `git diff --stat e950d76..HEAD -- src/audit/log.ts src/audit/canary-leak.ts` empty
</verification>

<success_criteria>
- Restore operations have a hash-only, discriminated audit record type with a structurally no-raw builder
- Status counters are derivable from the audit stream alone (A2 pin) — no new state files, no janitor surface
- RED -> GREEN (-> REFACTOR) commits in order
</success_criteria>

<output>
Create `.planning/phases/10-operator-restore/10-03-SUMMARY.md` when done (include RED/GREEN/REFACTOR narrative + commits).
</output>
