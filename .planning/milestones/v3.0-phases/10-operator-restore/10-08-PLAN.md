---
phase: 10-operator-restore
plan: 08
type: execute
wave: 3
depends_on: ["10-05", "10-06"]
files_modified:
  - tests/audit/restore-canary-leak.test.ts
  - tests/state/inspection.test.ts
autonomous: true
requirements: [REVMODE-09]

must_haves:
  truths:
    - "Secret-class canaries appear on NO surface anywhere — audit.jsonl, restore stderr, raw map/key artifact bytes, or restore stdout (the placeholder survives); restorable canaries appear ONLY on restore stdout (the feature), never in audit/stderr/artifacts (the load-bearing leak framing)"
    - "Every forced-failure path (garbage key, EACCES, hand-poisoned secret-class original) leaks nothing to stderr or audit — error paths swept, not just the happy path (Phase 7 lesson)"
    - "A restore run leaves the session store byte-inert: artifact byte-scan still clean, listings + mtimes unchanged (inspection extension)"
    - "Phase regression holds: full suite green, typecheck at the 38-error baseline with zero Phase 10 errors, frozen trees (src/hook, src/detect, src/placeholder + their test trees) zero-diff since phase base e950d76"
  artifacts:
    - path: "tests/audit/restore-canary-leak.test.ts"
      provides: "REVMODE-09 leak-grep: audit + stderr sweep over success AND error paths with non-vacuity guards"
      min_lines: 120
    - path: "tests/state/inspection.test.ts"
      provides: "Extended: post-restore artifact byte-scan + no-litter/mtime proof"
      contains: "restore"
  key_links:
    - from: "tests/audit/restore-canary-leak.test.ts"
      to: "src/audit/canary-leak.ts"
      via: "assertNoCanaryLeak over heterogeneous audit.jsonl (works unchanged per 10-03 proof)"
      pattern: "assertNoCanaryLeak"
    - from: "tests/audit/restore-canary-leak.test.ts"
      to: "src/restore/cli.ts"
      via: "runRestore with captured stdout/stderr across success + every degrade/error path"
      pattern: "runRestore"
---

<objective>
Extend the leak-grep regression over the complete restore surface (REVMODE-09's second half — 10-03 locked the record shape; this locks the integrated system including error paths), extend the inspection artifact scan across a restore run, and close the phase with the regression gate the ROADMAP names.

Purpose: SC4 — "the extended leak-grep regression passes over map artifacts, restore code paths, and their error paths". The v2.0 Phase 7 lesson is binding: leak tests must audit the fully-integrated surface and cover exception paths, not just the happy path. STATE.md's v3.0 note pins this file's scope: "Phase 10 extends [assertNoCanaryLeak] over map artifacts + restore code/error paths."
Output: tests/audit/restore-canary-leak.test.ts + inspection extension + the phase-gate verification bundle (full suite / typecheck differential / frozen-tree zero-diff).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-restore/10-RESEARCH.md (Leak-Grep Extension Strategy — the four-point spine is normative; §Pitfall 10; Validation Architecture phase-regression row)
@.planning/phases/10-operator-restore/10-PATTERNS.md (§tests/audit/restore-canary-leak.test.ts — non-vacuity + leak-assertion excerpts; §tests/state/inspection.test.ts extension — byte-scan block)
@tests/audit/pii-canary-leak.test.ts (canary corpus style 40-52; NON-VACUITY line-count guard 105-124; leak assertion shape 126-137 — the harness to clone)
@tests/state/inspection.test.ts (runReversibleEvent 65-85; byte-scan block 169-177; exact-listing assertions 160-165 — extend in place)
@tests/state/chaos.test.ts (garbage-key + chmod recipes 306-311; stderr spy 159; IS_WIN32/IS_ROOT guards 69-71)
@src/audit/canary-leak.ts (assertNoCanaryLeak 56-97 — ENOENT-clean + malformed-line-as-leak semantics)
@src/restore/cli.ts (RunRestoreOpts seam — 10-05 contract)
@src/state/map-store.ts (encryptMapBuffer 124 + ensureSessionKey 246 — the poison recipe)

<interfaces>
<!-- Canary corpus pinned for this plan (and reusable by Phase 11): -->

- RESTORABLE_CANARIES: 'zz-canary-project-path-7g2' (WORD), 'kim.canary@zz.invalid' (PII_EMAIL)
- SECRET_CANARY: fake AWS key NOT ending in 'EXAMPLE' (gitleaks-allowlist landmine), e.g. 'AKIAIOSFODNN7EXAMPLX'
- Leak invariant matrix (load-bearing framing from RESEARCH):
  | Surface | restorable canaries | secret canary |
  | restore stdout | REQUIRED (the feature) | FORBIDDEN (placeholder survives) |
  | restore stderr | FORBIDDEN | FORBIDDEN |
  | audit.jsonl | FORBIDDEN (hashes only) | FORBIDDEN |
  | sessions/*.map + keys/* raw bytes | FORBIDDEN (ciphertext) | FORBIDDEN |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore canary-leak suite — success and every error path</name>
  <files>tests/audit/restore-canary-leak.test.ts</files>
  <read_first>
    - tests/audit/pii-canary-leak.test.ts (ENTIRE file — clone the corpus constants, line-count non-vacuity guard, assertNoCanaryLeak call shape, console.error-on-leak diagnostics)
    - tests/state/inspection.test.ts (runReversibleEvent 65-85 — seed real state) and tests/state/chaos.test.ts (306-311 garbage-key; chmod recipe; stderr capture — capture, do not silence, to assert warning copy)
    - src/state/map-store.ts (encryptMapBuffer — hand-poison recipe: encrypt a JSON where the AWS entry carries original: SECRET_CANARY, write envelope bytes to mapPathFor)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (Leak-Grep Extension Strategy items 1-4)
  </read_first>
  <action>
    Create tests/audit/restore-canary-leak.test.ts in the unit project with two describe blocks. SUCCESS PATH: seed real state (runReversibleEvent recipe) with both RESTORABLE_CANARIES + SECRET_CANARY; runRestore over a document carrying all three placeholders with stdout/stderr captured and a tmp cwd audit sink. Non-vacuity FIRST (pii-canary precedent): assert the summary reports restored >= 2, assert audit.jsonl exists with >= 1 action:'restore' line. Then the invariant matrix: restorable canaries present in stdout but absent from stderr + audit (assertNoCanaryLeak with RESTORABLE_CANARIES over the audit path; substring greps over captured stderr); SECRET_CANARY absent from stdout, stderr, AND audit; the AWS placeholder present byte-identical in stdout. ERROR PATHS (each seeded fresh, each asserting stderr + audit leak-free AND stdout === input): (a) garbage 17-byte key overwrite; (b) chmod-000 map (describe.skipIf IS_WIN32 + root guard + visible placeholder test); (c) hand-poisoned ciphertext where the secret entry carries original: SECRET_CANARY encrypted with the real key — restore must neither restore it nor leak it on any surface (assert SECRET_CANARY absent from stdout/stderr/audit). Commit `test(10-08): extend leak-grep over restore surfaces + error paths`.
  </action>
  <verify>
    <automated>npx vitest run tests/audit/restore-canary-leak.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - Success block: non-vacuity guards precede every cleanliness assertion (restored >= 2; audit line-count >= 1)
    - All four surfaces swept per the invariant matrix; secret canary checked on ALL surfaces including stdout
    - Error block covers >= 3 forced failures incl. the hand-poisoned secret-original ciphertext
    - Existing tests/audit/pii-canary-leak.test.ts byte-untouched (`git diff --stat -- tests/audit/pii-canary-leak.test.ts` empty)
    - Secret canary does not end in 'EXAMPLE'
  </acceptance_criteria>
  <done>REVMODE-09's leak-grep spine green over the integrated restore surface, error paths included.</done>
</task>

<task type="auto">
  <name>Task 2: Inspection extension + phase regression gate</name>
  <files>tests/state/inspection.test.ts</files>
  <read_first>
    - tests/state/inspection.test.ts (ENTIRE file — the byte-scan block 169-177 and listing assertions 160-165 being extended; existing WORD_ORIGINAL/SECRET_ORIGINAL constants)
    - src/restore/cli.ts (RunRestoreOpts — inject the test's existing baseDir)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (Validation Architecture — the phase-regression row commands)
  </read_first>
  <action>
    Extend tests/state/inspection.test.ts with a new test AFTER the existing flow: run runRestore (stdin = a document carrying the persisted placeholders, tmp cwd for audit) against the already-persisted state, then (1) re-run the exact byte-scan over sessions/*.map + keys/* raw bytes (WORD_ORIGINAL, SECRET_ORIGINAL, '"entries"' all absent — no plaintext litter appeared); (2) assert sessions/keys readdir listings AND per-file mtimeMs are identical to pre-restore snapshots (Pitfall 7 — restore's write surface is exactly stdout/stderr/audit.jsonl; map mtime is the TTL heartbeat). Keep every existing test byte-identical — additions only. Then run the phase regression gate and record results in the SUMMARY: (a) `npm test` fully green; (b) `npm run typecheck` — total error count == 38 AND zero errors mention phase-10 files (src/restore, src/audit/restore-log, src/state/counts, tests/restore, tests/cli/restore, tests/audit/restore-log, tests/audit/restore-canary-leak, tests/mcp/status-reversible, tests/state/counts, tests/config/reversible-raw-keys); (c) frozen-tree zero-diff since phase base. Commit `test(10-08): inspection covers restore-flow artifacts + no-litter proof`.
  </action>
  <verify>
    <automated>npx vitest run tests/state/inspection.test.ts --project=unit && npm test && npm run typecheck 2>&1 | grep -c "error TS" | grep -qx "38" && git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder tests/placeholder tests/detect | wc -l | grep -qx "0" && echo PHASE-GATE-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Inspection suite green; `git diff tests/state/inspection.test.ts` shows only appended test(s) + any snapshot-capture hoists (existing assertions untouched)
    - `npm test` exit 0 (full suite: unit + integration, tools-list included)
    - `npm run typecheck 2>&1 | grep -c "error TS"` outputs exactly 38, and `npm run typecheck 2>&1 | grep -cE "src/restore|restore-log|src/state/counts|tests/restore|tests/cli/restore|tests/audit/restore-|tests/mcp/status-reversible|tests/state/counts|reversible-raw-keys"` outputs 0
    - `git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder tests/placeholder tests/detect` produces empty output (additive-phase proof)
    - No dist/ paths in any commit from this plan (`git log e950d76..HEAD --name-only -- dist/ | wc -l` outputs 0)
  </acceptance_criteria>
  <done>Store byte-inert across restore; full-suite + typecheck + zero-diff phase gate recorded green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| restore process -> every observable sink (stdout/stderr/audit/fs) | the sweep boundary — the invariant matrix defines exactly what may cross where |
| corrupted/poisoned store -> restore error handling | error paths are the historically-leaky surface (Phase 7 lesson) — swept explicitly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Info disclosure | audit/stderr on success | mitigate | assertNoCanaryLeak + substring greps per the invariant matrix, guarded non-vacuous (restored >= 2 first) |
| T-10-08-02 | Info disclosure | error/exception paths | mitigate | Forced-failure block: garbage key, EACCES, hand-poisoned secret original — every path swept for both canary classes |
| T-10-08-03 | Info disclosure | secret-class round-trip via poisoned store | mitigate | Poisoned-ciphertext fixture (real-key encryption) asserts SECRET_CANARY on NO surface including stdout — the triple gate (GCM/parse-drop/read-gate) proven end-to-end |
| T-10-08-04 | Tampering | restore mutating the store (TTL spoof / litter) | mitigate | Byte-scan + listing/mtime identity across a restore run in the inspection harness |
| T-10-08-05 | Tampering (regression) | frozen v1/v2 trees drifting | mitigate | Zero-diff gate over src/hook, src/detect, src/placeholder + test trees since e950d76; typecheck differential pinned at 38 |
</threat_model>

<verification>
- `npx vitest run tests/audit/restore-canary-leak.test.ts tests/state/inspection.test.ts --project=unit` green
- `npm test` exit 0; typecheck differential == 38 with zero phase-10 errors
- `git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder tests/placeholder tests/detect` empty
</verification>

<success_criteria>
- SC4 complete: hash-only audit + leak-grep green over map artifacts, restore paths, and error paths
- Phase regression gate green and recorded — the phase is provably additive outside its four sanctioned seams
</success_criteria>

<output>
Create `.planning/phases/10-operator-restore/10-08-SUMMARY.md` when done (include the three phase-gate command outputs).
</output>
