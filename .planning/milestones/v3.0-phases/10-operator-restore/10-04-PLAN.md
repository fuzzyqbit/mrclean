---
phase: 10-operator-restore
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/config/index.ts
  - src/doctor/checks.ts
  - tests/config/reversible-raw-keys.test.ts
  - tests/doctor/checks.test.ts
autonomous: true
requirements: [REVMODE-12]

must_haves:
  truths:
    - "An unknown key inside [reversible] in EITHER config layer makes `mrclean doctor` FAIL with exitCodeOnFail 1, naming the offending file and key — the silent no-op REVMODE-12 bans is dead"
    - "Supported-keys-only configs keep the three LOCKED PASS/SKIP detail strings byte-identical (Tests 15-17 pass unmodified)"
    - "Malformed config keeps the SKIP path — checkConfigLoad owns that FAIL; one root cause never double-FAILs"
    - "The LOCKED doctor exit-code map (1,2,3,4,6) is untouched — exit 1 was reserved for exactly this"
  artifacts:
    - path: "src/config/index.ts"
      provides: "Raw-layer [reversible] key scan (sees keys BEFORE the tolerant validator drops them)"
      exports: ["readRawReversibleKeys", "SUPPORTED_REVERSIBLE_KEYS"]
      contains: "readRawReversibleKeys"
    - path: "src/doctor/checks.ts"
      provides: "checkReversibleState upgraded with unknown-key FAIL branch"
      contains: "unsupported [reversible] key"
    - path: "tests/doctor/checks.test.ts"
      provides: "FAIL-loud rows (user layer, project layer, both) + byte-lock proof for existing rows"
      min_lines: 30
  key_links:
    - from: "src/doctor/checks.ts"
      to: "src/config/index.ts"
      via: "readRawReversibleKeys per layer (NEVER loadEffectiveConfig — Pitfall 4: the validator is blind to unknown keys)"
      pattern: "readRawReversibleKeys"
---

<objective>
Upgrade doctor check 8 (`checkReversibleState`) to FAIL loud on unsupported `[reversible]` configuration: any key outside the supported set `{enabled, ttl_hours}` in either config layer's raw TOML.

Purpose: REVMODE-12's FAIL-loud clause. Phase 8 research pinned the meaning ("unknown keys inside [reversible] are ignored today... REVMODE-12's FAIL-loud lands in Phase 10") and 08-03 reserved `exitCodeOnFail: 1` in the check-8 header for exactly this. Planner pin A1: the FAIL set is unknown keys ONLY — win32+enabled stays a documented inherited gap (no FAIL, no new detail copy), and the pre-reshape CC-version floor is dead (T1 cut in-session restore). No new PASS-detail copy is added; the three LOCKED detail constants survive byte-identically.
Output: A raw-keys helper export in src/config/ + the upgraded check + test rows. TDD because the check has exact I/O: planted config content in, byte-locked CheckResult out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 7, §Pitfall 4, Assumption A1, Open Question 1)
@.planning/phases/10-operator-restore/10-PATTERNS.md (§src/doctor/checks.ts — LOCKED detail constants, checkConfigLoad FAIL precedent, validator-blindness excerpt; §src/config/index.ts raw-keys helper routes)
@src/doctor/checks.ts (checkReversibleState 552-597 — the function being upgraded; REVERSIBLE_DETAIL_* constants 557-560 BYTE-LOCKED; checkConfigLoad 495-546 — path derivation + FAIL-detail-with-path precedent; LOCKED exit map 10-15)
@src/config/index.ts (readConfigLayer 485-499 — ENOENT/empty semantics + ConfigReadError throw to mirror; validateReversibleConfig ~352-355 — why the loader drops unknown keys)
@tests/doctor/checks.test.ts (Tests 15-17 at 482-552 — the toEqual byte-lock row shape + makeTmpDir lifecycle to clone)

<interfaces>
<!-- Contracts this plan CREATES (additive to src/config/index.ts). -->

```typescript
// src/config/index.ts — ADDITIVE exports beside readConfigLayer.
export const SUPPORTED_REVERSIBLE_KEYS: readonly string[] // Object.freeze(['enabled', 'ttl_hours'])

export async function readRawReversibleKeys(filePath: string): Promise<string[]>
// Raw smol-toml parse of the file, returning Object.keys of the [reversible] table
// BEFORE validation drops anything. Semantics mirror readConfigLayer:
//   - ENOENT / empty file / no [reversible] table / non-record [reversible] => []
//   - malformed TOML => throws ConfigReadError (checkConfigLoad owns that FAIL)
```

Upgraded check contract (src/doctor/checks.ts):
- FAIL result shape: `{ name: 'reversible', status: 'FAIL', detail: 'unsupported [reversible] key(s): <file>: <key>[; <file>: <key>...]', exitCodeOnFail: 1 }` — offenders aggregated across BOTH layers, user layer listed first (checkConfigLoad precedent: paths ARE allowed in FAIL details; the T-08-08 constant-detail constraint applied to SKIP only)
- Layer paths: join(homeDir, '.mrclean', 'config.toml') and join(cwd, '.mrclean', 'config.toml') (same derivation as checkConfigLoad lines 500-501)
- ConfigReadError from either raw read => existing SKIP path with REVERSIBLE_DETAIL_CONFIG_ERROR, unchanged
- No offenders => existing PASS logic byte-identical (three LOCKED constants)
</interfaces>
</context>

<feature>
  <name>Doctor FAIL-loud on unknown [reversible] keys</name>
  <files>src/config/index.ts, src/doctor/checks.ts, tests/config/reversible-raw-keys.test.ts, tests/doctor/checks.test.ts</files>
  <behavior>
    readRawReversibleKeys:
    - file with `[reversible]\nenabled = true\nttl_hours = 12\n` => ['enabled', 'ttl_hours']
    - file with `[reversible]\nrestore_secrets = true\n` => ['restore_secrets']
    - absent file => []; empty file => []; file with other tables but no [reversible] => []
    - malformed TOML (`[reversible\n`) => throws ConfigReadError
    checkReversibleState (new rows; homeDir/cwd tmp fixtures per Tests 15-17 pattern):
    - user layer plants `[reversible]\nenabled = true\nrestore_secrets = true\n` => FAIL, exitCodeOnFail 1, detail matches /unsupported \[reversible\] key/ AND contains the user config path AND 'restore_secrets'
    - project layer plants `[reversible]\nttl_hour = 12\n` (typo) => FAIL naming the project path + 'ttl_hour'
    - BOTH layers offend => single FAIL whose detail names both file+key pairs, user first
    - supported keys only (both layers) => Tests 15-16 PASS rows pass byte-identically (no edits to those tests)
    - malformed config => Test 17 SKIP row passes byte-identically
  </behavior>
  <implementation>
    Helper (preferred route per PATTERNS): implement readRawReversibleKeys beside readConfigLayer reusing its readFile + ENOENT-catch + smol-toml parse + ConfigReadError wrap idioms, then return Object.keys(parsed['reversible']) when it is a plain record, else []. Check upgrade: inside checkReversibleState, FIRST run the two raw-key reads in a try/catch — ConfigReadError => return the existing SKIP result (REVERSIBLE_DETAIL_CONFIG_ERROR, never double-FAIL); THEN compute offenders = keys not in SUPPORTED_REVERSIBLE_KEYS per layer; offenders non-empty => return the FAIL shape from the interfaces block; ELSE fall through to the existing PASS body unchanged. Doctor wiring needs zero edits (src/doctor/index.ts line 150 already pushes checkReversibleState and computeExitCode already aggregates exitCodeOnFail — PATTERNS landmine note).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing raw-keys helper suite + FAIL-loud doctor rows</name>
  <files>tests/config/reversible-raw-keys.test.ts, tests/doctor/checks.test.ts</files>
  <read_first>
    - tests/doctor/checks.test.ts (Tests 15-17 at 482-552 — clone the tmp homeDir/cwd setup + toEqual row style; new rows APPEND after Test 17, existing rows byte-untouched)
    - src/doctor/checks.ts (552-597 current body + 557-560 LOCKED constants — know exactly what must survive)
    - src/config/index.ts (readConfigLayer 485-499 + ConfigReadError 52 — the semantics the helper mirrors)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 7 — FAIL matrix pins)
  </read_first>
  <action>
    (1) Create tests/config/reversible-raw-keys.test.ts covering the six readRawReversibleKeys behavior rows (import from '../../src/config/index.js'; tmp-file fixtures; expect().rejects.toThrow(ConfigReadError) for malformed). (2) Append the four new checkReversibleState rows to tests/doctor/checks.test.ts (user-layer offender, project-layer offender, both-layers aggregate, and an explicit guard test asserting the FAIL detail starts with 'unsupported [reversible] key'); use toMatch for path+key presence and toEqual for status/exitCodeOnFail fields. Do NOT edit Tests 15-17. Run both files: raw-keys suite fails on missing export; new doctor rows fail because the current check returns PASS. Commit `test(10-04): add failing FAIL-loud reversible-config rows`.
  </action>
  <verify>
    <automated>npx vitest run tests/config/reversible-raw-keys.test.ts tests/doctor/checks.test.ts --project=unit 2>&1 | grep -q "FAIL\|failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - reversible-raw-keys suite has >= 6 tests (supported keys, unknown key, absent, empty, no-table, malformed-throws)
    - checks.test.ts gains >= 4 new rows; `git diff tests/doctor/checks.test.ts` shows additions only within the new describe/rows (Tests 15-17 lines untouched)
    - Both suites FAIL before implementation
    - RED commit present: `test(10-04): ...`
  </acceptance_criteria>
  <done>Failing suites committed pinning helper semantics + FAIL matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement helper + check upgrade</name>
  <files>src/config/index.ts, src/doctor/checks.ts</files>
  <read_first>
    - tests/config/reversible-raw-keys.test.ts and the new tests/doctor/checks.test.ts rows (the contracts just written)
    - src/config/index.ts (readConfigLayer body 485-499 — copy its error-wrap + isRecord idioms)
    - src/doctor/checks.ts (checkReversibleState 552-597 — surgical upgrade; checkConfigLoad 529-536 FAIL-shape precedent)
  </read_first>
  <action>
    Implement per the interfaces block: SUPPORTED_REVERSIBLE_KEYS + readRawReversibleKeys in src/config/index.ts (additive — zero changes to existing exports, loadEffectiveConfig, or validator behavior); the FAIL branch in checkReversibleState per the implementation notes (raw reads first, ConfigReadError => existing SKIP, offenders => FAIL, else existing PASS body verbatim). The three REVERSIBLE_DETAIL_* constants stay byte-identical. Zero edits to src/doctor/index.ts. Run the full doctor + config unit suites green. Commit `feat(10-04): doctor FAILs loud on unknown [reversible] keys`.
  </action>
  <verify>
    <automated>npx vitest run tests/config/ tests/doctor/checks.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 tests pass unmodified; Tests 15-17 pass unmodified (byte-lock proof)
    - `grep -c "REVERSIBLE_DETAIL_DISABLED\|REVERSIBLE_DETAIL_ENABLED\|REVERSIBLE_DETAIL_CONFIG_ERROR" src/doctor/checks.ts` unchanged from before the edit (constants untouched)
    - `grep -v '^\s*\*\|^\s*//' src/doctor/checks.ts | grep -c "loadEffectiveConfig()" ` — the unknown-key scan does NOT route through the tolerant loader (Pitfall 4): the new branch references readRawReversibleKeys, verified by `grep -c "readRawReversibleKeys" src/doctor/checks.ts` >= 1
    - `git diff --stat -- src/doctor/index.ts` empty
    - `npm run typecheck` reports no errors referencing src/config/ or src/doctor/ beyond the 38-error baseline set
    - GREEN commit present: `feat(10-04): ...`
  </acceptance_criteria>
  <done>Unknown [reversible] keys now FAIL doctor with exit 1, naming file + key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR, if needed): Tidy offender aggregation</name>
  <files>src/doctor/checks.ts</files>
  <read_first>
    - src/doctor/checks.ts (as just upgraded)
  </read_first>
  <action>
    Only if obvious improvements exist (offender-formatting inline logic worth a named local helper, checkReversibleState body exceeding 50 lines): extract within checks.ts, re-run suites. Skip the commit if nothing to clean. Commit (only if changed): `refactor(10-04): clean up reversible check offender formatting`.
  </action>
  <verify>
    <automated>npx vitest run tests/doctor/checks.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - Suite still green with zero test edits
    - LOCKED constants + exit map still byte-identical
  </acceptance_criteria>
  <done>Check clean; 2-3 atomic commits present per TDD gate rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-authored config.toml (both layers) -> doctor verdict | misconfiguration must surface loudly here, not silently no-op downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Repudiation / Info disclosure (false safety) | silent unknown-key tolerance | mitigate | Raw-layer scan sees keys BEFORE the validator drops them; FAIL exit 1 names file + key; the exact silent no-op REVMODE-12 bans is test-locked dead |
| T-10-04-02 | Elevation (config-widened restore) | a future `restore_secrets = true` style key | mitigate | Any unrecognized [reversible] key FAILs doctor — no unknown knob can pretend to work; the restorable set itself is frozen code (Phase 9), not config |
| T-10-04-03 | DoS (noisy doctor) | over-broad FAIL set | mitigate | A1 pin: FAIL = unknown keys only; win32+enabled and orphan counts add NO new copy; byte-locked existing rows prevent drift |
| T-10-04-04 | Tampering | malformed config double-FAIL | mitigate | ConfigReadError routes to the existing SKIP (checkConfigLoad owns the FAIL) — one root cause, one FAIL (T-08-10 discipline) |
</threat_model>

<verification>
- `npx vitest run tests/config/ tests/doctor/checks.test.ts --project=unit` green
- TDD gates: `git log --oneline --grep="test(10-04)"` and `--grep="feat(10-04)"` each non-empty
- `git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder src/doctor/index.ts` empty
</verification>

<success_criteria>
- SC5's doctor half: planted `restore_secrets = true` (either layer) => FAIL exit 1 naming file + key; supported configs byte-identical to shipped behavior
- LOCKED exit map and detail constants provably untouched
- RED -> GREEN (-> REFACTOR) commits in order
</success_criteria>

<output>
Create `.planning/phases/10-operator-restore/10-04-SUMMARY.md` when done (include RED/GREEN/REFACTOR narrative + commits).
</output>
