---
phase: 10-operator-restore
plan: 05
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - src/restore/cli.ts
  - src/cli.ts
  - tests/cli/restore.test.ts
  - tests/restore/degrade.test.ts
autonomous: true
requirements: [REVMODE-01, REVMODE-08]

must_haves:
  truths:
    - "Operator pipes redacted text (stdin or file) through `mrclean restore` and receives restored output on stdout; diagnostics and the counts summary go ONLY to stderr (HOOK-06 discipline)"
    - "Every cosmetic failure (map missing/corrupt/garbage-key/chmod'd, audit write failure) degrades one-way: stdout === input for affected tokens, one constant-shape warning, exit 0 — pipe-friendly, never blocking (REVMODE-08)"
    - "A held hook-side map lock never blocks or degrades restore: with proper-lockfile held on a seeded map, runRestore completes lock-free with correct restored output and the lock survives intact (SC3 'locked' clause — direct evidence, not inference)"
    - "Hard input errors exit 2: malformed --session (validated BEFORE any path derivation, sid never echoed) and unreadable input file"
    - "A restore run leaves sessions/ and keys/ byte-identical — listings and mtimes unchanged, zero litter (TTL heartbeat preserved)"
    - "Every completed restore appends one hash-only audit record; raw originals hashed (redactedHash, distinct+sorted) at this boundary and NOWHERE logged"
  artifacts:
    - path: "src/restore/cli.ts"
      provides: "runRestore(opts): stdin/file read -> index build -> engine -> stdout/stderr emission -> hash-only audit"
      exports: ["runRestore", "RunRestoreOpts"]
      min_lines: 80
    - path: "src/cli.ts"
      provides: "restore subcommand registration (dynamic import — hook cold path untouched)"
      contains: "restore [file]"
    - path: "tests/cli/restore.test.ts"
      provides: "CLI seam suite: stdin/file/--session, stream separation, audit write, exit codes"
      min_lines: 120
    - path: "tests/restore/degrade.test.ts"
      provides: "REVMODE-08 corruption/degrade matrix over real ciphertext fixtures + lock-held liveness row"
      min_lines: 120
  key_links:
    - from: "src/cli.ts"
      to: "src/restore/cli.ts"
      via: "await import('./restore/cli.js') inside .action() — the ONLY sanctioned import site of src/restore/ (10-07 fence-locks it)"
      pattern: "import\\('./restore/cli.js'\\)"
    - from: "src/restore/cli.ts"
      to: "src/restore/index.ts + src/restore/session-index.ts"
      via: "restoreText(input, index.placeholders, index.secretPlaceholders) over buildRestoreIndex(baseDir, session)"
      pattern: "restoreText"
    - from: "src/restore/cli.ts"
      to: "src/audit/restore-log.ts + src/detect/findings.ts"
      via: "redactedHash of distinct restored originals -> buildRestoreAuditRecord -> writeRestoreAuditRecord"
      pattern: "buildRestoreAuditRecord"
---

<objective>
Wire the operator surface: `mrclean restore [file] [--session <uuid>]` — a commander subcommand with dynamic import, a `runRestore` action module composing 10-01's engine over 10-02's index, hash-only audit via 10-03, and the full REVMODE-08 fail-one-way degrade matrix.

Purpose: REVMODE-01's CLI half (stdin or file -> restored stdout) and REVMODE-08's degrade semantics (placeholders stay visible + warning; restore and redact share NO kill switch — restore reads config for nothing, imports nothing from detect config/hook/state-write paths).
Output: The complete operator round-trip, unit-tested at the seam (direct runRestore calls with injected baseDir/cwd/stdin — no binary spawning). Planner pin A5: exit 0 on cosmetic degrade; exit 2 reserved for hard input errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 5, Code Example 3, §Pitfall 6, §Pitfall 7, §Pitfall 9, Assumption A4/A5)
@.planning/phases/10-operator-restore/10-PATTERNS.md (§src/restore/cli.ts — runIgnore analog + stdin no-analog note; §src/cli.ts — subcommand excerpts; §tests/cli/restore.test.ts — process seam harness; §tests/restore/degrade.test.ts — corruption recipes)
@src/install/ignore.ts (runIgnore 122-147 — opts object, [mrclean] stderr copy, exit-2 validation precedent)
@src/cli.ts (ignore subcommand 66-73; doctor option-taking subcommand 110-118; entrypoint guard 120-127 — stays untouched)
@src/restore/index.ts (restoreText + RestoreResult — created by 10-01)
@src/restore/session-index.ts (buildRestoreIndex + RestoreIndex — created by 10-02)
@src/audit/restore-log.ts (buildRestoreAuditRecord + writeRestoreAuditRecord — created by 10-03)
@src/detect/findings.ts (redactedHash 74-76 — hash at THIS boundary)

<interfaces>
<!-- Contract this plan CREATES. Wave 3 tests (mixed canary, leak-grep) drive runRestore directly. -->

```typescript
// src/restore/cli.ts
export interface RunRestoreOpts {
  file?: string                    // input file path; absent => read stdin to EOF
  session?: string                 // sid narrowing; validated here BEFORE buildRestoreIndex
  cwd?: string                     // default process.cwd() — audit sink scope
  baseDir?: string                 // default join(homedir(), '.mrclean') — TEST-ONLY override (state precedent)
  stdin?: NodeJS.ReadableStream    // default process.stdin — test seam
}
export async function runRestore(opts: RunRestoreOpts): Promise<void>
```

Pinned stderr copy (byte-locked by tests; single-line, '\n'-terminated, never values/map-paths/fs-error text):
- WARN no maps:      `[mrclean] restore: no readable session map — placeholders left unchanged`
- WARN audit failed: `[mrclean] restore: audit write failed — restored output unaffected`
- SUMMARY (always, last stderr line): `[mrclean] restore: restored=<N> unmatched=<N> secret-skipped=<N> sessions=<N>`
- ERR bad session (exit 2, sid NEVER echoed): `[mrclean] restore: invalid --session id (expected UUID)`
- ERR bad file (exit 2, operator's own path echoed): `[mrclean] restore: cannot read input file: <path>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runRestore + subcommand registration (CLI seam tests first)</name>
  <files>tests/cli/restore.test.ts, src/restore/cli.ts, src/cli.ts</files>
  <read_first>
    - tests/cli/ignore.test.ts (process.exit + stderr capture harness 127-152; makeTmpProject 17-21; in-test dynamic import idiom line 33 — clone all three, add a process.stdout.write mirror mock)
    - src/install/ignore.ts (runIgnore 122-147 — the action-module shape: opts destructure with defaults, [mrclean] prefix, exit-2 validation)
    - src/cli.ts (66-73 + 110-118 — registration idioms; 120-127 entrypoint guard MUST stay byte-identical)
    - src/restore/index.ts + src/restore/session-index.ts + src/audit/restore-log.ts (the wave-1 contracts being composed — exact signatures)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (Code Example 3 — readAll stdin accumulate; the hook's readStdinWithTimeout is deliberately WRONG here)
  </read_first>
  <behavior>
    tests/cli/restore.test.ts (write FIRST, watch fail, then implement):
    - stdin mode: seeded tmp baseDir (ensureSessionKey + writeSessionMapFile with one WORD entry), stdin = Readable.from([textWithToken]) => captured stdout === restored text exactly (no trailing additions); stderr contains ONLY the summary line
    - file mode: fixture file in tmp cwd passed as opts.file => same restored stdout
    - --session narrowing: two seeded sessions; opts.session = sid1 => sid2's token stays unmatched in output, summary shows sessions=1
    - malformed --session ('not-a-uuid', '../../etc') => process.exit(2) captured, stderr === ERR-bad-session line (grep asserts the raw arg string ABSENT from stderr), stdout empty, baseDir listings untouched
    - unreadable file => exit(2), ERR-bad-file line contains the path
    - audit: successful run appends exactly one action:'restore' line to <cwd>/.mrclean/audit.jsonl with hashes === distinct sorted redactedHash values of restored originals; raw originals absent from the file (substring grep)
    - audit degrade: cwd WITHOUT .mrclean/ => stdout still restored, stderr has WARN-audit-failed + summary, no exit(2), no throw
    - summary counts equal the engine's RestoreResult counts (restored/unmatched/skippedSecret) + index.sessions
  </behavior>
  <action>
    Implement src/restore/cli.ts per the interfaces block: (1) validate opts.session with isValidSessionId from '../state/session-map.js' BEFORE anything else — invalid => ERR line + process.exit(2); (2) read input: opts.file => readFile utf8 (ENOENT/EACCES => ERR line + exit(2)); else accumulate opts.stdin ?? process.stdin to EOF via for-await (RESEARCH Code Example 3 — NEVER import the hook's reader); (3) buildRestoreIndex(baseDir, session) with baseDir default join(homedir(), '.mrclean') via a private defaultBaseDir() matching the janitor/state 2-line idiom; (4) index.sessions === 0 => write WARN-no-maps; (5) restoreText(input, index.placeholders, index.secretPlaceholders); (6) process.stdout.write(result.text) — the ONLY stdout write; (7) hash boundary: hashes = [...new Set(result.restoredOriginals)].map(redactedHash).sort(); build + write the audit record (sessionScope = session ?? 'all'); AuditWriteError (or any audit throw) => WARN-audit-failed, continue; (8) final stderr summary line. Wrap 3-7 in a belt-and-braces try/catch (state/index.ts 251-282 posture): any unexpected throw => output=input passthrough via stdout, WARN-no-maps-shaped warning, summary, NO nonzero exit. Then register in src/cli.ts between the existing subcommands: .command('restore [file]') + .description('Restore policy-permitted placeholders from redacted text (stdin or file) — operator-only, local') + .option('--session <uuid>', 'Restrict restore to one session map') + dynamic-import action calling runRestore({ file, session: opts.session }). Run the seam suite green. Commit `feat(10-05): mrclean restore CLI — stdin/file to restored stdout with hash-only audit`.
  </action>
  <verify>
    <automated>npx vitest run tests/cli/restore.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - All behavior rows pass; test file committed BEFORE or WITH the implementation commit (tdd task discipline: failing run observed first)
    - `grep -c "await import('./restore/cli.js')" src/cli.ts` outputs 1 and `grep -v '^\s*//' src/cli.ts | grep -c "^import .*restore"` outputs 0 (dynamic-only — hook cold path untouched)
    - `grep -v '^\s*\*\|^\s*//' src/restore/cli.ts | grep -c "from '../hook\|from '../detect/index\|from '../config\|loadEffectiveConfig\|readStdinWithTimeout"` outputs 0 (restore reads config for NOTHING — Pitfall 6 no-shared-kill-switch; findings.js hash helper is the only detect import)
    - `grep -c "redactedHash" src/restore/cli.ts` >= 1 (hash-at-boundary present)
    - Existing tests/cli/ignore.test.ts still green (shared cli.ts edit is additive)
    - `npm run typecheck` reports no errors referencing src/restore/ or src/cli.ts
  </acceptance_criteria>
  <done>Operator can round-trip via stdin or file; audit record written hash-only; hard input errors exit 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REVMODE-08 degrade matrix (tests drive the handling)</name>
  <files>tests/restore/degrade.test.ts, src/restore/cli.ts</files>
  <read_first>
    - tests/state/chaos.test.ts (corruption fixture recipes 235-312: CT_OFFSET 37 byte-flip, truncated envelope, 17-byte garbage key, chmod-000; platform guards IS_WIN32/IS_ROOT 69-71; stderr spy 159; cleanupDirs 163-168)
    - tests/state/inspection.test.ts (160-165 exact-listing assertion `expect(sessionFiles).toEqual([...])` — the no-litter proof shape)
    - src/state/lock.ts (LOCK_OPTS 69-73 — the pinned realpath:false/stale:2500 acquire recipe; row (g) must hold the lock the way the hook holds it)
    - src/restore/cli.ts (as built in Task 1 — degrade paths to harden)
    - .planning/phases/10-operator-restore/10-RESEARCH.md (§Pattern 1 lock-free reads; §Pattern 5 degrade shape; §Pitfall 7 write-surface rule; §Pitfall 9 exit-0 rationale)
  </read_first>
  <behavior>
    tests/restore/degrade.test.ts — degrade rows (a)-(f) each assert the SAME degrade shape: stdout === input byte-identical, stderr === WARN-no-maps line + summary line (exactly 2 lines, no values, no fs-error text, no paths), no process.exit call (exit-equivalent 0), and sessions/ + keys/ readdir listings AND per-file mtimeMs snapshots identical before/after:
    - (a) absent baseDir entirely (nothing ever written)
    - (b) sessions dir exists, map file corrupt: ciphertext byte at offset 37 XOR 0x01
    - (c) truncated envelope: first 20 bytes only (below MIN_ENVELOPE)
    - (d) garbage key: overwrite key file with 17 ASCII bytes 'garbage-not-a-key'
    - (e) chmod 000 on the map file (describe.skipIf(IS_WIN32) + skip-if-root guard + visible placeholder test so the skip shows in reporter output)
    - (f) valid-but-unknown --session sid (well-formed UUID, no such map)
    - (g) lock-held liveness (SC3 'locked' clause — direct evidence, NOT a degrade row): seed a valid map (ensureSessionKey + writeSessionMapFile, one WORD entry), acquire proper-lockfile on the map path with the LOCK_OPTS recipe (fallback: plant the `<map>.lock` directory artifact) BEFORE the run; runRestore on input containing that session's token completes without hang or contention error (a hang surfaces as the vitest timeout — no bespoke timer) — stdout === RESTORED text (token replaced: reads are lock-free by atomic-rename design), stderr contains ONLY the summary line, no process.exit; after the run lockfile.check(mapPath, LOCK_OPTS) is still true and release() succeeds cleanly (hook-side lock never stolen, broken, or waited on); sessions/ + keys/ listings snapshotted AFTER acquisition are identical after the run (lock artifact still present) and .map/.key mtimeMs unchanged (the lock artifact's own mtime is holder-owned — exclude it from the comparison); non-vacuity: restored === 1 and the original IS present in stdout
    - input for rows (a)-(f) contains a well-formed v2 token (built via formatV2Token) so pass-through is non-vacuous (the token is PRESENT in stdout output)
  </behavior>
  <action>
    Write the matrix first against Task 1's implementation; where rows fail (e.g. an fs error message leaking into the warning, a stray stderr line, an unexpected throw), harden src/restore/cli.ts until every row passes — the total-error posture means every corruption shape funnels through buildRestoreIndex's silent-skip into the sessions===0 (or unmatched) path; runRestore itself must add NO new failure modes. Row (g) is ROADMAP SC3's 'locked' clause made direct instead of inferred: hold the lock exactly the way the hook holds it — `lockfile.lock(mapPath, LOCK_OPTS)` with LOCK_OPTS imported from src/state/lock.js (a src import, not a test-file import; the cold-path fence walks src/ only, so a test holding a lock is fence-safe) — or, as fallback, plant the `<map>.lock` directory artifact proper-lockfile creates. Expect ZERO hardening for row (g): readSessionMapFile takes no locks, so any hang, contention error, or degrade warning on this row is a design regression in the implementation, never a test-tolerance problem; release the lock in afterEach so a failing row cannot poison siblings. Give every row a tmp cwd containing `.mrclean/` so the best-effort audit write succeeds silently (the exact-line-count stderr assertions depend on it). Reuse chaos.test.ts fixture constants by value (duplicate CT_OFFSET = 37 locally with the derivation comment — never import from another test file). Commit `test(10-05): REVMODE-08 degrade matrix — fail one-way, exit 0, zero litter, lock-free under held lock` (plus a fix commit if hardening was needed: `fix(10-05): constant-shape degrade warnings`).
  </action>
  <verify>
    <automated>npx vitest run tests/restore/degrade.test.ts tests/state/chaos.test.ts --project=unit</automated>
  </verify>
  <acceptance_criteria>
    - All 7 matrix rows green (row e visibly skipped on win32/root with placeholder test; row g green on all platforms — proper-lockfile is mkdir-based, not posix-gated)
    - Degrade rows (a)-(f) each assert all four invariants: byte-identical stdout, exactly-2-line stderr, no exit, listings+mtimes unchanged; row (g) asserts the liveness set: restored stdout, summary-only stderr, no exit, lock still held (lockfile.check true) and cleanly released after the run
    - `grep -c "formatV2Token" tests/restore/degrade.test.ts` >= 1 (non-vacuous token fixtures)
    - `grep -Ec "lockfile\.lock\(|mkdir.*\.lock" tests/restore/degrade.test.ts` >= 1 (row g holds or plants a REAL lock — never simulated)
    - tests/state/chaos.test.ts still green unmodified (redaction's one-way parity untouched by restore's existence — REVMODE-08 separate error domains)
    - No commit in this plan touches dist/ (worktree rule: dist rebuilds happen on main post-merge)
  </acceptance_criteria>
  <done>Every corruption shape degrades one-way with constant-shape warnings; a held map lock never blocks, contends, or degrades a restore; redaction provably unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv/stdin -> runRestore | --session is hostile-shaped until isValidSessionId passes; input text is model-influenced |
| decrypted originals -> process stdout | the sanctioned exit point — restored values exist ONLY here |
| restore failure domain -> redact failure domain | REVMODE-08: must stay structurally disconnected (no shared config, flags, or modules) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering / Elevation | --session arg | mitigate | isValidSessionId before ANY path derivation; malformed => exit 2 with sid NEVER echoed (hostile-shaped input non-echo discipline, state/index.ts precedent) |
| T-10-05-02 | Info disclosure | stderr warnings + summary | mitigate | Byte-locked constant-shape copy: no values, no map paths, no fs-error text; degrade matrix greps every path |
| T-10-05-03 | Info disclosure | audit sink | mitigate | Hash boundary lives HERE: distinct+sorted redactedHash before the builder; raw-absence grep in seam tests; end-to-end leak-grep in 10-08 |
| T-10-05-04 | DoS on protection | shared kill switch | mitigate | runRestore imports zero config/hook/detect-engine modules (grep gate); reads take no locks — degrade row (g) holds a live proper-lockfile lock through a run, so lock-freedom is proven, not assumed; chaos suite re-run green proves redaction indifferent |
| T-10-05-05 | Tampering | restore writing into sessions/ | mitigate | Write surface is exactly stdout + stderr + audit.jsonl; listings+mtimes snapshot assertions in every degrade row (TTL heartbeat preserved); row (g) additionally proves the hook-side lock artifact is never stolen or removed |
| T-10-05-06 | Repudiation | audit write failure | accept (warned) | Best-effort audit matching the shipped hook posture (Phase 2 decision): WARN-audit-failed + continue — blocking restore on audit failure would push operators to disable the tool; residual documented in SUMMARY |
</threat_model>

<verification>
- `npx vitest run tests/cli/restore.test.ts tests/restore/degrade.test.ts tests/restore/engine.test.ts tests/restore/session-index.test.ts --project=unit` green
- `npx vitest run tests/state/chaos.test.ts --project=unit` green (separate error domains hold)
- `git diff --stat e950d76..HEAD -- src/hook src/detect src/placeholder tests/placeholder tests/detect` empty
</verification>

<success_criteria>
- SC1 CLI half: stdin and file input produce restored stdout; --session gates correctly; summary on stderr only
- SC3 complete at unit level: all six corruption shapes => pass-through + warning + exit 0 + zero litter; the 'locked' clause proven direct — row (g) completes lock-free under a held proper-lockfile lock with correct restored output and the lock intact
- Hash-only audit record written per restore invocation from this boundary
</success_criteria>

<output>
Create `.planning/phases/10-operator-restore/10-05-SUMMARY.md` when done.
</output>
