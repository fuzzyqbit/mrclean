---
phase: 11-wire-safety-verification-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/hook/dist-parity.test.ts
  - vitest.config.ts
autonomous: true
requirements: [REVMODE-11]

must_haves:
  truths:
    - "Spawning the shipped dist/cli.js hook twice with the same canary-bearing PostToolUse payload — one-way HOME vs reversible HOME — yields deep-equal parsed stdout after v2 nonce-tail normalization"
    - "Reversible-run stdout matches the v2 token probe while one-way stdout does not (branch engagement proven BEFORE parity is asserted)"
    - "Neither run's stdout contains the secret canary, and the reversible session map's raw bytes on disk lack the canary"
    - "dist-parity.test.ts is excluded from the unit project and included in the integration project — a unit-project run collects ZERO files (routing proven, 10-07 vacuous-pass hazard closed)"
  artifacts:
    - path: "tests/hook/dist-parity.test.ts"
      provides: "SC1a shipped-bundle stdout parity gate (spawn level — the tier that caught the two 09-08 dist-only bugs)"
      min_lines: 90
      contains: "stripNonceTails"
    - path: "vitest.config.ts"
      provides: "Dual-listing: unit exclude + integration include with Plan-11 non-vacuity comments"
      contains: "dist-parity"
  key_links:
    - from: "tests/hook/dist-parity.test.ts"
      to: "dist/cli.js"
      via: "spawnSync(process.execPath, [DIST_CLI, 'hook']) with per-run sandbox HOME"
      pattern: "DIST_CLI"
    - from: "vitest.config.ts"
      to: "tests/hook/dist-parity.test.ts"
      via: "paired exclude/include entries (stress.test.ts precedent)"
      pattern: "dist-parity"
---

<objective>
Build SC1a's deterministic CI leg: a dist-spawn parity gate proving the SHIPPED bundle's hook stdout is unchanged by reversible mode (modulo the sanctioned v2 nonce tail), with zero canary on stdout in either mode and zero plaintext canary in the persisted map.

Purpose: chaos parity already proves this at handler level, but 09-08 demonstrated that unit/tsx greens can hide dist-only bugs (tsup shims, `__filename`). REVMODE-11's "hook stdout is unchanged by reversible mode" needs artifact-level proof that runs in CI.
Output: tests/hook/dist-parity.test.ts (integration project) + vitest.config.ts dual-listing. This is the ONE sanctioned new file under tests/hook/ this phase — zero edits to the ten existing tests/hook files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Pattern 1 — spawn recipe + assertion list; §Pitfall 2 — vacuous CI routing; Locked Decisions — parity contract = deep-equal after nonce-tail strip)
@.planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§tests/hook/dist-parity.test.ts — verified line-level excerpts for every mechanic; §vitest.config.ts dual-listing edit)
@tests/hook/integration.test.ts (spawn recipe lines 19-42; payload shape 71-80 — READ ONLY, zero edits)
@tests/state/chaos.test.ts (pinned constants 78, 89-91; makeInput 128-139; makeHome 171-179; stripNonceTails + assertion order 141-144, 216-232; win32 skip 69, 153, 315-321 — READ ONLY)
@vitest.config.ts (stress.test.ts dual-listing: unit exclude 83-87, integration include 121-124; existing tests/hook/integration.test.ts unit-exclude at 75)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dist-parity gate + vitest.config dual-listing (one commit)</name>
  <files>tests/hook/dist-parity.test.ts, vitest.config.ts</files>
  <read_first>
    - tests/hook/integration.test.ts (lines 19-42 spawn recipe with sandbox HOME + USERPROFILE mirror; 71-80 PostToolUse payload shape incl. cwd handling; do NOT copy its beforeAll npm-build — globalSetup owns the build)
    - tests/state/chaos.test.ts (lines 78, 89-91 pinned SECRET/V2_TAIL_RE/V2_TAIL_PROBE; 128-139 makeInput canary payload; 141-144 stripNonceTails; 171-179 makeHome config.toml write; 216-232 assertion ORDER; 69/153/315-321 win32 visible-skip pattern)
    - vitest.config.ts (lines 83-87 unit-exclude entry style; 121-124 integration-include entry style; line 75 existing tests/hook exclude precedent; line 108 globalSetup ownership)
    - src/state/map-store.ts (mapPathFor / statePaths — exact sessions-dir + .map filename derivation for the spot-check; READ ONLY)
    - .planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§tests/hook/dist-parity.test.ts — the composed recipe)
  </read_first>
  <action>
    Create tests/hook/dist-parity.test.ts in a single commit WITH the vitest.config.ts dual-listing (the file must never exist without the unit exclude, or the unit glob runs it against a possibly-stale dist — Pitfall 2).

    Test structure (duplicate-don't-import discipline — copy pinned constants with a "duplicated, not re-derived" comment): DIST_CLI = path.resolve(process.cwd(), 'dist/cli.js'); SECRET = 'AKIAIOSFODNN7EXAMPLX' (the AWS-docs EXAMPLE key is gitleaks-allowlisted — the X suffix is load-bearing); V2_TAIL_RE (global) + V2_TAIL_PROBE (non-global copy — /g carries lastIndex across toMatch calls); IS_WIN32 with describe.skipIf(IS_WIN32) plus the visible win32 placeholder describe (HOME env ignored by os.homedir() on win32).

    Two sandbox HOMEs minted with mkdtempSync: oneway home (nothing written) and reversible home (mkdir `<home>/.mrclean` then write `<home>/.mrclean/config.toml` containing the two lines `[reversible]` and `enabled = true` — chaos makeHome recipe). runHook(home, payload) wraps spawnSync(process.execPath, [DIST_CLI, 'hook'], { input: JSON.stringify(payload), encoding: 'utf8', timeout: 30_000, env: { ...process.env, HOME: home, USERPROFILE: home } }). Payload per chaos makeInput: hook_event_name 'PostToolUse', fresh randomUUID() session_id PER run, transcript_path '/tmp/transcript', tool_name 'Bash', tool_input { command: 'deploy' }, tool_response embedding SECRET, tool_use_id — mirror integration.test.ts's cwd field handling exactly.

    Assertions in EXACTLY this order (non-vacuity before parity — a silent config miss makes parity trivially true): (1) exit status 0 both runs; (2) stdout non-empty both runs (substitution occurred); (3) parse both stdouts; neither raw stdout string contains SECRET; (4) reversible stdout matches V2_TAIL_PROBE (reversible branch REALLY engaged); (5) one-way stdout does NOT match V2_TAIL_PROBE; (6) stripNonceTails(parsedReversible) toEqual parsedOneway; (7) map spot-check — the reversible home's session map file for the run's sid exists (derive the exact path the way src/state/map-store.ts mapPathFor does) and its raw bytes read via readFileSync do NOT include SECRET; (8) the oneway home contains NO sessions directory (byte-identical one-way default writes no reversible state — REVMODE-05).

    vitest.config.ts: add 'tests/hook/dist-parity.test.ts' to the unit project exclude list AND the integration project include list, each entry carrying a Plan 11-01 comment in the stress.test.ts style explaining the non-vacuity reason (without the include, --project=integration reports zero files; without the exclude, the unit glob tests/**/*.test.ts collects it against stale dist).

    Routing verification before commit: `npx vitest run tests/hook/dist-parity.test.ts --project=integration` runs exactly 1 file green (globalSetup rebuilds dist); `npx vitest run tests/hook/dist-parity.test.ts --project=unit` collects ZERO test files (the exclude works — this expected-vacuous outcome IS the routing proof); full `npm test` green. Document in SUMMARY a sabotage spot-check: temporarily remove the integration include entry, observe the integration invocation report zero files/exit 0, restore it (10-07 discipline — proves the hazard is real and the entry is load-bearing).

    Commit `test(11-01): dist-spawn parity gate — reversible stdout deep-equals one-way after nonce strip (SC1a)`.
  </action>
  <verify>
    <automated>npx vitest run tests/hook/dist-parity.test.ts --project=integration && npx vitest run tests/hook/dist-parity.test.ts --project=unit 2>&1 | grep -q "No test files found" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - tests/hook/dist-parity.test.ts exists with all 8 numbered assertions in the specified order; constants carry the "duplicated, not re-derived" comment; win32 placeholder describe present
    - `npx vitest run tests/hook/dist-parity.test.ts --project=integration` reports 1 test file passed
    - `npx vitest run tests/hook/dist-parity.test.ts --project=unit` reports zero collected test files (exclude proven)
    - vitest.config.ts diff shows exactly two new entries (one exclude, one include), both with Plan 11-01 non-vacuity comments; no other config changes
    - Secret canary in the file is 'AKIAIOSFODNN7EXAMPLX' (grep confirms no string ending in 'EXAMPLE' used as a detection positive)
    - No beforeAll build step in the new file (globalSetup owns the tsup build)
    - `git diff --stat 8f370b9 -- tests/hook` shows ONLY the new dist-parity.test.ts; `git diff --stat 8f370b9 -- src/hook src/detect src/placeholder tests/placeholder tests/detect` is empty
    - `npm run typecheck` stays at the 38-error differential baseline with zero errors mentioning dist-parity.test.ts
    - SUMMARY documents the sabotage spot-check result
  </acceptance_criteria>
  <done>SC1a's CI proof exists at the shipped-artifact level: parity, branch-engagement, canary absence, and map hygiene all asserted through the real dist bundle with routing proven non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> shipped dist bundle | the gate must exercise the REAL artifact users run, not tsx source |
| sandbox HOMEs -> operator state | spawned hooks must never see the developer's real ~/.mrclean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering (of the proof) | vitest project routing | mitigate | Dual-listing in the same commit as the file; unit-run zero-file negative control in verify; sabotage spot-check documented in SUMMARY (10-07 demonstrated hazard) |
| T-11-01-02 | Info disclosure | canary on stdout / in map bytes | mitigate | Assertion order pins non-vacuity (v2 probe) BEFORE absence; raw-byte map read asserts ciphertext-only; canary is the pinned synthetic non-EXAMPLE marker |
| T-11-01-03 | Tampering | sandbox contaminating real HOME | mitigate | mkdtempSync per-run HOMEs with HOME+USERPROFILE both overridden in spawn env; payload cwd mirrors integration.test.ts precedent; win32 (where HOME is ignored) visibly skipped |

No package installs this phase (11-RESEARCH Package Legitimacy Audit: none — no T-11-SC row needed).
</threat_model>

<verification>
- `npx vitest run tests/hook/dist-parity.test.ts --project=integration` green (1 file)
- `npx vitest run tests/hook/dist-parity.test.ts --project=unit` collects zero files
- `npm test` green; `npm run typecheck` at 38-error baseline
- Frozen-tree diff vs 8f370b9: only tests/hook addition is dist-parity.test.ts; src/hook, src/detect, src/placeholder, tests/placeholder, tests/detect untouched
- `git status --porcelain dist/` empty at commit time (worktree rule: never commit dist)
</verification>

<success_criteria>
- SC1a carried by CI: shipped-bundle stdout parity one-way vs reversible with nonce normalization, canary absence both modes, reversible map ciphertext-only, one-way home state-free
- Routing hazard structurally closed for the new file (dual-listing + negative control)
</success_criteria>

<output>
Create `.planning/phases/11-wire-safety-verification-hardening/11-01-SUMMARY.md` when done (include the sabotage spot-check result and both routing-proof command outputs).
</output>
