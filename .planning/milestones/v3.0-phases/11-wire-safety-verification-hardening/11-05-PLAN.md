---
phase: 11-wire-safety-verification-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/uat/fixtures/echo-mcp-server.mjs
  - tests/uat/fixtures/postresp-log-hook.sh
  - tests/uat/wire-safety.test.ts
  - docs/HOOK-CONTRACT.md
autonomous: true
requirements: [REVMODE-11]

must_haves:
  truths:
    - "An opt-in wire-safety UAT suite exists that self-skips (exit 0, visible skip) without MRCLEAN_UAT=1 — never wired into CI, operator-run at verify-work per settled repo policy"
    - "Canaries are minted run-unique and originate ONLY inside the fixture MCP tool's env-fed output — never in prompts (a prompt canary enters model context pre-hook and makes the transcript grep unwinnable by design)"
    - "The suite's non-vacuity chain runs BEFORE any absence assertion: v2 tokens on the wire -> sandbox sessions map exists -> restore stdout carries the word original with restored>=1 -> only then canary-absence greps (stream, transcript, whole ~/.claude/projects tree) and the restore-then-resume re-entry proof"
    - "The hook command carries the HOME prefix ONLY (claude keeps real HOME for auth/transcripts); mrclean state lands in the sandbox mrclean-home and is cleaned up"
    - "docs/HOOK-CONTRACT.md carries the per-tool tool_response shapes section (re-homed STATE.md todo) quoting ZERO new session UUIDs — the copy-drift UUID-traceability gate stays green before the first live run"
  artifacts:
    - path: "tests/uat/wire-safety.test.ts"
      provides: "SC1b live canary gate + restore-then-resume re-entry proof + per-tool tool_response shape survey (record-don't-assert)"
      min_lines: 150
      contains: "MRCLEAN_UAT"
    - path: "tests/uat/fixtures/echo-mcp-server.mjs"
      provides: "Foreign-named MCP stdio echo server carrying env-fed canaries (the only reliable live substitution-on-the-wire path — string updatedToolOutput is honored for MCP, rejected for Bash)"
      contains: "CANARY_TEXT"
    - path: "tests/uat/fixtures/postresp-log-hook.sh"
      provides: "Parallel observer hook recording {tool_name, typeof tool_response, truncated raw JSON} per PostToolUse event"
      contains: "tool_response"
    - path: "docs/HOOK-CONTRACT.md"
      provides: "Per-tool tool_response shapes section (matrix pending first MRCLEAN_UAT run, zero UUIDs quoted)"
      contains: "Per-tool tool_response shapes"
  key_links:
    - from: "tests/uat/wire-safety.test.ts"
      to: "tests/uat/harness.ts"
      via: "runClaude/assertSessionRan imports — never reimplemented"
      pattern: "runClaude"
    - from: "tests/uat/wire-safety.test.ts"
      to: "tests/uat/fixtures/echo-mcp-server.mjs"
      via: "--mcp-config sandbox JSON with CANARY_TEXT env"
      pattern: "echo-mcp-server"
    - from: "tests/uat/wire-safety.test.ts"
      to: "dist/cli.js"
      via: "buildHookCommand-wrapped hook (HOME-prefixed) + spawned restore leg with sandbox HOME env"
      pattern: "buildHookCommand"
---

<objective>
Build SC1b's live wire-safety leg: a fixture MCP echo server carries run-unique canaries onto the wire, mrclean's REAL shipped hook substitutes them, and the suite proves zero canary originals in stream-json, the session transcript, and the whole ~/.claude/projects tree — then restores locally and resumes the session to prove restore never re-enters the wire. Plus the re-homed STATE.md todo: per-tool tool_response shape survey, record-don't-assert.

Purpose: the deterministic gates (11-01/11-02) prove the artifact; this leg proves the WIRE outcome through a real Claude Code session on the current CLI (2.1.212 vs the 2.1.209 stamps — drift is recorded, never hard-asserted). Opt-in by settled policy (token spend); the operator runs it at verify-work. Planner pin (adapting research OQ2): NO tee wrapper on hook stdout — dist-parity is the stdout proof, this leg is the wire proof.
Planner pin (canary uniqueness): the pinned unit corpus ('zz-canary-project-path-7g2', 'AKIAIOSFODNN7EXAMPLX') appears in this repo's planning docs, which prior GSD sessions have read into transcripts under ~/.claude/projects — a whole-dir grep for those strings would false-fail. The live leg therefore MINTS run-unique canaries, restoring the global-uniqueness premise the whole-dir grep needs. The pinned corpus remains authoritative for all deterministic gates.
Output: two fixtures + the UAT suite + the HOOK-CONTRACT.md section skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Pattern 2 — full sequencing incl. HOME-prefix seam A1, survey design A5; §Pitfall 3/4/5/9; §Code Examples — fixture server skeleton; Assumptions A1-A3, A5, A6)
@.planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§tests/uat/wire-safety.test.ts — harness imports, suite skeleton, settings/hookEntry/fixtureCmd excerpts, MCP wiring + eager tool discovery knobs, transcript/stream scan helpers, resume leg, non-vacuity chain, findings writer; §echo-mcp-server.mjs; §postresp-log-hook.sh)
@tests/uat/harness.ts (runClaude 85-138, assertSessionRan 69-82 — import, never reimplement)
@tests/uat/contract-verification.test.ts (writeSettings/hookEntry/fixtureCmd 122-138; extractToolResultText 154-163; findTranscript 181-195; extractTranscriptToolData 204-223; readLogEntries 231-243; preflight 307-330; MCP leg 345-353, 597-632; resume leg 707-728; findings writer 356-383 — READ ONLY)
@tests/uat/live-session.test.ts (buildHookCommand registration precedent 39, 61-75 — READ ONLY)
@tests/uat/findings-builder.ts (buildFindingsArtifact guard 27-44, 227 — import)
@src/hook/handlers/post-tool-use.ts (MRCLEAN_TOOL_RE line 64 — the self-exemption the fixture name must not match; READ ONLY, frozen tree)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture MCP echo server + tool_response observer hook</name>
  <files>tests/uat/fixtures/echo-mcp-server.mjs, tests/uat/fixtures/postresp-log-hook.sh</files>
  <read_first>
    - src/mcp/tools/status.ts (lines 82-96 — the in-repo registerTool signature ground truth; assumption A6 says copy from HERE, not from memory)
    - tests/uat/fixtures/log-hook.sh (ENTIRE file — the sibling to clone: jq-free node one-liner, E_LOG env var, exit 0 always, empty stdout, executable bit)
    - src/hook/handlers/post-tool-use.ts (line 64 MRCLEAN_TOOL_RE — verify the fixture's fully-qualified tool name 'mcp__wire-canary__echo_project_notes' does NOT match; READ ONLY)
    - .planning/phases/11-wire-safety-verification-hardening/11-RESEARCH.md (§Code Examples fixture skeleton)
  </read_first>
  <action>
    Create tests/uat/fixtures/echo-mcp-server.mjs: ~40 lines using the existing '@modelcontextprotocol/sdk' dep — McpServer named 'wire-canary' version '0.0.0', registerTool 'echo_project_notes' (description 'returns project notes'; empty input schema matching the shipped registerTool signature from src/mcp/tools/status.ts) whose handler returns a single text content block of process.env.CANARY_TEXT ?? ''; connect StdioServerTransport. The server name/tool name combination must produce a hook tool_name that fails MRCLEAN_TOOL_RE (mrclean self-exempts its own tools — a matching name would silently skip substitution and vacuous-pass the whole leg).

    Create tests/uat/fixtures/postresp-log-hook.sh: clone log-hook.sh's shape (POSIX sh, node -e one-liner reading stdin JSON, appendFileSync to process.env.E_LOG, exit 0 always, NOTHING on stdout) but emit one JSON line per event of the form {tool_name, t: typeof payload.tool_response, raw: JSON.stringify(payload.tool_response) truncated to 2000 chars}. chmod +x both fixture conventions matched (grep the existing fixtures dir for the shebang + exit style).
  </action>
  <verify>
    <automated>node --check tests/uat/fixtures/echo-mcp-server.mjs && sh -n tests/uat/fixtures/postresp-log-hook.sh && test -x tests/uat/fixtures/postresp-log-hook.sh</automated>
  </verify>
  <acceptance_criteria>
    - echo-mcp-server.mjs passes node --check; imports only '@modelcontextprotocol/sdk' subpaths already used in src/mcp.ts; canary text comes ONLY from CANARY_TEXT env (grep: no literal canary strings in the file)
    - 'mcp__wire-canary__echo_project_notes' tested against MRCLEAN_TOOL_RE in a comment or the wire-safety suite — it does not match
    - postresp-log-hook.sh: sh -n clean, executable, exits 0 unconditionally, writes nothing to stdout, truncates raw to 2000 chars
    - Commit `test(11-05): wire-canary MCP echo fixture + tool_response observer hook`
  </acceptance_criteria>
  <done>Both live-leg fixtures exist, syntax-checked, foreign-named, env-fed — zero canaries in fixture source.</done>
</task>

<task type="auto">
  <name>Task 2: wire-safety UAT suite — live canary gate, restore re-entry proof, shape survey</name>
  <files>tests/uat/wire-safety.test.ts</files>
  <read_first>
    - tests/uat/contract-verification.test.ts (the primary analog — preflight fail-LOUD 307-330, writeSettings/hookEntry/fixtureCmd 122-138, MCP leg knobs 345-353 + 597-632 incl. ENABLE_TOOL_SEARCH=false and --allowedTools/--disallowedTools, extractToolResultText 154-163, findTranscript 181-195, extractTranscriptToolData 204-223, readLogEntries 231-243, resume-with-fallback 707-728, findings writer 356-383)
    - tests/uat/harness.ts (runClaude options surface 85-138; assertSessionRan 69-82)
    - tests/uat/live-session.test.ts (buildHookCommand real-hook registration 39, 61-75; sandbox --settings discipline)
    - src/install/settings.ts (buildHookCommand — the fail-closed wrapper shape the HOME prefix goes in front of)
    - src/detect/layer4-words.ts (word|action grammar — the words-file line format; READ ONLY, frozen tree)
    - .planning/phases/11-wire-safety-verification-hardening/11-PATTERNS.md (§wire-safety — every excerpt verified at line level 2026-07-17)
  </read_first>
  <action>
    Create tests/uat/wire-safety.test.ts in the uat project (tests/uat/ glob — no config edit): describe.skipIf(!UAT_ENABLED)('@uat wire safety (REVMODE-11 SC1b)') where UAT_ENABLED = process.env.MRCLEAN_UAT === '1', plus a visible skip placeholder. beforeAll (240s timeout): claude --version preflight that FAILS loud (never skips) on error; fresh `npm run build` unless SKIP_BUILD=1; mkdtemp sandbox containing projectDir and a SEPARATE sandboxMrcleanHome.

    Run-unique canaries minted per run (planner pin — see objective): runToken = randomUUID().slice(0,8); WORD_LIVE = `zz-wire-canary-${runToken}`; SECRET_LIVE = 'AKIA' + exactly 16 chars from the base32 alphabet [A-Z2-7], derived deterministically from runToken (e.g. map each hex char's value into 'ABCDEFGHIJKLMNOPQRSTUVWXYZ234567' and extend/pad from the same alphabet to exactly 16), MUST NOT end in 'EXAMPLE'. The [A-Z2-7] charset is load-bearing dual-engine bait: [A-Z2-7] is a strict subset of [A-Z0-9], so the minted key trips BOTH shipped detectors — secretlint's aws rule ([A-Z0-9] body, enableIDScanRule on) AND the vendored gitleaks rule (AKIA[A-Z2-7]{16}); any 0/1/8/9 digit would silently drop the gitleaks engine from the live proof. Sandbox project config: `<projectDir>/.mrclean/config.toml` with `[reversible]` `enabled = true`, and a words entry `${WORD_LIVE}|warn` in the project words file per the layer4 word|action grammar (|warn so UserPromptSubmit can never block — Pitfall 3).

    Hook registration in sandbox --settings JSON (writeSettings/hookEntry precedent — NEVER ~/.claude/settings.json): PostToolUse command = `HOME="${sandboxMrcleanHome}" ` + buildHookCommand(process.execPath, DIST_CLI) (env prefix on the hook command ONLY — whole-process HOME override kills claude auth, Pitfall 4). Register postresp-log-hook.sh as an ADDITIONAL PostToolUse entry via fixtureCmd(POSTRESP_HOOK, 'E_LOG', logPath) — hooks on one event run in parallel (A5). MCP config JSON: wire-canary stdio server = process.execPath + [echo-mcp-server.mjs] with env CANARY_TEXT = `project notes: path ${WORD_LIVE} key ${SECRET_LIVE}` — canaries env-fed, NEVER in any prompt.

    Run 1: runClaude with a prompt naming the tool ('Call the MCP tool named mcp__wire-canary__echo_project_notes and quote its output verbatim'), cwd projectDir, mcpConfigPath, extraArgs --allowedTools mcp__wire-canary__echo_project_notes --disallowedTools Task, env ENABLE_TOOL_SEARCH=false, maxTurns 6, timeoutMs 220_000. assertSessionRan; assert the wire-canary MCP server status 'connected' (harness integrity).

    NON-VACUITY CHAIN in order, all BEFORE any absence assertion (Pitfall 9 / 10-08 discipline): (1) v2-token probe (duplicate V2_TAIL_PROBE non-global, "duplicated not re-derived" comment) matches the run-1 tool_result via extractToolResultText and/or the transcript — proves reversible substitution actually shipped on the wire; if this probe fails, record a contract-drift verdict (2.1.212 vs the 2.1.209 E1/MCP stamp) via the findings builder BEFORE the test fails — the failure is then honest, not mysterious (record-don't-assert applies to the upstream claim; canary absence stays hard-asserted as an mrclean-owned invariant); (2) sandbox map probe — `${sandboxMrcleanHome}/.mrclean` sessions area contains the run-1 sid's map file (A1 verification that the HOME prefix isolated state); (3) restore leg — spawnSync(process.execPath, [DIST_CLI, 'restore'], env {...process.env, HOME: sandboxMrcleanHome}, input = a doc built from the v2 tokens extracted from the run-1 transcript) asserts WORD_LIVE ON restore stdout with summary restored>=1, and the secret-class placeholder byte-survives unrestored.

    ABSENCE ASSERTIONS (hard, mrclean-owned): WORD_LIVE and SECRET_LIVE absent from run-1 stream-json output, absent from the run-1 transcript (findTranscript + extractTranscriptToolData), and absent from EVERY file matched by a recursive walk of ~/.claude/projects/**/*.jsonl (run-unique mint makes the whole-dir grep safe and strictly stronger).

    Run 2 (re-entry ratchet proof): runClaude '--resume <sid1>' with a follow-up prompt ('summarize the tool result you saw earlier') and the E3 --continue fallback if resume yields no resultEvent; assertSessionRan; re-run BOTH absence greps (stream + whole-dir) — zero hits proves the local restore did not re-enter the wire (restore is store-byte-inert per 10-08).

    SURVEY LEGS (record-don't-assert, re-homed STATE.md todo): three cheap runs with ONLY postresp-log-hook registered (no mrclean hook — shapes don't need mrclean in the loop, A5 fallback): Bash leg ('run: echo survey-ok'), Read leg (plant projectDir/notes.txt, prompt to read it), Grep leg ('search this project for survey-ok'); the MCP shape rides run 1's log. Parse each side file via the readLogEntries shape; hard-assert ONLY harness integrity (each log non-empty); build verdicts {tool: Bash|Read|Grep|mcp__wire-canary__echo_project_notes, typeof, shape summary, cc_version} into tests/uat/artifacts/contract-findings.json via buildFindingsArtifact (guarded: zero-verdict runs refuse overwrite; unparseable artifact fails loud).

    afterAll: write findings; rmSync sandbox recursive-force (removes sandboxMrcleanHome map+key); note in a comment that transcript residue under ~/.claude/projects is the standing UAT behavior and IS the assertion surface.

    Deterministic verification for THIS task (no tokens spent): the suite self-skips cleanly without MRCLEAN_UAT, and typecheck passes. Commit `test(11-05): live wire-safety leg — canary never on the wire, restore-then-resume no re-entry, shape survey`.
  </action>
  <verify>
    <automated>npx vitest run tests/uat/wire-safety.test.ts --project=uat 2>&1 | grep -Eq "skipped|Skipped" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Without MRCLEAN_UAT=1 the suite reports skipped and exits 0; no network/claude invocation occurs (grep: all live work inside the skipIf describe)
    - Canaries are minted run-unique (grep: no 'zz-canary-project-path-7g2' and no 'AKIAIOSFODNN7EXAMPLX' used as LIVE canaries in this file); SECRET_LIVE construction enforces AKIA + exactly 16 chars from [A-Z2-7] (dual-engine: strict subset of secretlint's [A-Z0-9] body, exact match for the vendored gitleaks AKIA[A-Z2-7]{16}) and a non-EXAMPLE tail
    - Prompts contain zero canary values (grep every prompt string literal)
    - Non-vacuity chain (v2 probe -> map exists -> restored>=1) is ordered before all absence greps in the source
    - HOME prefix appears ONLY inside the hook command string and the spawned restore env — runClaude itself receives no HOME override
    - Harness helpers imported from tests/uat/harness.ts; transcript/stream helpers imported or duplicated-by-value per the c-v convention; findings via buildFindingsArtifact
    - `npm run typecheck` clean of new-file errors
  </acceptance_criteria>
  <done>The operator can run MRCLEAN_UAT=1 npm run test:uat -t 'wire safety' at verify-work and get the full SC1b proof chain + survey re-stamps in one run.</done>
</task>

<task type="auto">
  <name>Task 3: HOOK-CONTRACT.md per-tool tool_response shapes section (zero UUIDs)</name>
  <files>docs/HOOK-CONTRACT.md</files>
  <read_first>
    - docs/HOOK-CONTRACT.md (E1 section lines 19-76 — the heading + matrix + evidence + implications template; Re-verification procedure at line 211; existing version stamps 2.1.209)
    - tests/copy-drift.test.ts (UUID-traceability gate lines 188-215 — every session UUID quoted in the doc must exist in contract-findings.json; the new section must quote NONE until the live run re-stamps)
  </read_first>
  <action>
    Append a section '## Per-tool tool_response shapes (PostToolUse input)' to docs/HOOK-CONTRACT.md following the E-section shape: purpose paragraph (does the handler Step 3 JSON.stringify coercion scan structured tool_response shapes adequately per tool — the STATE.md todo Phase 10 re-homed here); '### Per-tool matrix' table with rows Bash, Read, Grep, MCP (mcp__wire-canary__echo_project_notes) and columns: typeof tool_response / structure observed / stringify-coercion adequacy — every cell initially 'pending first MRCLEAN_UAT=1 wire-safety run (CLI 2.1.212)'; '### Evidence' pointing at tests/uat/artifacts/contract-findings.json wire-safety records and the postresp-log-hook.sh mechanism; '### Downstream implications' noting the E1 asymmetry stays the interpretation lens (string updatedToolOutput honored for MCP, rejected for Bash — #68951/#77587 both OPEN as of 2026-07-17). State explicitly that verdicts are recorded, not asserted (Pattern 2 discipline), and that the 2.1.209 E-section stamps remain until the run re-stamps them. Quote ZERO session UUIDs anywhere in the new section. Run the copy-drift suite to prove the UUID gate and every doc gate stay green. Commit `docs(11-05): HOOK-CONTRACT per-tool tool_response shapes section (pending live re-stamp)`.
  </action>
  <verify>
    <automated>npx vitest run tests/copy-drift.test.ts --project=unit && grep -q "Per-tool tool_response shapes" docs/HOOK-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - New section present with the four-row matrix and pending-stamp cells naming CLI 2.1.212
    - Zero new session UUIDs in the section (grep the diff for UUID-shaped strings)
    - tests/copy-drift.test.ts green after the edit
    - Section cross-references the Re-verification procedure and the findings artifact path
  </acceptance_criteria>
  <done>The survey has a durable documentation home whose evidence slots are filled by the operator's verify-work run — with the copy-drift UUID gate keeping doc and artifact consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture MCP output -> Claude Code context -> Anthropic wire | the path the canary must NEVER survive in original form |
| sandbox mrclean-home -> operator's real ~/.mrclean and ~/.claude | live leg must not contaminate real state or auth |
| local restore output -> resumed session | restore is operator-local; the resumed wire must stay token-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Info disclosure | real-looking secrets in public transcript dirs | mitigate | Synthetic run-unique markers only, env-fed to the fixture; never in prompts; SECRET_LIVE is a fake AKIA+[A-Z2-7]{16} shape (dual-engine detectable: secretlint aws rule + vendored gitleaks rule), non-EXAMPLE tail |
| T-11-05-02 | Tampering | operator state contamination (auth, real ~/.mrclean, real settings) | mitigate | --settings + --mcp-config --strict-mcp-config sandbox JSONs; HOME prefix baked into the hook command string ONLY; sandbox map-exists probe verifies A1; afterAll cleanup of sandbox home |
| T-11-05-03 | Tampering (of the proof) | reversible branch silently off -> vacuous absence pass | mitigate | Ordered non-vacuity chain: v2-tail probe, map-exists, restored>=1 — all before any absence grep (Pitfall 9) |
| T-11-05-04 | Repudiation | upstream 2.1.212 drift turning contract observations into mysterious reds | mitigate | Record-don't-assert for upstream behavior (findings verdict written before failure); hard-assert only mrclean-owned invariants (canary absence, harness integrity) |

No package installs this phase (11-RESEARCH Package Legitimacy Audit: none).
</threat_model>

<verification>
- `npx vitest run tests/uat/wire-safety.test.ts --project=uat` self-skips green without MRCLEAN_UAT
- `node --check` + `sh -n` + executable bit on both fixtures
- `npx vitest run tests/copy-drift.test.ts --project=unit` green after the doc edit
- `npm test` green; `npm run typecheck` at 38-error baseline
- Live execution: operator-run at verify-work — `MRCLEAN_UAT=1 npm run test:uat` (documented as the phase gate's manual leg in 11-VALIDATION.md)
</verification>

<success_criteria>
- SC1b harness complete: live canary gate + whole-tree jsonl grep + restore-then-resume re-entry proof + tool_response survey, all composed from shipped UAT machinery
- Zero token spend during execution; one operator opt-in run delivers the live proof and the 2.1.212 re-stamps
</success_criteria>

<output>
Create `.planning/phases/11-wire-safety-verification-hardening/11-05-SUMMARY.md` when done (state explicitly that the live run is deferred to verify-work and list the exact operator command).
</output>
