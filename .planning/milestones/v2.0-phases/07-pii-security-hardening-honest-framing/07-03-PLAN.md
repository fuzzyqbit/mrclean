---
phase: 07-pii-security-hardening-honest-framing
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/shared/strings.ts
  - src/mcp/tools/check.ts
  - src/mcp/tools/redact.ts
  - src/doctor/report.ts
  - src/hook/handlers/session-start.ts
  - tests/hook/handlers.test.ts
  - README.md
  - docs/SCOPE-FENCE.md
  - tests/copy-drift.test.ts
  - .github/workflows/canary-leak.yml
autonomous: false
requirements: [PIISEC-01, PIISEC-02]
user_setup: []
must_haves:
  truths:
    - "A single best-effort PII disclaimer string is centralized in src/shared/strings.ts and reused across all surfaces"
    - "The disclaimer surfaces ONCE per output on the README PII section, the mrclean doctor / CLI report (renderReport trailing line — ALWAYS printed), the SessionStart banner additionalContext, and the MCP tool descriptions"
    - "The doctor/CLI disclaimer is GUARANTEED-present (rendered by renderReport on every doctor run) — it does NOT ride a conditional check detail that SKIPs when no NER model is cached (SC-3 'consistent across CLI output and mrclean doctor')"
    - "User-facing copy contains no compliance/guarantee CLAIM language (redacts all PII / GDPR/HIPAA compliant / guarantees all|every) — but the allowed negation 'not a guarantee' is permitted"
    - "A vitest copy-drift test fails the build if a banned claim phrase appears in any scanned string source, and is non-vacuous (a positive control fires)"
    - "The PII leak-grep regression test (07-01) has a CI job plus a belt-and-suspenders grep pass over .mrclean/audit*.jsonl"
    - "README PII framing is consistent with docs/SCOPE-FENCE.md and never contradicts the locked scope fence"
    - "MCP check/redact isError text routes through sanitizeForOutput (D-03) so tool-error output cannot echo raw PII"
  artifacts:
    - path: "src/shared/strings.ts"
      provides: "Centralized best-effort disclaimer text + banned-phrase list (single source of truth)"
      contains: "export const"
    - path: "src/doctor/report.ts"
      provides: "renderReport trailing disclaimer line — the guaranteed doctor/CLI SC-3 surface (always printed)"
      contains: "PII_BEST_EFFORT_DISCLAIMER"
    - path: "README.md"
      provides: "New PII/NER framing section (best-effort recall aid, not a guarantee — D-07)"
      contains: "best-effort"
    - path: "tests/copy-drift.test.ts"
      provides: "Banned-phrase scan + disclaimer-presence assertions (D-05/D-08)"
    - path: ".github/workflows/canary-leak.yml"
      provides: "PII leak-test CI job + belt-and-suspenders PII grep (PIISEC-01 CI gate)"
      contains: "pii-canary-leak"
  key_links:
    - from: "README.md / src/doctor/report.ts / src/hook/handlers/session-start.ts / src/mcp/tools/*.ts"
      to: "src/shared/strings.ts"
      via: "all surfaces import/reuse the one disclaimer constant (no copy drift)"
      pattern: "PII_BEST_EFFORT|strings"
    - from: "src/hook/handlers/session-start.ts"
      to: "src/shared/strings.ts"
      via: "appends PII_BEST_EFFORT_DISCLAIMER to the returned additionalContext string (banner + '\\n' + disclaimer) — NOT a stderr write; the handler returns additionalContext"
      pattern: "additionalContext.*PII_BEST_EFFORT|PII_BEST_EFFORT.*additionalContext"
    - from: "src/doctor/report.ts renderReport"
      to: "src/shared/strings.ts"
      via: "renderReport always writes the disclaimer as a trailing stdout line after the version line (guaranteed doctor/CLI surface)"
      pattern: "PII_BEST_EFFORT_DISCLAIMER"
    - from: "tests/copy-drift.test.ts"
      to: "README.md + src/shared/strings.ts + MCP tool descriptions + banner + doctor report"
      via: "regex scan of user-facing string sources for banned claim phrases + README disclaimer-presence assertion"
      pattern: "BANNED|redacts? all PII"
    - from: ".github/workflows/canary-leak.yml"
      to: "tests/audit/pii-canary-leak.test.ts (from 07-01)"
      via: "integration vitest job + grep -F over audit*.jsonl"
      pattern: "pii-canary-leak"
    - from: "src/mcp/tools/check.ts / redact.ts isError returns"
      to: "src/shared/sanitize-output.ts (from 07-01)"
      via: "tool-error text routed through sanitizeForOutput (D-03 / PATTERNS.md:228-229)"
      pattern: "sanitizeForOutput"
---

<objective>
Frame the PII/NER layer honestly everywhere it surfaces, lock that framing against future copy-drift, wire the PIISEC-01 leak test into CI, and close the D-03 MCP isError chokepoint gap. Delivers the D-05/D-07/D-08 half of PIISEC-02 plus the CI gate + isError routing for PIISEC-01: a single centralized best-effort disclaimer (src/shared/strings.ts) surfaced once-per-output on README + the `mrclean doctor` / CLI report (renderReport trailing line, ALWAYS printed) + the SessionStart banner additionalContext + the MCP tool descriptions, a vitest copy-drift test (D-08) that fails the build on compliance/guarantee CLAIM language, a canary-leak.yml PII job that runs 07-01's leak test plus a belt-and-suspenders grep, and routing of the MCP check/redact isError text through 07-01's sanitizeForOutput (D-03 / PATTERNS.md:228-229).

PIISEC-02 is split by design (see ROADMAP §Phase 7): 07-02 owns the D-06 machine-readable bestEffort flag; THIS plan (07-03) owns the human-facing framing (D-05/D-07) and its enforcement (D-08), the MCP isError chokepoint routing (D-03), and the PIISEC-01 CI gate. Together 07-01 + 07-02 + 07-03 fully cover PIISEC-01 + PIISEC-02 — every locked decision (D-01..D-08) has a covering task.

Purpose: Secrets are the deterministic guarantee; PII/NER is a best-effort recall aid whose false negatives CAN leak. Copy must never blur the two into "mrclean redacts your PII"; the disclaimer points users to `words.txt` + deterministic layers as the real must-not-leak lever (D-07). The CI job makes the leak guarantee permanent; the isError routing makes tool-error leakage structurally impossible. SC-3 requires the framing be consistent across CLI output, `mrclean doctor`, and docs — so the doctor/CLI surface is the renderReport trailing line (ALWAYS rendered on every doctor run), not a conditional check detail that disappears when no NER model is cached.
Output: src/shared/strings.ts; README PII section; renderReport (doctor/CLI) + SessionStart additionalContext + MCP-description disclaimer surfacing; MCP isError routing through sanitizeForOutput; tests/copy-drift.test.ts; canary-leak.yml PII job. Copy wording is staged for human review (D-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-CONTEXT.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-PATTERNS.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-01-SUMMARY.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase, use directly. -->

From src/shared/version.ts (the const-export module shape to mirror for the NEW strings.ts):
```typescript
export const VERSION: string = pkg.version   // single-purpose const-export module in src/shared/
```

From src/hook/banner.ts (KEEP PURE — do NOT append the disclaimer here; strict tests assert exact banner text):
```typescript
export function buildBanner(config, ruleCount, allowlistCount): string
// returns EXACTLY: `mrclean active vN.N.N (rules: N, allowlist: N, mode: active|dry-run)`
```

From src/hook/handlers/session-start.ts (THE BANNER IS RETURNED VIA additionalContext — there is NO stderr write in this handler; do NOT invent one):
```typescript
// line 46: const banner = buildBanner(config, ruleCount, allowlistCount)
// lines 49-54: return { hookSpecificOutput: { hookEventName: 'SessionStart', additionalContext: banner } }
//
// CORRECT MECHANISM (checker-directed): append the centralized disclaimer to the additionalContext STRING:
//   additionalContext: banner + '\n' + PII_BEST_EFFORT_DISCLAIMER
// This is NOT a stderr write. The handler emits the banner to the user/model via additionalContext, so the
// disclaimer MUST ride additionalContext to actually reach the surface. buildBanner stays pure (do NOT suffix it).
```

From tests/hook/handlers.test.ts (THE ANCHORED BANNER TEST — it asserts on the WHOLE additionalContext, so appending a disclaimer line BREAKS it unless Test 7 is updated):
```typescript
// line 67: const BANNER_PATTERN = /^mrclean active v\d+\.\d+\.\d+[^ ]* \(rules: \d+, allowlist: \d+, mode: (active|dry-run)\)$/
// line 116 (Test 7, SessionStart): additionalContext: expect.stringMatching(BANNER_PATTERN)
//   → stringMatching runs RegExp.test on the WHOLE string; with an anchored ^...$ (no /m flag) the $ fails when
//     a disclaimer line follows. Appending '\n' + disclaimer to additionalContext WILL fail this. UPDATE Test 7 (Task 1).
// line 133 (Test 8, UserPromptSubmit): additionalContext.toMatch(BANNER_PATTERN)
//   → UserPromptSubmit's additionalContext is NOT modified by this plan (disclaimer is once-per-SessionStart),
//     so Test 8 stays unchanged and keeps passing. Only the SessionStart handler appends the disclaimer.
```

From src/doctor/canary.ts (the doctor canary only does startsWith on additionalContext — a trailing disclaimer line is SAFE):
```typescript
// canary.ts:87 — if (!ctx.startsWith('mrclean active v')) { ... fail }
//   → startsWith tolerates a trailing '\n' + disclaimer on additionalContext (the banner is still the prefix).
```

From src/doctor/report.ts (renderReport — the GUARANTEED doctor/CLI surface; it ALWAYS runs and ends with the version line. THIS is the SC-3 CLI/doctor surface, not checkModelCache):
```typescript
export function renderReport(results: CheckResult[], versionResult: ClaudeVersionResult): void
// loops check results → writes `[STATUS] name — detail` lines, then writes the trailing version line (always).
// runDoctor (src/doctor/index.ts) calls renderReport; `mrclean doctor` (cli.ts:116-117) calls runDoctor.
// ADD a final stdout.write of PII_BEST_EFFORT_DISCLAIMER after the version line (optionally pc.dim) — printed on
//   EVERY doctor run, regardless of whether a NER model is cached. PATTERNS.md:286-289 names this the D-05 surface.
```

From src/doctor/checks.ts (checkModelCache SKIPs when no model is cached — DO NOT route the SC-3 disclaimer here; it would disappear for non-NER users):
```typescript
// checkModelCache returns status 'SKIP' (detail: 'NER model not downloaded...') when no model is present.
// Routing the always-on disclaimer through this detail would make it vanish in the common (no-model) case.
// checks.ts is therefore NO LONGER a disclaimer site in this plan — leave checkModelCache untouched.
```

From src/mcp/tools/check.ts / redact.ts (description string is the once-per-output MCP surface; the isError text is the D-03 routing site):
```typescript
// check.ts:99-102 description: 'Scan text ... return findings. ...'  → append the centralized best-effort clause ONCE
// redact.ts:93-97 description: '...'                                  → same (mirror)
// check.ts:124-128 isError text: `mrclean_check error: ${outcome.error}`   → route through sanitizeForOutput (D-03)
// redact.ts:118-123 + :127-137 isError text                                → route through sanitizeForOutput (mirror)
```

From src/shared/sanitize-output.ts (created in 07-01 — the chokepoint to route MCP isError text through):
```typescript
export function sanitizeForOutput(message: string, detectedSpans: readonly { value: string }[], fallback?: string): string
// outcome.error is already scrubbed at the supervisor catch in 07-01; routing the surfaced tool text is belt-and-suspenders
// (D-03 / PATTERNS.md:228-229). Context-free at the tool boundary → pass [] spans.
```

From src/audit/canary-leak.ts (the line-by-line scan + offender-accumulation shape to INVERT for the banned-phrase test):
```typescript
export async function assertNoCanaryLeak(logPath: string, canaries: string[]): Promise<{ ok: boolean; leaked: ... }>
```

From .github/workflows/canary-leak.yml (the double-pass gate pattern to extend — see read_first):
```yaml
# existing job: npx vitest run --project=integration tests/fixtures-corpus.test.ts ...
# + belt-and-suspenders: for f in tests/fixtures/positive/*.txt; grep -F "$val" .mrclean/audit*.jsonl
```
</interfaces>

<read_first>
@src/shared/version.ts            # const-export module shape to mirror for the new strings.ts
@src/hook/banner.ts               # buildBanner — MUST stay pure; do NOT suffix (BANNER_PATTERN would break)
@src/hook/handlers/session-start.ts  # lines 46-54 — banner is RETURNED via additionalContext (NO stderr); append disclaimer to that string
@tests/hook/handlers.test.ts      # line 67 BANNER_PATTERN (anchored) + lines 108-124 Test 7 — UPDATE the SessionStart assertion to tolerate the trailing disclaimer line
@src/doctor/report.ts             # renderReport — add the trailing always-printed disclaimer line (SC-3 doctor/CLI surface; PATTERNS.md:286-289)
@src/doctor/checks.ts             # checkModelCache SKIPs when no model — do NOT route the disclaimer here (it would vanish)
@src/doctor/canary.ts             # canary.ts:87 startsWith — confirms the trailing additionalContext disclaimer is safe
@src/mcp/tools/check.ts           # description (99-102) + isError text (124-128) — disclaimer site + D-03 routing site (post-07-02)
@src/mcp/tools/redact.ts          # description (93-97) + isError text (118-123, 127-137) — mirror sites
@src/shared/sanitize-output.ts    # 07-01 chokepoint — route the isError text through this
@README.md                        # §8 MCP tools + §10 "What this does NOT defend against" (242-259) tone analog; no PII section yet
@docs/SCOPE-FENCE.md              # framing voice; new README section MUST NOT contradict the locked scope fence
@src/audit/canary-leak.ts         # scan-and-report engine to invert for the banned-phrase test
@tests/audit/canary-leak.test.ts  # AAA + tmpdir isolation structure for the new copy-drift test
@.github/workflows/canary-leak.yml  # double-pass gate to extend with the PII leak job (07-01's test)
  </read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize disclaimer + banned-phrase list; surface disclaimer once-per-output on all runtime surfaces (MCP descriptions, doctor/CLI report, SessionStart additionalContext)</name>
  <files>src/shared/strings.ts, src/mcp/tools/check.ts, src/mcp/tools/redact.ts, src/doctor/report.ts, src/hook/handlers/session-start.ts, tests/hook/handlers.test.ts</files>
  <read_first>
@src/shared/version.ts
@src/hook/banner.ts
@src/hook/handlers/session-start.ts
@tests/hook/handlers.test.ts
@src/doctor/report.ts
@src/doctor/canary.ts
@src/mcp/tools/check.ts
@src/mcp/tools/redact.ts
@docs/SCOPE-FENCE.md
  </read_first>
  <action>
    Create src/shared/strings.ts mirroring the single-const-export shape of src/shared/version.ts. Export TWO constants: (1) `PII_BEST_EFFORT_DISCLAIMER` — one short sentence per the D-07 stance: "best-effort ML PII hint, not a guarantee; NER false negatives can leak; use words.txt + the deterministic layers (secrets + checksum'd PII) for must-not-leak data." (draft per stance; exact wording reviewed in Task 5). (2) `BANNED_COPY_PHRASES` — the regex list consumed by the copy-drift test (Task 3). Keep the file < 200 lines, no I/O, no engine imports.
    Surface the disclaimer ONCE per output (D-05) on each runtime surface by importing `PII_BEST_EFFORT_DISCLAIMER`:
    - MCP: append the disclaimer clause to the `description` string in src/mcp/tools/check.ts (99-102) and src/mcp/tools/redact.ts (93-97) — once per tool description, NOT per finding. (07-02 already added the per-finding bestEffort boolean; this is the human-readable once-per-output note.)
    - doctor / CLI: in src/doctor/report.ts `renderReport`, AFTER the trailing version line, add a final `process.stdout.write(...)` of `PII_BEST_EFFORT_DISCLAIMER` (one line, optionally `pc.dim(...)` for de-emphasis like the SKIP lines). This is the GUARANTEED SC-3 doctor/CLI surface — renderReport runs on every `mrclean doctor` invocation (cli.ts:116-117 → runDoctor → renderReport), satisfying SC-3's "consistent across CLI output and mrclean doctor." DO NOT route the disclaimer through checkModelCache's `detail` (src/doctor/checks.ts): that check returns status 'SKIP' when no NER model is cached, so the note would VANISH for the common no-model user. Leave checkModelCache untouched. (PATTERNS.md:286-289 names report.ts as the D-05 surface; checks.ts is marked optional and is NOT used here.)
    - banner / SessionStart: in src/hook/handlers/session-start.ts (lines 46-54), the banner is RETURNED via `additionalContext`, NOT written to stderr — there is no stderr write in this handler, so do NOT invent one (a stderr line would not reach the additionalContext surface the banner uses). Append the disclaimer to the returned string: `additionalContext: banner + '\n' + PII_BEST_EFFORT_DISCLAIMER` (once per SessionStart). DO NOT modify src/hook/banner.ts buildBanner — it must stay pure. The doctor canary (canary.ts:87) only checks `additionalContext.startsWith('mrclean active v')`, so the trailing disclaimer line is safe there. BUT tests/hook/handlers.test.ts Test 7 (line 116) asserts the WHOLE additionalContext via `expect.stringMatching(BANNER_PATTERN)` with an anchored `^...$` regex — appending the disclaimer BREAKS that assertion. UPDATE Test 7 (lines 108-124): change the SessionStart additionalContext check from the anchored whole-string match to (a) assert the FIRST line matches BANNER_PATTERN (e.g. `additionalContext.split('\n')[0]` matches BANNER_PATTERN) OR assert `additionalContext.startsWith('mrclean active v')` plus the existing `.toContain('rules:')` / `.toContain('allowlist:')` checks, AND (b) add a new assertion that the disclaimer is present (`additionalContext.toContain` a key phrase from PII_BEST_EFFORT_DISCLAIMER such as 'not a guarantee'). Do NOT touch Test 8 (UserPromptSubmit, line 133): this plan does NOT append the disclaimer to UserPromptSubmit's additionalContext (once-per-SessionStart), so its BANNER_PATTERN assertion stays valid.
    All disclaimer copy must avoid banned CLAIM phrases (Task 3 enforces) — the negation "not a guarantee" is allowed by design.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp tests/hook tests/doctor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PII_BEST_EFFORT_DISCLAIMER\|BANNED_COPY_PHRASES" src/shared/strings.ts` shows both exports.
    - `grep -rln "PII_BEST_EFFORT_DISCLAIMER" src/mcp/tools/check.ts src/mcp/tools/redact.ts src/doctor/report.ts src/hook/handlers/session-start.ts` lists all four consumers (single source of truth, no inline copies).
    - `grep -n "additionalContext: banner" src/hook/handlers/session-start.ts` shows the disclaimer appended to the returned additionalContext (NOT a new stderr write); `grep -c "process.stderr.write" src/hook/handlers/session-start.ts` is 0 (no invented stderr write).
    - `grep -n "PII_BEST_EFFORT_DISCLAIMER" src/doctor/report.ts` shows the trailing renderReport line; `git diff --stat src/doctor/checks.ts` shows NO change to checks.ts (checkModelCache untouched; disclaimer is not on the conditional SKIP detail).
    - `git diff --stat src/hook/banner.ts` shows NO change to banner.ts (buildBanner stays pure; BANNER_PATTERN regex intact for Test 8).
    - `npx vitest run tests/mcp tests/hook tests/doctor` is green: Test 7 updated to tolerate the trailing disclaimer line AND assert the disclaimer is present; Test 8 (UserPromptSubmit) still passes unchanged.
  </acceptance_criteria>
  <done>One centralized disclaimer constant reused across MCP descriptions, the renderReport trailing line (guaranteed doctor/CLI surface), and the SessionStart additionalContext string; banner.ts + checks.ts unchanged; Test 7 updated, Test 8 intact; existing tests green.</done>
</task>

<task type="auto">
  <name>Task 2: README PII/NER framing section + SCOPE-FENCE consistency</name>
  <files>README.md, docs/SCOPE-FENCE.md</files>
  <read_first>
@README.md
@docs/SCOPE-FENCE.md
@.planning/phases/07-pii-security-hardening-honest-framing/07-CONTEXT.md
@src/shared/strings.ts
  </read_first>
  <action>
    Add a NEW PII/NER framing section to README.md (greenfield — §1-§11 currently have no PII heading). Place it near §8 "MCP tools (in-session, opt-in)" (line 200, which exposes NER) or alongside §10 "What this does NOT defend against"; match the candid, bulleted limitations tone of §10 (242-259) and the framing voice of docs/SCOPE-FENCE.md. Stance is LOCKED per D-07: "best-effort ML PII hint, not a guarantee"; NER false negatives CAN leak; `words.txt` + deterministic layers (secrets + checksum'd PII like SSN/credit-card) are the real must-not-leak mechanism; secrets remain the deterministic guarantee. Keep the prose consistent with the centralized PII_BEST_EFFORT_DISCLAIMER (same stance, README may expand it).
    Review docs/SCOPE-FENCE.md for the secrets=deterministic-guarantee vs PII/NER=best-effort framing; align the README section so it does not contradict the locked scope fence (Banned Categories, In-Scope Allowlist). Only edit SCOPE-FENCE.md if a contradiction or a missing cross-reference is found (e.g. add a one-line pointer to the new README section); do not weaken any ban.
    The section MUST NOT contain banned CLAIM phrases ("redacts all PII", "GDPR/HIPAA compliant", "guarantees all/every") — Task 3's test will fail the build otherwise. The negation "not a guarantee" is acceptable.
  </action>
  <verify>
    <automated>npx vitest run tests/copy-drift.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - README.md has a new heading containing "PII": `grep -nE "^#+ .*PII" README.md` matches.
    - The section names `words.txt` + deterministic layers as the real must-not-leak lever AND states NER false negatives can leak: `grep -niE "words.txt" README.md` and `grep -niE "false negative|can leak" README.md` both match.
    - No banned overclaim phrases: `grep -niE "redacts? all pii|gdpr|hipaa" README.md` returns nothing; any "compliant" hit is not a self-claim.
    - AUTOMATION-GATED (not grep-gated): tests/copy-drift.test.ts (Task 3) scans README.md for banned claim phrases AND asserts the disclaimer key phrase is present in the README PII section. `npx vitest run tests/copy-drift.test.ts` is the gate that fails the build if README copy drifts toward overclaiming or drops the disclaimer — README content is covered by an automated test, not only the manual grep above.
    - docs/SCOPE-FENCE.md is unchanged OR changed only to add a cross-reference / fix a contradiction (no ban weakened): `git diff docs/SCOPE-FENCE.md` shows no removal of a Ban section.
  </acceptance_criteria>
  <done>README has an honest PII/NER section matching D-07 stance, consistent with SCOPE-FENCE.md, free of overclaim phrases, and gated by the copy-drift test (Task 3) for banned phrases + disclaimer presence.</done>
</task>

<task type="auto">
  <name>Task 3: Copy-drift (banned-phrase) vitest test + canary-leak.yml PII job</name>
  <files>tests/copy-drift.test.ts, .github/workflows/canary-leak.yml</files>
  <read_first>
@src/audit/canary-leak.ts
@tests/audit/canary-leak.test.ts
@.github/workflows/canary-leak.yml
@src/shared/strings.ts
@README.md
@src/doctor/report.ts
  </read_first>
  <action>
    Create tests/copy-drift.test.ts. Import `BANNED_COPY_PHRASES` and `PII_BEST_EFFORT_DISCLAIMER` from src/shared/strings.ts (single source of truth). Reuse the line-by-line scan + offender-accumulation shape from src/audit/canary-leak.ts (inverted intent: forbidden phrases). Scan these user-facing SOURCES with `readFileSync` (D-08): README.md, src/shared/strings.ts, src/hook/handlers/session-start.ts, src/doctor/report.ts, src/mcp/tools/check.ts, src/mcp/tools/redact.ts.
    Banned CLAIM-form regexes (CRITICAL — Pitfall 5: ban the CLAIM, never the bare word; the disclaimer itself says "not a guarantee"): `/redacts? all PII/i`, `/\b(GDPR|HIPAA|CCPA)\b[^.]*compliant/i`, `/\bfully compliant\b/i`, `/\bguarantees? (that )?(all|every) /i`. Do NOT ban a bare `/guarantee/`. Grep-hygiene: strip comment-only lines before scanning source files (skip lines matching `^\s*[/*]`) so JSDoc prose mentioning "guarantee" cannot self-trip the gate; README is prose so scan it whole.
    Add a non-vacuous positive control: assert the scanner flags a synthetic input string containing "redacts all PII" (offenders length >= 1) — proves the detector works.
    Add a disclaimer-presence assertion (D-05): assert the `PII_BEST_EFFORT_DISCLAIMER` key phrase (e.g. "not a guarantee") appears in README.md (the README PII section — this gates Task 2's content automatically), and that the disclaimer string itself passes the banned-phrase scan (Pitfall 5 self-check).
    Place the test under tests/ so the default unit project picks it up (runs in `npm test`).
    Extend .github/workflows/canary-leak.yml: add a step running 07-01's PII leak test — `npx vitest run --project=integration tests/audit/pii-canary-leak.test.ts` — and a belt-and-suspenders second pass mirroring the existing `grep -F` loop (48-62): for each synthetic PII canary value (the 07-01 PII_CANARIES set), `grep -F` against `.mrclean/audit*.jsonl` and `exit 1` with `::error::` on any hit. Copy the `set -e` + per-value loop discipline.
  </action>
  <verify>
    <automated>npx vitest run tests/copy-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/copy-drift.test.ts` passes against current real sources.
    - Non-vacuous: a synthetic "redacts all PII" input is detected (the positive-control assertion is present and passes).
    - Banned regexes use claim-form and allow the negation: `grep -n "all\|every\|compliant\|redacts" tests/copy-drift.test.ts` matches AND the test asserts PII_BEST_EFFORT_DISCLAIMER (containing "not a guarantee") is NOT flagged.
    - Scanned-sources list includes README.md + both MCP tool files + session-start + doctor report + strings.ts: `grep -nE "README|session-start|report|check.ts|redact.ts|strings" tests/copy-drift.test.ts` matches.
    - Disclaimer-presence assertion covers README: the test asserts the disclaimer key phrase appears in README.md (gates Task 2's content automatically).
    - canary-leak.yml runs the PII leak test and a PII grep pass: `grep -n "pii-canary-leak" .github/workflows/canary-leak.yml` matches and a `grep -F`/`::error::` PII block is present.
  </acceptance_criteria>
  <done>copy-drift test green over real copy, non-vacuous, comment-safe, allows "not a guarantee", and asserts the disclaimer is present in README (gating Task 2); canary-leak.yml gains a PII leak-test job + belt-and-suspenders grep.</done>
</task>

<task type="auto">
  <name>Task 4: Route MCP check/redact isError text through sanitizeForOutput (D-03 / PATTERNS.md:228-229)</name>
  <files>src/mcp/tools/check.ts, src/mcp/tools/redact.ts</files>
  <read_first>
@src/mcp/tools/check.ts
@src/mcp/tools/redact.ts
@src/shared/sanitize-output.ts
  </read_first>
  <action>
    In src/mcp/tools/check.ts:124-128, route the isError text `mrclean_check error: ${outcome.error}` through sanitizeForOutput (context-free → pass `[]` spans; the tool boundary has no detection spans). This is belt-and-suspenders on top of 07-01's supervisor-level routing (outcome.error is already scrubbed at the supervisor catch) — D-03 / PATTERNS.md:228-229 require the surfaced tool text to pass through the chokepoint too. Apply the IDENTICAL pattern to src/mcp/tools/redact.ts:118-123 AND the budget-exhausted isError return at :127-137. BOTH redact isError returns MUST be handled. For each, EITHER route the surfaced text through sanitizeForOutput (preferred — uniform with check.ts), OR — only if the return is a fixed string literal — leave it unrouted AND record in the SUMMARY the EXACT literal quoted verbatim, proving it contains no `${outcome.error}` / `${...}` interpolation and therefore carries no raw input. A claim that "it is safe" without the quoted literal is not acceptable; the acceptance below is checkable, not optional. Import sanitizeForOutput from '../../shared/sanitize-output.js'. Do NOT alter the success path, the structuredContent shape, the bestEffort flag (07-02), or the description disclaimer (Task 1) — this is isError-text-only. Keep check.ts and redact.ts as mirrors.
  </action>
  <verify>
    <automated>npx vitest run tests/mcp 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sanitizeForOutput" src/mcp/tools/check.ts src/mcp/tools/redact.ts` shows the call routing the isError text in BOTH files.
    - ALL isError returns are accounted for: check.ts:124-128 routed; redact.ts:118-123 routed; redact.ts:127-137 (budget-exhausted) EITHER routed through sanitizeForOutput OR proven literal — `grep -nE "isError|budget" src/mcp/tools/redact.ts` shows every isError-text site either calls sanitizeForOutput or is a fixed string with no `${` interpolation.
    - If the budget-exhausted return is left unrouted, the 07-03 SUMMARY quotes the EXACT literal verbatim and shows it contains no `${outcome.error}` / `${...}` substitution (checkable proof, not a "it's safe" assertion). Otherwise it is routed.
    - Existing MCP tests pass: `npx vitest run tests/mcp` is green (no success-path, bestEffort, or description regression).
    - `npm run typecheck` clean; the structuredContent / findingSchema (incl. bestEffort from 07-02) is unchanged.
    - check.ts and redact.ts remain mirrors for the isError routing.
  </acceptance_criteria>
  <done>Both MCP tools' isError text routes through sanitizeForOutput (D-03) — including BOTH redact returns (118-123 and the budget-exhausted 127-137), the latter either routed or proven a fixed non-interpolated literal quoted in the SUMMARY; success path + bestEffort + description untouched; tools stay mirrors; MCP tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human review of PII framing copy wording (D-07 sign-off)</name>
  <files>src/shared/strings.ts, README.md, src/doctor/report.ts, src/hook/handlers/session-start.ts, src/mcp/tools/check.ts, src/mcp/tools/redact.ts</files>
  <action>Pause for human sign-off on the user-facing PII framing WORDING produced by Tasks 1-2. No code changes in this task — the executor presents the copy and waits. The D-07 stance is locked (best-effort ML hint, not a guarantee; false negatives can leak; words.txt + deterministic layers are the real lever); exact phrasing is Claude's discretion + user review per VALIDATION.md Manual-Only. If the user requests wording changes, edit ONLY the PII_BEST_EFFORT_DISCLAIMER const in src/shared/strings.ts (single edit point — all surfaces re-derive) and the README prose, then re-run Task 3's copy-drift test before resuming.</action>
  <verify><human-check>User confirms the disclaimer wording reads honestly and consistently across README, the doctor/CLI report, the SessionStart banner additionalContext, and the MCP descriptions, and replies "approved".</human-check></verify>
  <done>User has reviewed the framing copy on all four surfaces and typed "approved" (or requested changes which were applied via the strings.ts const + README and re-verified by the copy-drift test).</done>
  <what-built>The user-facing PII framing copy: the centralized PII_BEST_EFFORT_DISCLAIMER (src/shared/strings.ts), the README PII/NER section, the `mrclean doctor` / CLI report trailing line (renderReport), the SessionStart banner disclaimer line (appended to additionalContext), and the MCP tool description clauses. All copy passes the automated banned-phrase gate; this checkpoint is the human sign-off on the WORDING (D-07 stance is locked, exact phrasing is Claude's discretion + user review per VALIDATION.md Manual-Only).</what-built>
  <how-to-verify>
    1. Read the new README PII/NER section: `grep -nA 20 -E "^#+ .*PII" README.md` — confirm it frames NER as "best-effort ML PII hint, not a guarantee," states NER false negatives can leak, and points to words.txt + deterministic layers as the real must-not-leak mechanism. No drift toward "redacts all PII" or compliance claims.
    2. Run `npm run build && node dist/cli.js doctor` (or the project's doctor entry) and confirm the once-per-output best-effort note appears as a trailing line in the report — and that it appears EVEN WITH NO NER model cached (it rides renderReport, not the SKIPing model-cache check).
    3. Inspect the SessionStart additionalContext disclaimer line and the MCP tool description clause: `grep -n "PII_BEST_EFFORT_DISCLAIMER" src/shared/strings.ts` then read the constant — confirm the wording reads honestly and consistently across all four surfaces.
    4. Confirm the asymmetry is clear: secrets = deterministic guarantee; PII/NER = best-effort recall aid.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the wording, or describe specific wording changes (the disclaimer constant is the single edit point — changing it updates all surfaces).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NER (probabilistic) → operator/model expectation | Best-effort recall must never be represented as a guarantee; false negatives can leak (honest-framing misrepresentation boundary) |
| user-facing copy (README/CLI/doctor/MCP desc) → operator trust | Overclaim/compliance language would misrepresent the security posture and induce unsafe behavior |
| detection internals → .mrclean/audit.jsonl (CI gate) | The PIISEC-01 leak guarantee must be enforced on every push/PR, not only locally |
| MCP tool isError text → model/operator context | Tool-error strings cross to the caller and re-enter the model context — must not echo raw PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Repudiation/Misrepresentation (honest-framing) | user-facing copy (README/banner/doctor/MCP desc) | mitigate | Centralized disclaimer (D-07 stance) on all surfaces + banned-phrase copy-drift test (Task 3) failing the build on compliance/guarantee CLAIM drift |
| T-07-03-02 | Tampering | future copy-drift toward overclaiming | mitigate | copy-drift vitest test wired into npm test + scans the single-source strings.ts (D-08); non-vacuous positive control proves the gate works |
| T-07-03-03 | Repudiation | README framing contradicting docs/SCOPE-FENCE.md | mitigate | README authored against SCOPE-FENCE voice; acceptance check confirms no ban weakened, no contradiction |
| T-07-03-04 | Information Disclosure | .mrclean/audit*.jsonl leaking raw PII (regression over time) | mitigate | canary-leak.yml PII job runs 07-01's leak test + belt-and-suspenders `grep -F` per PII canary on every push/PR |
| T-07-03-05 | Tampering | banned-phrase gate self-tripping on the honest negation | mitigate | Pitfall 5: claim-form regexes only; test asserts the disclaimer ("not a guarantee") is NOT flagged; comment lines stripped before scanning |
| T-07-03-06 | Information Disclosure | MCP check/redact isError text echoing raw PII | mitigate | Task 4 routes the surfaced tool-error text through sanitizeForOutput (D-03) — belt-and-suspenders over 07-01 supervisor routing |
| T-07-03-07 | Spoofing (false assurance) | SC-3 doctor disclaimer silently disappearing when no NER model is cached | mitigate | Disclaimer rides renderReport's trailing line (always printed), NOT checkModelCache's SKIP detail; Task 5 step 2 verifies it appears with no model cached |
| T-07-03-SC | Tampering | npm installs | accept | No new runtime or dev dependency added (uses installed vitest + Node built-ins + 07-01 sanitize-output + GNU grep on the runner) — slopcheck not applicable |
</threat_model>

<verification>
- `npx vitest run tests/copy-drift.test.ts` green (no overclaim copy; non-vacuous; allows "not a guarantee"; README disclaimer-presence asserted)
- `npx vitest run tests/mcp tests/hook tests/doctor` green (disclaimer surfaced on MCP desc + renderReport + SessionStart additionalContext; banner.ts + checks.ts unchanged; Test 7 updated; Test 8 intact; isError routing no regression)
- `grep -rln "PII_BEST_EFFORT_DISCLAIMER" src/` lists strings.ts + all four consumers (single source of truth)
- `grep -n "PII_BEST_EFFORT_DISCLAIMER" src/doctor/report.ts` shows the guaranteed doctor/CLI surface (trailing renderReport line)
- `grep -n "sanitizeForOutput" src/mcp/tools/check.ts src/mcp/tools/redact.ts` shows isError routing in both
- `grep -nE "^#+ .*PII" README.md` shows the new framing section
- `grep -n "pii-canary-leak" .github/workflows/canary-leak.yml` shows the PII CI job
- Human checkpoint: copy wording approved
</verification>

<success_criteria>
The D-05/D-07/D-08 half of PIISEC-02, the PIISEC-01 CI gate, and the D-03 MCP isError routing are satisfied: a single centralized best-effort disclaimer surfaces once-per-output on README, the `mrclean doctor` / CLI report (renderReport trailing line, ALWAYS printed — satisfying SC-3's "consistent across CLI output and mrclean doctor"), the SessionStart banner additionalContext, and the MCP tool descriptions; a copy-drift vitest test permanently fails the build if compliance/guarantee CLAIM language enters user-facing copy (while allowing the honest negation) and asserts the disclaimer is present in README; the README framing is consistent with the locked scope fence; the PIISEC-01 leak test runs in CI with a belt-and-suspenders grep; and the MCP check/redact isError text routes through sanitizeForOutput. Framing is consistent across all surfaces and never drifts toward "redacts all PII." Copy wording is human-approved. Together with 07-02 (D-06 flag) and 07-01 (chokepoint + leak proof), PIISEC-01 + PIISEC-02 are fully covered.
</success_criteria>

<output>
Create `.planning/phases/07-pii-security-hardening-honest-framing/07-03-SUMMARY.md` when done.
</output>
</content>
