---
phase: 06-ner-inference-l6b-mcp-wiring
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/mcp/server.ts
  - src/mcp/tools/check.ts
  - src/mcp/tools/redact.ts
  - src/model/constants.ts
  - src/detect/ner-entities.ts
  - tests/mcp/check-redact-ner.test.ts
  - tests/mcp/server-ner-preload.test.ts
  - tests/detect/ner-entities-piiranha.test.ts
autonomous: false
requirements: [NER-01, NER-04]
user_setup:
  - service: huggingface-model-piiranha
    why: "NER-04 higher-recall tier; base model is NonCommercial-NoDerivatives (cc-by-nc-nd-4.0)"
    dashboard_config:
      - task: "Confirm acceptance of the piiranha base-model license (cc-by-nc-nd-4.0) and provide/confirm the pinned SHA-256 of onnx-community/piiranha-v1-detect-personal-information-ONNX onnx/model_int8.onnx"
        location: "huggingface.co/onnx-community/piiranha-v1-detect-personal-information-ONNX"
must_haves:
  truths:
    - "When pii.ner.enabled, the MCP server eager-preloads the NER singleton as fire-and-forget at boot (D-04) — it NEVER blocks server.connect() and the secret tools register/connect immediately"
    - "If the model fails to load at boot, the server STILL starts and serves secret detection; nerStatus flips to 'unavailable' (fail-closed for NER only, D-05)"
    - "mcp__mrclean__check and mcp__mrclean__redact pass {ner:true} to detection and surface nerStatus in structuredContent (D-03), and their finding DTOs still omit value/span (no PII leak)"
    - "The default tier is Xenova/bert-base-NER int8; the piiranha tier is selectable via pii.ner.model (D-13) with its own pinned SHA-256 and a per-model label map ({GIVENNAME,SURNAME}→PERSON, {CITY,STREET,ZIPCODE,BUILDINGNUM}→LOC, no ORG)"
    - "The piiranha tier is gated behind a blocking human checkpoint confirming the NonCommercial-ND license and the second pinned hash"
    - "The MCP-03 invariant holds: no new write/unredact tool; tools-list test still passes"
  artifacts:
    - path: "src/mcp/server.ts"
      provides: "eager fire-and-forget preload + getNerStatus closure threaded into check/redact"
      contains: "getNerStatus"
    - path: "src/model/constants.ts"
      provides: "piiranha MODEL_ID/URL/PINNED_SHA256/CACHE_PATH quartet (NER-04)"
      contains: "PIIRANHA"
    - path: "src/detect/ner-entities.ts"
      provides: "piiranha label→canonical branch added to mapModelLabel"
      contains: "piiranha"
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/model/pipeline-singleton.js getNerPipeline"
      via: "fire-and-forget async preload flipping nerStatus loading→ready|unavailable"
      pattern: "void \\(async"
    - from: "src/mcp/tools/check.ts"
      to: "runDetectionReadOnly"
      via: "passes { ner: true } and reads outcome.result.nerStatus"
      pattern: "ner: true"
    - from: "src/mcp/tools/redact.ts"
      to: "runDetection"
      via: "passes { ner: true } and reads outcome.result.nerStatus"
      pattern: "ner: true"
---

<objective>
Surface NER end-to-end through the MCP server: eager fail-closed preload at boot, `{ner:true}` on the
two tools, and `nerStatus` in their structuredContent — then add the optional piiranha higher-recall
tier behind a blocking license/hash checkpoint. After this plan, an operator with `pii.ner.enabled`
sees PERSON/ORG/LOC findings via check/redact while the hook never loads a model.

Purpose: NER-01 (the MCP-only enablement + boot preload + nerStatus surfacing — the user-visible
half of "structurally unreachable from the hook") and NER-04 (piiranha tier swap with its second
pinned hash + label remap + license gate).

Output: modified src/mcp/{server.ts,tools/check.ts,tools/redact.ts}, the piiranha constants quartet,
the piiranha branch in mapModelLabel, and tests for preload/fail-closed/nerStatus/piiranha mapping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-CONTEXT.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-RESEARCH.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-PATTERNS.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-01-SUMMARY.md
@.planning/phases/06-ner-inference-l6b-mcp-wiring/06-02-SUMMARY.md

<interfaces>
<!-- From 06-01/06-02 and the shipped MCP surface. Use directly. -->

From src/detect/index.ts (06-02):
```typescript
export interface DetectionOptions { ner?: boolean }
// runDetection / runDetectionReadOnly now accept a trailing `opts: DetectionOptions = {}`
// DetectionResult now carries `nerStatus?: NerStatus`
```

From src/detect/layer6b-ner.ts (06-01): `export type NerStatus = 'ready'|'unavailable'|'loading'|'disabled'`

From src/model/pipeline-singleton.ts (06-01):
```typescript
export function getNerPipeline(ner: MrcleanPiiNerConfig): Promise<NerPipeline>
```

From src/detect/ner-entities.ts (06-01): `export function mapModelLabel(model: string, label: string): 'PERSON'|'ORG'|'LOC'|null`

Shipped MCP surface:
- src/mcp/server.ts: lazy SDK + tool imports; getConfig/getSessionState/getCwd closures (lines 44-47); registerCheckTool/registerRedactTool calls (lines 57-58); shutdown chain (lines 71-75)
- src/mcp/tools/check.ts: registerCheckTool(server, getConfig, getSessionState, getCwd); supervisedToolCall → runDetectionReadOnly; checkOutputSchema { findings, count }; toFindingDTO omits value/span
- src/mcp/tools/redact.ts: registerRedactTool(...); runDetection; redactOutputSchema { redacted, findings }
- src/model/constants.ts: MODEL_ID/MODEL_DOWNLOAD_URL/PINNED_MODEL_SHA256/MODEL_CACHE_PATH(homeDir) quartet to mirror

piiranha facts (VERIFIED, RESEARCH Pitfall 6/7): onnx-community/piiranha-v1-detect-personal-information-ONNX,
onnx/model_int8.onnx ≈ 317.1 MB, NO pinned SHA yet, base license cc-by-nc-nd-4.0; id2label = 17 PII labels,
NO PERSON/ORG/LOC and NO ORG concept. Map {GIVENNAME,SURNAME}→PERSON, {CITY,STREET,ZIPCODE,BUILDINGNUM}→LOC, no ORG.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP eager preload (fail-closed) + nerStatus in check/redact structuredContent</name>
  <files>src/mcp/server.ts, src/mcp/tools/check.ts, src/mcp/tools/redact.ts, tests/mcp/check-redact-ner.test.ts, tests/mcp/server-ner-preload.test.ts</files>
  <read_first>
    - src/mcp/server.ts (full file — lazy imports, closures, registration, shutdown chain)
    - src/mcp/tools/check.ts + src/mcp/tools/redact.ts (full — registration signatures, supervisedToolCall sites, output schemas, toFindingDTO)
    - src/detect/index.ts (06-02 — DetectionOptions, nerStatus on DetectionResult)
    - src/model/pipeline-singleton.ts (06-01 — getNerPipeline)
    - tests/mcp/check.test.ts, tests/mcp/redact.test.ts, tests/mcp/server-lifecycle.test.ts (analog test structure)
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-RESEARCH.md §Pattern 5, §Pitfall 8, Code E6
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-PATTERNS.md §src/mcp/server.ts, §check.ts/redact.ts
  </read_first>
  <behavior>
    - server boot with pii.ner.enabled=false → nerStatus starts 'disabled', no preload, no transformers import
    - server boot with pii.ner.enabled=true → nerStatus starts 'loading'; a fire-and-forget async preload flips it to 'ready' on success or 'unavailable' on throw; server.connect() is NOT awaited on the preload
    - preload throw (corrupt/missing model, native-load failure) → nerStatus 'unavailable', server still serves; a stderr line is written carrying NO matched text
    - check tool passes { ner: true } to runDetectionReadOnly and includes nerStatus in structuredContent
    - redact tool passes { ner: true } to runDetection and includes nerStatus in structuredContent
    - finding DTOs still omit value/span (no PII leak); outputSchema gains nerStatus enum
  </behavior>
  <action>
    Modify src/mcp/server.ts per RESEARCH Pattern 5 / Pitfall 8: after loading config, declare
    `let nerStatus: NerStatus = config.pii.ner.enabled ? 'loading' : 'disabled'` and `const getNerStatus = () => nerStatus`.
    If `config.pii.ner.enabled`, start a fire-and-forget `void (async () => { try { const { getNerPipeline } =
    await import('../model/pipeline-singleton.js'); await getNerPipeline(config.pii.ner); nerStatus = 'ready' }
    catch (err) { nerStatus = 'unavailable'; process.stderr.write('mrclean-mcp: NER unavailable; serving secrets only\n') } })()`
    — this MUST run before/independent of server.connect and never be awaited (D-04/D-05). The stderr string carries
    NO matched text (Pitfall 5). Thread `getNerStatus` into registerCheckTool and registerRedactTool as a new trailing arg.
    Modify src/mcp/tools/check.ts: add `getNerStatus: () => NerStatus` param; pass `{ ner: true }` as the 5th arg to
    runDetectionReadOnly; add `nerStatus: z.enum(['ready','unavailable','loading','disabled'])` to checkOutputSchema;
    include `nerStatus: outcome.result.nerStatus ?? getNerStatus()` in the structured object. Keep toFindingDTO
    omitting value/span (Pitfall 5). Do the symmetric change in src/mcp/tools/redact.ts (pass `{ner:true}` to
    runDetection, add nerStatus to redactOutputSchema + structuredContent). Do NOT catch NER errors in the tools or in
    supervisedToolCall — runLayer6bNer already fails closed internally (RESEARCH Pattern 5); a tool-level catch would
    wrongly fail the secret gate.
    Tests: mock pipeline-singleton/layer6b so CI never downloads. server-ner-preload.test.ts asserts boot does not
    block on preload, the disabled vs loading→ready vs loading→unavailable transitions, and that a preload throw does
    not prevent the server from serving secret detection. check-redact-ner.test.ts asserts both tools emit nerStatus in
    structuredContent, the DTO omits value/span, and a NER-unavailable run still returns secret findings.
  </action>
  <acceptance_criteria>
    - `grep -c "ner: true" src/mcp/tools/check.ts` ≥ 1 AND `grep -c "ner: true" src/mcp/tools/redact.ts` ≥ 1
    - `grep -q "getNerStatus" src/mcp/server.ts` AND `grep -q "void (async" src/mcp/server.ts` (fire-and-forget preload present)
    - check/redact outputSchema both contain `nerStatus` enum; finding DTO schemas still have no `value`/`span`/`word` key (grep returns 0)
    - server-ner-preload.test.ts proves the preload throw path leaves the server serving secrets with nerStatus 'unavailable'
    - tests/mcp/tools-list.test.ts still passes (MCP-03: no new tool added)
    - `npm test -- check-redact-ner server-ner-preload tools-list check redact` all pass with no network access
  </acceptance_criteria>
  <verify>
    <automated>grep -q "void (async" src/mcp/server.ts && grep -q "ner: true" src/mcp/tools/check.ts && grep -q "ner: true" src/mcp/tools/redact.ts && npm test -- check-redact-ner server-ner-preload tools-list</automated>
  </verify>
  <done>MCP server eager-preloads NER fail-closed without blocking connect; check/redact pass {ner:true} and surface nerStatus while keeping DTOs PII-free; MCP-03 tool surface unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: piiranha license + second pinned SHA-256 confirmation</name>
  <files>src/model/constants.ts (gated — not written until approval)</files>
  <what-built>
    The piiranha NER-04 tier requires a SECOND pinned model hash and adds a model whose base license is
    cc-by-nc-nd-4.0 (NonCommercial-NoDerivatives) — a genuine concern for an MIT-distributed tool. Per
    RESEARCH Pitfall 7 this is gated behind a blocking human checkpoint BEFORE the constants/hash land.
    This checkpoint is NOT auto-approvable (workflow.auto_advance ignored) — it is a supply-chain + license gate.
  </what-built>
  <how-to-verify>
    1. Open huggingface.co/onnx-community/piiranha-v1-detect-personal-information-ONNX and the base model card
       huggingface.co/iiiorg/piiranha-v1-detect-personal-information — confirm the base license is cc-by-nc-nd-4.0
       (NonCommercial-NoDerivatives) and that you accept it as an OPT-IN, never-default, lazy-downloaded tier.
    2. Compute or confirm the SHA-256 of onnx/model_int8.onnx (≈317.1 MB) for the ONNX export and record it
       (download the file and run `shasum -a 256`, or confirm a maintainer-provided pinned hash).
    3. Confirm the entity scope: piiranha maps to PERSON/LOC only (no ORG) per the label remap below.
    4. Reply with the confirmed pinned SHA-256 (64 hex chars) and "license accepted, opt-in only".
  </how-to-verify>
  <action>
    HUMAN-GATE — no autonomous work in this task. Pause and present the license + hash confirmation request to
    the operator per how-to-verify. Do NOT write PIIRANHA_PINNED_SHA256 or any piiranha constant until the operator
    replies with a 64-hex SHA-256 AND explicit license acceptance. This gate is NOT auto-approvable
    (workflow.auto_advance ignored) — it is a supply-chain + NonCommercial-ND license gate (RESEARCH Pitfall 7).
    Carry the confirmed 64-hex value forward into Task 3 verbatim.
  </action>
  <verify>
    <automated>MISSING — human checkpoint; cleared by an operator reply carrying a 64-hex SHA-256 + "license accepted, opt-in only". The pinned hash is enforced downstream by Task 3's grep gate (`PIIRANHA_PINNED_SHA256 = '[0-9a-f]{64}'`, no TODO/PLACEHOLDER).</automated>
  </verify>
  <resume-signal>Reply "approved" with the 64-hex SHA-256 of the piiranha int8 ONNX file, or describe a blocking concern</resume-signal>
  <done>Operator confirmed the piiranha base-model license (cc-by-nc-nd-4.0, opt-in only) and supplied the 64-hex pinned SHA-256, carried into Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: piiranha constants quartet + mapModelLabel piiranha branch (NER-04)</name>
  <files>src/model/constants.ts, src/detect/ner-entities.ts, tests/detect/ner-entities-piiranha.test.ts</files>
  <read_first>
    - src/model/constants.ts (the bert MODEL_ID/MODEL_DOWNLOAD_URL/PINNED_MODEL_SHA256/MODEL_CACHE_PATH quartet to mirror)
    - src/detect/ner-entities.ts (06-01 output — the bert branch in mapModelLabel + the model-keyed shape)
    - The Task 2 checkpoint reply (the confirmed piiranha SHA-256)
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-RESEARCH.md §Pitfall 6 (label divergence), §Pitfall 7 (license), Assumption A2
    - .planning/phases/06-ner-inference-l6b-mcp-wiring/06-PATTERNS.md §src/model/constants.ts, §ner-entities.ts
  </read_first>
  <behavior>
    - constants exports a parallel piiranha quartet: PIIRANHA_MODEL_ID='onnx-community/piiranha-v1-detect-personal-information-ONNX', a resolve URL for onnx/model_int8.onnx, PIIRANHA_PINNED_SHA256=<from checkpoint>, PIIRANHA_CACHE_PATH(homeDir)→~/.mrclean/models/onnx-community/.../onnx/model_int8.onnx
    - mapModelLabel for the piiranha model id maps GIVENNAME→PERSON, SURNAME→PERSON, CITY→LOC, STREET→LOC, ZIPCODE→LOC, BUILDINGNUM→LOC; every other piiranha label (EMAIL, TELEPHONENUM, ORG-absent, etc.) → null
    - mapModelLabel for the bert model id is unchanged (06-01 behavior preserved)
    - piiranha never yields ORG (no ORG concept) — asserted explicitly
  </behavior>
  <action>
    Add the piiranha quartet to src/model/constants.ts mirroring the bert quartet shape: PIIRANHA_MODEL_ID,
    PIIRANHA_DOWNLOAD_URL (HF resolve/main/onnx/model_int8.onnx), PIIRANHA_PINNED_SHA256 (the 64-hex value
    confirmed in Task 2 — paste verbatim), and PIIRANHA_CACHE_PATH(homeDir). Keep constants.ts zero-ML-dep
    (stdlib only) so it stays cold-path safe. Add a JSDoc note that the base model is cc-by-nc-nd-4.0,
    opt-in only, lazy-downloaded (RESEARCH Pitfall 7).
    Extend src/detect/ner-entities.ts mapModelLabel with a per-model branch for PIIRANHA_MODEL_ID implementing
    the label remap above ({GIVENNAME,SURNAME}→PERSON, {CITY,STREET,ZIPCODE,BUILDINGNUM}→LOC, everything else→null,
    no ORG). Do NOT alter the bert branch. Keep the map keyed by model id (the 06-01 shape).
    NOTE: this plan does NOT make piiranha the default and does NOT wire piiranha into model-cache download
    plumbing beyond the constants — selecting it is via `pii.ner.model` config; the singleton already loads
    `ner.model` generically. (If a download/verify path for the second file is needed, it reuses the Phase 5
    model-cache functions with the piiranha URL+hash — flag any gap in the SUMMARY rather than expanding scope.)
    Tests: tests/detect/ner-entities-piiranha.test.ts asserts the full piiranha label table including the
    no-ORG cases and that bert mapping is unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "PIIRANHA_PINNED_SHA256" src/model/constants.ts` ≥ 1 and the value is a 64-hex string matching the Task 2 checkpoint reply (not a placeholder); `grep -Ei "TODO|PLACEHOLDER|XXXX" src/model/constants.ts` returns 0 lines for the piiranha hash
    - constants.ts imports no ML deps: `grep -c "@huggingface/transformers\|onnxruntime" src/model/constants.ts` returns 0
    - ner-entities-piiranha.test.ts asserts GIVENNAME/SURNAME→PERSON, CITY/STREET/ZIPCODE/BUILDINGNUM→LOC, EMAIL→null, and that no piiranha label ever yields 'ORG'
    - bert mapping unchanged: existing tests/detect/ner-entities.test.ts still passes
    - `npm test -- ner-entities-piiranha ner-entities` passes
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq "PIIRANHA_PINNED_SHA256 *= *'[0-9a-f]{64}'" src/model/constants.ts && test "$(grep -c "@huggingface/transformers\|onnxruntime" src/model/constants.ts)" = "0" && npm test -- ner-entities-piiranha ner-entities</automated>
  </verify>
  <done>piiranha tier has its own pinned SHA-256 + cache path constants and a per-model label remap (PERSON/LOC, no ORG); default bert tier unchanged; tier remains opt-in behind the license checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP boot → model load | preload failure must not crash the server or block secret tools |
| tool output → MCP client | check/redact DTOs cross to the model-facing surface; must omit raw PII |
| piiranha model artifact | second supply-chain artifact with a NonCommercial-ND license |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Denial of Service | eager preload blocking server.connect / hung download | mitigate | fire-and-forget `void (async…)()`; tools register/connect immediately; nerStatus flips loading→ready/unavailable (RESEARCH Pitfall 8, D-04) |
| T-06-03-02 | Denial of Service | model-load failure crashing the secret gate | mitigate | boot catch → nerStatus 'unavailable', server still serves secrets (D-05, NER-03); stderr carries no matched text |
| T-06-03-03 | Information Disclosure | raw PII in check/redact DTO or nerStatus string | mitigate | finding DTO omits value/span (verified, kept); nerStatus is an enum, no free-form text; stderr preload message carries model state only (Pitfall 5) |
| T-06-03-04 | Elevation of Privilege | prompt-injection adding an NER toggle/unredact tool | mitigate | MCP-03 invariant — no new write/unredact tool; tools-list test still passes |
| T-06-03-05 | Tampering | piiranha model file tampering | mitigate | second pinned SHA-256 (PIIRANHA_PINNED_SHA256) verified via Phase 5 fail-closed integrity path |
| T-06-03-SC | Tampering / Compliance | piiranha NonCommercial-ND license + unpinned hash | mitigate | blocking human checkpoint (Task 2, NOT auto-approvable) confirms license acceptance + supplies the 64-hex pinned hash before constants land; piiranha never default, lazy-download only (RESEARCH Pitfall 7) |
</threat_model>

<verification>
- `npm test` full suite green (check/redact/server/tools-list + new NER tests)
- Eager preload is fire-and-forget and fail-closed; secrets always served
- check/redact surface nerStatus; DTOs PII-free; MCP-03 tool surface unchanged
- piiranha constants carry a real 64-hex pinned hash (from checkpoint) + opt-in license note; mapModelLabel piiranha branch correct, no ORG
</verification>

<success_criteria>
- MCP server eager-preloads NER fail-closed without blocking connect (D-04/D-05)
- check/redact pass {ner:true} and surface nerStatus (D-03); DTOs omit value/span
- piiranha tier (NER-04) selectable via config with its own pinned SHA-256 + label remap, gated behind the license checkpoint
- MCP-03 invariant intact (no new write/unredact tool)
</success_criteria>

<output>
Create `.planning/phases/06-ner-inference-l6b-mcp-wiring/06-03-SUMMARY.md` when done.
Record: the confirmed piiranha pinned SHA-256, the license-acceptance disposition, the nerStatus shape
surfaced in check/redact, and any piiranha download/verify gap deferred to follow-up (do not expand scope silently).
</output>
