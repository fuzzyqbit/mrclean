---
milestone: v3.0
audited: 2026-07-18T20:30:00Z
status: tech_debt
scores:
  requirements: 12/12
  phases: 4/4
  integration: 12/12
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 08-contract-verification-reversible-plumbing
    items:
      - "atomic-json.ts stale docstring: says tmp cleanup happens 'in a finally block', implementation correctly uses catch (review IN-01, unaddressed)"
      - "fresh-home.test.ts: Number(match?.[1] ?? NaN) collapses regex-no-match into misleading 'expected NaN' failures (review IN-02, unaddressed)"
      - "atomic-json.ts rewrite resets file mode to default 644 on every write — user-hardened 600 configs silently widen (review WR-01, OWNER NEEDED)"
      - "readJsonOrEmpty casts JSON.parse output unvalidated — null/array/empty-file settings.json yields raw TypeError/unlabeled SyntaxError/silent fail-open (review WR-02, OWNER NEEDED)"
      - "runInstall has no error contextualization; new mkdir errno sources (EEXIST/ENOTDIR/EACCES/EROFS/ENOSPC) still surface as raw stacks (review WR-03, OWNER NEEDED)"
      - "No cross-process locking on read-mutate-write of settings.json/mcp-config.ts — concurrent Claude Code sessions can clobber each other (review WR-04, pre-existing design, OWNER NEEDED)"
      - "config/index.ts prototype-chain 'in MODEL_DESCRIPTORS' acceptance (carried from Phase 6, OWNER NEEDED)"
      - "config/index.ts DEFAULT_CONFIG applied twice — latent allowlist double-concat, currently invisible (carried, OWNER NEEDED)"
      - "Shipped updatedToolOutput is string-only; object-shape rewrite for built-in Bash has no tracked implementation owner despite HOOK-CONTRACT documenting the object-shape finding (carried, OWNER NEEDED)"
  - phase: 09-session-state-adapter
    items:
      - "pre/post-tool-use.ts comment claims 'EXACTLY ONE dynamic import site' but two exist (state/index.js + state/lock.js) — comment drift only, fence test still pins both counts (IN-02, open by design)"
      - "session-map.ts entries[key] = entry on a plain object — a crafted '__proto__' hex key could drop an entry; requires attacker who already holds the session decryption key (IN-01, open by design, low severity)"
  - phase: 10-operator-restore
    items:
      - "Stdout-stream-destroyed mid-write is an untested pathological path for the 'exit 0 on every cosmetic failure' claim (IN-01, informational)"
      - "readRawReversibleKeys duplicates ~20 lines of readConfigLayer scaffolding (IN-04, informational)"
      - "Unsalted 16-hex audit hashes are dictionary-confirmable — matches existing hook discipline, documented as an accepted decision (IN-03)"
  - phase: 11-wire-safety-verification-hardening
    items:
      - "docs/HOOK-CONTRACT.md per-tool tool_response matrix (Bash/Read/Grep/MCP) still shows 'pending first MRCLEAN_UAT=1 wire-safety run (CLI 2.1.212)' placeholders at lines 181, 190-193, even though the live run completed 2026-07-18 and tests/uat/artifacts/contract-findings.json already holds the real per-tool shapes (object w/ stdout/stderr/interrupted/isImage/noOutputExpected for Bash; type/file for Read; mode/filenames/numFiles/totalFiles for Grep; array-of-content-blocks for MCP). The plan (11-05 Task 3) called for re-stamping the docs from the artifact in the same commit as the live run; that step was never done despite PROJECT.md and 11-HUMAN-UAT.md declaring the gap-closure round fully resolved. Cheap fix (copy 4 cells from the artifact), but as-is the shipped docs understate what's actually known — notable given this project's whole design ethos is honest, non-overclaiming documentation (copy-drift CI gates etc.)."
      - "STATE.md reversible-overhead metric row still literally reads 'TBD (Phase 9/11)' even though 11-06 measured real values (p95 5.36ms reversible / 2.60ms one-way, +2.75ms delta) — orchestrator bookkeeping, not a phase-goal gap, but same class of stale-doc issue as the HOOK-CONTRACT item above"
      - "src/doctor/index.ts:85 doc comment says checkReversibleState is 'reporting-only... never FAIL' — stale since Phase 10 made it FAIL-loud on unsupported config; cosmetic, code behavior is correct and tested (found by integration checker)"
      - "src/placeholder/manager.ts:282-286 parseV2TokenIndex inlines a partial token-grammar regex instead of importing from state/session-map.ts's V2_TOKEN_RE/V2_TOKEN_SCAN_RE — narrow duplicate scoped to hydration index-seeding only, latent drift risk if the grammar ever changes (found by integration checker)"
      - "npm run typecheck holds at 38 pre-existing errors, none in any Phase 8-11 file — not a milestone regression, but the 'final cross-phase regression gate' isn't literally clean (found by integration checker)"
---

# Milestone v3.0 Audit: Reversible Redact Mode — Foundations + Operator Restore

**Audited:** 2026-07-18
**Phases:** 8, 9, 10, 11 (all complete)
**Status: ⚡ TECH DEBT** — all requirements satisfied, all cross-phase wiring proven, no blockers. Accumulated tech debt (mostly carried, review-deferred, OWNER-NEEDED items) plus one real doc-sync gap (HOOK-CONTRACT.md re-stamp) need a decision before shipping cleanly.

> Supersedes the 2026-07-17T17:02:17Z mid-milestone checkpoint audit in this same file (that pass ran after Phase 9 only, before Phases 10/11 existed — 7/12 requirements, status gaps_found. Those gaps were roadmap-pending, not defects, and are now closed.)

## Requirements Coverage (3-source cross-reference)

All 12 REVMODE requirements: VERIFICATION.md status `passed`/`SATISFIED` + SUMMARY.md frontmatter `requirements-completed` listed + REQUIREMENTS.md traceability `[x]`/Complete — all three sources agree, zero orphans.

| Requirement | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|---|---|---|---|---|---|
| REVMODE-01 | 10 | ✓ SATISFIED | 10-02, 10-05 | `[x]` Complete | **satisfied** |
| REVMODE-02 | 9 | ✓ SATISFIED | 09-01, 09-03, 09-07, 09-08 | `[x]` Complete | **satisfied** |
| REVMODE-03 | 8 | ✓ SATISFIED | 08-11 | `[x]` Complete | **satisfied** |
| REVMODE-04 | 9 | ✓ SATISFIED | 09-05, 09-07, 09-08 | `[x]` Complete | **satisfied** |
| REVMODE-05 | 9 | ✓ SATISFIED | 09-04, 09-07 | `[x]` Complete | **satisfied** |
| REVMODE-06 | 9 | ✓ SATISFIED | 09-02 | `[x]` Complete | **satisfied** |
| REVMODE-07 | 9 (installer clause: 8) | ✓ SATISFIED | 09-01, 09-06 | `[x]` Complete | **satisfied** |
| REVMODE-08 | 10 | ✓ SATISFIED | 10-05 | `[x]` Complete | **satisfied** |
| REVMODE-09 | 10 | ✓ SATISFIED | 10-03, 10-08 | `[x]` Complete | **satisfied** |
| REVMODE-10 | 8 | ✓ SATISFIED | 08-10, 08-11 | `[x]` Complete | **satisfied** |
| REVMODE-11 | 11 | ✓ SATISFIED (implementation) | 11-02, 11-05, 11-06, 11-07, 11-08, 11-09 | `[x]` Complete | **satisfied** |
| REVMODE-12 | 10 | ✓ SATISFIED | 10-06 | `[x]` Complete | **satisfied** |

**Orphan check:** REQUIREMENTS.md maps exactly these 12 REVMODE-IDs across Phases 8-11 — no orphaned requirements (assigned but unverified by any phase).

## Phase Verification Summary

| Phase | Score | Status | Notes |
|---|---|---|---|
| 8. Contract Verification & Reversible Plumbing | 20/20 (19 direct + 1 accepted override) | passed | 4th verification pass; one same-round traceability overclaim found and fixed by orchestrator (REVMODE-07 falsely marked Complete, reverted b157243), re-verified by file read |
| 9. Session State Adapter | 22/22 | passed | Initial verification, no gaps; 16-process stress gate, secret floor, janitor all independently re-run |
| 10. Operator Restore | 41/41 | passed | Initial verification, no gaps; live spot-checks of the shipped dist binary confirm CLI behavior |
| 11. Wire-Safety Verification & Hardening | 12/14 automated + 2/2 human-resolved | **passed (after gap closure)** | Original VERIFICATION.md is `human_needed` (2 deferred operator legs); both resolved 2026-07-18 per 11-HUMAN-UAT.md and independently re-confirmed by this audit (see below) |

### Phase 11 gap-closure independently re-verified by this audit

11-VERIFICATION.md's frontmatter still literally reads `status: human_needed` — it was never re-run after the two deferred items closed. This audit did not take the "resolved" claim in 11-HUMAN-UAT.md / PROJECT.md on faith:

- **SC1b live wire leg:** 11-HUMAN-UAT.md reports 8/8 clean on the second retry (after two rounds of `grepProjectsTreeForCanaries` scope fixes — a raw whole-file byte-scan bug, closed in two passes because the first fix missed a second identical instance). `tests/uat/artifacts/contract-findings.json` (checked directly by this audit) independently confirms a real `wire_safety` block dated 2026-07-18, CLI 2.1.212, `"restored": 1, "unmatched": 0, "secretSkipped": 1, "sessions": 1"`, verdict PASS.
- **A4 first ubuntu CI run:** this audit ran `gh run list` directly against the branch (not from docs) — confirmed both `test.yml` and `Canary Leak Gate` completed `success` on the resolution commit (`6a171d0`, run IDs `29649118240` / `29649118226`, 2026-07-18T15:00:07Z). Independently verified, not just trusted from PROJECT.md's claim.

Both deferrals are genuinely closed. Net effect: **14/14 Phase 11 truths hold**, even though the VERIFICATION.md artifact itself was never regenerated to say so (a minor doc-staleness item, not a functional gap — noted for hygiene, not tracked as tech debt since it's self-evidently superseded by the dated resolution record).

## Cross-Phase Integration

Spawned `gsd-integration-checker` (sonnet) with all 12 requirement IDs and 7 specific cross-phase seams to verify. Result: **all 7 seams WIRED, 0 blockers.**

**Verified seams:**
1. Phase 8's SessionEnd no-op stub → genuinely replaced by Phase 9's janitor wiring (not left as dead code)
2. Phase 9's encrypted map (`src/state/map-store.ts`) IS the same store Phase 10's restore CLI reads — same AES-256-GCM envelope, same session-ID scheme, no format drift
3. Phase 9's v2 token grammar (`V2_TOKEN_RE`/`V2_TOKEN_SCAN_RE` in `session-map.ts`) is a single source of truth consumed by both the emitter (`placeholder/manager.ts`) and the restore scanner (`restore/index.ts`) — one narrow duplicate regex flagged (see tech debt)
4. REVMODE-08's separate error domains are structurally enforced by import-graph fence tests (`cold-path.test.ts`), not just asserted in prose — restore code cannot reach config/hook/lock modules
5. Phase 11's CI gates (dist-parity, fs-interception, chaos, stress) drive the REAL Phase 9/10 modules — verified by reading test bodies, not test names; `dist-parity` even spawns the actual shipped binary
6. Doctor's reversible-mode reporting vs. Phase 9 state / Phase 10 config is a deliberate REVMODE-12 split (doctor = config validity, `mrclean_status` = counts) — confirmed by design, not a gap; one stale doc-comment found (tech debt)
7. Live-run regression gate on HEAD (not trusted from prior reports): `npm test` → **989 passed / 29 skipped, 0 failures**; `npm run typecheck` → 38 pre-existing errors, zero touching any Phase 8-11 file

**Requirements integration map:** all 12 REVMODE-IDs traced to a concrete cross-phase code path — see integration checker's full report (this audit's session transcript) for the file-level detail per requirement.

## E2E Flows

**Complete:**
1. **Hook lifecycle (reversible mode ON):** SessionStart (TTL sweep) → PreToolUse/PostToolUse (hydrate → allocate v2 token → persist under lock → apply renames to emitted payload) → SessionEnd (reason-aware janitor). Proven via real chaos-suite handler runs and real shipped-binary dist-parity spawns.
2. **Operator restore round-trip:** hook redacts under reversible mode → `mrclean restore` reads the identical encrypted map → restores restorable-typed originals, leaves secret-class placeholders byte-intact → audits hash-only. Proven via `fs-interception.test.ts` (every fs-write buffer captured across a real two-event flow + real restore run, zero plaintext canary) and `mixed-canary.test.ts`.

**Broken:** none.

## Nyquist Compliance Discovery

| Phase | VALIDATION.md | nyquist_compliant | Task table | Classification |
|---|---|---|---|---|
| 8 | exists | true | 30/31 rows green (1 is the legend line) | **COMPLIANT** |
| 9 | exists | true | 23/24 rows green (1 is the legend line) | **COMPLIANT** |
| 10 | exists | true | 14/14 task rows still `⬜ pending`; frontmatter `status: planned`, `wave_0_complete: false`, `Approval: pending` | **PARTIAL** (tracking-doc debt — VERIFICATION.md independently re-ran and passed all named suites; the plan-time tracking table was simply never flipped to green post-execution) |
| 11 | exists | true | 17/17 task rows still `⬜ pending`; frontmatter `status: planned`, `wave_0_complete: false` | **PARTIAL** (same pattern) |

Discovery only, per workflow contract — not auto-routed to `/gsd:validate-phase`. Recommend backfilling the Phase 10/11 VALIDATION.md task tables to green (cosmetic, ~5 min each) so future audits don't have to re-derive this from VERIFICATION.md.

## Tech Debt Summary

**23 items across 4 phases** (see YAML frontmatter `tech_debt` for the full itemized list). Breakdown:
- **Phase 8:** 9 items — mostly install-primitive hardening (file-mode reset, unvalidated JSON.parse, missing error contextualization, no cross-process config locking) and 2 carried Phase-6 defects. All marked "OWNER NEEDED" in 08-VERIFICATION.md across 3+ verification rounds — these are the oldest unowned debt in the milestone.
- **Phase 9:** 2 items — both "open by design" per the phase's own review (comment drift, low-severity prototype-pollution edge case requiring an attacker who already holds the session key).
- **Phase 10:** 3 items — all informational, documented and accepted in 10-REVIEW.md.
- **Phase 11:** 5 items — includes the **HOOK-CONTRACT.md re-stamp gap** (the one item this audit considers worth fixing before merge; see below) plus 4 minor/cosmetic items surfaced by the integration checker.

### Recommended before merge (cheap, high-value)
- **Re-stamp docs/HOOK-CONTRACT.md's per-tool matrix** from `tests/uat/artifacts/contract-findings.json`'s `tool_response_shapes` block (4 cells: Bash/Read/Grep/MCP). Data already exists from the confirmed 2026-07-18 live run; this is a copy-paste, not new work. Closes the gap between what PROJECT.md/11-HUMAN-UAT.md claim ("resolved") and what the shipped docs actually say ("pending").
- Fix the stale `src/doctor/index.ts:85` comment (1-line).

### Can defer to backlog
- The 9 Phase-8 OWNER-NEEDED install-hardening items (real but low-severity, none block the milestone's core reversible-mode claims)
- Phase 9/10 informational items (already accepted/documented)
- `manager.ts:282-286` regex duplication (low risk, narrow blast radius)
- Nyquist VALIDATION.md task-table backfill for phases 10/11 (cosmetic)
- Typecheck's 38-error pre-existing baseline (unrelated to this milestone; own cleanup effort)

## Gaps

**None that block milestone completion.** No unsatisfied requirement, no broken integration, no broken E2E flow. The one process gap found (HOOK-CONTRACT.md re-stamp) does not correspond to an unsatisfied REQ-ID — the underlying REVMODE-11/SC1b behavior is proven correct live; only the documentation transcription step was skipped.

---
_Audited: 2026-07-18T20:30:00Z_
_Auditor: Claude (gsd-audit-milestone) + gsd-integration-checker (sonnet)_
